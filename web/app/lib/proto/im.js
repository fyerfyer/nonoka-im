/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $String = $util.global.String, $Number = $util.global.Number, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $Array = $util.global.Array, $Boolean = $util.global.Boolean, $isFinite = $util.global.isFinite;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const api = $root.api = (() => {

    /**
     * Namespace api.
     * @exports api
     * @namespace
     */
    const api = {};

    api.im = (function() {

        /**
         * Namespace im.
         * @memberof api
         * @namespace
         */
        const im = {};

        im.v1 = (function() {

            /**
             * Namespace v1.
             * @memberof api.im
             * @namespace
             */
            const v1 = {};

            v1.AuthService = (function() {

                /**
                 * Constructs a new AuthService service.
                 * @memberof api.im.v1
                 * @classdesc Represents an AuthService
                 * @extends $protobuf.rpc.Service
                 * @constructor
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 */
                const AuthService = function(rpcImpl, requestDelimited, responseDelimited) {
                    $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
                };

                (AuthService.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = AuthService;

                /**
                 * Creates new AuthService service using the specified rpc implementation.
                 * @function create
                 * @memberof api.im.v1.AuthService
                 * @static
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 * @returns {AuthService} RPC service. Useful where requests and/or responses are streamed.
                 */
                AuthService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                    return new this(rpcImpl, requestDelimited, responseDelimited);
                };

                /**
                 * Callback as used by {@link api.im.v1.AuthService#register}.
                 * @memberof api.im.v1.AuthService
                 * @typedef RegisterCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.RegisterReply} [response] RegisterReply
                 */

                /**
                 * Calls Register.
                 * @memberof api.im.v1.AuthService
                 * @typedef Register
                 * @type {{
                 *   (request: api.im.v1.IRegisterRequest, callback: api.im.v1.AuthService.RegisterCallback): void;
                 *   (request: api.im.v1.IRegisterRequest): Promise<api.im.v1.RegisterReply>;
                 *   readonly name: "Register";
                 *   readonly path: "/api.im.v1.AuthService/Register";
                 *   readonly requestType: "RegisterRequest";
                 *   readonly responseType: "RegisterReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls Register.
                 * @name api.im.v1.AuthService#register
                 * @type {api.im.v1.AuthService.Register}
                 */
                $Object.defineProperties(AuthService.prototype.register = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, AuthService.prototype.register, $root.api.im.v1.RegisterRequest, $root.api.im.v1.RegisterReply, request, callback);
                }, {
                    name: { value: "Register" },
                    path: { value: "/api.im.v1.AuthService/Register" },
                    requestType: { value: "RegisterRequest" },
                    responseType: { value: "RegisterReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.AuthService#login}.
                 * @memberof api.im.v1.AuthService
                 * @typedef LoginCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.LoginReply} [response] LoginReply
                 */

                /**
                 * Calls Login.
                 * @memberof api.im.v1.AuthService
                 * @typedef Login
                 * @type {{
                 *   (request: api.im.v1.ILoginRequest, callback: api.im.v1.AuthService.LoginCallback): void;
                 *   (request: api.im.v1.ILoginRequest): Promise<api.im.v1.LoginReply>;
                 *   readonly name: "Login";
                 *   readonly path: "/api.im.v1.AuthService/Login";
                 *   readonly requestType: "LoginRequest";
                 *   readonly responseType: "LoginReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls Login.
                 * @name api.im.v1.AuthService#login
                 * @type {api.im.v1.AuthService.Login}
                 */
                $Object.defineProperties(AuthService.prototype.login = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, AuthService.prototype.login, $root.api.im.v1.LoginRequest, $root.api.im.v1.LoginReply, request, callback);
                }, {
                    name: { value: "Login" },
                    path: { value: "/api.im.v1.AuthService/Login" },
                    requestType: { value: "LoginRequest" },
                    responseType: { value: "LoginReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                return AuthService;
            })();

            v1.RegisterRequest = (function() {

                /**
                 * Properties of a RegisterRequest.
                 * @typedef {Object} api.im.v1.RegisterRequest.$Properties
                 * @property {string|null} [username] RegisterRequest username
                 * @property {string|null} [password] RegisterRequest password
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a RegisterRequest.
                 * @memberof api.im.v1
                 * @interface IRegisterRequest
                 * @augments api.im.v1.RegisterRequest.$Properties
                 * @deprecated Use api.im.v1.RegisterRequest.$Properties instead.
                 */

                /**
                 * Shape of a RegisterRequest.
                 * @typedef {api.im.v1.RegisterRequest.$Properties} api.im.v1.RegisterRequest.$Shape
                 */

                /**
                 * Constructs a new RegisterRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a RegisterRequest.
                 * @constructor
                 * @param {api.im.v1.RegisterRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const RegisterRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * RegisterRequest username.
                 * @member {string} username
                 * @memberof api.im.v1.RegisterRequest
                 * @instance
                 */
                RegisterRequest.prototype.username = "";

                /**
                 * RegisterRequest password.
                 * @member {string} password
                 * @memberof api.im.v1.RegisterRequest
                 * @instance
                 */
                RegisterRequest.prototype.password = "";

                /**
                 * Creates a new RegisterRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {api.im.v1.RegisterRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.RegisterRequest} RegisterRequest instance
                 * @type {{
                 *   (properties: api.im.v1.RegisterRequest.$Shape): api.im.v1.RegisterRequest & api.im.v1.RegisterRequest.$Shape;
                 *   (properties?: api.im.v1.RegisterRequest.$Properties): api.im.v1.RegisterRequest;
                 * }}
                 */
                RegisterRequest.create = function(properties) {
                    return new RegisterRequest(properties);
                };

                /**
                 * Encodes the specified RegisterRequest message. Does not implicitly {@link api.im.v1.RegisterRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {api.im.v1.RegisterRequest.$Properties} message RegisterRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RegisterRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.password);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified RegisterRequest message, length delimited. Does not implicitly {@link api.im.v1.RegisterRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {api.im.v1.RegisterRequest.$Properties} message RegisterRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RegisterRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a RegisterRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.RegisterRequest & api.im.v1.RegisterRequest.$Shape} RegisterRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RegisterRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.RegisterRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.username = value;
                                else
                                    delete message.username;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.password = value;
                                else
                                    delete message.password;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a RegisterRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.RegisterRequest & api.im.v1.RegisterRequest.$Shape} RegisterRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RegisterRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a RegisterRequest message.
                 * @function verify
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                RegisterRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        if (!$util.isString(message.username))
                            return "username: string expected";
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        if (!$util.isString(message.password))
                            return "password: string expected";
                    return null;
                };

                /**
                 * Creates a RegisterRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.RegisterRequest} RegisterRequest
                 */
                RegisterRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.RegisterRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.RegisterRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.RegisterRequest();
                    if (object.username != null)
                        if (typeof object.username !== "string" || object.username.length)
                            message.username = $String(object.username);
                    if (object.password != null)
                        if (typeof object.password !== "string" || object.password.length)
                            message.password = $String(object.password);
                    return message;
                };

                /**
                 * Creates a plain object from a RegisterRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {api.im.v1.RegisterRequest} message RegisterRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                RegisterRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.username = "";
                        object.password = "";
                    }
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        object.username = message.username;
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        object.password = message.password;
                    return object;
                };

                /**
                 * Converts this RegisterRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.RegisterRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                RegisterRequest.prototype.toJSON = function() {
                    return RegisterRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for RegisterRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.RegisterRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                RegisterRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.RegisterRequest";
                };

                return RegisterRequest;
            })();

            v1.RegisterReply = (function() {

                /**
                 * Properties of a RegisterReply.
                 * @typedef {Object} api.im.v1.RegisterReply.$Properties
                 * @property {number|Long|null} [userId] RegisterReply userId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a RegisterReply.
                 * @memberof api.im.v1
                 * @interface IRegisterReply
                 * @augments api.im.v1.RegisterReply.$Properties
                 * @deprecated Use api.im.v1.RegisterReply.$Properties instead.
                 */

                /**
                 * Shape of a RegisterReply.
                 * @typedef {api.im.v1.RegisterReply.$Properties} api.im.v1.RegisterReply.$Shape
                 */

                /**
                 * Constructs a new RegisterReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a RegisterReply.
                 * @constructor
                 * @param {api.im.v1.RegisterReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const RegisterReply = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * RegisterReply userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.RegisterReply
                 * @instance
                 */
                RegisterReply.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * Creates a new RegisterReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {api.im.v1.RegisterReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.RegisterReply} RegisterReply instance
                 * @type {{
                 *   (properties: api.im.v1.RegisterReply.$Shape): api.im.v1.RegisterReply & api.im.v1.RegisterReply.$Shape;
                 *   (properties?: api.im.v1.RegisterReply.$Properties): api.im.v1.RegisterReply;
                 * }}
                 */
                RegisterReply.create = function(properties) {
                    return new RegisterReply(properties);
                };

                /**
                 * Encodes the specified RegisterReply message. Does not implicitly {@link api.im.v1.RegisterReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {api.im.v1.RegisterReply.$Properties} message RegisterReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RegisterReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified RegisterReply message, length delimited. Does not implicitly {@link api.im.v1.RegisterReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {api.im.v1.RegisterReply.$Properties} message RegisterReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RegisterReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a RegisterReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.RegisterReply & api.im.v1.RegisterReply.$Shape} RegisterReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RegisterReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.RegisterReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a RegisterReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.RegisterReply & api.im.v1.RegisterReply.$Shape} RegisterReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RegisterReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a RegisterReply message.
                 * @function verify
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                RegisterReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a RegisterReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.RegisterReply} RegisterReply
                 */
                RegisterReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.RegisterReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.RegisterReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.RegisterReply();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a RegisterReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {api.im.v1.RegisterReply} message RegisterReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                RegisterReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults)
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    return object;
                };

                /**
                 * Converts this RegisterReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.RegisterReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                RegisterReply.prototype.toJSON = function() {
                    return RegisterReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for RegisterReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.RegisterReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                RegisterReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.RegisterReply";
                };

                return RegisterReply;
            })();

            v1.LoginRequest = (function() {

                /**
                 * Properties of a LoginRequest.
                 * @typedef {Object} api.im.v1.LoginRequest.$Properties
                 * @property {string|null} [username] LoginRequest username
                 * @property {string|null} [password] LoginRequest password
                 * @property {string|null} [deviceId] LoginRequest deviceId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a LoginRequest.
                 * @memberof api.im.v1
                 * @interface ILoginRequest
                 * @augments api.im.v1.LoginRequest.$Properties
                 * @deprecated Use api.im.v1.LoginRequest.$Properties instead.
                 */

                /**
                 * Shape of a LoginRequest.
                 * @typedef {api.im.v1.LoginRequest.$Properties} api.im.v1.LoginRequest.$Shape
                 */

                /**
                 * Constructs a new LoginRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a LoginRequest.
                 * @constructor
                 * @param {api.im.v1.LoginRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const LoginRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * LoginRequest username.
                 * @member {string} username
                 * @memberof api.im.v1.LoginRequest
                 * @instance
                 */
                LoginRequest.prototype.username = "";

                /**
                 * LoginRequest password.
                 * @member {string} password
                 * @memberof api.im.v1.LoginRequest
                 * @instance
                 */
                LoginRequest.prototype.password = "";

                /**
                 * LoginRequest deviceId.
                 * @member {string} deviceId
                 * @memberof api.im.v1.LoginRequest
                 * @instance
                 */
                LoginRequest.prototype.deviceId = "";

                /**
                 * Creates a new LoginRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {api.im.v1.LoginRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.LoginRequest} LoginRequest instance
                 * @type {{
                 *   (properties: api.im.v1.LoginRequest.$Shape): api.im.v1.LoginRequest & api.im.v1.LoginRequest.$Shape;
                 *   (properties?: api.im.v1.LoginRequest.$Properties): api.im.v1.LoginRequest;
                 * }}
                 */
                LoginRequest.create = function(properties) {
                    return new LoginRequest(properties);
                };

                /**
                 * Encodes the specified LoginRequest message. Does not implicitly {@link api.im.v1.LoginRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {api.im.v1.LoginRequest.$Properties} message LoginRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                LoginRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.password);
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.deviceId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified LoginRequest message, length delimited. Does not implicitly {@link api.im.v1.LoginRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {api.im.v1.LoginRequest.$Properties} message LoginRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                LoginRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a LoginRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.LoginRequest & api.im.v1.LoginRequest.$Shape} LoginRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                LoginRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.LoginRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.username = value;
                                else
                                    delete message.username;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.password = value;
                                else
                                    delete message.password;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.deviceId = value;
                                else
                                    delete message.deviceId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a LoginRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.LoginRequest & api.im.v1.LoginRequest.$Shape} LoginRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                LoginRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a LoginRequest message.
                 * @function verify
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                LoginRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        if (!$util.isString(message.username))
                            return "username: string expected";
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        if (!$util.isString(message.password))
                            return "password: string expected";
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        if (!$util.isString(message.deviceId))
                            return "deviceId: string expected";
                    return null;
                };

                /**
                 * Creates a LoginRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.LoginRequest} LoginRequest
                 */
                LoginRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.LoginRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.LoginRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.LoginRequest();
                    if (object.username != null)
                        if (typeof object.username !== "string" || object.username.length)
                            message.username = $String(object.username);
                    if (object.password != null)
                        if (typeof object.password !== "string" || object.password.length)
                            message.password = $String(object.password);
                    if (object.deviceId != null)
                        if (typeof object.deviceId !== "string" || object.deviceId.length)
                            message.deviceId = $String(object.deviceId);
                    return message;
                };

                /**
                 * Creates a plain object from a LoginRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {api.im.v1.LoginRequest} message LoginRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                LoginRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.username = "";
                        object.password = "";
                        object.deviceId = "";
                    }
                    if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                        object.username = message.username;
                    if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                        object.password = message.password;
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        object.deviceId = message.deviceId;
                    return object;
                };

                /**
                 * Converts this LoginRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.LoginRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                LoginRequest.prototype.toJSON = function() {
                    return LoginRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for LoginRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.LoginRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                LoginRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.LoginRequest";
                };

                return LoginRequest;
            })();

            v1.LoginReply = (function() {

                /**
                 * Properties of a LoginReply.
                 * @typedef {Object} api.im.v1.LoginReply.$Properties
                 * @property {number|Long|null} [userId] LoginReply userId
                 * @property {string|null} [token] LoginReply token
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a LoginReply.
                 * @memberof api.im.v1
                 * @interface ILoginReply
                 * @augments api.im.v1.LoginReply.$Properties
                 * @deprecated Use api.im.v1.LoginReply.$Properties instead.
                 */

                /**
                 * Shape of a LoginReply.
                 * @typedef {api.im.v1.LoginReply.$Properties} api.im.v1.LoginReply.$Shape
                 */

                /**
                 * Constructs a new LoginReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a LoginReply.
                 * @constructor
                 * @param {api.im.v1.LoginReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const LoginReply = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * LoginReply userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.LoginReply
                 * @instance
                 */
                LoginReply.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * LoginReply token.
                 * @member {string} token
                 * @memberof api.im.v1.LoginReply
                 * @instance
                 */
                LoginReply.prototype.token = "";

                /**
                 * Creates a new LoginReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {api.im.v1.LoginReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.LoginReply} LoginReply instance
                 * @type {{
                 *   (properties: api.im.v1.LoginReply.$Shape): api.im.v1.LoginReply & api.im.v1.LoginReply.$Shape;
                 *   (properties?: api.im.v1.LoginReply.$Properties): api.im.v1.LoginReply;
                 * }}
                 */
                LoginReply.create = function(properties) {
                    return new LoginReply(properties);
                };

                /**
                 * Encodes the specified LoginReply message. Does not implicitly {@link api.im.v1.LoginReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {api.im.v1.LoginReply.$Properties} message LoginReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                LoginReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified LoginReply message, length delimited. Does not implicitly {@link api.im.v1.LoginReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {api.im.v1.LoginReply.$Properties} message LoginReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                LoginReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a LoginReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.LoginReply & api.im.v1.LoginReply.$Shape} LoginReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                LoginReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.LoginReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.token = value;
                                else
                                    delete message.token;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a LoginReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.LoginReply & api.im.v1.LoginReply.$Shape} LoginReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                LoginReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a LoginReply message.
                 * @function verify
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                LoginReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        if (!$util.isString(message.token))
                            return "token: string expected";
                    return null;
                };

                /**
                 * Creates a LoginReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.LoginReply} LoginReply
                 */
                LoginReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.LoginReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.LoginReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.LoginReply();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    if (object.token != null)
                        if (typeof object.token !== "string" || object.token.length)
                            message.token = $String(object.token);
                    return message;
                };

                /**
                 * Creates a plain object from a LoginReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {api.im.v1.LoginReply} message LoginReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                LoginReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.token = "";
                    }
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        object.token = message.token;
                    return object;
                };

                /**
                 * Converts this LoginReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.LoginReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                LoginReply.prototype.toJSON = function() {
                    return LoginReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for LoginReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.LoginReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                LoginReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.LoginReply";
                };

                return LoginReply;
            })();

            v1.DispatchService = (function() {

                /**
                 * Constructs a new DispatchService service.
                 * @memberof api.im.v1
                 * @classdesc Represents a DispatchService
                 * @extends $protobuf.rpc.Service
                 * @constructor
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 */
                const DispatchService = function(rpcImpl, requestDelimited, responseDelimited) {
                    $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
                };

                (DispatchService.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = DispatchService;

                /**
                 * Creates new DispatchService service using the specified rpc implementation.
                 * @function create
                 * @memberof api.im.v1.DispatchService
                 * @static
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 * @returns {DispatchService} RPC service. Useful where requests and/or responses are streamed.
                 */
                DispatchService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                    return new this(rpcImpl, requestDelimited, responseDelimited);
                };

                /**
                 * Callback as used by {@link api.im.v1.DispatchService#gateway}.
                 * @memberof api.im.v1.DispatchService
                 * @typedef GatewayCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.GetGatewayReply} [response] GetGatewayReply
                 */

                /**
                 * Calls Gateway.
                 * @memberof api.im.v1.DispatchService
                 * @typedef Gateway
                 * @type {{
                 *   (request: api.im.v1.IGetGatewayRequest, callback: api.im.v1.DispatchService.GatewayCallback): void;
                 *   (request: api.im.v1.IGetGatewayRequest): Promise<api.im.v1.GetGatewayReply>;
                 *   readonly name: "Gateway";
                 *   readonly path: "/api.im.v1.DispatchService/Gateway";
                 *   readonly requestType: "GetGatewayRequest";
                 *   readonly responseType: "GetGatewayReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls Gateway.
                 * @name api.im.v1.DispatchService#gateway
                 * @type {api.im.v1.DispatchService.Gateway}
                 */
                $Object.defineProperties(DispatchService.prototype.gateway = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, DispatchService.prototype.gateway, $root.api.im.v1.GetGatewayRequest, $root.api.im.v1.GetGatewayReply, request, callback);
                }, {
                    name: { value: "Gateway" },
                    path: { value: "/api.im.v1.DispatchService/Gateway" },
                    requestType: { value: "GetGatewayRequest" },
                    responseType: { value: "GetGatewayReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                return DispatchService;
            })();

            v1.GetGatewayRequest = (function() {

                /**
                 * Properties of a GetGatewayRequest.
                 * @typedef {Object} api.im.v1.GetGatewayRequest.$Properties
                 * @property {number|Long|null} [userId] GetGatewayRequest userId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a GetGatewayRequest.
                 * @memberof api.im.v1
                 * @interface IGetGatewayRequest
                 * @augments api.im.v1.GetGatewayRequest.$Properties
                 * @deprecated Use api.im.v1.GetGatewayRequest.$Properties instead.
                 */

                /**
                 * Shape of a GetGatewayRequest.
                 * @typedef {api.im.v1.GetGatewayRequest.$Properties} api.im.v1.GetGatewayRequest.$Shape
                 */

                /**
                 * Constructs a new GetGatewayRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a GetGatewayRequest.
                 * @constructor
                 * @param {api.im.v1.GetGatewayRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const GetGatewayRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * GetGatewayRequest userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.GetGatewayRequest
                 * @instance
                 */
                GetGatewayRequest.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * Creates a new GetGatewayRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {api.im.v1.GetGatewayRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.GetGatewayRequest} GetGatewayRequest instance
                 * @type {{
                 *   (properties: api.im.v1.GetGatewayRequest.$Shape): api.im.v1.GetGatewayRequest & api.im.v1.GetGatewayRequest.$Shape;
                 *   (properties?: api.im.v1.GetGatewayRequest.$Properties): api.im.v1.GetGatewayRequest;
                 * }}
                 */
                GetGatewayRequest.create = function(properties) {
                    return new GetGatewayRequest(properties);
                };

                /**
                 * Encodes the specified GetGatewayRequest message. Does not implicitly {@link api.im.v1.GetGatewayRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {api.im.v1.GetGatewayRequest.$Properties} message GetGatewayRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GetGatewayRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified GetGatewayRequest message, length delimited. Does not implicitly {@link api.im.v1.GetGatewayRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {api.im.v1.GetGatewayRequest.$Properties} message GetGatewayRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GetGatewayRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a GetGatewayRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.GetGatewayRequest & api.im.v1.GetGatewayRequest.$Shape} GetGatewayRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GetGatewayRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.GetGatewayRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a GetGatewayRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.GetGatewayRequest & api.im.v1.GetGatewayRequest.$Shape} GetGatewayRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GetGatewayRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a GetGatewayRequest message.
                 * @function verify
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                GetGatewayRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a GetGatewayRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.GetGatewayRequest} GetGatewayRequest
                 */
                GetGatewayRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.GetGatewayRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.GetGatewayRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.GetGatewayRequest();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a GetGatewayRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {api.im.v1.GetGatewayRequest} message GetGatewayRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                GetGatewayRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults)
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    return object;
                };

                /**
                 * Converts this GetGatewayRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.GetGatewayRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                GetGatewayRequest.prototype.toJSON = function() {
                    return GetGatewayRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for GetGatewayRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.GetGatewayRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                GetGatewayRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.GetGatewayRequest";
                };

                return GetGatewayRequest;
            })();

            v1.GetGatewayReply = (function() {

                /**
                 * Properties of a GetGatewayReply.
                 * @typedef {Object} api.im.v1.GetGatewayReply.$Properties
                 * @property {string|null} [gatewayUrl] GetGatewayReply gatewayUrl
                 * @property {Array.<string>|null} [gatewayUrls] GetGatewayReply gatewayUrls
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a GetGatewayReply.
                 * @memberof api.im.v1
                 * @interface IGetGatewayReply
                 * @augments api.im.v1.GetGatewayReply.$Properties
                 * @deprecated Use api.im.v1.GetGatewayReply.$Properties instead.
                 */

                /**
                 * Shape of a GetGatewayReply.
                 * @typedef {api.im.v1.GetGatewayReply.$Properties} api.im.v1.GetGatewayReply.$Shape
                 */

                /**
                 * Constructs a new GetGatewayReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a GetGatewayReply.
                 * @constructor
                 * @param {api.im.v1.GetGatewayReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const GetGatewayReply = function (properties) {
                    this.gatewayUrls = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * GetGatewayReply gatewayUrl.
                 * @member {string} gatewayUrl
                 * @memberof api.im.v1.GetGatewayReply
                 * @instance
                 */
                GetGatewayReply.prototype.gatewayUrl = "";

                /**
                 * GetGatewayReply gatewayUrls.
                 * @member {Array.<string>} gatewayUrls
                 * @memberof api.im.v1.GetGatewayReply
                 * @instance
                 */
                GetGatewayReply.prototype.gatewayUrls = $util.emptyArray;

                /**
                 * Creates a new GetGatewayReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {api.im.v1.GetGatewayReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.GetGatewayReply} GetGatewayReply instance
                 * @type {{
                 *   (properties: api.im.v1.GetGatewayReply.$Shape): api.im.v1.GetGatewayReply & api.im.v1.GetGatewayReply.$Shape;
                 *   (properties?: api.im.v1.GetGatewayReply.$Properties): api.im.v1.GetGatewayReply;
                 * }}
                 */
                GetGatewayReply.create = function(properties) {
                    return new GetGatewayReply(properties);
                };

                /**
                 * Encodes the specified GetGatewayReply message. Does not implicitly {@link api.im.v1.GetGatewayReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {api.im.v1.GetGatewayReply.$Properties} message GetGatewayReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GetGatewayReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.gatewayUrl != null && $Object.hasOwnProperty.call(message, "gatewayUrl"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.gatewayUrl);
                    if (message.gatewayUrls != null && message.gatewayUrls.length)
                        for (let i = 0; i < message.gatewayUrls.length; ++i)
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.gatewayUrls[i]);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified GetGatewayReply message, length delimited. Does not implicitly {@link api.im.v1.GetGatewayReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {api.im.v1.GetGatewayReply.$Properties} message GetGatewayReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GetGatewayReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a GetGatewayReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.GetGatewayReply & api.im.v1.GetGatewayReply.$Shape} GetGatewayReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GetGatewayReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.GetGatewayReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.gatewayUrl = value;
                                else
                                    delete message.gatewayUrl;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.gatewayUrls && message.gatewayUrls.length))
                                    message.gatewayUrls = [];
                                message.gatewayUrls.push(reader.stringVerify());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a GetGatewayReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.GetGatewayReply & api.im.v1.GetGatewayReply.$Shape} GetGatewayReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GetGatewayReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a GetGatewayReply message.
                 * @function verify
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                GetGatewayReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.gatewayUrl != null && $Object.hasOwnProperty.call(message, "gatewayUrl"))
                        if (!$util.isString(message.gatewayUrl))
                            return "gatewayUrl: string expected";
                    if (message.gatewayUrls != null && $Object.hasOwnProperty.call(message, "gatewayUrls")) {
                        if (!$Array.isArray(message.gatewayUrls))
                            return "gatewayUrls: array expected";
                        for (let i = 0; i < message.gatewayUrls.length; ++i)
                            if (!$util.isString(message.gatewayUrls[i]))
                                return "gatewayUrls: string[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a GetGatewayReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.GetGatewayReply} GetGatewayReply
                 */
                GetGatewayReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.GetGatewayReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.GetGatewayReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.GetGatewayReply();
                    if (object.gatewayUrl != null)
                        if (typeof object.gatewayUrl !== "string" || object.gatewayUrl.length)
                            message.gatewayUrl = $String(object.gatewayUrl);
                    if (object.gatewayUrls) {
                        if (!$Array.isArray(object.gatewayUrls))
                            throw $TypeError(".api.im.v1.GetGatewayReply.gatewayUrls: array expected");
                        message.gatewayUrls = $Array(object.gatewayUrls.length);
                        for (let i = 0; i < object.gatewayUrls.length; ++i)
                            message.gatewayUrls[i] = $String(object.gatewayUrls[i]);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a GetGatewayReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {api.im.v1.GetGatewayReply} message GetGatewayReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                GetGatewayReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.gatewayUrls = [];
                    if (options.defaults)
                        object.gatewayUrl = "";
                    if (message.gatewayUrl != null && $Object.hasOwnProperty.call(message, "gatewayUrl"))
                        object.gatewayUrl = message.gatewayUrl;
                    if (message.gatewayUrls && message.gatewayUrls.length) {
                        object.gatewayUrls = $Array(message.gatewayUrls.length);
                        for (let j = 0; j < message.gatewayUrls.length; ++j)
                            object.gatewayUrls[j] = message.gatewayUrls[j];
                    }
                    return object;
                };

                /**
                 * Converts this GetGatewayReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.GetGatewayReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                GetGatewayReply.prototype.toJSON = function() {
                    return GetGatewayReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for GetGatewayReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.GetGatewayReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                GetGatewayReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.GetGatewayReply";
                };

                return GetGatewayReply;
            })();

            v1.MessageService = (function() {

                /**
                 * Constructs a new MessageService service.
                 * @memberof api.im.v1
                 * @classdesc Represents a MessageService
                 * @extends $protobuf.rpc.Service
                 * @constructor
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 */
                const MessageService = function(rpcImpl, requestDelimited, responseDelimited) {
                    $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
                };

                (MessageService.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = MessageService;

                /**
                 * Creates new MessageService service using the specified rpc implementation.
                 * @function create
                 * @memberof api.im.v1.MessageService
                 * @static
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 * @returns {MessageService} RPC service. Useful where requests and/or responses are streamed.
                 */
                MessageService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                    return new this(rpcImpl, requestDelimited, responseDelimited);
                };

                /**
                 * Callback as used by {@link api.im.v1.MessageService#sendMessage}.
                 * @memberof api.im.v1.MessageService
                 * @typedef SendMessageCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.SendMessageReply} [response] SendMessageReply
                 */

                /**
                 * Calls SendMessage.
                 * @memberof api.im.v1.MessageService
                 * @typedef SendMessage
                 * @type {{
                 *   (request: api.im.v1.ISendMessageRequest, callback: api.im.v1.MessageService.SendMessageCallback): void;
                 *   (request: api.im.v1.ISendMessageRequest): Promise<api.im.v1.SendMessageReply>;
                 *   readonly name: "SendMessage";
                 *   readonly path: "/api.im.v1.MessageService/SendMessage";
                 *   readonly requestType: "SendMessageRequest";
                 *   readonly responseType: "SendMessageReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls SendMessage.
                 * @name api.im.v1.MessageService#sendMessage
                 * @type {api.im.v1.MessageService.SendMessage}
                 */
                $Object.defineProperties(MessageService.prototype.sendMessage = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, MessageService.prototype.sendMessage, $root.api.im.v1.SendMessageRequest, $root.api.im.v1.SendMessageReply, request, callback);
                }, {
                    name: { value: "SendMessage" },
                    path: { value: "/api.im.v1.MessageService/SendMessage" },
                    requestType: { value: "SendMessageRequest" },
                    responseType: { value: "SendMessageReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.MessageService#pullMessages}.
                 * @memberof api.im.v1.MessageService
                 * @typedef PullMessagesCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.PullReply} [response] PullReply
                 */

                /**
                 * Calls PullMessages.
                 * @memberof api.im.v1.MessageService
                 * @typedef PullMessages
                 * @type {{
                 *   (request: api.im.v1.IPullRequest, callback: api.im.v1.MessageService.PullMessagesCallback): void;
                 *   (request: api.im.v1.IPullRequest): Promise<api.im.v1.PullReply>;
                 *   readonly name: "PullMessages";
                 *   readonly path: "/api.im.v1.MessageService/PullMessages";
                 *   readonly requestType: "PullRequest";
                 *   readonly responseType: "PullReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls PullMessages.
                 * @name api.im.v1.MessageService#pullMessages
                 * @type {api.im.v1.MessageService.PullMessages}
                 */
                $Object.defineProperties(MessageService.prototype.pullMessages = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, MessageService.prototype.pullMessages, $root.api.im.v1.PullRequest, $root.api.im.v1.PullReply, request, callback);
                }, {
                    name: { value: "PullMessages" },
                    path: { value: "/api.im.v1.MessageService/PullMessages" },
                    requestType: { value: "PullRequest" },
                    responseType: { value: "PullReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                return MessageService;
            })();

            /**
             * MsgType enum.
             * @name api.im.v1.MsgType
             * @enum {number}
             * @property {number} MSG_TYPE_UNSPECIFIED=0 MSG_TYPE_UNSPECIFIED value
             * @property {number} MSG_TYPE_TEXT=1 MSG_TYPE_TEXT value
             * @property {number} MSG_TYPE_IMAGE=2 MSG_TYPE_IMAGE value
             * @property {number} MSG_TYPE_FILE=3 MSG_TYPE_FILE value
             * @property {number} MSG_TYPE_VOICE=4 MSG_TYPE_VOICE value
             */
            v1.MsgType = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "MSG_TYPE_UNSPECIFIED"] = 0;
                values[valuesById[1] = "MSG_TYPE_TEXT"] = 1;
                values[valuesById[2] = "MSG_TYPE_IMAGE"] = 2;
                values[valuesById[3] = "MSG_TYPE_FILE"] = 3;
                values[valuesById[4] = "MSG_TYPE_VOICE"] = 4;
                return values;
            })();

            v1.SendMessageRequest = (function() {

                /**
                 * Properties of a SendMessageRequest.
                 * @typedef {Object} api.im.v1.SendMessageRequest.$Properties
                 * @property {string|null} [topic] SendMessageRequest topic
                 * @property {api.im.v1.MsgType|null} [msgType] SendMessageRequest msgType
                 * @property {Uint8Array|null} [content] SendMessageRequest content
                 * @property {string|null} [clientMsgId] SendMessageRequest clientMsgId
                 * @property {Array.<number|Long>|null} [mentionedUserIds] SendMessageRequest mentionedUserIds
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a SendMessageRequest.
                 * @memberof api.im.v1
                 * @interface ISendMessageRequest
                 * @augments api.im.v1.SendMessageRequest.$Properties
                 * @deprecated Use api.im.v1.SendMessageRequest.$Properties instead.
                 */

                /**
                 * Shape of a SendMessageRequest.
                 * @typedef {api.im.v1.SendMessageRequest.$Properties} api.im.v1.SendMessageRequest.$Shape
                 */

                /**
                 * Constructs a new SendMessageRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a SendMessageRequest.
                 * @constructor
                 * @param {api.im.v1.SendMessageRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const SendMessageRequest = function (properties) {
                    this.mentionedUserIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * SendMessageRequest topic.
                 * @member {string} topic
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 */
                SendMessageRequest.prototype.topic = "";

                /**
                 * SendMessageRequest msgType.
                 * @member {api.im.v1.MsgType} msgType
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 */
                SendMessageRequest.prototype.msgType = 0;

                /**
                 * SendMessageRequest content.
                 * @member {Uint8Array} content
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 */
                SendMessageRequest.prototype.content = $util.newBuffer([]);

                /**
                 * SendMessageRequest clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 */
                SendMessageRequest.prototype.clientMsgId = "";

                /**
                 * SendMessageRequest mentionedUserIds.
                 * @member {Array.<number|Long>} mentionedUserIds
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 */
                SendMessageRequest.prototype.mentionedUserIds = $util.emptyArray;

                /**
                 * Creates a new SendMessageRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {api.im.v1.SendMessageRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.SendMessageRequest} SendMessageRequest instance
                 * @type {{
                 *   (properties: api.im.v1.SendMessageRequest.$Shape): api.im.v1.SendMessageRequest & api.im.v1.SendMessageRequest.$Shape;
                 *   (properties?: api.im.v1.SendMessageRequest.$Properties): api.im.v1.SendMessageRequest;
                 * }}
                 */
                SendMessageRequest.create = function(properties) {
                    return new SendMessageRequest(properties);
                };

                /**
                 * Encodes the specified SendMessageRequest message. Does not implicitly {@link api.im.v1.SendMessageRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {api.im.v1.SendMessageRequest.$Properties} message SendMessageRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendMessageRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.topic);
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.msgType);
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.content);
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.clientMsgId);
                    if (message.mentionedUserIds != null && message.mentionedUserIds.length) {
                        writer.uint32(/* id 5, wireType 2 =*/42).fork();
                        for (let i = 0; i < message.mentionedUserIds.length; ++i)
                            writer.int64(message.mentionedUserIds[i]);
                        writer.ldelim();
                    }
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SendMessageRequest message, length delimited. Does not implicitly {@link api.im.v1.SendMessageRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {api.im.v1.SendMessageRequest.$Properties} message SendMessageRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendMessageRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SendMessageRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.SendMessageRequest & api.im.v1.SendMessageRequest.$Shape} SendMessageRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendMessageRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.SendMessageRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.msgType = value;
                                else
                                    delete message.msgType;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.bytes()).length)
                                    message.content = value;
                                else
                                    delete message.content;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        case 5: {
                                if (wireType === 2) {
                                    if (!(message.mentionedUserIds && message.mentionedUserIds.length))
                                        message.mentionedUserIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.mentionedUserIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.mentionedUserIds && message.mentionedUserIds.length))
                                    message.mentionedUserIds = [];
                                message.mentionedUserIds.push(reader.int64());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SendMessageRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.SendMessageRequest & api.im.v1.SendMessageRequest.$Shape} SendMessageRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendMessageRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SendMessageRequest message.
                 * @function verify
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SendMessageRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        switch (message.msgType) {
                        default:
                            return "msgType: enum value expected";
                        case 0:
                        case 1:
                        case 2:
                        case 3:
                        case 4:
                            break;
                        }
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                            return "content: buffer expected";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    if (message.mentionedUserIds != null && $Object.hasOwnProperty.call(message, "mentionedUserIds")) {
                        if (!$Array.isArray(message.mentionedUserIds))
                            return "mentionedUserIds: array expected";
                        for (let i = 0; i < message.mentionedUserIds.length; ++i)
                            if (!$util.isInteger(message.mentionedUserIds[i]) && !(message.mentionedUserIds[i] && $util.isInteger(message.mentionedUserIds[i].low) && $util.isInteger(message.mentionedUserIds[i].high)))
                                return "mentionedUserIds: integer|Long[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a SendMessageRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.SendMessageRequest} SendMessageRequest
                 */
                SendMessageRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.SendMessageRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.SendMessageRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.SendMessageRequest();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.msgType !== 0 && (typeof object.msgType !== "string" || $root.api.im.v1.MsgType[object.msgType] !== 0))
                        switch (object.msgType) {
                        default:
                            if (typeof object.msgType === "number") {
                                message.msgType = object.msgType;
                                break;
                            }
                            break;
                        case "MSG_TYPE_UNSPECIFIED":
                        case 0:
                            message.msgType = 0;
                            break;
                        case "MSG_TYPE_TEXT":
                        case 1:
                            message.msgType = 1;
                            break;
                        case "MSG_TYPE_IMAGE":
                        case 2:
                            message.msgType = 2;
                            break;
                        case "MSG_TYPE_FILE":
                        case 3:
                            message.msgType = 3;
                            break;
                        case "MSG_TYPE_VOICE":
                        case 4:
                            message.msgType = 4;
                            break;
                        }
                    if (object.content != null)
                        if (object.content.length)
                            if (typeof object.content === "string")
                                $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                            else if (object.content.length >= 0)
                                message.content = object.content;
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    if (object.mentionedUserIds) {
                        if (!$Array.isArray(object.mentionedUserIds))
                            throw $TypeError(".api.im.v1.SendMessageRequest.mentionedUserIds: array expected");
                        message.mentionedUserIds = $Array(object.mentionedUserIds.length);
                        for (let i = 0; i < object.mentionedUserIds.length; ++i)
                            if ($util.Long)
                                message.mentionedUserIds[i] = $util.Long.fromValue(object.mentionedUserIds[i], false);
                            else if (typeof object.mentionedUserIds[i] === "string")
                                message.mentionedUserIds[i] = $parseInt(object.mentionedUserIds[i], 10);
                            else if (typeof object.mentionedUserIds[i] === "number")
                                message.mentionedUserIds[i] = object.mentionedUserIds[i];
                            else if (typeof object.mentionedUserIds[i] === "object")
                                message.mentionedUserIds[i] = new $util.LongBits(object.mentionedUserIds[i].low >>> 0, object.mentionedUserIds[i].high >>> 0).toNumber();
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a SendMessageRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {api.im.v1.SendMessageRequest} message SendMessageRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SendMessageRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.mentionedUserIds = [];
                    if (options.defaults) {
                        object.topic = "";
                        object.msgType = options.enums === $String ? "MSG_TYPE_UNSPECIFIED" : 0;
                        if (options.bytes === $String)
                            object.content = "";
                        else {
                            object.content = [];
                            if (options.bytes !== $Array)
                                object.content = $util.newBuffer(object.content);
                        }
                        object.clientMsgId = "";
                    }
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        object.msgType = options.enums === $String ? $root.api.im.v1.MsgType[message.msgType] === $undefined ? message.msgType : $root.api.im.v1.MsgType[message.msgType] : message.msgType;
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    if (message.mentionedUserIds && message.mentionedUserIds.length) {
                        object.mentionedUserIds = $Array(message.mentionedUserIds.length);
                        for (let j = 0; j < message.mentionedUserIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.mentionedUserIds[j] = typeof message.mentionedUserIds[j] === "number" ? $BigInt(message.mentionedUserIds[j]) : $util.Long.fromBits(message.mentionedUserIds[j].low >>> 0, message.mentionedUserIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.mentionedUserIds[j] === "number")
                                object.mentionedUserIds[j] = options.longs === $String ? $String(message.mentionedUserIds[j]) : message.mentionedUserIds[j];
                            else
                                object.mentionedUserIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.mentionedUserIds[j]) : options.longs === $Number ? new $util.LongBits(message.mentionedUserIds[j].low >>> 0, message.mentionedUserIds[j].high >>> 0).toNumber() : message.mentionedUserIds[j];
                    }
                    return object;
                };

                /**
                 * Converts this SendMessageRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.SendMessageRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SendMessageRequest.prototype.toJSON = function() {
                    return SendMessageRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for SendMessageRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.SendMessageRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                SendMessageRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.SendMessageRequest";
                };

                return SendMessageRequest;
            })();

            v1.SendMessageReply = (function() {

                /**
                 * Properties of a SendMessageReply.
                 * @typedef {Object} api.im.v1.SendMessageReply.$Properties
                 * @property {string|null} [clientMsgId] SendMessageReply clientMsgId
                 * @property {number|Long|null} [msgId] SendMessageReply msgId
                 * @property {number|Long|null} [timestamp] SendMessageReply timestamp
                 * @property {number|Long|null} [topicSeq] SendMessageReply topicSeq
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a SendMessageReply.
                 * @memberof api.im.v1
                 * @interface ISendMessageReply
                 * @augments api.im.v1.SendMessageReply.$Properties
                 * @deprecated Use api.im.v1.SendMessageReply.$Properties instead.
                 */

                /**
                 * Shape of a SendMessageReply.
                 * @typedef {api.im.v1.SendMessageReply.$Properties} api.im.v1.SendMessageReply.$Shape
                 */

                /**
                 * Constructs a new SendMessageReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a SendMessageReply.
                 * @constructor
                 * @param {api.im.v1.SendMessageReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const SendMessageReply = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * SendMessageReply clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.SendMessageReply
                 * @instance
                 */
                SendMessageReply.prototype.clientMsgId = "";

                /**
                 * SendMessageReply msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.SendMessageReply
                 * @instance
                 */
                SendMessageReply.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * SendMessageReply timestamp.
                 * @member {number|Long} timestamp
                 * @memberof api.im.v1.SendMessageReply
                 * @instance
                 */
                SendMessageReply.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * SendMessageReply topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.SendMessageReply
                 * @instance
                 */
                SendMessageReply.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * Creates a new SendMessageReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {api.im.v1.SendMessageReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.SendMessageReply} SendMessageReply instance
                 * @type {{
                 *   (properties: api.im.v1.SendMessageReply.$Shape): api.im.v1.SendMessageReply & api.im.v1.SendMessageReply.$Shape;
                 *   (properties?: api.im.v1.SendMessageReply.$Properties): api.im.v1.SendMessageReply;
                 * }}
                 */
                SendMessageReply.create = function(properties) {
                    return new SendMessageReply(properties);
                };

                /**
                 * Encodes the specified SendMessageReply message. Does not implicitly {@link api.im.v1.SendMessageReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {api.im.v1.SendMessageReply.$Properties} message SendMessageReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendMessageReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientMsgId);
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int64(message.msgId);
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.topicSeq);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SendMessageReply message, length delimited. Does not implicitly {@link api.im.v1.SendMessageReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {api.im.v1.SendMessageReply.$Properties} message SendMessageReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendMessageReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SendMessageReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.SendMessageReply & api.im.v1.SendMessageReply.$Shape} SendMessageReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendMessageReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.SendMessageReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.timestamp = value;
                                else
                                    delete message.timestamp;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SendMessageReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.SendMessageReply & api.im.v1.SendMessageReply.$Shape} SendMessageReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendMessageReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SendMessageReply message.
                 * @function verify
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SendMessageReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a SendMessageReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.SendMessageReply} SendMessageReply
                 */
                SendMessageReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.SendMessageReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.SendMessageReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.SendMessageReply();
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    if (object.timestamp != null)
                        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                            if ($util.Long)
                                message.timestamp = $util.Long.fromValue(object.timestamp, false);
                            else if (typeof object.timestamp === "string")
                                message.timestamp = $parseInt(object.timestamp, 10);
                            else if (typeof object.timestamp === "number")
                                message.timestamp = object.timestamp;
                            else if (typeof object.timestamp === "object")
                                message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    return message;
                };

                /**
                 * Creates a plain object from a SendMessageReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {api.im.v1.SendMessageReply} message SendMessageReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SendMessageReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.clientMsgId = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    return object;
                };

                /**
                 * Converts this SendMessageReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.SendMessageReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SendMessageReply.prototype.toJSON = function() {
                    return SendMessageReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for SendMessageReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.SendMessageReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                SendMessageReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.SendMessageReply";
                };

                return SendMessageReply;
            })();

            v1.MessagePush = (function() {

                /**
                 * Properties of a MessagePush.
                 * @typedef {Object} api.im.v1.MessagePush.$Properties
                 * @property {number|Long|null} [msgId] MessagePush msgId
                 * @property {string|null} [topic] MessagePush topic
                 * @property {number|Long|null} [senderId] MessagePush senderId
                 * @property {number|null} [msgType] MessagePush msgType
                 * @property {Uint8Array|null} [content] MessagePush content
                 * @property {number|Long|null} [timestamp] MessagePush timestamp
                 * @property {number|Long|null} [topicSeq] MessagePush topicSeq
                 * @property {string|null} [clientMsgId] MessagePush clientMsgId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a MessagePush.
                 * @memberof api.im.v1
                 * @interface IMessagePush
                 * @augments api.im.v1.MessagePush.$Properties
                 * @deprecated Use api.im.v1.MessagePush.$Properties instead.
                 */

                /**
                 * Shape of a MessagePush.
                 * @typedef {api.im.v1.MessagePush.$Properties} api.im.v1.MessagePush.$Shape
                 */

                /**
                 * Constructs a new MessagePush.
                 * @memberof api.im.v1
                 * @classdesc Represents a MessagePush.
                 * @constructor
                 * @param {api.im.v1.MessagePush.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const MessagePush = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * MessagePush msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * MessagePush topic.
                 * @member {string} topic
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.topic = "";

                /**
                 * MessagePush senderId.
                 * @member {number|Long} senderId
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.senderId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * MessagePush msgType.
                 * @member {number} msgType
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.msgType = 0;

                /**
                 * MessagePush content.
                 * @member {Uint8Array} content
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.content = $util.newBuffer([]);

                /**
                 * MessagePush timestamp.
                 * @member {number|Long} timestamp
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * MessagePush topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * MessagePush clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 */
                MessagePush.prototype.clientMsgId = "";

                /**
                 * Creates a new MessagePush instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {api.im.v1.MessagePush.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.MessagePush} MessagePush instance
                 * @type {{
                 *   (properties: api.im.v1.MessagePush.$Shape): api.im.v1.MessagePush & api.im.v1.MessagePush.$Shape;
                 *   (properties?: api.im.v1.MessagePush.$Properties): api.im.v1.MessagePush;
                 * }}
                 */
                MessagePush.create = function(properties) {
                    return new MessagePush(properties);
                };

                /**
                 * Encodes the specified MessagePush message. Does not implicitly {@link api.im.v1.MessagePush.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {api.im.v1.MessagePush.$Properties} message MessagePush message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                MessagePush.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.msgId);
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.topic);
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderId);
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int32(message.msgType);
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.content);
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 6, wireType 0 =*/48).int64(message.timestamp);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.topicSeq);
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.clientMsgId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified MessagePush message, length delimited. Does not implicitly {@link api.im.v1.MessagePush.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {api.im.v1.MessagePush.$Properties} message MessagePush message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                MessagePush.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a MessagePush message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.MessagePush & api.im.v1.MessagePush.$Shape} MessagePush
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                MessagePush.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.MessagePush(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.senderId = value;
                                else
                                    delete message.senderId;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.msgType = value;
                                else
                                    delete message.msgType;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.bytes()).length)
                                    message.content = value;
                                else
                                    delete message.content;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.timestamp = value;
                                else
                                    delete message.timestamp;
                                continue;
                            }
                        case 7: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        case 8: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a MessagePush message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.MessagePush & api.im.v1.MessagePush.$Shape} MessagePush
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                MessagePush.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a MessagePush message.
                 * @function verify
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                MessagePush.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (!$util.isInteger(message.senderId) && !(message.senderId && $util.isInteger(message.senderId.low) && $util.isInteger(message.senderId.high)))
                            return "senderId: integer|Long expected";
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        if (!$util.isInteger(message.msgType))
                            return "msgType: integer expected";
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                            return "content: buffer expected";
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    return null;
                };

                /**
                 * Creates a MessagePush message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.MessagePush} MessagePush
                 */
                MessagePush.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.MessagePush)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.MessagePush: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.MessagePush();
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.senderId != null)
                        if (typeof object.senderId === "object" ? object.senderId.low || object.senderId.high : $Number(object.senderId) !== 0)
                            if ($util.Long)
                                message.senderId = $util.Long.fromValue(object.senderId, false);
                            else if (typeof object.senderId === "string")
                                message.senderId = $parseInt(object.senderId, 10);
                            else if (typeof object.senderId === "number")
                                message.senderId = object.senderId;
                            else if (typeof object.senderId === "object")
                                message.senderId = new $util.LongBits(object.senderId.low >>> 0, object.senderId.high >>> 0).toNumber();
                    if (object.msgType != null)
                        if ($Number(object.msgType) !== 0)
                            message.msgType = object.msgType | 0;
                    if (object.content != null)
                        if (object.content.length)
                            if (typeof object.content === "string")
                                $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                            else if (object.content.length >= 0)
                                message.content = object.content;
                    if (object.timestamp != null)
                        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                            if ($util.Long)
                                message.timestamp = $util.Long.fromValue(object.timestamp, false);
                            else if (typeof object.timestamp === "string")
                                message.timestamp = $parseInt(object.timestamp, 10);
                            else if (typeof object.timestamp === "number")
                                message.timestamp = object.timestamp;
                            else if (typeof object.timestamp === "object")
                                message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    return message;
                };

                /**
                 * Creates a plain object from a MessagePush message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {api.im.v1.MessagePush} message MessagePush
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                MessagePush.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.senderId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.senderId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.msgType = 0;
                        if (options.bytes === $String)
                            object.content = "";
                        else {
                            object.content = [];
                            if (options.bytes !== $Array)
                                object.content = $util.newBuffer(object.content);
                        }
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.clientMsgId = "";
                    }
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.senderId = typeof message.senderId === "number" ? $BigInt(message.senderId) : $util.Long.fromBits(message.senderId.low >>> 0, message.senderId.high >>> 0, false).toBigInt();
                        else if (typeof message.senderId === "number")
                            object.senderId = options.longs === $String ? $String(message.senderId) : message.senderId;
                        else
                            object.senderId = options.longs === $String ? $util.Long.prototype.toString.call(message.senderId) : options.longs === $Number ? new $util.LongBits(message.senderId.low >>> 0, message.senderId.high >>> 0).toNumber() : message.senderId;
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        object.msgType = message.msgType;
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    return object;
                };

                /**
                 * Converts this MessagePush to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.MessagePush
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                MessagePush.prototype.toJSON = function() {
                    return MessagePush.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for MessagePush
                 * @function getTypeUrl
                 * @memberof api.im.v1.MessagePush
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                MessagePush.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.MessagePush";
                };

                return MessagePush;
            })();

            v1.UpstreamMessage = (function() {

                /**
                 * Properties of an UpstreamMessage.
                 * @typedef {Object} api.im.v1.UpstreamMessage.$Properties
                 * @property {number|Long|null} [senderId] UpstreamMessage senderId
                 * @property {string|null} [topic] UpstreamMessage topic
                 * @property {number|null} [msgType] UpstreamMessage msgType
                 * @property {Uint8Array|null} [content] UpstreamMessage content
                 * @property {string|null} [clientMsgId] UpstreamMessage clientMsgId
                 * @property {number|Long|null} [timestamp] UpstreamMessage timestamp
                 * @property {Array.<number|Long>|null} [mentionedUserIds] UpstreamMessage mentionedUserIds
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an UpstreamMessage.
                 * @memberof api.im.v1
                 * @interface IUpstreamMessage
                 * @augments api.im.v1.UpstreamMessage.$Properties
                 * @deprecated Use api.im.v1.UpstreamMessage.$Properties instead.
                 */

                /**
                 * Shape of an UpstreamMessage.
                 * @typedef {api.im.v1.UpstreamMessage.$Properties} api.im.v1.UpstreamMessage.$Shape
                 */

                /**
                 * Constructs a new UpstreamMessage.
                 * @memberof api.im.v1
                 * @classdesc Represents an UpstreamMessage.
                 * @constructor
                 * @param {api.im.v1.UpstreamMessage.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const UpstreamMessage = function (properties) {
                    this.mentionedUserIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * UpstreamMessage senderId.
                 * @member {number|Long} senderId
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.senderId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * UpstreamMessage topic.
                 * @member {string} topic
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.topic = "";

                /**
                 * UpstreamMessage msgType.
                 * @member {number} msgType
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.msgType = 0;

                /**
                 * UpstreamMessage content.
                 * @member {Uint8Array} content
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.content = $util.newBuffer([]);

                /**
                 * UpstreamMessage clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.clientMsgId = "";

                /**
                 * UpstreamMessage timestamp.
                 * @member {number|Long} timestamp
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * UpstreamMessage mentionedUserIds.
                 * @member {Array.<number|Long>} mentionedUserIds
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 */
                UpstreamMessage.prototype.mentionedUserIds = $util.emptyArray;

                /**
                 * Creates a new UpstreamMessage instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {api.im.v1.UpstreamMessage.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.UpstreamMessage} UpstreamMessage instance
                 * @type {{
                 *   (properties: api.im.v1.UpstreamMessage.$Shape): api.im.v1.UpstreamMessage & api.im.v1.UpstreamMessage.$Shape;
                 *   (properties?: api.im.v1.UpstreamMessage.$Properties): api.im.v1.UpstreamMessage;
                 * }}
                 */
                UpstreamMessage.create = function(properties) {
                    return new UpstreamMessage(properties);
                };

                /**
                 * Encodes the specified UpstreamMessage message. Does not implicitly {@link api.im.v1.UpstreamMessage.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {api.im.v1.UpstreamMessage.$Properties} message UpstreamMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                UpstreamMessage.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.senderId);
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.topic);
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.msgType);
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.content);
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.clientMsgId);
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 6, wireType 0 =*/48).int64(message.timestamp);
                    if (message.mentionedUserIds != null && message.mentionedUserIds.length) {
                        writer.uint32(/* id 7, wireType 2 =*/58).fork();
                        for (let i = 0; i < message.mentionedUserIds.length; ++i)
                            writer.int64(message.mentionedUserIds[i]);
                        writer.ldelim();
                    }
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified UpstreamMessage message, length delimited. Does not implicitly {@link api.im.v1.UpstreamMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {api.im.v1.UpstreamMessage.$Properties} message UpstreamMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                UpstreamMessage.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an UpstreamMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.UpstreamMessage & api.im.v1.UpstreamMessage.$Shape} UpstreamMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                UpstreamMessage.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.UpstreamMessage(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.senderId = value;
                                else
                                    delete message.senderId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.msgType = value;
                                else
                                    delete message.msgType;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.bytes()).length)
                                    message.content = value;
                                else
                                    delete message.content;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.timestamp = value;
                                else
                                    delete message.timestamp;
                                continue;
                            }
                        case 7: {
                                if (wireType === 2) {
                                    if (!(message.mentionedUserIds && message.mentionedUserIds.length))
                                        message.mentionedUserIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.mentionedUserIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.mentionedUserIds && message.mentionedUserIds.length))
                                    message.mentionedUserIds = [];
                                message.mentionedUserIds.push(reader.int64());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an UpstreamMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.UpstreamMessage & api.im.v1.UpstreamMessage.$Shape} UpstreamMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                UpstreamMessage.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an UpstreamMessage message.
                 * @function verify
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                UpstreamMessage.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (!$util.isInteger(message.senderId) && !(message.senderId && $util.isInteger(message.senderId.low) && $util.isInteger(message.senderId.high)))
                            return "senderId: integer|Long expected";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        if (!$util.isInteger(message.msgType))
                            return "msgType: integer expected";
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                            return "content: buffer expected";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    if (message.mentionedUserIds != null && $Object.hasOwnProperty.call(message, "mentionedUserIds")) {
                        if (!$Array.isArray(message.mentionedUserIds))
                            return "mentionedUserIds: array expected";
                        for (let i = 0; i < message.mentionedUserIds.length; ++i)
                            if (!$util.isInteger(message.mentionedUserIds[i]) && !(message.mentionedUserIds[i] && $util.isInteger(message.mentionedUserIds[i].low) && $util.isInteger(message.mentionedUserIds[i].high)))
                                return "mentionedUserIds: integer|Long[] expected";
                    }
                    return null;
                };

                /**
                 * Creates an UpstreamMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.UpstreamMessage} UpstreamMessage
                 */
                UpstreamMessage.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.UpstreamMessage)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.UpstreamMessage: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.UpstreamMessage();
                    if (object.senderId != null)
                        if (typeof object.senderId === "object" ? object.senderId.low || object.senderId.high : $Number(object.senderId) !== 0)
                            if ($util.Long)
                                message.senderId = $util.Long.fromValue(object.senderId, false);
                            else if (typeof object.senderId === "string")
                                message.senderId = $parseInt(object.senderId, 10);
                            else if (typeof object.senderId === "number")
                                message.senderId = object.senderId;
                            else if (typeof object.senderId === "object")
                                message.senderId = new $util.LongBits(object.senderId.low >>> 0, object.senderId.high >>> 0).toNumber();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.msgType != null)
                        if ($Number(object.msgType) !== 0)
                            message.msgType = object.msgType | 0;
                    if (object.content != null)
                        if (object.content.length)
                            if (typeof object.content === "string")
                                $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                            else if (object.content.length >= 0)
                                message.content = object.content;
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    if (object.timestamp != null)
                        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                            if ($util.Long)
                                message.timestamp = $util.Long.fromValue(object.timestamp, false);
                            else if (typeof object.timestamp === "string")
                                message.timestamp = $parseInt(object.timestamp, 10);
                            else if (typeof object.timestamp === "number")
                                message.timestamp = object.timestamp;
                            else if (typeof object.timestamp === "object")
                                message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    if (object.mentionedUserIds) {
                        if (!$Array.isArray(object.mentionedUserIds))
                            throw $TypeError(".api.im.v1.UpstreamMessage.mentionedUserIds: array expected");
                        message.mentionedUserIds = $Array(object.mentionedUserIds.length);
                        for (let i = 0; i < object.mentionedUserIds.length; ++i)
                            if ($util.Long)
                                message.mentionedUserIds[i] = $util.Long.fromValue(object.mentionedUserIds[i], false);
                            else if (typeof object.mentionedUserIds[i] === "string")
                                message.mentionedUserIds[i] = $parseInt(object.mentionedUserIds[i], 10);
                            else if (typeof object.mentionedUserIds[i] === "number")
                                message.mentionedUserIds[i] = object.mentionedUserIds[i];
                            else if (typeof object.mentionedUserIds[i] === "object")
                                message.mentionedUserIds[i] = new $util.LongBits(object.mentionedUserIds[i].low >>> 0, object.mentionedUserIds[i].high >>> 0).toNumber();
                    }
                    return message;
                };

                /**
                 * Creates a plain object from an UpstreamMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {api.im.v1.UpstreamMessage} message UpstreamMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                UpstreamMessage.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.mentionedUserIds = [];
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.senderId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.senderId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.topic = "";
                        object.msgType = 0;
                        if (options.bytes === $String)
                            object.content = "";
                        else {
                            object.content = [];
                            if (options.bytes !== $Array)
                                object.content = $util.newBuffer(object.content);
                        }
                        object.clientMsgId = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.senderId = typeof message.senderId === "number" ? $BigInt(message.senderId) : $util.Long.fromBits(message.senderId.low >>> 0, message.senderId.high >>> 0, false).toBigInt();
                        else if (typeof message.senderId === "number")
                            object.senderId = options.longs === $String ? $String(message.senderId) : message.senderId;
                        else
                            object.senderId = options.longs === $String ? $util.Long.prototype.toString.call(message.senderId) : options.longs === $Number ? new $util.LongBits(message.senderId.low >>> 0, message.senderId.high >>> 0).toNumber() : message.senderId;
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        object.msgType = message.msgType;
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    if (message.mentionedUserIds && message.mentionedUserIds.length) {
                        object.mentionedUserIds = $Array(message.mentionedUserIds.length);
                        for (let j = 0; j < message.mentionedUserIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.mentionedUserIds[j] = typeof message.mentionedUserIds[j] === "number" ? $BigInt(message.mentionedUserIds[j]) : $util.Long.fromBits(message.mentionedUserIds[j].low >>> 0, message.mentionedUserIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.mentionedUserIds[j] === "number")
                                object.mentionedUserIds[j] = options.longs === $String ? $String(message.mentionedUserIds[j]) : message.mentionedUserIds[j];
                            else
                                object.mentionedUserIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.mentionedUserIds[j]) : options.longs === $Number ? new $util.LongBits(message.mentionedUserIds[j].low >>> 0, message.mentionedUserIds[j].high >>> 0).toNumber() : message.mentionedUserIds[j];
                    }
                    return object;
                };

                /**
                 * Converts this UpstreamMessage to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.UpstreamMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                UpstreamMessage.prototype.toJSON = function() {
                    return UpstreamMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for UpstreamMessage
                 * @function getTypeUrl
                 * @memberof api.im.v1.UpstreamMessage
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                UpstreamMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.UpstreamMessage";
                };

                return UpstreamMessage;
            })();

            v1.PullRequest = (function() {

                /**
                 * Properties of a PullRequest.
                 * @typedef {Object} api.im.v1.PullRequest.$Properties
                 * @property {string|null} [topic] PullRequest topic
                 * @property {number|Long|null} [lastSeq] PullRequest lastSeq
                 * @property {number|null} [limit] PullRequest limit
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PullRequest.
                 * @memberof api.im.v1
                 * @interface IPullRequest
                 * @augments api.im.v1.PullRequest.$Properties
                 * @deprecated Use api.im.v1.PullRequest.$Properties instead.
                 */

                /**
                 * Shape of a PullRequest.
                 * @typedef {api.im.v1.PullRequest.$Properties} api.im.v1.PullRequest.$Shape
                 */

                /**
                 * Constructs a new PullRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a PullRequest.
                 * @constructor
                 * @param {api.im.v1.PullRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PullRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PullRequest topic.
                 * @member {string} topic
                 * @memberof api.im.v1.PullRequest
                 * @instance
                 */
                PullRequest.prototype.topic = "";

                /**
                 * PullRequest lastSeq.
                 * @member {number|Long} lastSeq
                 * @memberof api.im.v1.PullRequest
                 * @instance
                 */
                PullRequest.prototype.lastSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * PullRequest limit.
                 * @member {number} limit
                 * @memberof api.im.v1.PullRequest
                 * @instance
                 */
                PullRequest.prototype.limit = 0;

                /**
                 * Creates a new PullRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {api.im.v1.PullRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PullRequest} PullRequest instance
                 * @type {{
                 *   (properties: api.im.v1.PullRequest.$Shape): api.im.v1.PullRequest & api.im.v1.PullRequest.$Shape;
                 *   (properties?: api.im.v1.PullRequest.$Properties): api.im.v1.PullRequest;
                 * }}
                 */
                PullRequest.create = function(properties) {
                    return new PullRequest(properties);
                };

                /**
                 * Encodes the specified PullRequest message. Does not implicitly {@link api.im.v1.PullRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {api.im.v1.PullRequest.$Properties} message PullRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.topic);
                    if (message.lastSeq != null && $Object.hasOwnProperty.call(message, "lastSeq"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.lastSeq);
                    if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.limit);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PullRequest message, length delimited. Does not implicitly {@link api.im.v1.PullRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {api.im.v1.PullRequest.$Properties} message PullRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PullRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PullRequest & api.im.v1.PullRequest.$Shape} PullRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PullRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.lastSeq = value;
                                else
                                    delete message.lastSeq;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.limit = value;
                                else
                                    delete message.limit;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PullRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PullRequest & api.im.v1.PullRequest.$Shape} PullRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PullRequest message.
                 * @function verify
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PullRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.lastSeq != null && $Object.hasOwnProperty.call(message, "lastSeq"))
                        if (!$util.isInteger(message.lastSeq) && !(message.lastSeq && $util.isInteger(message.lastSeq.low) && $util.isInteger(message.lastSeq.high)))
                            return "lastSeq: integer|Long expected";
                    if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                        if (!$util.isInteger(message.limit))
                            return "limit: integer expected";
                    return null;
                };

                /**
                 * Creates a PullRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PullRequest} PullRequest
                 */
                PullRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PullRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PullRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PullRequest();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.lastSeq != null)
                        if (typeof object.lastSeq === "object" ? object.lastSeq.low || object.lastSeq.high : $Number(object.lastSeq) !== 0)
                            if ($util.Long)
                                message.lastSeq = $util.Long.fromValue(object.lastSeq, true);
                            else if (typeof object.lastSeq === "string")
                                message.lastSeq = $parseInt(object.lastSeq, 10);
                            else if (typeof object.lastSeq === "number")
                                message.lastSeq = object.lastSeq;
                            else if (typeof object.lastSeq === "object")
                                message.lastSeq = new $util.LongBits(object.lastSeq.low >>> 0, object.lastSeq.high >>> 0).toNumber(true);
                    if (object.limit != null)
                        if ($Number(object.limit) !== 0)
                            message.limit = object.limit | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a PullRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {api.im.v1.PullRequest} message PullRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PullRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.lastSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.lastSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.limit = 0;
                    }
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.lastSeq != null && $Object.hasOwnProperty.call(message, "lastSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.lastSeq = typeof message.lastSeq === "number" ? $BigInt(message.lastSeq) : $util.Long.fromBits(message.lastSeq.low >>> 0, message.lastSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.lastSeq === "number")
                            object.lastSeq = options.longs === $String ? $String(message.lastSeq) : message.lastSeq;
                        else
                            object.lastSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.lastSeq) : options.longs === $Number ? new $util.LongBits(message.lastSeq.low >>> 0, message.lastSeq.high >>> 0).toNumber(true) : message.lastSeq;
                    if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                        object.limit = message.limit;
                    return object;
                };

                /**
                 * Converts this PullRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PullRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PullRequest.prototype.toJSON = function() {
                    return PullRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PullRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.PullRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PullRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PullRequest";
                };

                return PullRequest;
            })();

            v1.PullMessage = (function() {

                /**
                 * Properties of a PullMessage.
                 * @typedef {Object} api.im.v1.PullMessage.$Properties
                 * @property {number|Long|null} [msgId] PullMessage msgId
                 * @property {string|null} [topic] PullMessage topic
                 * @property {number|Long|null} [senderId] PullMessage senderId
                 * @property {number|null} [msgType] PullMessage msgType
                 * @property {Uint8Array|null} [content] PullMessage content
                 * @property {number|Long|null} [timestamp] PullMessage timestamp
                 * @property {number|Long|null} [topicSeq] PullMessage topicSeq
                 * @property {string|null} [clientMsgId] PullMessage clientMsgId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PullMessage.
                 * @memberof api.im.v1
                 * @interface IPullMessage
                 * @augments api.im.v1.PullMessage.$Properties
                 * @deprecated Use api.im.v1.PullMessage.$Properties instead.
                 */

                /**
                 * Shape of a PullMessage.
                 * @typedef {api.im.v1.PullMessage.$Properties} api.im.v1.PullMessage.$Shape
                 */

                /**
                 * Constructs a new PullMessage.
                 * @memberof api.im.v1
                 * @classdesc Represents a PullMessage.
                 * @constructor
                 * @param {api.im.v1.PullMessage.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PullMessage = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PullMessage msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * PullMessage topic.
                 * @member {string} topic
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.topic = "";

                /**
                 * PullMessage senderId.
                 * @member {number|Long} senderId
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.senderId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * PullMessage msgType.
                 * @member {number} msgType
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.msgType = 0;

                /**
                 * PullMessage content.
                 * @member {Uint8Array} content
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.content = $util.newBuffer([]);

                /**
                 * PullMessage timestamp.
                 * @member {number|Long} timestamp
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * PullMessage topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * PullMessage clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 */
                PullMessage.prototype.clientMsgId = "";

                /**
                 * Creates a new PullMessage instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {api.im.v1.PullMessage.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PullMessage} PullMessage instance
                 * @type {{
                 *   (properties: api.im.v1.PullMessage.$Shape): api.im.v1.PullMessage & api.im.v1.PullMessage.$Shape;
                 *   (properties?: api.im.v1.PullMessage.$Properties): api.im.v1.PullMessage;
                 * }}
                 */
                PullMessage.create = function(properties) {
                    return new PullMessage(properties);
                };

                /**
                 * Encodes the specified PullMessage message. Does not implicitly {@link api.im.v1.PullMessage.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {api.im.v1.PullMessage.$Properties} message PullMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullMessage.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.msgId);
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.topic);
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderId);
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int32(message.msgType);
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.content);
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 6, wireType 0 =*/48).int64(message.timestamp);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.topicSeq);
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.clientMsgId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PullMessage message, length delimited. Does not implicitly {@link api.im.v1.PullMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {api.im.v1.PullMessage.$Properties} message PullMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullMessage.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PullMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PullMessage & api.im.v1.PullMessage.$Shape} PullMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullMessage.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PullMessage(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.senderId = value;
                                else
                                    delete message.senderId;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.msgType = value;
                                else
                                    delete message.msgType;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.bytes()).length)
                                    message.content = value;
                                else
                                    delete message.content;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.timestamp = value;
                                else
                                    delete message.timestamp;
                                continue;
                            }
                        case 7: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        case 8: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PullMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PullMessage & api.im.v1.PullMessage.$Shape} PullMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullMessage.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PullMessage message.
                 * @function verify
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PullMessage.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (!$util.isInteger(message.senderId) && !(message.senderId && $util.isInteger(message.senderId.low) && $util.isInteger(message.senderId.high)))
                            return "senderId: integer|Long expected";
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        if (!$util.isInteger(message.msgType))
                            return "msgType: integer expected";
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
                            return "content: buffer expected";
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    return null;
                };

                /**
                 * Creates a PullMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PullMessage} PullMessage
                 */
                PullMessage.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PullMessage)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PullMessage: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PullMessage();
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.senderId != null)
                        if (typeof object.senderId === "object" ? object.senderId.low || object.senderId.high : $Number(object.senderId) !== 0)
                            if ($util.Long)
                                message.senderId = $util.Long.fromValue(object.senderId, false);
                            else if (typeof object.senderId === "string")
                                message.senderId = $parseInt(object.senderId, 10);
                            else if (typeof object.senderId === "number")
                                message.senderId = object.senderId;
                            else if (typeof object.senderId === "object")
                                message.senderId = new $util.LongBits(object.senderId.low >>> 0, object.senderId.high >>> 0).toNumber();
                    if (object.msgType != null)
                        if ($Number(object.msgType) !== 0)
                            message.msgType = object.msgType | 0;
                    if (object.content != null)
                        if (object.content.length)
                            if (typeof object.content === "string")
                                $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                            else if (object.content.length >= 0)
                                message.content = object.content;
                    if (object.timestamp != null)
                        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                            if ($util.Long)
                                message.timestamp = $util.Long.fromValue(object.timestamp, false);
                            else if (typeof object.timestamp === "string")
                                message.timestamp = $parseInt(object.timestamp, 10);
                            else if (typeof object.timestamp === "number")
                                message.timestamp = object.timestamp;
                            else if (typeof object.timestamp === "object")
                                message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    return message;
                };

                /**
                 * Creates a plain object from a PullMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {api.im.v1.PullMessage} message PullMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PullMessage.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.senderId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.senderId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.msgType = 0;
                        if (options.bytes === $String)
                            object.content = "";
                        else {
                            object.content = [];
                            if (options.bytes !== $Array)
                                object.content = $util.newBuffer(object.content);
                        }
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.clientMsgId = "";
                    }
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.senderId = typeof message.senderId === "number" ? $BigInt(message.senderId) : $util.Long.fromBits(message.senderId.low >>> 0, message.senderId.high >>> 0, false).toBigInt();
                        else if (typeof message.senderId === "number")
                            object.senderId = options.longs === $String ? $String(message.senderId) : message.senderId;
                        else
                            object.senderId = options.longs === $String ? $util.Long.prototype.toString.call(message.senderId) : options.longs === $Number ? new $util.LongBits(message.senderId.low >>> 0, message.senderId.high >>> 0).toNumber() : message.senderId;
                    if (message.msgType != null && $Object.hasOwnProperty.call(message, "msgType"))
                        object.msgType = message.msgType;
                    if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                        object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    return object;
                };

                /**
                 * Converts this PullMessage to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PullMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PullMessage.prototype.toJSON = function() {
                    return PullMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PullMessage
                 * @function getTypeUrl
                 * @memberof api.im.v1.PullMessage
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PullMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PullMessage";
                };

                return PullMessage;
            })();

            v1.PullReply = (function() {

                /**
                 * Properties of a PullReply.
                 * @typedef {Object} api.im.v1.PullReply.$Properties
                 * @property {Array.<api.im.v1.PullMessage.$Properties>|null} [messages] PullReply messages
                 * @property {boolean|null} [hasMore] PullReply hasMore
                 * @property {number|Long|null} [nextSeq] PullReply nextSeq
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PullReply.
                 * @memberof api.im.v1
                 * @interface IPullReply
                 * @augments api.im.v1.PullReply.$Properties
                 * @deprecated Use api.im.v1.PullReply.$Properties instead.
                 */

                /**
                 * Shape of a PullReply.
                 * @typedef {api.im.v1.PullReply.$Properties} api.im.v1.PullReply.$Shape
                 */

                /**
                 * Constructs a new PullReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a PullReply.
                 * @constructor
                 * @param {api.im.v1.PullReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PullReply = function (properties) {
                    this.messages = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PullReply messages.
                 * @member {Array.<api.im.v1.PullMessage.$Properties>} messages
                 * @memberof api.im.v1.PullReply
                 * @instance
                 */
                PullReply.prototype.messages = $util.emptyArray;

                /**
                 * PullReply hasMore.
                 * @member {boolean} hasMore
                 * @memberof api.im.v1.PullReply
                 * @instance
                 */
                PullReply.prototype.hasMore = false;

                /**
                 * PullReply nextSeq.
                 * @member {number|Long} nextSeq
                 * @memberof api.im.v1.PullReply
                 * @instance
                 */
                PullReply.prototype.nextSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * Creates a new PullReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {api.im.v1.PullReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PullReply} PullReply instance
                 * @type {{
                 *   (properties: api.im.v1.PullReply.$Shape): api.im.v1.PullReply & api.im.v1.PullReply.$Shape;
                 *   (properties?: api.im.v1.PullReply.$Properties): api.im.v1.PullReply;
                 * }}
                 */
                PullReply.create = function(properties) {
                    return new PullReply(properties);
                };

                /**
                 * Encodes the specified PullReply message. Does not implicitly {@link api.im.v1.PullReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {api.im.v1.PullReply.$Properties} message PullReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.messages != null && message.messages.length)
                        for (let i = 0; i < message.messages.length; ++i)
                            $root.api.im.v1.PullMessage.encode(message.messages[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                    if (message.hasMore != null && $Object.hasOwnProperty.call(message, "hasMore"))
                        writer.uint32(/* id 2, wireType 0 =*/16).bool(message.hasMore);
                    if (message.nextSeq != null && $Object.hasOwnProperty.call(message, "nextSeq"))
                        writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.nextSeq);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PullReply message, length delimited. Does not implicitly {@link api.im.v1.PullReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {api.im.v1.PullReply.$Properties} message PullReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PullReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PullReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PullReply & api.im.v1.PullReply.$Shape} PullReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PullReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.messages && message.messages.length))
                                    message.messages = [];
                                message.messages.push($root.api.im.v1.PullMessage.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.hasMore = value;
                                else
                                    delete message.hasMore;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.nextSeq = value;
                                else
                                    delete message.nextSeq;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PullReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PullReply & api.im.v1.PullReply.$Shape} PullReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PullReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PullReply message.
                 * @function verify
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PullReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.messages != null && $Object.hasOwnProperty.call(message, "messages")) {
                        if (!$Array.isArray(message.messages))
                            return "messages: array expected";
                        for (let i = 0; i < message.messages.length; ++i) {
                            let error = $root.api.im.v1.PullMessage.verify(message.messages[i], _depth + 1);
                            if (error)
                                return "messages." + error;
                        }
                    }
                    if (message.hasMore != null && $Object.hasOwnProperty.call(message, "hasMore"))
                        if (typeof message.hasMore !== "boolean")
                            return "hasMore: boolean expected";
                    if (message.nextSeq != null && $Object.hasOwnProperty.call(message, "nextSeq"))
                        if (!$util.isInteger(message.nextSeq) && !(message.nextSeq && $util.isInteger(message.nextSeq.low) && $util.isInteger(message.nextSeq.high)))
                            return "nextSeq: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a PullReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PullReply} PullReply
                 */
                PullReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PullReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PullReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PullReply();
                    if (object.messages) {
                        if (!$Array.isArray(object.messages))
                            throw $TypeError(".api.im.v1.PullReply.messages: array expected");
                        message.messages = $Array(object.messages.length);
                        for (let i = 0; i < object.messages.length; ++i) {
                            if (!$util.isObject(object.messages[i]))
                                throw $TypeError(".api.im.v1.PullReply.messages: object expected");
                            message.messages[i] = $root.api.im.v1.PullMessage.fromObject(object.messages[i], _depth + 1);
                        }
                    }
                    if (object.hasMore != null)
                        if (object.hasMore)
                            message.hasMore = $Boolean(object.hasMore);
                    if (object.nextSeq != null)
                        if (typeof object.nextSeq === "object" ? object.nextSeq.low || object.nextSeq.high : $Number(object.nextSeq) !== 0)
                            if ($util.Long)
                                message.nextSeq = $util.Long.fromValue(object.nextSeq, true);
                            else if (typeof object.nextSeq === "string")
                                message.nextSeq = $parseInt(object.nextSeq, 10);
                            else if (typeof object.nextSeq === "number")
                                message.nextSeq = object.nextSeq;
                            else if (typeof object.nextSeq === "object")
                                message.nextSeq = new $util.LongBits(object.nextSeq.low >>> 0, object.nextSeq.high >>> 0).toNumber(true);
                    return message;
                };

                /**
                 * Creates a plain object from a PullReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {api.im.v1.PullReply} message PullReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PullReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.messages = [];
                    if (options.defaults) {
                        object.hasMore = false;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.nextSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.nextSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.messages && message.messages.length) {
                        object.messages = $Array(message.messages.length);
                        for (let j = 0; j < message.messages.length; ++j)
                            object.messages[j] = $root.api.im.v1.PullMessage.toObject(message.messages[j], options, _depth + 1);
                    }
                    if (message.hasMore != null && $Object.hasOwnProperty.call(message, "hasMore"))
                        object.hasMore = message.hasMore;
                    if (message.nextSeq != null && $Object.hasOwnProperty.call(message, "nextSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.nextSeq = typeof message.nextSeq === "number" ? $BigInt(message.nextSeq) : $util.Long.fromBits(message.nextSeq.low >>> 0, message.nextSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.nextSeq === "number")
                            object.nextSeq = options.longs === $String ? $String(message.nextSeq) : message.nextSeq;
                        else
                            object.nextSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.nextSeq) : options.longs === $Number ? new $util.LongBits(message.nextSeq.low >>> 0, message.nextSeq.high >>> 0).toNumber(true) : message.nextSeq;
                    return object;
                };

                /**
                 * Converts this PullReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PullReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PullReply.prototype.toJSON = function() {
                    return PullReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PullReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.PullReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PullReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PullReply";
                };

                return PullReply;
            })();

            /**
             * Command enum.
             * @name api.im.v1.Command
             * @enum {number}
             * @property {number} CMD_UNKNOWN=0 CMD_UNKNOWN value
             * @property {number} CMD_HEARTBEAT=1 CMD_HEARTBEAT value
             * @property {number} CMD_AUTH=2 CMD_AUTH value
             * @property {number} CMD_PUBLISH=3 CMD_PUBLISH value
             * @property {number} CMD_ACK=4 CMD_ACK value
             * @property {number} CMD_PULL=5 CMD_PULL value
             * @property {number} CMD_NOTIFY=6 CMD_NOTIFY value
             * @property {number} CMD_READ_RECEIPT=7 CMD_READ_RECEIPT value
             * @property {number} CMD_DELIVERY_RECEIPT=8 CMD_DELIVERY_RECEIPT value
             * @property {number} CMD_SEND_RECEIPT=9 CMD_SEND_RECEIPT value
             */
            v1.Command = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "CMD_UNKNOWN"] = 0;
                values[valuesById[1] = "CMD_HEARTBEAT"] = 1;
                values[valuesById[2] = "CMD_AUTH"] = 2;
                values[valuesById[3] = "CMD_PUBLISH"] = 3;
                values[valuesById[4] = "CMD_ACK"] = 4;
                values[valuesById[5] = "CMD_PULL"] = 5;
                values[valuesById[6] = "CMD_NOTIFY"] = 6;
                values[valuesById[7] = "CMD_READ_RECEIPT"] = 7;
                values[valuesById[8] = "CMD_DELIVERY_RECEIPT"] = 8;
                values[valuesById[9] = "CMD_SEND_RECEIPT"] = 9;
                return values;
            })();

            v1.Packet = (function() {

                /**
                 * Properties of a Packet.
                 * @typedef {Object} api.im.v1.Packet.$Properties
                 * @property {api.im.v1.Command|null} [cmd] Packet cmd
                 * @property {number|Long|null} [seq] Packet seq
                 * @property {api.im.v1.AuthRequest.$Properties|null} [authReq] Packet authReq
                 * @property {api.im.v1.AuthResponse.$Properties|null} [authResp] Packet authResp
                 * @property {api.im.v1.SendMessageRequest.$Properties|null} [sendReq] Packet sendReq
                 * @property {api.im.v1.SendMessageReply.$Properties|null} [sendReply] Packet sendReply
                 * @property {api.im.v1.PullRequest.$Properties|null} [pullReq] Packet pullReq
                 * @property {api.im.v1.PullReply.$Properties|null} [pullReply] Packet pullReply
                 * @property {api.im.v1.AckRequest.$Properties|null} [ackReq] Packet ackReq
                 * @property {api.im.v1.MessagePush.$Properties|null} [notify] Packet notify
                 * @property {api.im.v1.ReadReceipt.$Properties|null} [readReceipt] Packet readReceipt
                 * @property {api.im.v1.DeliveryReceipt.$Properties|null} [deliveryReceipt] Packet deliveryReceipt
                 * @property {api.im.v1.SendReceipt.$Properties|null} [sendReceipt] Packet sendReceipt
                 * @property {api.im.v1.ErrorResponse.$Properties|null} [error] Packet error
                 * @property {"authReq"|"authResp"|"sendReq"|"sendReply"|"pullReq"|"pullReply"|"ackReq"|"notify"|"readReceipt"|"deliveryReceipt"|"sendReceipt"|"error"} [payload] Packet payload
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a Packet.
                 * @memberof api.im.v1
                 * @interface IPacket
                 * @augments api.im.v1.Packet.$Properties
                 * @deprecated Use api.im.v1.Packet.$Properties instead.
                 */

                /**
                 * Narrowed shape of a Packet.
                 * @typedef {{
                 *   cmd?: api.im.v1.Command|null;
                 *   seq?: number|Long|null;
                 *   authReq?: api.im.v1.AuthRequest.$Shape|null;
                 *   authResp?: api.im.v1.AuthResponse.$Shape|null;
                 *   sendReq?: api.im.v1.SendMessageRequest.$Shape|null;
                 *   sendReply?: api.im.v1.SendMessageReply.$Shape|null;
                 *   pullReq?: api.im.v1.PullRequest.$Shape|null;
                 *   pullReply?: api.im.v1.PullReply.$Shape|null;
                 *   ackReq?: api.im.v1.AckRequest.$Shape|null;
                 *   notify?: api.im.v1.MessagePush.$Shape|null;
                 *   readReceipt?: api.im.v1.ReadReceipt.$Shape|null;
                 *   deliveryReceipt?: api.im.v1.DeliveryReceipt.$Shape|null;
                 *   sendReceipt?: api.im.v1.SendReceipt.$Shape|null;
                 *   error?: api.im.v1.ErrorResponse.$Shape|null;
                 *   $unknowns?: Array.<Uint8Array>;
                 * } & (
                 *   ({ payload?: undefined; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "authReq"; authReq: api.im.v1.AuthRequest.$Shape; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "authResp"; authReq?: null; authResp: api.im.v1.AuthResponse.$Shape; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "sendReq"; authReq?: null; authResp?: null; sendReq: api.im.v1.SendMessageRequest.$Shape; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "sendReply"; authReq?: null; authResp?: null; sendReq?: null; sendReply: api.im.v1.SendMessageReply.$Shape; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "pullReq"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq: api.im.v1.PullRequest.$Shape; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "pullReply"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply: api.im.v1.PullReply.$Shape; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "ackReq"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq: api.im.v1.AckRequest.$Shape; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "notify"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify: api.im.v1.MessagePush.$Shape; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "readReceipt"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt: api.im.v1.ReadReceipt.$Shape; deliveryReceipt?: null; sendReceipt?: null; error?: null }|{ payload?: "deliveryReceipt"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt: api.im.v1.DeliveryReceipt.$Shape; sendReceipt?: null; error?: null }|{ payload?: "sendReceipt"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt: api.im.v1.SendReceipt.$Shape; error?: null }|{ payload?: "error"; authReq?: null; authResp?: null; sendReq?: null; sendReply?: null; pullReq?: null; pullReply?: null; ackReq?: null; notify?: null; readReceipt?: null; deliveryReceipt?: null; sendReceipt?: null; error: api.im.v1.ErrorResponse.$Shape })
                 * )} api.im.v1.Packet.$Shape
                 */

                /**
                 * Constructs a new Packet.
                 * @memberof api.im.v1
                 * @classdesc Represents a Packet.
                 * @constructor
                 * @param {api.im.v1.Packet.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const Packet = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * Packet cmd.
                 * @member {api.im.v1.Command} cmd
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.cmd = 0;

                /**
                 * Packet seq.
                 * @member {number|Long} seq
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.seq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * Packet authReq.
                 * @member {api.im.v1.AuthRequest.$Properties|null|undefined} authReq
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.authReq = null;

                /**
                 * Packet authResp.
                 * @member {api.im.v1.AuthResponse.$Properties|null|undefined} authResp
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.authResp = null;

                /**
                 * Packet sendReq.
                 * @member {api.im.v1.SendMessageRequest.$Properties|null|undefined} sendReq
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.sendReq = null;

                /**
                 * Packet sendReply.
                 * @member {api.im.v1.SendMessageReply.$Properties|null|undefined} sendReply
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.sendReply = null;

                /**
                 * Packet pullReq.
                 * @member {api.im.v1.PullRequest.$Properties|null|undefined} pullReq
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.pullReq = null;

                /**
                 * Packet pullReply.
                 * @member {api.im.v1.PullReply.$Properties|null|undefined} pullReply
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.pullReply = null;

                /**
                 * Packet ackReq.
                 * @member {api.im.v1.AckRequest.$Properties|null|undefined} ackReq
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.ackReq = null;

                /**
                 * Packet notify.
                 * @member {api.im.v1.MessagePush.$Properties|null|undefined} notify
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.notify = null;

                /**
                 * Packet readReceipt.
                 * @member {api.im.v1.ReadReceipt.$Properties|null|undefined} readReceipt
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.readReceipt = null;

                /**
                 * Packet deliveryReceipt.
                 * @member {api.im.v1.DeliveryReceipt.$Properties|null|undefined} deliveryReceipt
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.deliveryReceipt = null;

                /**
                 * Packet sendReceipt.
                 * @member {api.im.v1.SendReceipt.$Properties|null|undefined} sendReceipt
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.sendReceipt = null;

                /**
                 * Packet error.
                 * @member {api.im.v1.ErrorResponse.$Properties|null|undefined} error
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                Packet.prototype.error = null;

                // OneOf field names bound to virtual getters and setters
                let $oneOfFields;

                /**
                 * Packet payload.
                 * @member {"authReq"|"authResp"|"sendReq"|"sendReply"|"pullReq"|"pullReply"|"ackReq"|"notify"|"readReceipt"|"deliveryReceipt"|"sendReceipt"|"error"|undefined} payload
                 * @memberof api.im.v1.Packet
                 * @instance
                 */
                $Object.defineProperty(Packet.prototype, "payload", {
                    get: $util.oneOfGetter($oneOfFields = ["authReq", "authResp", "sendReq", "sendReply", "pullReq", "pullReply", "ackReq", "notify", "readReceipt", "deliveryReceipt", "sendReceipt", "error"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Packet instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {api.im.v1.Packet.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.Packet} Packet instance
                 * @type {{
                 *   (properties: api.im.v1.Packet.$Shape): api.im.v1.Packet & api.im.v1.Packet.$Shape;
                 *   (properties?: api.im.v1.Packet.$Properties): api.im.v1.Packet;
                 * }}
                 */
                Packet.create = function(properties) {
                    return new Packet(properties);
                };

                /**
                 * Encodes the specified Packet message. Does not implicitly {@link api.im.v1.Packet.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {api.im.v1.Packet.$Properties} message Packet message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Packet.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.cmd);
                    if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.seq);
                    if (message.authReq != null && $Object.hasOwnProperty.call(message, "authReq"))
                        $root.api.im.v1.AuthRequest.encode(message.authReq, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                    if (message.authResp != null && $Object.hasOwnProperty.call(message, "authResp"))
                        $root.api.im.v1.AuthResponse.encode(message.authResp, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                    if (message.sendReq != null && $Object.hasOwnProperty.call(message, "sendReq"))
                        $root.api.im.v1.SendMessageRequest.encode(message.sendReq, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
                    if (message.sendReply != null && $Object.hasOwnProperty.call(message, "sendReply"))
                        $root.api.im.v1.SendMessageReply.encode(message.sendReply, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                    if (message.pullReq != null && $Object.hasOwnProperty.call(message, "pullReq"))
                        $root.api.im.v1.PullRequest.encode(message.pullReq, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
                    if (message.pullReply != null && $Object.hasOwnProperty.call(message, "pullReply"))
                        $root.api.im.v1.PullReply.encode(message.pullReply, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
                    if (message.ackReq != null && $Object.hasOwnProperty.call(message, "ackReq"))
                        $root.api.im.v1.AckRequest.encode(message.ackReq, writer.uint32(/* id 40, wireType 2 =*/322).fork(), _depth + 1).ldelim();
                    if (message.notify != null && $Object.hasOwnProperty.call(message, "notify"))
                        $root.api.im.v1.MessagePush.encode(message.notify, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
                    if (message.readReceipt != null && $Object.hasOwnProperty.call(message, "readReceipt"))
                        $root.api.im.v1.ReadReceipt.encode(message.readReceipt, writer.uint32(/* id 60, wireType 2 =*/482).fork(), _depth + 1).ldelim();
                    if (message.deliveryReceipt != null && $Object.hasOwnProperty.call(message, "deliveryReceipt"))
                        $root.api.im.v1.DeliveryReceipt.encode(message.deliveryReceipt, writer.uint32(/* id 70, wireType 2 =*/562).fork(), _depth + 1).ldelim();
                    if (message.sendReceipt != null && $Object.hasOwnProperty.call(message, "sendReceipt"))
                        $root.api.im.v1.SendReceipt.encode(message.sendReceipt, writer.uint32(/* id 80, wireType 2 =*/642).fork(), _depth + 1).ldelim();
                    if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                        $root.api.im.v1.ErrorResponse.encode(message.error, writer.uint32(/* id 99, wireType 2 =*/794).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Packet message, length delimited. Does not implicitly {@link api.im.v1.Packet.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {api.im.v1.Packet.$Properties} message Packet message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Packet.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Packet message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.Packet & api.im.v1.Packet.$Shape} Packet
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Packet.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.Packet(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.cmd = value;
                                else
                                    delete message.cmd;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.seq = value;
                                else
                                    delete message.seq;
                                continue;
                            }
                        case 10: {
                                if (wireType !== 2)
                                    break;
                                message.authReq = $root.api.im.v1.AuthRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.authReq);
                                message.payload = "authReq";
                                continue;
                            }
                        case 11: {
                                if (wireType !== 2)
                                    break;
                                message.authResp = $root.api.im.v1.AuthResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.authResp);
                                message.payload = "authResp";
                                continue;
                            }
                        case 20: {
                                if (wireType !== 2)
                                    break;
                                message.sendReq = $root.api.im.v1.SendMessageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sendReq);
                                message.payload = "sendReq";
                                continue;
                            }
                        case 21: {
                                if (wireType !== 2)
                                    break;
                                message.sendReply = $root.api.im.v1.SendMessageReply.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sendReply);
                                message.payload = "sendReply";
                                continue;
                            }
                        case 30: {
                                if (wireType !== 2)
                                    break;
                                message.pullReq = $root.api.im.v1.PullRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pullReq);
                                message.payload = "pullReq";
                                continue;
                            }
                        case 31: {
                                if (wireType !== 2)
                                    break;
                                message.pullReply = $root.api.im.v1.PullReply.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pullReply);
                                message.payload = "pullReply";
                                continue;
                            }
                        case 40: {
                                if (wireType !== 2)
                                    break;
                                message.ackReq = $root.api.im.v1.AckRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ackReq);
                                message.payload = "ackReq";
                                continue;
                            }
                        case 50: {
                                if (wireType !== 2)
                                    break;
                                message.notify = $root.api.im.v1.MessagePush.decode(reader, reader.uint32(), $undefined, _depth + 1, message.notify);
                                message.payload = "notify";
                                continue;
                            }
                        case 60: {
                                if (wireType !== 2)
                                    break;
                                message.readReceipt = $root.api.im.v1.ReadReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.readReceipt);
                                message.payload = "readReceipt";
                                continue;
                            }
                        case 70: {
                                if (wireType !== 2)
                                    break;
                                message.deliveryReceipt = $root.api.im.v1.DeliveryReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.deliveryReceipt);
                                message.payload = "deliveryReceipt";
                                continue;
                            }
                        case 80: {
                                if (wireType !== 2)
                                    break;
                                message.sendReceipt = $root.api.im.v1.SendReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sendReceipt);
                                message.payload = "sendReceipt";
                                continue;
                            }
                        case 99: {
                                if (wireType !== 2)
                                    break;
                                message.error = $root.api.im.v1.ErrorResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.error);
                                message.payload = "error";
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Packet message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.Packet & api.im.v1.Packet.$Shape} Packet
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Packet.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Packet message.
                 * @function verify
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Packet.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    let properties = {};
                    if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                        switch (message.cmd) {
                        default:
                            return "cmd: enum value expected";
                        case 0:
                        case 1:
                        case 2:
                        case 3:
                        case 4:
                        case 5:
                        case 6:
                        case 7:
                        case 8:
                        case 9:
                            break;
                        }
                    if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                        if (!$util.isInteger(message.seq) && !(message.seq && $util.isInteger(message.seq.low) && $util.isInteger(message.seq.high)))
                            return "seq: integer|Long expected";
                    if (message.authReq != null && $Object.hasOwnProperty.call(message, "authReq")) {
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.AuthRequest.verify(message.authReq, _depth + 1);
                            if (error)
                                return "authReq." + error;
                        }
                    }
                    if (message.authResp != null && $Object.hasOwnProperty.call(message, "authResp")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.AuthResponse.verify(message.authResp, _depth + 1);
                            if (error)
                                return "authResp." + error;
                        }
                    }
                    if (message.sendReq != null && $Object.hasOwnProperty.call(message, "sendReq")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.SendMessageRequest.verify(message.sendReq, _depth + 1);
                            if (error)
                                return "sendReq." + error;
                        }
                    }
                    if (message.sendReply != null && $Object.hasOwnProperty.call(message, "sendReply")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.SendMessageReply.verify(message.sendReply, _depth + 1);
                            if (error)
                                return "sendReply." + error;
                        }
                    }
                    if (message.pullReq != null && $Object.hasOwnProperty.call(message, "pullReq")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.PullRequest.verify(message.pullReq, _depth + 1);
                            if (error)
                                return "pullReq." + error;
                        }
                    }
                    if (message.pullReply != null && $Object.hasOwnProperty.call(message, "pullReply")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.PullReply.verify(message.pullReply, _depth + 1);
                            if (error)
                                return "pullReply." + error;
                        }
                    }
                    if (message.ackReq != null && $Object.hasOwnProperty.call(message, "ackReq")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.AckRequest.verify(message.ackReq, _depth + 1);
                            if (error)
                                return "ackReq." + error;
                        }
                    }
                    if (message.notify != null && $Object.hasOwnProperty.call(message, "notify")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.MessagePush.verify(message.notify, _depth + 1);
                            if (error)
                                return "notify." + error;
                        }
                    }
                    if (message.readReceipt != null && $Object.hasOwnProperty.call(message, "readReceipt")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.ReadReceipt.verify(message.readReceipt, _depth + 1);
                            if (error)
                                return "readReceipt." + error;
                        }
                    }
                    if (message.deliveryReceipt != null && $Object.hasOwnProperty.call(message, "deliveryReceipt")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.DeliveryReceipt.verify(message.deliveryReceipt, _depth + 1);
                            if (error)
                                return "deliveryReceipt." + error;
                        }
                    }
                    if (message.sendReceipt != null && $Object.hasOwnProperty.call(message, "sendReceipt")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.SendReceipt.verify(message.sendReceipt, _depth + 1);
                            if (error)
                                return "sendReceipt." + error;
                        }
                    }
                    if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                        if (properties.payload === 1)
                            return "payload: multiple values";
                        properties.payload = 1;
                        {
                            let error = $root.api.im.v1.ErrorResponse.verify(message.error, _depth + 1);
                            if (error)
                                return "error." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Packet message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.Packet} Packet
                 */
                Packet.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.Packet)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.Packet: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.Packet();
                    if (object.cmd !== 0 && (typeof object.cmd !== "string" || $root.api.im.v1.Command[object.cmd] !== 0))
                        switch (object.cmd) {
                        default:
                            if (typeof object.cmd === "number") {
                                message.cmd = object.cmd;
                                break;
                            }
                            break;
                        case "CMD_UNKNOWN":
                        case 0:
                            message.cmd = 0;
                            break;
                        case "CMD_HEARTBEAT":
                        case 1:
                            message.cmd = 1;
                            break;
                        case "CMD_AUTH":
                        case 2:
                            message.cmd = 2;
                            break;
                        case "CMD_PUBLISH":
                        case 3:
                            message.cmd = 3;
                            break;
                        case "CMD_ACK":
                        case 4:
                            message.cmd = 4;
                            break;
                        case "CMD_PULL":
                        case 5:
                            message.cmd = 5;
                            break;
                        case "CMD_NOTIFY":
                        case 6:
                            message.cmd = 6;
                            break;
                        case "CMD_READ_RECEIPT":
                        case 7:
                            message.cmd = 7;
                            break;
                        case "CMD_DELIVERY_RECEIPT":
                        case 8:
                            message.cmd = 8;
                            break;
                        case "CMD_SEND_RECEIPT":
                        case 9:
                            message.cmd = 9;
                            break;
                        }
                    if (object.seq != null)
                        if (typeof object.seq === "object" ? object.seq.low || object.seq.high : $Number(object.seq) !== 0)
                            if ($util.Long)
                                message.seq = $util.Long.fromValue(object.seq, true);
                            else if (typeof object.seq === "string")
                                message.seq = $parseInt(object.seq, 10);
                            else if (typeof object.seq === "number")
                                message.seq = object.seq;
                            else if (typeof object.seq === "object")
                                message.seq = new $util.LongBits(object.seq.low >>> 0, object.seq.high >>> 0).toNumber(true);
                    if (object.authReq != null) {
                        if (!$util.isObject(object.authReq))
                            throw $TypeError(".api.im.v1.Packet.authReq: object expected");
                        message.authReq = $root.api.im.v1.AuthRequest.fromObject(object.authReq, _depth + 1);
                    }
                    if (object.authResp != null) {
                        if (!$util.isObject(object.authResp))
                            throw $TypeError(".api.im.v1.Packet.authResp: object expected");
                        message.authResp = $root.api.im.v1.AuthResponse.fromObject(object.authResp, _depth + 1);
                    }
                    if (object.sendReq != null) {
                        if (!$util.isObject(object.sendReq))
                            throw $TypeError(".api.im.v1.Packet.sendReq: object expected");
                        message.sendReq = $root.api.im.v1.SendMessageRequest.fromObject(object.sendReq, _depth + 1);
                    }
                    if (object.sendReply != null) {
                        if (!$util.isObject(object.sendReply))
                            throw $TypeError(".api.im.v1.Packet.sendReply: object expected");
                        message.sendReply = $root.api.im.v1.SendMessageReply.fromObject(object.sendReply, _depth + 1);
                    }
                    if (object.pullReq != null) {
                        if (!$util.isObject(object.pullReq))
                            throw $TypeError(".api.im.v1.Packet.pullReq: object expected");
                        message.pullReq = $root.api.im.v1.PullRequest.fromObject(object.pullReq, _depth + 1);
                    }
                    if (object.pullReply != null) {
                        if (!$util.isObject(object.pullReply))
                            throw $TypeError(".api.im.v1.Packet.pullReply: object expected");
                        message.pullReply = $root.api.im.v1.PullReply.fromObject(object.pullReply, _depth + 1);
                    }
                    if (object.ackReq != null) {
                        if (!$util.isObject(object.ackReq))
                            throw $TypeError(".api.im.v1.Packet.ackReq: object expected");
                        message.ackReq = $root.api.im.v1.AckRequest.fromObject(object.ackReq, _depth + 1);
                    }
                    if (object.notify != null) {
                        if (!$util.isObject(object.notify))
                            throw $TypeError(".api.im.v1.Packet.notify: object expected");
                        message.notify = $root.api.im.v1.MessagePush.fromObject(object.notify, _depth + 1);
                    }
                    if (object.readReceipt != null) {
                        if (!$util.isObject(object.readReceipt))
                            throw $TypeError(".api.im.v1.Packet.readReceipt: object expected");
                        message.readReceipt = $root.api.im.v1.ReadReceipt.fromObject(object.readReceipt, _depth + 1);
                    }
                    if (object.deliveryReceipt != null) {
                        if (!$util.isObject(object.deliveryReceipt))
                            throw $TypeError(".api.im.v1.Packet.deliveryReceipt: object expected");
                        message.deliveryReceipt = $root.api.im.v1.DeliveryReceipt.fromObject(object.deliveryReceipt, _depth + 1);
                    }
                    if (object.sendReceipt != null) {
                        if (!$util.isObject(object.sendReceipt))
                            throw $TypeError(".api.im.v1.Packet.sendReceipt: object expected");
                        message.sendReceipt = $root.api.im.v1.SendReceipt.fromObject(object.sendReceipt, _depth + 1);
                    }
                    if (object.error != null) {
                        if (!$util.isObject(object.error))
                            throw $TypeError(".api.im.v1.Packet.error: object expected");
                        message.error = $root.api.im.v1.ErrorResponse.fromObject(object.error, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Packet message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {api.im.v1.Packet} message Packet
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Packet.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.cmd = options.enums === $String ? "CMD_UNKNOWN" : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.seq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.seq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                        object.cmd = options.enums === $String ? $root.api.im.v1.Command[message.cmd] === $undefined ? message.cmd : $root.api.im.v1.Command[message.cmd] : message.cmd;
                    if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.seq = typeof message.seq === "number" ? $BigInt(message.seq) : $util.Long.fromBits(message.seq.low >>> 0, message.seq.high >>> 0, true).toBigInt();
                        else if (typeof message.seq === "number")
                            object.seq = options.longs === $String ? $String(message.seq) : message.seq;
                        else
                            object.seq = options.longs === $String ? $util.Long.prototype.toString.call(message.seq) : options.longs === $Number ? new $util.LongBits(message.seq.low >>> 0, message.seq.high >>> 0).toNumber(true) : message.seq;
                    if (message.authReq != null && $Object.hasOwnProperty.call(message, "authReq")) {
                        object.authReq = $root.api.im.v1.AuthRequest.toObject(message.authReq, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "authReq";
                    }
                    if (message.authResp != null && $Object.hasOwnProperty.call(message, "authResp")) {
                        object.authResp = $root.api.im.v1.AuthResponse.toObject(message.authResp, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "authResp";
                    }
                    if (message.sendReq != null && $Object.hasOwnProperty.call(message, "sendReq")) {
                        object.sendReq = $root.api.im.v1.SendMessageRequest.toObject(message.sendReq, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "sendReq";
                    }
                    if (message.sendReply != null && $Object.hasOwnProperty.call(message, "sendReply")) {
                        object.sendReply = $root.api.im.v1.SendMessageReply.toObject(message.sendReply, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "sendReply";
                    }
                    if (message.pullReq != null && $Object.hasOwnProperty.call(message, "pullReq")) {
                        object.pullReq = $root.api.im.v1.PullRequest.toObject(message.pullReq, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "pullReq";
                    }
                    if (message.pullReply != null && $Object.hasOwnProperty.call(message, "pullReply")) {
                        object.pullReply = $root.api.im.v1.PullReply.toObject(message.pullReply, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "pullReply";
                    }
                    if (message.ackReq != null && $Object.hasOwnProperty.call(message, "ackReq")) {
                        object.ackReq = $root.api.im.v1.AckRequest.toObject(message.ackReq, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "ackReq";
                    }
                    if (message.notify != null && $Object.hasOwnProperty.call(message, "notify")) {
                        object.notify = $root.api.im.v1.MessagePush.toObject(message.notify, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "notify";
                    }
                    if (message.readReceipt != null && $Object.hasOwnProperty.call(message, "readReceipt")) {
                        object.readReceipt = $root.api.im.v1.ReadReceipt.toObject(message.readReceipt, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "readReceipt";
                    }
                    if (message.deliveryReceipt != null && $Object.hasOwnProperty.call(message, "deliveryReceipt")) {
                        object.deliveryReceipt = $root.api.im.v1.DeliveryReceipt.toObject(message.deliveryReceipt, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "deliveryReceipt";
                    }
                    if (message.sendReceipt != null && $Object.hasOwnProperty.call(message, "sendReceipt")) {
                        object.sendReceipt = $root.api.im.v1.SendReceipt.toObject(message.sendReceipt, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "sendReceipt";
                    }
                    if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                        object.error = $root.api.im.v1.ErrorResponse.toObject(message.error, options, _depth + 1);
                        if (options.oneofs)
                            object.payload = "error";
                    }
                    return object;
                };

                /**
                 * Converts this Packet to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.Packet
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Packet.prototype.toJSON = function() {
                    return Packet.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for Packet
                 * @function getTypeUrl
                 * @memberof api.im.v1.Packet
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                Packet.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.Packet";
                };

                return Packet;
            })();

            v1.AuthRequest = (function() {

                /**
                 * Properties of an AuthRequest.
                 * @typedef {Object} api.im.v1.AuthRequest.$Properties
                 * @property {string|null} [token] AuthRequest token
                 * @property {string|null} [deviceId] AuthRequest deviceId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an AuthRequest.
                 * @memberof api.im.v1
                 * @interface IAuthRequest
                 * @augments api.im.v1.AuthRequest.$Properties
                 * @deprecated Use api.im.v1.AuthRequest.$Properties instead.
                 */

                /**
                 * Shape of an AuthRequest.
                 * @typedef {api.im.v1.AuthRequest.$Properties} api.im.v1.AuthRequest.$Shape
                 */

                /**
                 * Constructs a new AuthRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents an AuthRequest.
                 * @constructor
                 * @param {api.im.v1.AuthRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const AuthRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * AuthRequest token.
                 * @member {string} token
                 * @memberof api.im.v1.AuthRequest
                 * @instance
                 */
                AuthRequest.prototype.token = "";

                /**
                 * AuthRequest deviceId.
                 * @member {string} deviceId
                 * @memberof api.im.v1.AuthRequest
                 * @instance
                 */
                AuthRequest.prototype.deviceId = "";

                /**
                 * Creates a new AuthRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {api.im.v1.AuthRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.AuthRequest} AuthRequest instance
                 * @type {{
                 *   (properties: api.im.v1.AuthRequest.$Shape): api.im.v1.AuthRequest & api.im.v1.AuthRequest.$Shape;
                 *   (properties?: api.im.v1.AuthRequest.$Properties): api.im.v1.AuthRequest;
                 * }}
                 */
                AuthRequest.create = function(properties) {
                    return new AuthRequest(properties);
                };

                /**
                 * Encodes the specified AuthRequest message. Does not implicitly {@link api.im.v1.AuthRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {api.im.v1.AuthRequest.$Properties} message AuthRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AuthRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.token);
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.deviceId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified AuthRequest message, length delimited. Does not implicitly {@link api.im.v1.AuthRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {api.im.v1.AuthRequest.$Properties} message AuthRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AuthRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an AuthRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.AuthRequest & api.im.v1.AuthRequest.$Shape} AuthRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AuthRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.AuthRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.token = value;
                                else
                                    delete message.token;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.deviceId = value;
                                else
                                    delete message.deviceId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an AuthRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.AuthRequest & api.im.v1.AuthRequest.$Shape} AuthRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AuthRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an AuthRequest message.
                 * @function verify
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                AuthRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        if (!$util.isString(message.token))
                            return "token: string expected";
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        if (!$util.isString(message.deviceId))
                            return "deviceId: string expected";
                    return null;
                };

                /**
                 * Creates an AuthRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.AuthRequest} AuthRequest
                 */
                AuthRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.AuthRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.AuthRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.AuthRequest();
                    if (object.token != null)
                        if (typeof object.token !== "string" || object.token.length)
                            message.token = $String(object.token);
                    if (object.deviceId != null)
                        if (typeof object.deviceId !== "string" || object.deviceId.length)
                            message.deviceId = $String(object.deviceId);
                    return message;
                };

                /**
                 * Creates a plain object from an AuthRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {api.im.v1.AuthRequest} message AuthRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                AuthRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.token = "";
                        object.deviceId = "";
                    }
                    if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                        object.token = message.token;
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        object.deviceId = message.deviceId;
                    return object;
                };

                /**
                 * Converts this AuthRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.AuthRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                AuthRequest.prototype.toJSON = function() {
                    return AuthRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for AuthRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.AuthRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                AuthRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.AuthRequest";
                };

                return AuthRequest;
            })();

            v1.AuthResponse = (function() {

                /**
                 * Properties of an AuthResponse.
                 * @typedef {Object} api.im.v1.AuthResponse.$Properties
                 * @property {boolean|null} [success] AuthResponse success
                 * @property {number|Long|null} [userId] AuthResponse userId
                 * @property {string|null} [deviceId] AuthResponse deviceId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an AuthResponse.
                 * @memberof api.im.v1
                 * @interface IAuthResponse
                 * @augments api.im.v1.AuthResponse.$Properties
                 * @deprecated Use api.im.v1.AuthResponse.$Properties instead.
                 */

                /**
                 * Shape of an AuthResponse.
                 * @typedef {api.im.v1.AuthResponse.$Properties} api.im.v1.AuthResponse.$Shape
                 */

                /**
                 * Constructs a new AuthResponse.
                 * @memberof api.im.v1
                 * @classdesc Represents an AuthResponse.
                 * @constructor
                 * @param {api.im.v1.AuthResponse.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const AuthResponse = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * AuthResponse success.
                 * @member {boolean} success
                 * @memberof api.im.v1.AuthResponse
                 * @instance
                 */
                AuthResponse.prototype.success = false;

                /**
                 * AuthResponse userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.AuthResponse
                 * @instance
                 */
                AuthResponse.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * AuthResponse deviceId.
                 * @member {string} deviceId
                 * @memberof api.im.v1.AuthResponse
                 * @instance
                 */
                AuthResponse.prototype.deviceId = "";

                /**
                 * Creates a new AuthResponse instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {api.im.v1.AuthResponse.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.AuthResponse} AuthResponse instance
                 * @type {{
                 *   (properties: api.im.v1.AuthResponse.$Shape): api.im.v1.AuthResponse & api.im.v1.AuthResponse.$Shape;
                 *   (properties?: api.im.v1.AuthResponse.$Properties): api.im.v1.AuthResponse;
                 * }}
                 */
                AuthResponse.create = function(properties) {
                    return new AuthResponse(properties);
                };

                /**
                 * Encodes the specified AuthResponse message. Does not implicitly {@link api.im.v1.AuthResponse.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {api.im.v1.AuthResponse.$Properties} message AuthResponse message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AuthResponse.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int64(message.userId);
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.deviceId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified AuthResponse message, length delimited. Does not implicitly {@link api.im.v1.AuthResponse.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {api.im.v1.AuthResponse.$Properties} message AuthResponse message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AuthResponse.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an AuthResponse message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.AuthResponse & api.im.v1.AuthResponse.$Shape} AuthResponse
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AuthResponse.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.AuthResponse(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.success = value;
                                else
                                    delete message.success;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.deviceId = value;
                                else
                                    delete message.deviceId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an AuthResponse message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.AuthResponse & api.im.v1.AuthResponse.$Shape} AuthResponse
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AuthResponse.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an AuthResponse message.
                 * @function verify
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                AuthResponse.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        if (typeof message.success !== "boolean")
                            return "success: boolean expected";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        if (!$util.isString(message.deviceId))
                            return "deviceId: string expected";
                    return null;
                };

                /**
                 * Creates an AuthResponse message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.AuthResponse} AuthResponse
                 */
                AuthResponse.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.AuthResponse)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.AuthResponse: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.AuthResponse();
                    if (object.success != null)
                        if (object.success)
                            message.success = $Boolean(object.success);
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    if (object.deviceId != null)
                        if (typeof object.deviceId !== "string" || object.deviceId.length)
                            message.deviceId = $String(object.deviceId);
                    return message;
                };

                /**
                 * Creates a plain object from an AuthResponse message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {api.im.v1.AuthResponse} message AuthResponse
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                AuthResponse.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.success = false;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.deviceId = "";
                    }
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        object.success = message.success;
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    if (message.deviceId != null && $Object.hasOwnProperty.call(message, "deviceId"))
                        object.deviceId = message.deviceId;
                    return object;
                };

                /**
                 * Converts this AuthResponse to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.AuthResponse
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                AuthResponse.prototype.toJSON = function() {
                    return AuthResponse.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for AuthResponse
                 * @function getTypeUrl
                 * @memberof api.im.v1.AuthResponse
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                AuthResponse.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.AuthResponse";
                };

                return AuthResponse;
            })();

            v1.AckRequest = (function() {

                /**
                 * Properties of an AckRequest.
                 * @typedef {Object} api.im.v1.AckRequest.$Properties
                 * @property {number|Long|null} [msgId] AckRequest msgId
                 * @property {string|null} [topic] AckRequest topic
                 * @property {number|Long|null} [topicSeq] AckRequest topicSeq
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an AckRequest.
                 * @memberof api.im.v1
                 * @interface IAckRequest
                 * @augments api.im.v1.AckRequest.$Properties
                 * @deprecated Use api.im.v1.AckRequest.$Properties instead.
                 */

                /**
                 * Shape of an AckRequest.
                 * @typedef {api.im.v1.AckRequest.$Properties} api.im.v1.AckRequest.$Shape
                 */

                /**
                 * Constructs a new AckRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents an AckRequest.
                 * @constructor
                 * @param {api.im.v1.AckRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const AckRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * AckRequest msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.AckRequest
                 * @instance
                 */
                AckRequest.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * AckRequest topic.
                 * @member {string} topic
                 * @memberof api.im.v1.AckRequest
                 * @instance
                 */
                AckRequest.prototype.topic = "";

                /**
                 * AckRequest topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.AckRequest
                 * @instance
                 */
                AckRequest.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * Creates a new AckRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {api.im.v1.AckRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.AckRequest} AckRequest instance
                 * @type {{
                 *   (properties: api.im.v1.AckRequest.$Shape): api.im.v1.AckRequest & api.im.v1.AckRequest.$Shape;
                 *   (properties?: api.im.v1.AckRequest.$Properties): api.im.v1.AckRequest;
                 * }}
                 */
                AckRequest.create = function(properties) {
                    return new AckRequest(properties);
                };

                /**
                 * Encodes the specified AckRequest message. Does not implicitly {@link api.im.v1.AckRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {api.im.v1.AckRequest.$Properties} message AckRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AckRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.msgId);
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.topic);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.topicSeq);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified AckRequest message, length delimited. Does not implicitly {@link api.im.v1.AckRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {api.im.v1.AckRequest.$Properties} message AckRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AckRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an AckRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.AckRequest & api.im.v1.AckRequest.$Shape} AckRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AckRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.AckRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an AckRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.AckRequest & api.im.v1.AckRequest.$Shape} AckRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AckRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an AckRequest message.
                 * @function verify
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                AckRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    return null;
                };

                /**
                 * Creates an AckRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.AckRequest} AckRequest
                 */
                AckRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.AckRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.AckRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.AckRequest();
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    return message;
                };

                /**
                 * Creates a plain object from an AckRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {api.im.v1.AckRequest} message AckRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                AckRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    return object;
                };

                /**
                 * Converts this AckRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.AckRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                AckRequest.prototype.toJSON = function() {
                    return AckRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for AckRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.AckRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                AckRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.AckRequest";
                };

                return AckRequest;
            })();

            v1.ErrorResponse = (function() {

                /**
                 * Properties of an ErrorResponse.
                 * @typedef {Object} api.im.v1.ErrorResponse.$Properties
                 * @property {number|null} [code] ErrorResponse code
                 * @property {string|null} [message] ErrorResponse message
                 * @property {boolean|null} [retryable] ErrorResponse retryable
                 * @property {number|Long|null} [retryAfterMs] ErrorResponse retryAfterMs
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an ErrorResponse.
                 * @memberof api.im.v1
                 * @interface IErrorResponse
                 * @augments api.im.v1.ErrorResponse.$Properties
                 * @deprecated Use api.im.v1.ErrorResponse.$Properties instead.
                 */

                /**
                 * Shape of an ErrorResponse.
                 * @typedef {api.im.v1.ErrorResponse.$Properties} api.im.v1.ErrorResponse.$Shape
                 */

                /**
                 * Constructs a new ErrorResponse.
                 * @memberof api.im.v1
                 * @classdesc Represents an ErrorResponse.
                 * @constructor
                 * @param {api.im.v1.ErrorResponse.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const ErrorResponse = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * ErrorResponse code.
                 * @member {number} code
                 * @memberof api.im.v1.ErrorResponse
                 * @instance
                 */
                ErrorResponse.prototype.code = 0;

                /**
                 * ErrorResponse message.
                 * @member {string} message
                 * @memberof api.im.v1.ErrorResponse
                 * @instance
                 */
                ErrorResponse.prototype.message = "";

                /**
                 * ErrorResponse retryable.
                 * @member {boolean} retryable
                 * @memberof api.im.v1.ErrorResponse
                 * @instance
                 */
                ErrorResponse.prototype.retryable = false;

                /**
                 * ErrorResponse retryAfterMs.
                 * @member {number|Long} retryAfterMs
                 * @memberof api.im.v1.ErrorResponse
                 * @instance
                 */
                ErrorResponse.prototype.retryAfterMs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * Creates a new ErrorResponse instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {api.im.v1.ErrorResponse.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.ErrorResponse} ErrorResponse instance
                 * @type {{
                 *   (properties: api.im.v1.ErrorResponse.$Shape): api.im.v1.ErrorResponse & api.im.v1.ErrorResponse.$Shape;
                 *   (properties?: api.im.v1.ErrorResponse.$Properties): api.im.v1.ErrorResponse;
                 * }}
                 */
                ErrorResponse.create = function(properties) {
                    return new ErrorResponse(properties);
                };

                /**
                 * Encodes the specified ErrorResponse message. Does not implicitly {@link api.im.v1.ErrorResponse.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {api.im.v1.ErrorResponse.$Properties} message ErrorResponse message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ErrorResponse.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.code);
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
                    if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable"))
                        writer.uint32(/* id 3, wireType 0 =*/24).bool(message.retryable);
                    if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs"))
                        writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.retryAfterMs);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ErrorResponse message, length delimited. Does not implicitly {@link api.im.v1.ErrorResponse.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {api.im.v1.ErrorResponse.$Properties} message ErrorResponse message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ErrorResponse.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an ErrorResponse message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.ErrorResponse & api.im.v1.ErrorResponse.$Shape} ErrorResponse
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ErrorResponse.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.ErrorResponse(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.code = value;
                                else
                                    delete message.code;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.message = value;
                                else
                                    delete message.message;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.retryable = value;
                                else
                                    delete message.retryable;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.retryAfterMs = value;
                                else
                                    delete message.retryAfterMs;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an ErrorResponse message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.ErrorResponse & api.im.v1.ErrorResponse.$Shape} ErrorResponse
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ErrorResponse.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an ErrorResponse message.
                 * @function verify
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ErrorResponse.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                        if (!$util.isInteger(message.code))
                            return "code: integer expected";
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        if (!$util.isString(message.message))
                            return "message: string expected";
                    if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable"))
                        if (typeof message.retryable !== "boolean")
                            return "retryable: boolean expected";
                    if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs"))
                        if (!$util.isInteger(message.retryAfterMs) && !(message.retryAfterMs && $util.isInteger(message.retryAfterMs.low) && $util.isInteger(message.retryAfterMs.high)))
                            return "retryAfterMs: integer|Long expected";
                    return null;
                };

                /**
                 * Creates an ErrorResponse message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.ErrorResponse} ErrorResponse
                 */
                ErrorResponse.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.ErrorResponse)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.ErrorResponse: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.ErrorResponse();
                    if (object.code != null)
                        if ($Number(object.code) !== 0)
                            message.code = object.code | 0;
                    if (object.message != null)
                        if (typeof object.message !== "string" || object.message.length)
                            message.message = $String(object.message);
                    if (object.retryable != null)
                        if (object.retryable)
                            message.retryable = $Boolean(object.retryable);
                    if (object.retryAfterMs != null)
                        if (typeof object.retryAfterMs === "object" ? object.retryAfterMs.low || object.retryAfterMs.high : $Number(object.retryAfterMs) !== 0)
                            if ($util.Long)
                                message.retryAfterMs = $util.Long.fromValue(object.retryAfterMs, true);
                            else if (typeof object.retryAfterMs === "string")
                                message.retryAfterMs = $parseInt(object.retryAfterMs, 10);
                            else if (typeof object.retryAfterMs === "number")
                                message.retryAfterMs = object.retryAfterMs;
                            else if (typeof object.retryAfterMs === "object")
                                message.retryAfterMs = new $util.LongBits(object.retryAfterMs.low >>> 0, object.retryAfterMs.high >>> 0).toNumber(true);
                    return message;
                };

                /**
                 * Creates a plain object from an ErrorResponse message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {api.im.v1.ErrorResponse} message ErrorResponse
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ErrorResponse.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.code = 0;
                        object.message = "";
                        object.retryable = false;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.retryAfterMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.retryAfterMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                        object.code = message.code;
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        object.message = message.message;
                    if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable"))
                        object.retryable = message.retryable;
                    if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.retryAfterMs = typeof message.retryAfterMs === "number" ? $BigInt(message.retryAfterMs) : $util.Long.fromBits(message.retryAfterMs.low >>> 0, message.retryAfterMs.high >>> 0, true).toBigInt();
                        else if (typeof message.retryAfterMs === "number")
                            object.retryAfterMs = options.longs === $String ? $String(message.retryAfterMs) : message.retryAfterMs;
                        else
                            object.retryAfterMs = options.longs === $String ? $util.Long.prototype.toString.call(message.retryAfterMs) : options.longs === $Number ? new $util.LongBits(message.retryAfterMs.low >>> 0, message.retryAfterMs.high >>> 0).toNumber(true) : message.retryAfterMs;
                    return object;
                };

                /**
                 * Converts this ErrorResponse to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.ErrorResponse
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ErrorResponse.prototype.toJSON = function() {
                    return ErrorResponse.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for ErrorResponse
                 * @function getTypeUrl
                 * @memberof api.im.v1.ErrorResponse
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                ErrorResponse.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.ErrorResponse";
                };

                return ErrorResponse;
            })();

            v1.ReadReceipt = (function() {

                /**
                 * Properties of a ReadReceipt.
                 * @typedef {Object} api.im.v1.ReadReceipt.$Properties
                 * @property {string|null} [topic] ReadReceipt topic
                 * @property {number|Long|null} [upToSeq] ReadReceipt upToSeq
                 * @property {number|Long|null} [readerId] ReadReceipt readerId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a ReadReceipt.
                 * @memberof api.im.v1
                 * @interface IReadReceipt
                 * @augments api.im.v1.ReadReceipt.$Properties
                 * @deprecated Use api.im.v1.ReadReceipt.$Properties instead.
                 */

                /**
                 * Shape of a ReadReceipt.
                 * @typedef {api.im.v1.ReadReceipt.$Properties} api.im.v1.ReadReceipt.$Shape
                 */

                /**
                 * Constructs a new ReadReceipt.
                 * @memberof api.im.v1
                 * @classdesc Represents a ReadReceipt.
                 * @constructor
                 * @param {api.im.v1.ReadReceipt.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const ReadReceipt = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * ReadReceipt topic.
                 * @member {string} topic
                 * @memberof api.im.v1.ReadReceipt
                 * @instance
                 */
                ReadReceipt.prototype.topic = "";

                /**
                 * ReadReceipt upToSeq.
                 * @member {number|Long} upToSeq
                 * @memberof api.im.v1.ReadReceipt
                 * @instance
                 */
                ReadReceipt.prototype.upToSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * ReadReceipt readerId.
                 * @member {number|Long} readerId
                 * @memberof api.im.v1.ReadReceipt
                 * @instance
                 */
                ReadReceipt.prototype.readerId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * Creates a new ReadReceipt instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {api.im.v1.ReadReceipt.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.ReadReceipt} ReadReceipt instance
                 * @type {{
                 *   (properties: api.im.v1.ReadReceipt.$Shape): api.im.v1.ReadReceipt & api.im.v1.ReadReceipt.$Shape;
                 *   (properties?: api.im.v1.ReadReceipt.$Properties): api.im.v1.ReadReceipt;
                 * }}
                 */
                ReadReceipt.create = function(properties) {
                    return new ReadReceipt(properties);
                };

                /**
                 * Encodes the specified ReadReceipt message. Does not implicitly {@link api.im.v1.ReadReceipt.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {api.im.v1.ReadReceipt.$Properties} message ReadReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReadReceipt.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.topic);
                    if (message.upToSeq != null && $Object.hasOwnProperty.call(message, "upToSeq"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.upToSeq);
                    if (message.readerId != null && $Object.hasOwnProperty.call(message, "readerId"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.readerId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ReadReceipt message, length delimited. Does not implicitly {@link api.im.v1.ReadReceipt.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {api.im.v1.ReadReceipt.$Properties} message ReadReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReadReceipt.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ReadReceipt message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.ReadReceipt & api.im.v1.ReadReceipt.$Shape} ReadReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReadReceipt.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.ReadReceipt(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.upToSeq = value;
                                else
                                    delete message.upToSeq;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.readerId = value;
                                else
                                    delete message.readerId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ReadReceipt message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.ReadReceipt & api.im.v1.ReadReceipt.$Shape} ReadReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReadReceipt.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ReadReceipt message.
                 * @function verify
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ReadReceipt.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.upToSeq != null && $Object.hasOwnProperty.call(message, "upToSeq"))
                        if (!$util.isInteger(message.upToSeq) && !(message.upToSeq && $util.isInteger(message.upToSeq.low) && $util.isInteger(message.upToSeq.high)))
                            return "upToSeq: integer|Long expected";
                    if (message.readerId != null && $Object.hasOwnProperty.call(message, "readerId"))
                        if (!$util.isInteger(message.readerId) && !(message.readerId && $util.isInteger(message.readerId.low) && $util.isInteger(message.readerId.high)))
                            return "readerId: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a ReadReceipt message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.ReadReceipt} ReadReceipt
                 */
                ReadReceipt.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.ReadReceipt)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.ReadReceipt: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.ReadReceipt();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.upToSeq != null)
                        if (typeof object.upToSeq === "object" ? object.upToSeq.low || object.upToSeq.high : $Number(object.upToSeq) !== 0)
                            if ($util.Long)
                                message.upToSeq = $util.Long.fromValue(object.upToSeq, true);
                            else if (typeof object.upToSeq === "string")
                                message.upToSeq = $parseInt(object.upToSeq, 10);
                            else if (typeof object.upToSeq === "number")
                                message.upToSeq = object.upToSeq;
                            else if (typeof object.upToSeq === "object")
                                message.upToSeq = new $util.LongBits(object.upToSeq.low >>> 0, object.upToSeq.high >>> 0).toNumber(true);
                    if (object.readerId != null)
                        if (typeof object.readerId === "object" ? object.readerId.low || object.readerId.high : $Number(object.readerId) !== 0)
                            if ($util.Long)
                                message.readerId = $util.Long.fromValue(object.readerId, false);
                            else if (typeof object.readerId === "string")
                                message.readerId = $parseInt(object.readerId, 10);
                            else if (typeof object.readerId === "number")
                                message.readerId = object.readerId;
                            else if (typeof object.readerId === "object")
                                message.readerId = new $util.LongBits(object.readerId.low >>> 0, object.readerId.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a ReadReceipt message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {api.im.v1.ReadReceipt} message ReadReceipt
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ReadReceipt.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.upToSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.upToSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.readerId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.readerId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.upToSeq != null && $Object.hasOwnProperty.call(message, "upToSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.upToSeq = typeof message.upToSeq === "number" ? $BigInt(message.upToSeq) : $util.Long.fromBits(message.upToSeq.low >>> 0, message.upToSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.upToSeq === "number")
                            object.upToSeq = options.longs === $String ? $String(message.upToSeq) : message.upToSeq;
                        else
                            object.upToSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.upToSeq) : options.longs === $Number ? new $util.LongBits(message.upToSeq.low >>> 0, message.upToSeq.high >>> 0).toNumber(true) : message.upToSeq;
                    if (message.readerId != null && $Object.hasOwnProperty.call(message, "readerId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.readerId = typeof message.readerId === "number" ? $BigInt(message.readerId) : $util.Long.fromBits(message.readerId.low >>> 0, message.readerId.high >>> 0, false).toBigInt();
                        else if (typeof message.readerId === "number")
                            object.readerId = options.longs === $String ? $String(message.readerId) : message.readerId;
                        else
                            object.readerId = options.longs === $String ? $util.Long.prototype.toString.call(message.readerId) : options.longs === $Number ? new $util.LongBits(message.readerId.low >>> 0, message.readerId.high >>> 0).toNumber() : message.readerId;
                    return object;
                };

                /**
                 * Converts this ReadReceipt to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.ReadReceipt
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ReadReceipt.prototype.toJSON = function() {
                    return ReadReceipt.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for ReadReceipt
                 * @function getTypeUrl
                 * @memberof api.im.v1.ReadReceipt
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                ReadReceipt.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.ReadReceipt";
                };

                return ReadReceipt;
            })();

            v1.DeliveryReceipt = (function() {

                /**
                 * Properties of a DeliveryReceipt.
                 * @typedef {Object} api.im.v1.DeliveryReceipt.$Properties
                 * @property {string|null} [topic] DeliveryReceipt topic
                 * @property {number|Long|null} [topicSeq] DeliveryReceipt topicSeq
                 * @property {number|Long|null} [msgId] DeliveryReceipt msgId
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a DeliveryReceipt.
                 * @memberof api.im.v1
                 * @interface IDeliveryReceipt
                 * @augments api.im.v1.DeliveryReceipt.$Properties
                 * @deprecated Use api.im.v1.DeliveryReceipt.$Properties instead.
                 */

                /**
                 * Shape of a DeliveryReceipt.
                 * @typedef {api.im.v1.DeliveryReceipt.$Properties} api.im.v1.DeliveryReceipt.$Shape
                 */

                /**
                 * Constructs a new DeliveryReceipt.
                 * @memberof api.im.v1
                 * @classdesc Represents a DeliveryReceipt.
                 * @constructor
                 * @param {api.im.v1.DeliveryReceipt.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const DeliveryReceipt = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * DeliveryReceipt topic.
                 * @member {string} topic
                 * @memberof api.im.v1.DeliveryReceipt
                 * @instance
                 */
                DeliveryReceipt.prototype.topic = "";

                /**
                 * DeliveryReceipt topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.DeliveryReceipt
                 * @instance
                 */
                DeliveryReceipt.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * DeliveryReceipt msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.DeliveryReceipt
                 * @instance
                 */
                DeliveryReceipt.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * Creates a new DeliveryReceipt instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {api.im.v1.DeliveryReceipt.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.DeliveryReceipt} DeliveryReceipt instance
                 * @type {{
                 *   (properties: api.im.v1.DeliveryReceipt.$Shape): api.im.v1.DeliveryReceipt & api.im.v1.DeliveryReceipt.$Shape;
                 *   (properties?: api.im.v1.DeliveryReceipt.$Properties): api.im.v1.DeliveryReceipt;
                 * }}
                 */
                DeliveryReceipt.create = function(properties) {
                    return new DeliveryReceipt(properties);
                };

                /**
                 * Encodes the specified DeliveryReceipt message. Does not implicitly {@link api.im.v1.DeliveryReceipt.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {api.im.v1.DeliveryReceipt.$Properties} message DeliveryReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                DeliveryReceipt.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.topic);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.topicSeq);
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.msgId);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified DeliveryReceipt message, length delimited. Does not implicitly {@link api.im.v1.DeliveryReceipt.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {api.im.v1.DeliveryReceipt.$Properties} message DeliveryReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                DeliveryReceipt.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a DeliveryReceipt message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.DeliveryReceipt & api.im.v1.DeliveryReceipt.$Shape} DeliveryReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                DeliveryReceipt.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.DeliveryReceipt(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a DeliveryReceipt message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.DeliveryReceipt & api.im.v1.DeliveryReceipt.$Shape} DeliveryReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                DeliveryReceipt.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a DeliveryReceipt message.
                 * @function verify
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                DeliveryReceipt.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a DeliveryReceipt message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.DeliveryReceipt} DeliveryReceipt
                 */
                DeliveryReceipt.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.DeliveryReceipt)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.DeliveryReceipt: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.DeliveryReceipt();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a DeliveryReceipt message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {api.im.v1.DeliveryReceipt} message DeliveryReceipt
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                DeliveryReceipt.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    return object;
                };

                /**
                 * Converts this DeliveryReceipt to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.DeliveryReceipt
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                DeliveryReceipt.prototype.toJSON = function() {
                    return DeliveryReceipt.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for DeliveryReceipt
                 * @function getTypeUrl
                 * @memberof api.im.v1.DeliveryReceipt
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                DeliveryReceipt.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.DeliveryReceipt";
                };

                return DeliveryReceipt;
            })();

            v1.SendReceipt = (function() {

                /**
                 * Properties of a SendReceipt.
                 * @typedef {Object} api.im.v1.SendReceipt.$Properties
                 * @property {string|null} [clientMsgId] SendReceipt clientMsgId
                 * @property {number|Long|null} [msgId] SendReceipt msgId
                 * @property {string|null} [topic] SendReceipt topic
                 * @property {number|Long|null} [topicSeq] SendReceipt topicSeq
                 * @property {number|Long|null} [timestamp] SendReceipt timestamp
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a SendReceipt.
                 * @memberof api.im.v1
                 * @interface ISendReceipt
                 * @augments api.im.v1.SendReceipt.$Properties
                 * @deprecated Use api.im.v1.SendReceipt.$Properties instead.
                 */

                /**
                 * Shape of a SendReceipt.
                 * @typedef {api.im.v1.SendReceipt.$Properties} api.im.v1.SendReceipt.$Shape
                 */

                /**
                 * Constructs a new SendReceipt.
                 * @memberof api.im.v1
                 * @classdesc Represents a SendReceipt.
                 * @constructor
                 * @param {api.im.v1.SendReceipt.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const SendReceipt = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * SendReceipt clientMsgId.
                 * @member {string} clientMsgId
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 */
                SendReceipt.prototype.clientMsgId = "";

                /**
                 * SendReceipt msgId.
                 * @member {number|Long} msgId
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 */
                SendReceipt.prototype.msgId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * SendReceipt topic.
                 * @member {string} topic
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 */
                SendReceipt.prototype.topic = "";

                /**
                 * SendReceipt topicSeq.
                 * @member {number|Long} topicSeq
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 */
                SendReceipt.prototype.topicSeq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                /**
                 * SendReceipt timestamp.
                 * @member {number|Long} timestamp
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 */
                SendReceipt.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * Creates a new SendReceipt instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {api.im.v1.SendReceipt.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.SendReceipt} SendReceipt instance
                 * @type {{
                 *   (properties: api.im.v1.SendReceipt.$Shape): api.im.v1.SendReceipt & api.im.v1.SendReceipt.$Shape;
                 *   (properties?: api.im.v1.SendReceipt.$Properties): api.im.v1.SendReceipt;
                 * }}
                 */
                SendReceipt.create = function(properties) {
                    return new SendReceipt(properties);
                };

                /**
                 * Encodes the specified SendReceipt message. Does not implicitly {@link api.im.v1.SendReceipt.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {api.im.v1.SendReceipt.$Properties} message SendReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendReceipt.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientMsgId);
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int64(message.msgId);
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.topic);
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.topicSeq);
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 5, wireType 0 =*/40).int64(message.timestamp);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SendReceipt message, length delimited. Does not implicitly {@link api.im.v1.SendReceipt.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {api.im.v1.SendReceipt.$Properties} message SendReceipt message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SendReceipt.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SendReceipt message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.SendReceipt & api.im.v1.SendReceipt.$Shape} SendReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendReceipt.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.SendReceipt(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.clientMsgId = value;
                                else
                                    delete message.clientMsgId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.msgId = value;
                                else
                                    delete message.msgId;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.stringVerify()).length)
                                    message.topic = value;
                                else
                                    delete message.topic;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                    message.topicSeq = value;
                                else
                                    delete message.topicSeq;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.timestamp = value;
                                else
                                    delete message.timestamp;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SendReceipt message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.SendReceipt & api.im.v1.SendReceipt.$Shape} SendReceipt
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SendReceipt.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SendReceipt message.
                 * @function verify
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SendReceipt.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        if (!$util.isString(message.clientMsgId))
                            return "clientMsgId: string expected";
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (!$util.isInteger(message.msgId) && !(message.msgId && $util.isInteger(message.msgId.low) && $util.isInteger(message.msgId.high)))
                            return "msgId: integer|Long expected";
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        if (!$util.isString(message.topic))
                            return "topic: string expected";
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (!$util.isInteger(message.topicSeq) && !(message.topicSeq && $util.isInteger(message.topicSeq.low) && $util.isInteger(message.topicSeq.high)))
                            return "topicSeq: integer|Long expected";
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    return null;
                };

                /**
                 * Creates a SendReceipt message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.SendReceipt} SendReceipt
                 */
                SendReceipt.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.SendReceipt)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.SendReceipt: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.SendReceipt();
                    if (object.clientMsgId != null)
                        if (typeof object.clientMsgId !== "string" || object.clientMsgId.length)
                            message.clientMsgId = $String(object.clientMsgId);
                    if (object.msgId != null)
                        if (typeof object.msgId === "object" ? object.msgId.low || object.msgId.high : $Number(object.msgId) !== 0)
                            if ($util.Long)
                                message.msgId = $util.Long.fromValue(object.msgId, false);
                            else if (typeof object.msgId === "string")
                                message.msgId = $parseInt(object.msgId, 10);
                            else if (typeof object.msgId === "number")
                                message.msgId = object.msgId;
                            else if (typeof object.msgId === "object")
                                message.msgId = new $util.LongBits(object.msgId.low >>> 0, object.msgId.high >>> 0).toNumber();
                    if (object.topic != null)
                        if (typeof object.topic !== "string" || object.topic.length)
                            message.topic = $String(object.topic);
                    if (object.topicSeq != null)
                        if (typeof object.topicSeq === "object" ? object.topicSeq.low || object.topicSeq.high : $Number(object.topicSeq) !== 0)
                            if ($util.Long)
                                message.topicSeq = $util.Long.fromValue(object.topicSeq, true);
                            else if (typeof object.topicSeq === "string")
                                message.topicSeq = $parseInt(object.topicSeq, 10);
                            else if (typeof object.topicSeq === "number")
                                message.topicSeq = object.topicSeq;
                            else if (typeof object.topicSeq === "object")
                                message.topicSeq = new $util.LongBits(object.topicSeq.low >>> 0, object.topicSeq.high >>> 0).toNumber(true);
                    if (object.timestamp != null)
                        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0)
                            if ($util.Long)
                                message.timestamp = $util.Long.fromValue(object.timestamp, false);
                            else if (typeof object.timestamp === "string")
                                message.timestamp = $parseInt(object.timestamp, 10);
                            else if (typeof object.timestamp === "number")
                                message.timestamp = object.timestamp;
                            else if (typeof object.timestamp === "object")
                                message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a SendReceipt message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {api.im.v1.SendReceipt} message SendReceipt
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SendReceipt.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.clientMsgId = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.msgId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.msgId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.topic = "";
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, true);
                            object.topicSeq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.topicSeq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    }
                    if (message.clientMsgId != null && $Object.hasOwnProperty.call(message, "clientMsgId"))
                        object.clientMsgId = message.clientMsgId;
                    if (message.msgId != null && $Object.hasOwnProperty.call(message, "msgId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.msgId = typeof message.msgId === "number" ? $BigInt(message.msgId) : $util.Long.fromBits(message.msgId.low >>> 0, message.msgId.high >>> 0, false).toBigInt();
                        else if (typeof message.msgId === "number")
                            object.msgId = options.longs === $String ? $String(message.msgId) : message.msgId;
                        else
                            object.msgId = options.longs === $String ? $util.Long.prototype.toString.call(message.msgId) : options.longs === $Number ? new $util.LongBits(message.msgId.low >>> 0, message.msgId.high >>> 0).toNumber() : message.msgId;
                    if (message.topic != null && $Object.hasOwnProperty.call(message, "topic"))
                        object.topic = message.topic;
                    if (message.topicSeq != null && $Object.hasOwnProperty.call(message, "topicSeq"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.topicSeq = typeof message.topicSeq === "number" ? $BigInt(message.topicSeq) : $util.Long.fromBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0, true).toBigInt();
                        else if (typeof message.topicSeq === "number")
                            object.topicSeq = options.longs === $String ? $String(message.topicSeq) : message.topicSeq;
                        else
                            object.topicSeq = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSeq) : options.longs === $Number ? new $util.LongBits(message.topicSeq.low >>> 0, message.topicSeq.high >>> 0).toNumber(true) : message.topicSeq;
                    if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    return object;
                };

                /**
                 * Converts this SendReceipt to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.SendReceipt
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SendReceipt.prototype.toJSON = function() {
                    return SendReceipt.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for SendReceipt
                 * @function getTypeUrl
                 * @memberof api.im.v1.SendReceipt
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                SendReceipt.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.SendReceipt";
                };

                return SendReceipt;
            })();

            v1.PushService = (function() {

                /**
                 * Constructs a new PushService service.
                 * @memberof api.im.v1
                 * @classdesc Represents a PushService
                 * @extends $protobuf.rpc.Service
                 * @constructor
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 */
                const PushService = function(rpcImpl, requestDelimited, responseDelimited) {
                    $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
                };

                (PushService.prototype = $Object.create($protobuf.rpc.Service.prototype)).constructor = PushService;

                /**
                 * Creates new PushService service using the specified rpc implementation.
                 * @function create
                 * @memberof api.im.v1.PushService
                 * @static
                 * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
                 * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
                 * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
                 * @returns {PushService} RPC service. Useful where requests and/or responses are streamed.
                 */
                PushService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                    return new this(rpcImpl, requestDelimited, responseDelimited);
                };

                /**
                 * Callback as used by {@link api.im.v1.PushService#pushToUser}.
                 * @memberof api.im.v1.PushService
                 * @typedef PushToUserCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.PushToUserReply} [response] PushToUserReply
                 */

                /**
                 * Calls PushToUser.
                 * @memberof api.im.v1.PushService
                 * @typedef PushToUser
                 * @type {{
                 *   (request: api.im.v1.IPushToUserRequest, callback: api.im.v1.PushService.PushToUserCallback): void;
                 *   (request: api.im.v1.IPushToUserRequest): Promise<api.im.v1.PushToUserReply>;
                 *   readonly name: "PushToUser";
                 *   readonly path: "/api.im.v1.PushService/PushToUser";
                 *   readonly requestType: "PushToUserRequest";
                 *   readonly responseType: "PushToUserReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls PushToUser.
                 * @name api.im.v1.PushService#pushToUser
                 * @type {api.im.v1.PushService.PushToUser}
                 */
                $Object.defineProperties(PushService.prototype.pushToUser = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, PushService.prototype.pushToUser, $root.api.im.v1.PushToUserRequest, $root.api.im.v1.PushToUserReply, request, callback);
                }, {
                    name: { value: "PushToUser" },
                    path: { value: "/api.im.v1.PushService/PushToUser" },
                    requestType: { value: "PushToUserRequest" },
                    responseType: { value: "PushToUserReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.PushService#batchPushToUsers}.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushToUsersCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.BatchPushToUsersReply} [response] BatchPushToUsersReply
                 */

                /**
                 * Calls BatchPushToUsers.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushToUsers
                 * @type {{
                 *   (request: api.im.v1.IBatchPushToUsersRequest, callback: api.im.v1.PushService.BatchPushToUsersCallback): void;
                 *   (request: api.im.v1.IBatchPushToUsersRequest): Promise<api.im.v1.BatchPushToUsersReply>;
                 *   readonly name: "BatchPushToUsers";
                 *   readonly path: "/api.im.v1.PushService/BatchPushToUsers";
                 *   readonly requestType: "BatchPushToUsersRequest";
                 *   readonly responseType: "BatchPushToUsersReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls BatchPushToUsers.
                 * @name api.im.v1.PushService#batchPushToUsers
                 * @type {api.im.v1.PushService.BatchPushToUsers}
                 */
                $Object.defineProperties(PushService.prototype.batchPushToUsers = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, PushService.prototype.batchPushToUsers, $root.api.im.v1.BatchPushToUsersRequest, $root.api.im.v1.BatchPushToUsersReply, request, callback);
                }, {
                    name: { value: "BatchPushToUsers" },
                    path: { value: "/api.im.v1.PushService/BatchPushToUsers" },
                    requestType: { value: "BatchPushToUsersRequest" },
                    responseType: { value: "BatchPushToUsersReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.PushService#pushReceiptToUser}.
                 * @memberof api.im.v1.PushService
                 * @typedef PushReceiptToUserCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.PushReceiptToUserReply} [response] PushReceiptToUserReply
                 */

                /**
                 * Calls PushReceiptToUser.
                 * @memberof api.im.v1.PushService
                 * @typedef PushReceiptToUser
                 * @type {{
                 *   (request: api.im.v1.IPushReceiptToUserRequest, callback: api.im.v1.PushService.PushReceiptToUserCallback): void;
                 *   (request: api.im.v1.IPushReceiptToUserRequest): Promise<api.im.v1.PushReceiptToUserReply>;
                 *   readonly name: "PushReceiptToUser";
                 *   readonly path: "/api.im.v1.PushService/PushReceiptToUser";
                 *   readonly requestType: "PushReceiptToUserRequest";
                 *   readonly responseType: "PushReceiptToUserReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls PushReceiptToUser.
                 * @name api.im.v1.PushService#pushReceiptToUser
                 * @type {api.im.v1.PushService.PushReceiptToUser}
                 */
                $Object.defineProperties(PushService.prototype.pushReceiptToUser = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, PushService.prototype.pushReceiptToUser, $root.api.im.v1.PushReceiptToUserRequest, $root.api.im.v1.PushReceiptToUserReply, request, callback);
                }, {
                    name: { value: "PushReceiptToUser" },
                    path: { value: "/api.im.v1.PushService/PushReceiptToUser" },
                    requestType: { value: "PushReceiptToUserRequest" },
                    responseType: { value: "PushReceiptToUserReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.PushService#batchPushReceiptToUsers}.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushReceiptToUsersCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.BatchPushReceiptToUsersReply} [response] BatchPushReceiptToUsersReply
                 */

                /**
                 * Calls BatchPushReceiptToUsers.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushReceiptToUsers
                 * @type {{
                 *   (request: api.im.v1.IBatchPushReceiptToUsersRequest, callback: api.im.v1.PushService.BatchPushReceiptToUsersCallback): void;
                 *   (request: api.im.v1.IBatchPushReceiptToUsersRequest): Promise<api.im.v1.BatchPushReceiptToUsersReply>;
                 *   readonly name: "BatchPushReceiptToUsers";
                 *   readonly path: "/api.im.v1.PushService/BatchPushReceiptToUsers";
                 *   readonly requestType: "BatchPushReceiptToUsersRequest";
                 *   readonly responseType: "BatchPushReceiptToUsersReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls BatchPushReceiptToUsers.
                 * @name api.im.v1.PushService#batchPushReceiptToUsers
                 * @type {api.im.v1.PushService.BatchPushReceiptToUsers}
                 */
                $Object.defineProperties(PushService.prototype.batchPushReceiptToUsers = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, PushService.prototype.batchPushReceiptToUsers, $root.api.im.v1.BatchPushReceiptToUsersRequest, $root.api.im.v1.BatchPushReceiptToUsersReply, request, callback);
                }, {
                    name: { value: "BatchPushReceiptToUsers" },
                    path: { value: "/api.im.v1.PushService/BatchPushReceiptToUsers" },
                    requestType: { value: "BatchPushReceiptToUsersRequest" },
                    responseType: { value: "BatchPushReceiptToUsersReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                /**
                 * Callback as used by {@link api.im.v1.PushService#batchPushReceiptsToUsers}.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushReceiptsToUsersCallback
                 * @type {function}
                 * @param {Error|null} error Error, if any
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply} [response] BatchPushReceiptsToUsersReply
                 */

                /**
                 * Calls BatchPushReceiptsToUsers.
                 * @memberof api.im.v1.PushService
                 * @typedef BatchPushReceiptsToUsers
                 * @type {{
                 *   (request: api.im.v1.IBatchPushReceiptsToUsersRequest, callback: api.im.v1.PushService.BatchPushReceiptsToUsersCallback): void;
                 *   (request: api.im.v1.IBatchPushReceiptsToUsersRequest): Promise<api.im.v1.BatchPushReceiptsToUsersReply>;
                 *   readonly name: "BatchPushReceiptsToUsers";
                 *   readonly path: "/api.im.v1.PushService/BatchPushReceiptsToUsers";
                 *   readonly requestType: "BatchPushReceiptsToUsersRequest";
                 *   readonly responseType: "BatchPushReceiptsToUsersReply";
                 *   readonly requestStream: undefined;
                 *   readonly responseStream: undefined;
                 * }}
                 */

                /**
                 * Calls BatchPushReceiptsToUsers.
                 * @name api.im.v1.PushService#batchPushReceiptsToUsers
                 * @type {api.im.v1.PushService.BatchPushReceiptsToUsers}
                 */
                $Object.defineProperties(PushService.prototype.batchPushReceiptsToUsers = function(request, callback) {
                    return $protobuf.rpc.Service.prototype.rpcCall.call(this, PushService.prototype.batchPushReceiptsToUsers, $root.api.im.v1.BatchPushReceiptsToUsersRequest, $root.api.im.v1.BatchPushReceiptsToUsersReply, request, callback);
                }, {
                    name: { value: "BatchPushReceiptsToUsers" },
                    path: { value: "/api.im.v1.PushService/BatchPushReceiptsToUsers" },
                    requestType: { value: "BatchPushReceiptsToUsersRequest" },
                    responseType: { value: "BatchPushReceiptsToUsersReply" },
                    requestStream: { value: $undefined },
                    responseStream: { value: $undefined }
                });

                return PushService;
            })();

            v1.PushToUserRequest = (function() {

                /**
                 * Properties of a PushToUserRequest.
                 * @typedef {Object} api.im.v1.PushToUserRequest.$Properties
                 * @property {number|Long|null} [userId] PushToUserRequest userId
                 * @property {api.im.v1.MessagePush.$Properties|null} [message] PushToUserRequest message
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PushToUserRequest.
                 * @memberof api.im.v1
                 * @interface IPushToUserRequest
                 * @augments api.im.v1.PushToUserRequest.$Properties
                 * @deprecated Use api.im.v1.PushToUserRequest.$Properties instead.
                 */

                /**
                 * Shape of a PushToUserRequest.
                 * @typedef {api.im.v1.PushToUserRequest.$Properties} api.im.v1.PushToUserRequest.$Shape
                 */

                /**
                 * Constructs a new PushToUserRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a PushToUserRequest.
                 * @constructor
                 * @param {api.im.v1.PushToUserRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PushToUserRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PushToUserRequest userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.PushToUserRequest
                 * @instance
                 */
                PushToUserRequest.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * PushToUserRequest message.
                 * @member {api.im.v1.MessagePush.$Properties|null|undefined} message
                 * @memberof api.im.v1.PushToUserRequest
                 * @instance
                 */
                PushToUserRequest.prototype.message = null;

                /**
                 * Creates a new PushToUserRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {api.im.v1.PushToUserRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PushToUserRequest} PushToUserRequest instance
                 * @type {{
                 *   (properties: api.im.v1.PushToUserRequest.$Shape): api.im.v1.PushToUserRequest & api.im.v1.PushToUserRequest.$Shape;
                 *   (properties?: api.im.v1.PushToUserRequest.$Properties): api.im.v1.PushToUserRequest;
                 * }}
                 */
                PushToUserRequest.create = function(properties) {
                    return new PushToUserRequest(properties);
                };

                /**
                 * Encodes the specified PushToUserRequest message. Does not implicitly {@link api.im.v1.PushToUserRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {api.im.v1.PushToUserRequest.$Properties} message PushToUserRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushToUserRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        $root.api.im.v1.MessagePush.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PushToUserRequest message, length delimited. Does not implicitly {@link api.im.v1.PushToUserRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {api.im.v1.PushToUserRequest.$Properties} message PushToUserRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushToUserRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PushToUserRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PushToUserRequest & api.im.v1.PushToUserRequest.$Shape} PushToUserRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushToUserRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PushToUserRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.message = $root.api.im.v1.MessagePush.decode(reader, reader.uint32(), $undefined, _depth + 1, message.message);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PushToUserRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PushToUserRequest & api.im.v1.PushToUserRequest.$Shape} PushToUserRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushToUserRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PushToUserRequest message.
                 * @function verify
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PushToUserRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message")) {
                        let error = $root.api.im.v1.MessagePush.verify(message.message, _depth + 1);
                        if (error)
                            return "message." + error;
                    }
                    return null;
                };

                /**
                 * Creates a PushToUserRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PushToUserRequest} PushToUserRequest
                 */
                PushToUserRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PushToUserRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PushToUserRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PushToUserRequest();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    if (object.message != null) {
                        if (!$util.isObject(object.message))
                            throw $TypeError(".api.im.v1.PushToUserRequest.message: object expected");
                        message.message = $root.api.im.v1.MessagePush.fromObject(object.message, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a PushToUserRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {api.im.v1.PushToUserRequest} message PushToUserRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PushToUserRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.message = null;
                    }
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        object.message = $root.api.im.v1.MessagePush.toObject(message.message, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this PushToUserRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PushToUserRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PushToUserRequest.prototype.toJSON = function() {
                    return PushToUserRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PushToUserRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.PushToUserRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PushToUserRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PushToUserRequest";
                };

                return PushToUserRequest;
            })();

            v1.PushToUserReply = (function() {

                /**
                 * Properties of a PushToUserReply.
                 * @typedef {Object} api.im.v1.PushToUserReply.$Properties
                 * @property {boolean|null} [success] PushToUserReply success
                 * @property {number|null} [deliveredCount] PushToUserReply deliveredCount
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PushToUserReply.
                 * @memberof api.im.v1
                 * @interface IPushToUserReply
                 * @augments api.im.v1.PushToUserReply.$Properties
                 * @deprecated Use api.im.v1.PushToUserReply.$Properties instead.
                 */

                /**
                 * Shape of a PushToUserReply.
                 * @typedef {api.im.v1.PushToUserReply.$Properties} api.im.v1.PushToUserReply.$Shape
                 */

                /**
                 * Constructs a new PushToUserReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a PushToUserReply.
                 * @constructor
                 * @param {api.im.v1.PushToUserReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PushToUserReply = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PushToUserReply success.
                 * @member {boolean} success
                 * @memberof api.im.v1.PushToUserReply
                 * @instance
                 */
                PushToUserReply.prototype.success = false;

                /**
                 * PushToUserReply deliveredCount.
                 * @member {number} deliveredCount
                 * @memberof api.im.v1.PushToUserReply
                 * @instance
                 */
                PushToUserReply.prototype.deliveredCount = 0;

                /**
                 * Creates a new PushToUserReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {api.im.v1.PushToUserReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PushToUserReply} PushToUserReply instance
                 * @type {{
                 *   (properties: api.im.v1.PushToUserReply.$Shape): api.im.v1.PushToUserReply & api.im.v1.PushToUserReply.$Shape;
                 *   (properties?: api.im.v1.PushToUserReply.$Properties): api.im.v1.PushToUserReply;
                 * }}
                 */
                PushToUserReply.create = function(properties) {
                    return new PushToUserReply(properties);
                };

                /**
                 * Encodes the specified PushToUserReply message. Does not implicitly {@link api.im.v1.PushToUserReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {api.im.v1.PushToUserReply.$Properties} message PushToUserReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushToUserReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deliveredCount);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PushToUserReply message, length delimited. Does not implicitly {@link api.im.v1.PushToUserReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {api.im.v1.PushToUserReply.$Properties} message PushToUserReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushToUserReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PushToUserReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PushToUserReply & api.im.v1.PushToUserReply.$Shape} PushToUserReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushToUserReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PushToUserReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.success = value;
                                else
                                    delete message.success;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.deliveredCount = value;
                                else
                                    delete message.deliveredCount;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PushToUserReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PushToUserReply & api.im.v1.PushToUserReply.$Shape} PushToUserReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushToUserReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PushToUserReply message.
                 * @function verify
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PushToUserReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        if (typeof message.success !== "boolean")
                            return "success: boolean expected";
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        if (!$util.isInteger(message.deliveredCount))
                            return "deliveredCount: integer expected";
                    return null;
                };

                /**
                 * Creates a PushToUserReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PushToUserReply} PushToUserReply
                 */
                PushToUserReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PushToUserReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PushToUserReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PushToUserReply();
                    if (object.success != null)
                        if (object.success)
                            message.success = $Boolean(object.success);
                    if (object.deliveredCount != null)
                        if ($Number(object.deliveredCount) !== 0)
                            message.deliveredCount = object.deliveredCount | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a PushToUserReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {api.im.v1.PushToUserReply} message PushToUserReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PushToUserReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.success = false;
                        object.deliveredCount = 0;
                    }
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        object.success = message.success;
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        object.deliveredCount = message.deliveredCount;
                    return object;
                };

                /**
                 * Converts this PushToUserReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PushToUserReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PushToUserReply.prototype.toJSON = function() {
                    return PushToUserReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PushToUserReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.PushToUserReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PushToUserReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PushToUserReply";
                };

                return PushToUserReply;
            })();

            v1.BatchPushToUsersRequest = (function() {

                /**
                 * Properties of a BatchPushToUsersRequest.
                 * @typedef {Object} api.im.v1.BatchPushToUsersRequest.$Properties
                 * @property {Array.<number|Long>|null} [userIds] BatchPushToUsersRequest userIds
                 * @property {api.im.v1.MessagePush.$Properties|null} [message] BatchPushToUsersRequest message
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushToUsersRequest.
                 * @memberof api.im.v1
                 * @interface IBatchPushToUsersRequest
                 * @augments api.im.v1.BatchPushToUsersRequest.$Properties
                 * @deprecated Use api.im.v1.BatchPushToUsersRequest.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushToUsersRequest.
                 * @typedef {api.im.v1.BatchPushToUsersRequest.$Properties} api.im.v1.BatchPushToUsersRequest.$Shape
                 */

                /**
                 * Constructs a new BatchPushToUsersRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushToUsersRequest.
                 * @constructor
                 * @param {api.im.v1.BatchPushToUsersRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushToUsersRequest = function (properties) {
                    this.userIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushToUsersRequest userIds.
                 * @member {Array.<number|Long>} userIds
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @instance
                 */
                BatchPushToUsersRequest.prototype.userIds = $util.emptyArray;

                /**
                 * BatchPushToUsersRequest message.
                 * @member {api.im.v1.MessagePush.$Properties|null|undefined} message
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @instance
                 */
                BatchPushToUsersRequest.prototype.message = null;

                /**
                 * Creates a new BatchPushToUsersRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushToUsersRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushToUsersRequest} BatchPushToUsersRequest instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushToUsersRequest.$Shape): api.im.v1.BatchPushToUsersRequest & api.im.v1.BatchPushToUsersRequest.$Shape;
                 *   (properties?: api.im.v1.BatchPushToUsersRequest.$Properties): api.im.v1.BatchPushToUsersRequest;
                 * }}
                 */
                BatchPushToUsersRequest.create = function(properties) {
                    return new BatchPushToUsersRequest(properties);
                };

                /**
                 * Encodes the specified BatchPushToUsersRequest message. Does not implicitly {@link api.im.v1.BatchPushToUsersRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushToUsersRequest.$Properties} message BatchPushToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushToUsersRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userIds != null && message.userIds.length) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork();
                        for (let i = 0; i < message.userIds.length; ++i)
                            writer.int64(message.userIds[i]);
                        writer.ldelim();
                    }
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        $root.api.im.v1.MessagePush.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushToUsersRequest message, length delimited. Does not implicitly {@link api.im.v1.BatchPushToUsersRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushToUsersRequest.$Properties} message BatchPushToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushToUsersRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushToUsersRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushToUsersRequest & api.im.v1.BatchPushToUsersRequest.$Shape} BatchPushToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushToUsersRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushToUsersRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType === 2) {
                                    if (!(message.userIds && message.userIds.length))
                                        message.userIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.userIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.userIds && message.userIds.length))
                                    message.userIds = [];
                                message.userIds.push(reader.int64());
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.message = $root.api.im.v1.MessagePush.decode(reader, reader.uint32(), $undefined, _depth + 1, message.message);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushToUsersRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushToUsersRequest & api.im.v1.BatchPushToUsersRequest.$Shape} BatchPushToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushToUsersRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushToUsersRequest message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushToUsersRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userIds != null && $Object.hasOwnProperty.call(message, "userIds")) {
                        if (!$Array.isArray(message.userIds))
                            return "userIds: array expected";
                        for (let i = 0; i < message.userIds.length; ++i)
                            if (!$util.isInteger(message.userIds[i]) && !(message.userIds[i] && $util.isInteger(message.userIds[i].low) && $util.isInteger(message.userIds[i].high)))
                                return "userIds: integer|Long[] expected";
                    }
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message")) {
                        let error = $root.api.im.v1.MessagePush.verify(message.message, _depth + 1);
                        if (error)
                            return "message." + error;
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushToUsersRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushToUsersRequest} BatchPushToUsersRequest
                 */
                BatchPushToUsersRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushToUsersRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushToUsersRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushToUsersRequest();
                    if (object.userIds) {
                        if (!$Array.isArray(object.userIds))
                            throw $TypeError(".api.im.v1.BatchPushToUsersRequest.userIds: array expected");
                        message.userIds = $Array(object.userIds.length);
                        for (let i = 0; i < object.userIds.length; ++i)
                            if ($util.Long)
                                message.userIds[i] = $util.Long.fromValue(object.userIds[i], false);
                            else if (typeof object.userIds[i] === "string")
                                message.userIds[i] = $parseInt(object.userIds[i], 10);
                            else if (typeof object.userIds[i] === "number")
                                message.userIds[i] = object.userIds[i];
                            else if (typeof object.userIds[i] === "object")
                                message.userIds[i] = new $util.LongBits(object.userIds[i].low >>> 0, object.userIds[i].high >>> 0).toNumber();
                    }
                    if (object.message != null) {
                        if (!$util.isObject(object.message))
                            throw $TypeError(".api.im.v1.BatchPushToUsersRequest.message: object expected");
                        message.message = $root.api.im.v1.MessagePush.fromObject(object.message, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushToUsersRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushToUsersRequest} message BatchPushToUsersRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushToUsersRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.userIds = [];
                    if (options.defaults)
                        object.message = null;
                    if (message.userIds && message.userIds.length) {
                        object.userIds = $Array(message.userIds.length);
                        for (let j = 0; j < message.userIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.userIds[j] = typeof message.userIds[j] === "number" ? $BigInt(message.userIds[j]) : $util.Long.fromBits(message.userIds[j].low >>> 0, message.userIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.userIds[j] === "number")
                                object.userIds[j] = options.longs === $String ? $String(message.userIds[j]) : message.userIds[j];
                            else
                                object.userIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.userIds[j]) : options.longs === $Number ? new $util.LongBits(message.userIds[j].low >>> 0, message.userIds[j].high >>> 0).toNumber() : message.userIds[j];
                    }
                    if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                        object.message = $root.api.im.v1.MessagePush.toObject(message.message, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this BatchPushToUsersRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushToUsersRequest.prototype.toJSON = function() {
                    return BatchPushToUsersRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushToUsersRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushToUsersRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushToUsersRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushToUsersRequest";
                };

                return BatchPushToUsersRequest;
            })();

            v1.BatchPushToUsersReply = (function() {

                /**
                 * Properties of a BatchPushToUsersReply.
                 * @typedef {Object} api.im.v1.BatchPushToUsersReply.$Properties
                 * @property {number|null} [totalDelivered] BatchPushToUsersReply totalDelivered
                 * @property {Array.<number|Long>|null} [failedUserIds] BatchPushToUsersReply failedUserIds
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushToUsersReply.
                 * @memberof api.im.v1
                 * @interface IBatchPushToUsersReply
                 * @augments api.im.v1.BatchPushToUsersReply.$Properties
                 * @deprecated Use api.im.v1.BatchPushToUsersReply.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushToUsersReply.
                 * @typedef {api.im.v1.BatchPushToUsersReply.$Properties} api.im.v1.BatchPushToUsersReply.$Shape
                 */

                /**
                 * Constructs a new BatchPushToUsersReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushToUsersReply.
                 * @constructor
                 * @param {api.im.v1.BatchPushToUsersReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushToUsersReply = function (properties) {
                    this.failedUserIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushToUsersReply totalDelivered.
                 * @member {number} totalDelivered
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @instance
                 */
                BatchPushToUsersReply.prototype.totalDelivered = 0;

                /**
                 * BatchPushToUsersReply failedUserIds.
                 * @member {Array.<number|Long>} failedUserIds
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @instance
                 */
                BatchPushToUsersReply.prototype.failedUserIds = $util.emptyArray;

                /**
                 * Creates a new BatchPushToUsersReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushToUsersReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushToUsersReply} BatchPushToUsersReply instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushToUsersReply.$Shape): api.im.v1.BatchPushToUsersReply & api.im.v1.BatchPushToUsersReply.$Shape;
                 *   (properties?: api.im.v1.BatchPushToUsersReply.$Properties): api.im.v1.BatchPushToUsersReply;
                 * }}
                 */
                BatchPushToUsersReply.create = function(properties) {
                    return new BatchPushToUsersReply(properties);
                };

                /**
                 * Encodes the specified BatchPushToUsersReply message. Does not implicitly {@link api.im.v1.BatchPushToUsersReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushToUsersReply.$Properties} message BatchPushToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushToUsersReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.totalDelivered);
                    if (message.failedUserIds != null && message.failedUserIds.length) {
                        writer.uint32(/* id 2, wireType 2 =*/18).fork();
                        for (let i = 0; i < message.failedUserIds.length; ++i)
                            writer.int64(message.failedUserIds[i]);
                        writer.ldelim();
                    }
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushToUsersReply message, length delimited. Does not implicitly {@link api.im.v1.BatchPushToUsersReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushToUsersReply.$Properties} message BatchPushToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushToUsersReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushToUsersReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushToUsersReply & api.im.v1.BatchPushToUsersReply.$Shape} BatchPushToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushToUsersReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushToUsersReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.totalDelivered = value;
                                else
                                    delete message.totalDelivered;
                                continue;
                            }
                        case 2: {
                                if (wireType === 2) {
                                    if (!(message.failedUserIds && message.failedUserIds.length))
                                        message.failedUserIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.failedUserIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.failedUserIds && message.failedUserIds.length))
                                    message.failedUserIds = [];
                                message.failedUserIds.push(reader.int64());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushToUsersReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushToUsersReply & api.im.v1.BatchPushToUsersReply.$Shape} BatchPushToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushToUsersReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushToUsersReply message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushToUsersReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        if (!$util.isInteger(message.totalDelivered))
                            return "totalDelivered: integer expected";
                    if (message.failedUserIds != null && $Object.hasOwnProperty.call(message, "failedUserIds")) {
                        if (!$Array.isArray(message.failedUserIds))
                            return "failedUserIds: array expected";
                        for (let i = 0; i < message.failedUserIds.length; ++i)
                            if (!$util.isInteger(message.failedUserIds[i]) && !(message.failedUserIds[i] && $util.isInteger(message.failedUserIds[i].low) && $util.isInteger(message.failedUserIds[i].high)))
                                return "failedUserIds: integer|Long[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushToUsersReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushToUsersReply} BatchPushToUsersReply
                 */
                BatchPushToUsersReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushToUsersReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushToUsersReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushToUsersReply();
                    if (object.totalDelivered != null)
                        if ($Number(object.totalDelivered) !== 0)
                            message.totalDelivered = object.totalDelivered | 0;
                    if (object.failedUserIds) {
                        if (!$Array.isArray(object.failedUserIds))
                            throw $TypeError(".api.im.v1.BatchPushToUsersReply.failedUserIds: array expected");
                        message.failedUserIds = $Array(object.failedUserIds.length);
                        for (let i = 0; i < object.failedUserIds.length; ++i)
                            if ($util.Long)
                                message.failedUserIds[i] = $util.Long.fromValue(object.failedUserIds[i], false);
                            else if (typeof object.failedUserIds[i] === "string")
                                message.failedUserIds[i] = $parseInt(object.failedUserIds[i], 10);
                            else if (typeof object.failedUserIds[i] === "number")
                                message.failedUserIds[i] = object.failedUserIds[i];
                            else if (typeof object.failedUserIds[i] === "object")
                                message.failedUserIds[i] = new $util.LongBits(object.failedUserIds[i].low >>> 0, object.failedUserIds[i].high >>> 0).toNumber();
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushToUsersReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushToUsersReply} message BatchPushToUsersReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushToUsersReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.failedUserIds = [];
                    if (options.defaults)
                        object.totalDelivered = 0;
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        object.totalDelivered = message.totalDelivered;
                    if (message.failedUserIds && message.failedUserIds.length) {
                        object.failedUserIds = $Array(message.failedUserIds.length);
                        for (let j = 0; j < message.failedUserIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.failedUserIds[j] = typeof message.failedUserIds[j] === "number" ? $BigInt(message.failedUserIds[j]) : $util.Long.fromBits(message.failedUserIds[j].low >>> 0, message.failedUserIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.failedUserIds[j] === "number")
                                object.failedUserIds[j] = options.longs === $String ? $String(message.failedUserIds[j]) : message.failedUserIds[j];
                            else
                                object.failedUserIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.failedUserIds[j]) : options.longs === $Number ? new $util.LongBits(message.failedUserIds[j].low >>> 0, message.failedUserIds[j].high >>> 0).toNumber() : message.failedUserIds[j];
                    }
                    return object;
                };

                /**
                 * Converts this BatchPushToUsersReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushToUsersReply.prototype.toJSON = function() {
                    return BatchPushToUsersReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushToUsersReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushToUsersReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushToUsersReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushToUsersReply";
                };

                return BatchPushToUsersReply;
            })();

            v1.PushReceiptToUserRequest = (function() {

                /**
                 * Properties of a PushReceiptToUserRequest.
                 * @typedef {Object} api.im.v1.PushReceiptToUserRequest.$Properties
                 * @property {number|Long|null} [userId] PushReceiptToUserRequest userId
                 * @property {api.im.v1.SendReceipt.$Properties|null} [receipt] PushReceiptToUserRequest receipt
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PushReceiptToUserRequest.
                 * @memberof api.im.v1
                 * @interface IPushReceiptToUserRequest
                 * @augments api.im.v1.PushReceiptToUserRequest.$Properties
                 * @deprecated Use api.im.v1.PushReceiptToUserRequest.$Properties instead.
                 */

                /**
                 * Shape of a PushReceiptToUserRequest.
                 * @typedef {api.im.v1.PushReceiptToUserRequest.$Properties} api.im.v1.PushReceiptToUserRequest.$Shape
                 */

                /**
                 * Constructs a new PushReceiptToUserRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a PushReceiptToUserRequest.
                 * @constructor
                 * @param {api.im.v1.PushReceiptToUserRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PushReceiptToUserRequest = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PushReceiptToUserRequest userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @instance
                 */
                PushReceiptToUserRequest.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * PushReceiptToUserRequest receipt.
                 * @member {api.im.v1.SendReceipt.$Properties|null|undefined} receipt
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @instance
                 */
                PushReceiptToUserRequest.prototype.receipt = null;

                /**
                 * Creates a new PushReceiptToUserRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {api.im.v1.PushReceiptToUserRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PushReceiptToUserRequest} PushReceiptToUserRequest instance
                 * @type {{
                 *   (properties: api.im.v1.PushReceiptToUserRequest.$Shape): api.im.v1.PushReceiptToUserRequest & api.im.v1.PushReceiptToUserRequest.$Shape;
                 *   (properties?: api.im.v1.PushReceiptToUserRequest.$Properties): api.im.v1.PushReceiptToUserRequest;
                 * }}
                 */
                PushReceiptToUserRequest.create = function(properties) {
                    return new PushReceiptToUserRequest(properties);
                };

                /**
                 * Encodes the specified PushReceiptToUserRequest message. Does not implicitly {@link api.im.v1.PushReceiptToUserRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {api.im.v1.PushReceiptToUserRequest.$Properties} message PushReceiptToUserRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushReceiptToUserRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        $root.api.im.v1.SendReceipt.encode(message.receipt, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PushReceiptToUserRequest message, length delimited. Does not implicitly {@link api.im.v1.PushReceiptToUserRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {api.im.v1.PushReceiptToUserRequest.$Properties} message PushReceiptToUserRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushReceiptToUserRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PushReceiptToUserRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PushReceiptToUserRequest & api.im.v1.PushReceiptToUserRequest.$Shape} PushReceiptToUserRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushReceiptToUserRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PushReceiptToUserRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.receipt = $root.api.im.v1.SendReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receipt);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PushReceiptToUserRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PushReceiptToUserRequest & api.im.v1.PushReceiptToUserRequest.$Shape} PushReceiptToUserRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushReceiptToUserRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PushReceiptToUserRequest message.
                 * @function verify
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PushReceiptToUserRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt")) {
                        let error = $root.api.im.v1.SendReceipt.verify(message.receipt, _depth + 1);
                        if (error)
                            return "receipt." + error;
                    }
                    return null;
                };

                /**
                 * Creates a PushReceiptToUserRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PushReceiptToUserRequest} PushReceiptToUserRequest
                 */
                PushReceiptToUserRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PushReceiptToUserRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PushReceiptToUserRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PushReceiptToUserRequest();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    if (object.receipt != null) {
                        if (!$util.isObject(object.receipt))
                            throw $TypeError(".api.im.v1.PushReceiptToUserRequest.receipt: object expected");
                        message.receipt = $root.api.im.v1.SendReceipt.fromObject(object.receipt, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a PushReceiptToUserRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {api.im.v1.PushReceiptToUserRequest} message PushReceiptToUserRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PushReceiptToUserRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.receipt = null;
                    }
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        object.receipt = $root.api.im.v1.SendReceipt.toObject(message.receipt, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this PushReceiptToUserRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PushReceiptToUserRequest.prototype.toJSON = function() {
                    return PushReceiptToUserRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PushReceiptToUserRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.PushReceiptToUserRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PushReceiptToUserRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PushReceiptToUserRequest";
                };

                return PushReceiptToUserRequest;
            })();

            v1.PushReceiptToUserReply = (function() {

                /**
                 * Properties of a PushReceiptToUserReply.
                 * @typedef {Object} api.im.v1.PushReceiptToUserReply.$Properties
                 * @property {boolean|null} [success] PushReceiptToUserReply success
                 * @property {number|null} [deliveredCount] PushReceiptToUserReply deliveredCount
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a PushReceiptToUserReply.
                 * @memberof api.im.v1
                 * @interface IPushReceiptToUserReply
                 * @augments api.im.v1.PushReceiptToUserReply.$Properties
                 * @deprecated Use api.im.v1.PushReceiptToUserReply.$Properties instead.
                 */

                /**
                 * Shape of a PushReceiptToUserReply.
                 * @typedef {api.im.v1.PushReceiptToUserReply.$Properties} api.im.v1.PushReceiptToUserReply.$Shape
                 */

                /**
                 * Constructs a new PushReceiptToUserReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a PushReceiptToUserReply.
                 * @constructor
                 * @param {api.im.v1.PushReceiptToUserReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const PushReceiptToUserReply = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * PushReceiptToUserReply success.
                 * @member {boolean} success
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @instance
                 */
                PushReceiptToUserReply.prototype.success = false;

                /**
                 * PushReceiptToUserReply deliveredCount.
                 * @member {number} deliveredCount
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @instance
                 */
                PushReceiptToUserReply.prototype.deliveredCount = 0;

                /**
                 * Creates a new PushReceiptToUserReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {api.im.v1.PushReceiptToUserReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.PushReceiptToUserReply} PushReceiptToUserReply instance
                 * @type {{
                 *   (properties: api.im.v1.PushReceiptToUserReply.$Shape): api.im.v1.PushReceiptToUserReply & api.im.v1.PushReceiptToUserReply.$Shape;
                 *   (properties?: api.im.v1.PushReceiptToUserReply.$Properties): api.im.v1.PushReceiptToUserReply;
                 * }}
                 */
                PushReceiptToUserReply.create = function(properties) {
                    return new PushReceiptToUserReply(properties);
                };

                /**
                 * Encodes the specified PushReceiptToUserReply message. Does not implicitly {@link api.im.v1.PushReceiptToUserReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {api.im.v1.PushReceiptToUserReply.$Properties} message PushReceiptToUserReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushReceiptToUserReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deliveredCount);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PushReceiptToUserReply message, length delimited. Does not implicitly {@link api.im.v1.PushReceiptToUserReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {api.im.v1.PushReceiptToUserReply.$Properties} message PushReceiptToUserReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PushReceiptToUserReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PushReceiptToUserReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.PushReceiptToUserReply & api.im.v1.PushReceiptToUserReply.$Shape} PushReceiptToUserReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushReceiptToUserReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.PushReceiptToUserReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.success = value;
                                else
                                    delete message.success;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.deliveredCount = value;
                                else
                                    delete message.deliveredCount;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PushReceiptToUserReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.PushReceiptToUserReply & api.im.v1.PushReceiptToUserReply.$Shape} PushReceiptToUserReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PushReceiptToUserReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PushReceiptToUserReply message.
                 * @function verify
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PushReceiptToUserReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        if (typeof message.success !== "boolean")
                            return "success: boolean expected";
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        if (!$util.isInteger(message.deliveredCount))
                            return "deliveredCount: integer expected";
                    return null;
                };

                /**
                 * Creates a PushReceiptToUserReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.PushReceiptToUserReply} PushReceiptToUserReply
                 */
                PushReceiptToUserReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.PushReceiptToUserReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.PushReceiptToUserReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.PushReceiptToUserReply();
                    if (object.success != null)
                        if (object.success)
                            message.success = $Boolean(object.success);
                    if (object.deliveredCount != null)
                        if ($Number(object.deliveredCount) !== 0)
                            message.deliveredCount = object.deliveredCount | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a PushReceiptToUserReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {api.im.v1.PushReceiptToUserReply} message PushReceiptToUserReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PushReceiptToUserReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.success = false;
                        object.deliveredCount = 0;
                    }
                    if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                        object.success = message.success;
                    if (message.deliveredCount != null && $Object.hasOwnProperty.call(message, "deliveredCount"))
                        object.deliveredCount = message.deliveredCount;
                    return object;
                };

                /**
                 * Converts this PushReceiptToUserReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PushReceiptToUserReply.prototype.toJSON = function() {
                    return PushReceiptToUserReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for PushReceiptToUserReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.PushReceiptToUserReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                PushReceiptToUserReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.PushReceiptToUserReply";
                };

                return PushReceiptToUserReply;
            })();

            v1.BatchPushReceiptToUsersRequest = (function() {

                /**
                 * Properties of a BatchPushReceiptToUsersRequest.
                 * @typedef {Object} api.im.v1.BatchPushReceiptToUsersRequest.$Properties
                 * @property {Array.<number|Long>|null} [userIds] BatchPushReceiptToUsersRequest userIds
                 * @property {api.im.v1.SendReceipt.$Properties|null} [receipt] BatchPushReceiptToUsersRequest receipt
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushReceiptToUsersRequest.
                 * @memberof api.im.v1
                 * @interface IBatchPushReceiptToUsersRequest
                 * @augments api.im.v1.BatchPushReceiptToUsersRequest.$Properties
                 * @deprecated Use api.im.v1.BatchPushReceiptToUsersRequest.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushReceiptToUsersRequest.
                 * @typedef {api.im.v1.BatchPushReceiptToUsersRequest.$Properties} api.im.v1.BatchPushReceiptToUsersRequest.$Shape
                 */

                /**
                 * Constructs a new BatchPushReceiptToUsersRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushReceiptToUsersRequest.
                 * @constructor
                 * @param {api.im.v1.BatchPushReceiptToUsersRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushReceiptToUsersRequest = function (properties) {
                    this.userIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushReceiptToUsersRequest userIds.
                 * @member {Array.<number|Long>} userIds
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @instance
                 */
                BatchPushReceiptToUsersRequest.prototype.userIds = $util.emptyArray;

                /**
                 * BatchPushReceiptToUsersRequest receipt.
                 * @member {api.im.v1.SendReceipt.$Properties|null|undefined} receipt
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @instance
                 */
                BatchPushReceiptToUsersRequest.prototype.receipt = null;

                /**
                 * Creates a new BatchPushReceiptToUsersRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushReceiptToUsersRequest} BatchPushReceiptToUsersRequest instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushReceiptToUsersRequest.$Shape): api.im.v1.BatchPushReceiptToUsersRequest & api.im.v1.BatchPushReceiptToUsersRequest.$Shape;
                 *   (properties?: api.im.v1.BatchPushReceiptToUsersRequest.$Properties): api.im.v1.BatchPushReceiptToUsersRequest;
                 * }}
                 */
                BatchPushReceiptToUsersRequest.create = function(properties) {
                    return new BatchPushReceiptToUsersRequest(properties);
                };

                /**
                 * Encodes the specified BatchPushReceiptToUsersRequest message. Does not implicitly {@link api.im.v1.BatchPushReceiptToUsersRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersRequest.$Properties} message BatchPushReceiptToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptToUsersRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userIds != null && message.userIds.length) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork();
                        for (let i = 0; i < message.userIds.length; ++i)
                            writer.int64(message.userIds[i]);
                        writer.ldelim();
                    }
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        $root.api.im.v1.SendReceipt.encode(message.receipt, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushReceiptToUsersRequest message, length delimited. Does not implicitly {@link api.im.v1.BatchPushReceiptToUsersRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersRequest.$Properties} message BatchPushReceiptToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptToUsersRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushReceiptToUsersRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushReceiptToUsersRequest & api.im.v1.BatchPushReceiptToUsersRequest.$Shape} BatchPushReceiptToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptToUsersRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushReceiptToUsersRequest(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType === 2) {
                                    if (!(message.userIds && message.userIds.length))
                                        message.userIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.userIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.userIds && message.userIds.length))
                                    message.userIds = [];
                                message.userIds.push(reader.int64());
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.receipt = $root.api.im.v1.SendReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receipt);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushReceiptToUsersRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushReceiptToUsersRequest & api.im.v1.BatchPushReceiptToUsersRequest.$Shape} BatchPushReceiptToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptToUsersRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushReceiptToUsersRequest message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushReceiptToUsersRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userIds != null && $Object.hasOwnProperty.call(message, "userIds")) {
                        if (!$Array.isArray(message.userIds))
                            return "userIds: array expected";
                        for (let i = 0; i < message.userIds.length; ++i)
                            if (!$util.isInteger(message.userIds[i]) && !(message.userIds[i] && $util.isInteger(message.userIds[i].low) && $util.isInteger(message.userIds[i].high)))
                                return "userIds: integer|Long[] expected";
                    }
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt")) {
                        let error = $root.api.im.v1.SendReceipt.verify(message.receipt, _depth + 1);
                        if (error)
                            return "receipt." + error;
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushReceiptToUsersRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushReceiptToUsersRequest} BatchPushReceiptToUsersRequest
                 */
                BatchPushReceiptToUsersRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushReceiptToUsersRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushReceiptToUsersRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushReceiptToUsersRequest();
                    if (object.userIds) {
                        if (!$Array.isArray(object.userIds))
                            throw $TypeError(".api.im.v1.BatchPushReceiptToUsersRequest.userIds: array expected");
                        message.userIds = $Array(object.userIds.length);
                        for (let i = 0; i < object.userIds.length; ++i)
                            if ($util.Long)
                                message.userIds[i] = $util.Long.fromValue(object.userIds[i], false);
                            else if (typeof object.userIds[i] === "string")
                                message.userIds[i] = $parseInt(object.userIds[i], 10);
                            else if (typeof object.userIds[i] === "number")
                                message.userIds[i] = object.userIds[i];
                            else if (typeof object.userIds[i] === "object")
                                message.userIds[i] = new $util.LongBits(object.userIds[i].low >>> 0, object.userIds[i].high >>> 0).toNumber();
                    }
                    if (object.receipt != null) {
                        if (!$util.isObject(object.receipt))
                            throw $TypeError(".api.im.v1.BatchPushReceiptToUsersRequest.receipt: object expected");
                        message.receipt = $root.api.im.v1.SendReceipt.fromObject(object.receipt, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushReceiptToUsersRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersRequest} message BatchPushReceiptToUsersRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushReceiptToUsersRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.userIds = [];
                    if (options.defaults)
                        object.receipt = null;
                    if (message.userIds && message.userIds.length) {
                        object.userIds = $Array(message.userIds.length);
                        for (let j = 0; j < message.userIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.userIds[j] = typeof message.userIds[j] === "number" ? $BigInt(message.userIds[j]) : $util.Long.fromBits(message.userIds[j].low >>> 0, message.userIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.userIds[j] === "number")
                                object.userIds[j] = options.longs === $String ? $String(message.userIds[j]) : message.userIds[j];
                            else
                                object.userIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.userIds[j]) : options.longs === $Number ? new $util.LongBits(message.userIds[j].low >>> 0, message.userIds[j].high >>> 0).toNumber() : message.userIds[j];
                    }
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        object.receipt = $root.api.im.v1.SendReceipt.toObject(message.receipt, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this BatchPushReceiptToUsersRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushReceiptToUsersRequest.prototype.toJSON = function() {
                    return BatchPushReceiptToUsersRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushReceiptToUsersRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushReceiptToUsersRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushReceiptToUsersRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushReceiptToUsersRequest";
                };

                return BatchPushReceiptToUsersRequest;
            })();

            v1.BatchPushReceiptToUsersReply = (function() {

                /**
                 * Properties of a BatchPushReceiptToUsersReply.
                 * @typedef {Object} api.im.v1.BatchPushReceiptToUsersReply.$Properties
                 * @property {number|null} [totalDelivered] BatchPushReceiptToUsersReply totalDelivered
                 * @property {Array.<number|Long>|null} [failedUserIds] BatchPushReceiptToUsersReply failedUserIds
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushReceiptToUsersReply.
                 * @memberof api.im.v1
                 * @interface IBatchPushReceiptToUsersReply
                 * @augments api.im.v1.BatchPushReceiptToUsersReply.$Properties
                 * @deprecated Use api.im.v1.BatchPushReceiptToUsersReply.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushReceiptToUsersReply.
                 * @typedef {api.im.v1.BatchPushReceiptToUsersReply.$Properties} api.im.v1.BatchPushReceiptToUsersReply.$Shape
                 */

                /**
                 * Constructs a new BatchPushReceiptToUsersReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushReceiptToUsersReply.
                 * @constructor
                 * @param {api.im.v1.BatchPushReceiptToUsersReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushReceiptToUsersReply = function (properties) {
                    this.failedUserIds = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushReceiptToUsersReply totalDelivered.
                 * @member {number} totalDelivered
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @instance
                 */
                BatchPushReceiptToUsersReply.prototype.totalDelivered = 0;

                /**
                 * BatchPushReceiptToUsersReply failedUserIds.
                 * @member {Array.<number|Long>} failedUserIds
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @instance
                 */
                BatchPushReceiptToUsersReply.prototype.failedUserIds = $util.emptyArray;

                /**
                 * Creates a new BatchPushReceiptToUsersReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushReceiptToUsersReply} BatchPushReceiptToUsersReply instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushReceiptToUsersReply.$Shape): api.im.v1.BatchPushReceiptToUsersReply & api.im.v1.BatchPushReceiptToUsersReply.$Shape;
                 *   (properties?: api.im.v1.BatchPushReceiptToUsersReply.$Properties): api.im.v1.BatchPushReceiptToUsersReply;
                 * }}
                 */
                BatchPushReceiptToUsersReply.create = function(properties) {
                    return new BatchPushReceiptToUsersReply(properties);
                };

                /**
                 * Encodes the specified BatchPushReceiptToUsersReply message. Does not implicitly {@link api.im.v1.BatchPushReceiptToUsersReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersReply.$Properties} message BatchPushReceiptToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptToUsersReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.totalDelivered);
                    if (message.failedUserIds != null && message.failedUserIds.length) {
                        writer.uint32(/* id 2, wireType 2 =*/18).fork();
                        for (let i = 0; i < message.failedUserIds.length; ++i)
                            writer.int64(message.failedUserIds[i]);
                        writer.ldelim();
                    }
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushReceiptToUsersReply message, length delimited. Does not implicitly {@link api.im.v1.BatchPushReceiptToUsersReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersReply.$Properties} message BatchPushReceiptToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptToUsersReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushReceiptToUsersReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushReceiptToUsersReply & api.im.v1.BatchPushReceiptToUsersReply.$Shape} BatchPushReceiptToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptToUsersReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushReceiptToUsersReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.totalDelivered = value;
                                else
                                    delete message.totalDelivered;
                                continue;
                            }
                        case 2: {
                                if (wireType === 2) {
                                    if (!(message.failedUserIds && message.failedUserIds.length))
                                        message.failedUserIds = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.failedUserIds.push(reader.int64());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.failedUserIds && message.failedUserIds.length))
                                    message.failedUserIds = [];
                                message.failedUserIds.push(reader.int64());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushReceiptToUsersReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushReceiptToUsersReply & api.im.v1.BatchPushReceiptToUsersReply.$Shape} BatchPushReceiptToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptToUsersReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushReceiptToUsersReply message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushReceiptToUsersReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        if (!$util.isInteger(message.totalDelivered))
                            return "totalDelivered: integer expected";
                    if (message.failedUserIds != null && $Object.hasOwnProperty.call(message, "failedUserIds")) {
                        if (!$Array.isArray(message.failedUserIds))
                            return "failedUserIds: array expected";
                        for (let i = 0; i < message.failedUserIds.length; ++i)
                            if (!$util.isInteger(message.failedUserIds[i]) && !(message.failedUserIds[i] && $util.isInteger(message.failedUserIds[i].low) && $util.isInteger(message.failedUserIds[i].high)))
                                return "failedUserIds: integer|Long[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushReceiptToUsersReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushReceiptToUsersReply} BatchPushReceiptToUsersReply
                 */
                BatchPushReceiptToUsersReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushReceiptToUsersReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushReceiptToUsersReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushReceiptToUsersReply();
                    if (object.totalDelivered != null)
                        if ($Number(object.totalDelivered) !== 0)
                            message.totalDelivered = object.totalDelivered | 0;
                    if (object.failedUserIds) {
                        if (!$Array.isArray(object.failedUserIds))
                            throw $TypeError(".api.im.v1.BatchPushReceiptToUsersReply.failedUserIds: array expected");
                        message.failedUserIds = $Array(object.failedUserIds.length);
                        for (let i = 0; i < object.failedUserIds.length; ++i)
                            if ($util.Long)
                                message.failedUserIds[i] = $util.Long.fromValue(object.failedUserIds[i], false);
                            else if (typeof object.failedUserIds[i] === "string")
                                message.failedUserIds[i] = $parseInt(object.failedUserIds[i], 10);
                            else if (typeof object.failedUserIds[i] === "number")
                                message.failedUserIds[i] = object.failedUserIds[i];
                            else if (typeof object.failedUserIds[i] === "object")
                                message.failedUserIds[i] = new $util.LongBits(object.failedUserIds[i].low >>> 0, object.failedUserIds[i].high >>> 0).toNumber();
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushReceiptToUsersReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptToUsersReply} message BatchPushReceiptToUsersReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushReceiptToUsersReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.failedUserIds = [];
                    if (options.defaults)
                        object.totalDelivered = 0;
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        object.totalDelivered = message.totalDelivered;
                    if (message.failedUserIds && message.failedUserIds.length) {
                        object.failedUserIds = $Array(message.failedUserIds.length);
                        for (let j = 0; j < message.failedUserIds.length; ++j)
                            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                object.failedUserIds[j] = typeof message.failedUserIds[j] === "number" ? $BigInt(message.failedUserIds[j]) : $util.Long.fromBits(message.failedUserIds[j].low >>> 0, message.failedUserIds[j].high >>> 0, false).toBigInt();
                            else if (typeof message.failedUserIds[j] === "number")
                                object.failedUserIds[j] = options.longs === $String ? $String(message.failedUserIds[j]) : message.failedUserIds[j];
                            else
                                object.failedUserIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.failedUserIds[j]) : options.longs === $Number ? new $util.LongBits(message.failedUserIds[j].low >>> 0, message.failedUserIds[j].high >>> 0).toNumber() : message.failedUserIds[j];
                    }
                    return object;
                };

                /**
                 * Converts this BatchPushReceiptToUsersReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushReceiptToUsersReply.prototype.toJSON = function() {
                    return BatchPushReceiptToUsersReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushReceiptToUsersReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushReceiptToUsersReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushReceiptToUsersReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushReceiptToUsersReply";
                };

                return BatchPushReceiptToUsersReply;
            })();

            v1.ReceiptBatchItem = (function() {

                /**
                 * Properties of a ReceiptBatchItem.
                 * @typedef {Object} api.im.v1.ReceiptBatchItem.$Properties
                 * @property {number|Long|null} [userId] ReceiptBatchItem userId
                 * @property {api.im.v1.SendReceipt.$Properties|null} [receipt] ReceiptBatchItem receipt
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a ReceiptBatchItem.
                 * @memberof api.im.v1
                 * @interface IReceiptBatchItem
                 * @augments api.im.v1.ReceiptBatchItem.$Properties
                 * @deprecated Use api.im.v1.ReceiptBatchItem.$Properties instead.
                 */

                /**
                 * Shape of a ReceiptBatchItem.
                 * @typedef {api.im.v1.ReceiptBatchItem.$Properties} api.im.v1.ReceiptBatchItem.$Shape
                 */

                /**
                 * Constructs a new ReceiptBatchItem.
                 * @memberof api.im.v1
                 * @classdesc Represents a ReceiptBatchItem.
                 * @constructor
                 * @param {api.im.v1.ReceiptBatchItem.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const ReceiptBatchItem = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * ReceiptBatchItem userId.
                 * @member {number|Long} userId
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @instance
                 */
                ReceiptBatchItem.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                /**
                 * ReceiptBatchItem receipt.
                 * @member {api.im.v1.SendReceipt.$Properties|null|undefined} receipt
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @instance
                 */
                ReceiptBatchItem.prototype.receipt = null;

                /**
                 * Creates a new ReceiptBatchItem instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {api.im.v1.ReceiptBatchItem.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.ReceiptBatchItem} ReceiptBatchItem instance
                 * @type {{
                 *   (properties: api.im.v1.ReceiptBatchItem.$Shape): api.im.v1.ReceiptBatchItem & api.im.v1.ReceiptBatchItem.$Shape;
                 *   (properties?: api.im.v1.ReceiptBatchItem.$Properties): api.im.v1.ReceiptBatchItem;
                 * }}
                 */
                ReceiptBatchItem.create = function(properties) {
                    return new ReceiptBatchItem(properties);
                };

                /**
                 * Encodes the specified ReceiptBatchItem message. Does not implicitly {@link api.im.v1.ReceiptBatchItem.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {api.im.v1.ReceiptBatchItem.$Properties} message ReceiptBatchItem message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReceiptBatchItem.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int64(message.userId);
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        $root.api.im.v1.SendReceipt.encode(message.receipt, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ReceiptBatchItem message, length delimited. Does not implicitly {@link api.im.v1.ReceiptBatchItem.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {api.im.v1.ReceiptBatchItem.$Properties} message ReceiptBatchItem message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReceiptBatchItem.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ReceiptBatchItem message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.ReceiptBatchItem & api.im.v1.ReceiptBatchItem.$Shape} ReceiptBatchItem
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReceiptBatchItem.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.ReceiptBatchItem(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                    message.userId = value;
                                else
                                    delete message.userId;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.receipt = $root.api.im.v1.SendReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receipt);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ReceiptBatchItem message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.ReceiptBatchItem & api.im.v1.ReceiptBatchItem.$Shape} ReceiptBatchItem
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReceiptBatchItem.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ReceiptBatchItem message.
                 * @function verify
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ReceiptBatchItem.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (!$util.isInteger(message.userId) && !(message.userId && $util.isInteger(message.userId.low) && $util.isInteger(message.userId.high)))
                            return "userId: integer|Long expected";
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt")) {
                        let error = $root.api.im.v1.SendReceipt.verify(message.receipt, _depth + 1);
                        if (error)
                            return "receipt." + error;
                    }
                    return null;
                };

                /**
                 * Creates a ReceiptBatchItem message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.ReceiptBatchItem} ReceiptBatchItem
                 */
                ReceiptBatchItem.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.ReceiptBatchItem)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.ReceiptBatchItem: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.ReceiptBatchItem();
                    if (object.userId != null)
                        if (typeof object.userId === "object" ? object.userId.low || object.userId.high : $Number(object.userId) !== 0)
                            if ($util.Long)
                                message.userId = $util.Long.fromValue(object.userId, false);
                            else if (typeof object.userId === "string")
                                message.userId = $parseInt(object.userId, 10);
                            else if (typeof object.userId === "number")
                                message.userId = object.userId;
                            else if (typeof object.userId === "object")
                                message.userId = new $util.LongBits(object.userId.low >>> 0, object.userId.high >>> 0).toNumber();
                    if (object.receipt != null) {
                        if (!$util.isObject(object.receipt))
                            throw $TypeError(".api.im.v1.ReceiptBatchItem.receipt: object expected");
                        message.receipt = $root.api.im.v1.SendReceipt.fromObject(object.receipt, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a ReceiptBatchItem message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {api.im.v1.ReceiptBatchItem} message ReceiptBatchItem
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ReceiptBatchItem.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        if ($util.Long) {
                            let long = new $util.Long(0, 0, false);
                            object.userId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                        } else
                            object.userId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                        object.receipt = null;
                    }
                    if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                            object.userId = typeof message.userId === "number" ? $BigInt(message.userId) : $util.Long.fromBits(message.userId.low >>> 0, message.userId.high >>> 0, false).toBigInt();
                        else if (typeof message.userId === "number")
                            object.userId = options.longs === $String ? $String(message.userId) : message.userId;
                        else
                            object.userId = options.longs === $String ? $util.Long.prototype.toString.call(message.userId) : options.longs === $Number ? new $util.LongBits(message.userId.low >>> 0, message.userId.high >>> 0).toNumber() : message.userId;
                    if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                        object.receipt = $root.api.im.v1.SendReceipt.toObject(message.receipt, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this ReceiptBatchItem to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ReceiptBatchItem.prototype.toJSON = function() {
                    return ReceiptBatchItem.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for ReceiptBatchItem
                 * @function getTypeUrl
                 * @memberof api.im.v1.ReceiptBatchItem
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                ReceiptBatchItem.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.ReceiptBatchItem";
                };

                return ReceiptBatchItem;
            })();

            v1.BatchPushReceiptsToUsersRequest = (function() {

                /**
                 * Properties of a BatchPushReceiptsToUsersRequest.
                 * @typedef {Object} api.im.v1.BatchPushReceiptsToUsersRequest.$Properties
                 * @property {Array.<api.im.v1.ReceiptBatchItem.$Properties>|null} [items] BatchPushReceiptsToUsersRequest items
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushReceiptsToUsersRequest.
                 * @memberof api.im.v1
                 * @interface IBatchPushReceiptsToUsersRequest
                 * @augments api.im.v1.BatchPushReceiptsToUsersRequest.$Properties
                 * @deprecated Use api.im.v1.BatchPushReceiptsToUsersRequest.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushReceiptsToUsersRequest.
                 * @typedef {api.im.v1.BatchPushReceiptsToUsersRequest.$Properties} api.im.v1.BatchPushReceiptsToUsersRequest.$Shape
                 */

                /**
                 * Constructs a new BatchPushReceiptsToUsersRequest.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushReceiptsToUsersRequest.
                 * @constructor
                 * @param {api.im.v1.BatchPushReceiptsToUsersRequest.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushReceiptsToUsersRequest = function (properties) {
                    this.items = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushReceiptsToUsersRequest items.
                 * @member {Array.<api.im.v1.ReceiptBatchItem.$Properties>} items
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @instance
                 */
                BatchPushReceiptsToUsersRequest.prototype.items = $util.emptyArray;

                /**
                 * Creates a new BatchPushReceiptsToUsersRequest instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersRequest.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushReceiptsToUsersRequest} BatchPushReceiptsToUsersRequest instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushReceiptsToUsersRequest.$Shape): api.im.v1.BatchPushReceiptsToUsersRequest & api.im.v1.BatchPushReceiptsToUsersRequest.$Shape;
                 *   (properties?: api.im.v1.BatchPushReceiptsToUsersRequest.$Properties): api.im.v1.BatchPushReceiptsToUsersRequest;
                 * }}
                 */
                BatchPushReceiptsToUsersRequest.create = function(properties) {
                    return new BatchPushReceiptsToUsersRequest(properties);
                };

                /**
                 * Encodes the specified BatchPushReceiptsToUsersRequest message. Does not implicitly {@link api.im.v1.BatchPushReceiptsToUsersRequest.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersRequest.$Properties} message BatchPushReceiptsToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptsToUsersRequest.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.items != null && message.items.length)
                        for (let i = 0; i < message.items.length; ++i)
                            $root.api.im.v1.ReceiptBatchItem.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushReceiptsToUsersRequest message, length delimited. Does not implicitly {@link api.im.v1.BatchPushReceiptsToUsersRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersRequest.$Properties} message BatchPushReceiptsToUsersRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptsToUsersRequest.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushReceiptsToUsersRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushReceiptsToUsersRequest & api.im.v1.BatchPushReceiptsToUsersRequest.$Shape} BatchPushReceiptsToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptsToUsersRequest.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushReceiptsToUsersRequest();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.items && message.items.length))
                                    message.items = [];
                                message.items.push($root.api.im.v1.ReceiptBatchItem.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushReceiptsToUsersRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushReceiptsToUsersRequest & api.im.v1.BatchPushReceiptsToUsersRequest.$Shape} BatchPushReceiptsToUsersRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptsToUsersRequest.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushReceiptsToUsersRequest message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushReceiptsToUsersRequest.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                        if (!$Array.isArray(message.items))
                            return "items: array expected";
                        for (let i = 0; i < message.items.length; ++i) {
                            let error = $root.api.im.v1.ReceiptBatchItem.verify(message.items[i], _depth + 1);
                            if (error)
                                return "items." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushReceiptsToUsersRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushReceiptsToUsersRequest} BatchPushReceiptsToUsersRequest
                 */
                BatchPushReceiptsToUsersRequest.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushReceiptsToUsersRequest)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersRequest: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushReceiptsToUsersRequest();
                    if (object.items) {
                        if (!$Array.isArray(object.items))
                            throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersRequest.items: array expected");
                        message.items = $Array(object.items.length);
                        for (let i = 0; i < object.items.length; ++i) {
                            if (!$util.isObject(object.items[i]))
                                throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersRequest.items: object expected");
                            message.items[i] = $root.api.im.v1.ReceiptBatchItem.fromObject(object.items[i], _depth + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushReceiptsToUsersRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersRequest} message BatchPushReceiptsToUsersRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushReceiptsToUsersRequest.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.items = [];
                    if (message.items && message.items.length) {
                        object.items = $Array(message.items.length);
                        for (let j = 0; j < message.items.length; ++j)
                            object.items[j] = $root.api.im.v1.ReceiptBatchItem.toObject(message.items[j], options, _depth + 1);
                    }
                    return object;
                };

                /**
                 * Converts this BatchPushReceiptsToUsersRequest to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushReceiptsToUsersRequest.prototype.toJSON = function() {
                    return BatchPushReceiptsToUsersRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushReceiptsToUsersRequest
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushReceiptsToUsersRequest
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushReceiptsToUsersRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushReceiptsToUsersRequest";
                };

                return BatchPushReceiptsToUsersRequest;
            })();

            v1.BatchPushReceiptsToUsersReply = (function() {

                /**
                 * Properties of a BatchPushReceiptsToUsersReply.
                 * @typedef {Object} api.im.v1.BatchPushReceiptsToUsersReply.$Properties
                 * @property {number|null} [totalDelivered] BatchPushReceiptsToUsersReply totalDelivered
                 * @property {Array.<api.im.v1.ReceiptBatchItem.$Properties>|null} [failedItems] BatchPushReceiptsToUsersReply failedItems
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a BatchPushReceiptsToUsersReply.
                 * @memberof api.im.v1
                 * @interface IBatchPushReceiptsToUsersReply
                 * @augments api.im.v1.BatchPushReceiptsToUsersReply.$Properties
                 * @deprecated Use api.im.v1.BatchPushReceiptsToUsersReply.$Properties instead.
                 */

                /**
                 * Shape of a BatchPushReceiptsToUsersReply.
                 * @typedef {api.im.v1.BatchPushReceiptsToUsersReply.$Properties} api.im.v1.BatchPushReceiptsToUsersReply.$Shape
                 */

                /**
                 * Constructs a new BatchPushReceiptsToUsersReply.
                 * @memberof api.im.v1
                 * @classdesc Represents a BatchPushReceiptsToUsersReply.
                 * @constructor
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const BatchPushReceiptsToUsersReply = function (properties) {
                    this.failedItems = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * BatchPushReceiptsToUsersReply totalDelivered.
                 * @member {number} totalDelivered
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @instance
                 */
                BatchPushReceiptsToUsersReply.prototype.totalDelivered = 0;

                /**
                 * BatchPushReceiptsToUsersReply failedItems.
                 * @member {Array.<api.im.v1.ReceiptBatchItem.$Properties>} failedItems
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @instance
                 */
                BatchPushReceiptsToUsersReply.prototype.failedItems = $util.emptyArray;

                /**
                 * Creates a new BatchPushReceiptsToUsersReply instance using the specified properties.
                 * @function create
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply.$Properties=} [properties] Properties to set
                 * @returns {api.im.v1.BatchPushReceiptsToUsersReply} BatchPushReceiptsToUsersReply instance
                 * @type {{
                 *   (properties: api.im.v1.BatchPushReceiptsToUsersReply.$Shape): api.im.v1.BatchPushReceiptsToUsersReply & api.im.v1.BatchPushReceiptsToUsersReply.$Shape;
                 *   (properties?: api.im.v1.BatchPushReceiptsToUsersReply.$Properties): api.im.v1.BatchPushReceiptsToUsersReply;
                 * }}
                 */
                BatchPushReceiptsToUsersReply.create = function(properties) {
                    return new BatchPushReceiptsToUsersReply(properties);
                };

                /**
                 * Encodes the specified BatchPushReceiptsToUsersReply message. Does not implicitly {@link api.im.v1.BatchPushReceiptsToUsersReply.verify|verify} messages.
                 * @function encode
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply.$Properties} message BatchPushReceiptsToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptsToUsersReply.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.totalDelivered);
                    if (message.failedItems != null && message.failedItems.length)
                        for (let i = 0; i < message.failedItems.length; ++i)
                            $root.api.im.v1.ReceiptBatchItem.encode(message.failedItems[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BatchPushReceiptsToUsersReply message, length delimited. Does not implicitly {@link api.im.v1.BatchPushReceiptsToUsersReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply.$Properties} message BatchPushReceiptsToUsersReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BatchPushReceiptsToUsersReply.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BatchPushReceiptsToUsersReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {api.im.v1.BatchPushReceiptsToUsersReply & api.im.v1.BatchPushReceiptsToUsersReply.$Shape} BatchPushReceiptsToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptsToUsersReply.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.api.im.v1.BatchPushReceiptsToUsersReply(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.totalDelivered = value;
                                else
                                    delete message.totalDelivered;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.failedItems && message.failedItems.length))
                                    message.failedItems = [];
                                message.failedItems.push($root.api.im.v1.ReceiptBatchItem.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BatchPushReceiptsToUsersReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {api.im.v1.BatchPushReceiptsToUsersReply & api.im.v1.BatchPushReceiptsToUsersReply.$Shape} BatchPushReceiptsToUsersReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BatchPushReceiptsToUsersReply.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BatchPushReceiptsToUsersReply message.
                 * @function verify
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BatchPushReceiptsToUsersReply.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        if (!$util.isInteger(message.totalDelivered))
                            return "totalDelivered: integer expected";
                    if (message.failedItems != null && $Object.hasOwnProperty.call(message, "failedItems")) {
                        if (!$Array.isArray(message.failedItems))
                            return "failedItems: array expected";
                        for (let i = 0; i < message.failedItems.length; ++i) {
                            let error = $root.api.im.v1.ReceiptBatchItem.verify(message.failedItems[i], _depth + 1);
                            if (error)
                                return "failedItems." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a BatchPushReceiptsToUsersReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {api.im.v1.BatchPushReceiptsToUsersReply} BatchPushReceiptsToUsersReply
                 */
                BatchPushReceiptsToUsersReply.fromObject = function (object, _depth) {
                    if (object instanceof $root.api.im.v1.BatchPushReceiptsToUsersReply)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersReply: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.api.im.v1.BatchPushReceiptsToUsersReply();
                    if (object.totalDelivered != null)
                        if ($Number(object.totalDelivered) !== 0)
                            message.totalDelivered = object.totalDelivered | 0;
                    if (object.failedItems) {
                        if (!$Array.isArray(object.failedItems))
                            throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersReply.failedItems: array expected");
                        message.failedItems = $Array(object.failedItems.length);
                        for (let i = 0; i < object.failedItems.length; ++i) {
                            if (!$util.isObject(object.failedItems[i]))
                                throw $TypeError(".api.im.v1.BatchPushReceiptsToUsersReply.failedItems: object expected");
                            message.failedItems[i] = $root.api.im.v1.ReceiptBatchItem.fromObject(object.failedItems[i], _depth + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BatchPushReceiptsToUsersReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {api.im.v1.BatchPushReceiptsToUsersReply} message BatchPushReceiptsToUsersReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BatchPushReceiptsToUsersReply.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.failedItems = [];
                    if (options.defaults)
                        object.totalDelivered = 0;
                    if (message.totalDelivered != null && $Object.hasOwnProperty.call(message, "totalDelivered"))
                        object.totalDelivered = message.totalDelivered;
                    if (message.failedItems && message.failedItems.length) {
                        object.failedItems = $Array(message.failedItems.length);
                        for (let j = 0; j < message.failedItems.length; ++j)
                            object.failedItems[j] = $root.api.im.v1.ReceiptBatchItem.toObject(message.failedItems[j], options, _depth + 1);
                    }
                    return object;
                };

                /**
                 * Converts this BatchPushReceiptsToUsersReply to JSON.
                 * @function toJSON
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BatchPushReceiptsToUsersReply.prototype.toJSON = function() {
                    return BatchPushReceiptsToUsersReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for BatchPushReceiptsToUsersReply
                 * @function getTypeUrl
                 * @memberof api.im.v1.BatchPushReceiptsToUsersReply
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                BatchPushReceiptsToUsersReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/api.im.v1.BatchPushReceiptsToUsersReply";
                };

                return BatchPushReceiptsToUsersReply;
            })();

            return v1;
        })();

        return im;
    })();

    return api;
})();

export const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.api = (function() {

        /**
         * Namespace api.
         * @memberof google
         * @namespace
         */
        const api = {};

        api.Http = (function() {

            /**
             * Properties of a Http.
             * @typedef {Object} google.api.Http.$Properties
             * @property {Array.<google.api.HttpRule.$Properties>|null} [rules] Http rules
             * @property {boolean|null} [fullyDecodeReservedExpansion] Http fullyDecodeReservedExpansion
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Http.
             * @memberof google.api
             * @interface IHttp
             * @augments google.api.Http.$Properties
             * @deprecated Use google.api.Http.$Properties instead.
             */

            /**
             * Shape of a Http.
             * @typedef {{
             *   rules?: Array.<google.api.HttpRule.$Shape>|null;
             *   fullyDecodeReservedExpansion?: boolean|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.api.Http.$Shape
             */

            /**
             * Constructs a new Http.
             * @memberof google.api
             * @classdesc Represents a Http.
             * @constructor
             * @param {google.api.Http.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Http = function (properties) {
                this.rules = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Http rules.
             * @member {Array.<google.api.HttpRule.$Properties>} rules
             * @memberof google.api.Http
             * @instance
             */
            Http.prototype.rules = $util.emptyArray;

            /**
             * Http fullyDecodeReservedExpansion.
             * @member {boolean} fullyDecodeReservedExpansion
             * @memberof google.api.Http
             * @instance
             */
            Http.prototype.fullyDecodeReservedExpansion = false;

            /**
             * Creates a new Http instance using the specified properties.
             * @function create
             * @memberof google.api.Http
             * @static
             * @param {google.api.Http.$Properties=} [properties] Properties to set
             * @returns {google.api.Http} Http instance
             * @type {{
             *   (properties: google.api.Http.$Shape): google.api.Http & google.api.Http.$Shape;
             *   (properties?: google.api.Http.$Properties): google.api.Http;
             * }}
             */
            Http.create = function(properties) {
                return new Http(properties);
            };

            /**
             * Encodes the specified Http message. Does not implicitly {@link google.api.Http.verify|verify} messages.
             * @function encode
             * @memberof google.api.Http
             * @static
             * @param {google.api.Http.$Properties} message Http message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Http.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.rules != null && message.rules.length)
                    for (let i = 0; i < message.rules.length; ++i)
                        $root.google.api.HttpRule.encode(message.rules[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.fullyDecodeReservedExpansion != null && $Object.hasOwnProperty.call(message, "fullyDecodeReservedExpansion"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.fullyDecodeReservedExpansion);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Http message, length delimited. Does not implicitly {@link google.api.Http.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.api.Http
             * @static
             * @param {google.api.Http.$Properties} message Http message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Http.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Http message from the specified reader or buffer.
             * @function decode
             * @memberof google.api.Http
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.api.Http & google.api.Http.$Shape} Http
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Http.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.api.Http(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rules && message.rules.length))
                                message.rules = [];
                            message.rules.push($root.google.api.HttpRule.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.fullyDecodeReservedExpansion = value;
                            else
                                delete message.fullyDecodeReservedExpansion;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Http message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.api.Http
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.api.Http & google.api.Http.$Shape} Http
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Http.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Http message.
             * @function verify
             * @memberof google.api.Http
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Http.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rules != null && $Object.hasOwnProperty.call(message, "rules")) {
                    if (!$Array.isArray(message.rules))
                        return "rules: array expected";
                    for (let i = 0; i < message.rules.length; ++i) {
                        let error = $root.google.api.HttpRule.verify(message.rules[i], _depth + 1);
                        if (error)
                            return "rules." + error;
                    }
                }
                if (message.fullyDecodeReservedExpansion != null && $Object.hasOwnProperty.call(message, "fullyDecodeReservedExpansion"))
                    if (typeof message.fullyDecodeReservedExpansion !== "boolean")
                        return "fullyDecodeReservedExpansion: boolean expected";
                return null;
            };

            /**
             * Creates a Http message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.api.Http
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.api.Http} Http
             */
            Http.fromObject = function (object, _depth) {
                if (object instanceof $root.google.api.Http)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.api.Http: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.api.Http();
                if (object.rules) {
                    if (!$Array.isArray(object.rules))
                        throw $TypeError(".google.api.Http.rules: array expected");
                    message.rules = $Array(object.rules.length);
                    for (let i = 0; i < object.rules.length; ++i) {
                        if (!$util.isObject(object.rules[i]))
                            throw $TypeError(".google.api.Http.rules: object expected");
                        message.rules[i] = $root.google.api.HttpRule.fromObject(object.rules[i], _depth + 1);
                    }
                }
                if (object.fullyDecodeReservedExpansion != null)
                    if (object.fullyDecodeReservedExpansion)
                        message.fullyDecodeReservedExpansion = $Boolean(object.fullyDecodeReservedExpansion);
                return message;
            };

            /**
             * Creates a plain object from a Http message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.api.Http
             * @static
             * @param {google.api.Http} message Http
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Http.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.rules = [];
                if (options.defaults)
                    object.fullyDecodeReservedExpansion = false;
                if (message.rules && message.rules.length) {
                    object.rules = $Array(message.rules.length);
                    for (let j = 0; j < message.rules.length; ++j)
                        object.rules[j] = $root.google.api.HttpRule.toObject(message.rules[j], options, _depth + 1);
                }
                if (message.fullyDecodeReservedExpansion != null && $Object.hasOwnProperty.call(message, "fullyDecodeReservedExpansion"))
                    object.fullyDecodeReservedExpansion = message.fullyDecodeReservedExpansion;
                return object;
            };

            /**
             * Converts this Http to JSON.
             * @function toJSON
             * @memberof google.api.Http
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Http.prototype.toJSON = function() {
                return Http.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Http
             * @function getTypeUrl
             * @memberof google.api.Http
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Http.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.api.Http";
            };

            return Http;
        })();

        api.HttpRule = (function() {

            /**
             * Properties of a HttpRule.
             * @typedef {Object} google.api.HttpRule.$Properties
             * @property {string|null} [selector] HttpRule selector
             * @property {string|null} [get] HttpRule get
             * @property {string|null} [put] HttpRule put
             * @property {string|null} [post] HttpRule post
             * @property {string|null} ["delete"] HttpRule delete
             * @property {string|null} [patch] HttpRule patch
             * @property {google.api.CustomHttpPattern.$Properties|null} [custom] HttpRule custom
             * @property {string|null} [body] HttpRule body
             * @property {string|null} [responseBody] HttpRule responseBody
             * @property {Array.<google.api.HttpRule.$Properties>|null} [additionalBindings] HttpRule additionalBindings
             * @property {"get"|"put"|"post"|"delete"|"patch"|"custom"} [pattern] HttpRule pattern
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HttpRule.
             * @memberof google.api
             * @interface IHttpRule
             * @augments google.api.HttpRule.$Properties
             * @deprecated Use google.api.HttpRule.$Properties instead.
             */

            /**
             * Narrowed shape of a HttpRule.
             * @typedef {{
             *   selector?: string|null;
             *   get?: string|null;
             *   put?: string|null;
             *   post?: string|null;
             *   "delete"?: string|null;
             *   patch?: string|null;
             *   custom?: google.api.CustomHttpPattern.$Shape|null;
             *   body?: string|null;
             *   responseBody?: string|null;
             *   additionalBindings?: Array.<google.api.HttpRule.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ pattern?: undefined; get?: null; put?: null; post?: null; "delete"?: null; patch?: null; custom?: null }|{ pattern?: "get"; get: string; put?: null; post?: null; "delete"?: null; patch?: null; custom?: null }|{ pattern?: "put"; get?: null; put: string; post?: null; "delete"?: null; patch?: null; custom?: null }|{ pattern?: "post"; get?: null; put?: null; post: string; "delete"?: null; patch?: null; custom?: null }|{ pattern?: "delete"; get?: null; put?: null; post?: null; "delete": string; patch?: null; custom?: null }|{ pattern?: "patch"; get?: null; put?: null; post?: null; "delete"?: null; patch: string; custom?: null }|{ pattern?: "custom"; get?: null; put?: null; post?: null; "delete"?: null; patch?: null; custom: google.api.CustomHttpPattern.$Shape })
             * )} google.api.HttpRule.$Shape
             */

            /**
             * Constructs a new HttpRule.
             * @memberof google.api
             * @classdesc Represents a HttpRule.
             * @constructor
             * @param {google.api.HttpRule.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const HttpRule = function (properties) {
                this.additionalBindings = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HttpRule selector.
             * @member {string} selector
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.selector = "";

            /**
             * HttpRule get.
             * @member {string|null|undefined} get
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.get = null;

            /**
             * HttpRule put.
             * @member {string|null|undefined} put
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.put = null;

            /**
             * HttpRule post.
             * @member {string|null|undefined} post
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.post = null;

            /**
             * HttpRule delete.
             * @member {string|null|undefined} delete
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype["delete"] = null;

            /**
             * HttpRule patch.
             * @member {string|null|undefined} patch
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.patch = null;

            /**
             * HttpRule custom.
             * @member {google.api.CustomHttpPattern.$Properties|null|undefined} custom
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.custom = null;

            /**
             * HttpRule body.
             * @member {string} body
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.body = "";

            /**
             * HttpRule responseBody.
             * @member {string} responseBody
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.responseBody = "";

            /**
             * HttpRule additionalBindings.
             * @member {Array.<google.api.HttpRule.$Properties>} additionalBindings
             * @memberof google.api.HttpRule
             * @instance
             */
            HttpRule.prototype.additionalBindings = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * HttpRule pattern.
             * @member {"get"|"put"|"post"|"delete"|"patch"|"custom"|undefined} pattern
             * @memberof google.api.HttpRule
             * @instance
             */
            $Object.defineProperty(HttpRule.prototype, "pattern", {
                get: $util.oneOfGetter($oneOfFields = ["get", "put", "post", "delete", "patch", "custom"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HttpRule instance using the specified properties.
             * @function create
             * @memberof google.api.HttpRule
             * @static
             * @param {google.api.HttpRule.$Properties=} [properties] Properties to set
             * @returns {google.api.HttpRule} HttpRule instance
             * @type {{
             *   (properties: google.api.HttpRule.$Shape): google.api.HttpRule & google.api.HttpRule.$Shape;
             *   (properties?: google.api.HttpRule.$Properties): google.api.HttpRule;
             * }}
             */
            HttpRule.create = function(properties) {
                return new HttpRule(properties);
            };

            /**
             * Encodes the specified HttpRule message. Does not implicitly {@link google.api.HttpRule.verify|verify} messages.
             * @function encode
             * @memberof google.api.HttpRule
             * @static
             * @param {google.api.HttpRule.$Properties} message HttpRule message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpRule.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.selector != null && $Object.hasOwnProperty.call(message, "selector"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.selector);
                if (message.get != null && $Object.hasOwnProperty.call(message, "get"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.get);
                if (message.put != null && $Object.hasOwnProperty.call(message, "put"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.put);
                if (message.post != null && $Object.hasOwnProperty.call(message, "post"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.post);
                if (message["delete"] != null && $Object.hasOwnProperty.call(message, "delete"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message["delete"]);
                if (message.patch != null && $Object.hasOwnProperty.call(message, "patch"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.patch);
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.body);
                if (message.custom != null && $Object.hasOwnProperty.call(message, "custom"))
                    $root.google.api.CustomHttpPattern.encode(message.custom, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.additionalBindings != null && message.additionalBindings.length)
                    for (let i = 0; i < message.additionalBindings.length; ++i)
                        $root.google.api.HttpRule.encode(message.additionalBindings[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.responseBody != null && $Object.hasOwnProperty.call(message, "responseBody"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.responseBody);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HttpRule message, length delimited. Does not implicitly {@link google.api.HttpRule.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.api.HttpRule
             * @static
             * @param {google.api.HttpRule.$Properties} message HttpRule message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpRule.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HttpRule message from the specified reader or buffer.
             * @function decode
             * @memberof google.api.HttpRule
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.api.HttpRule & google.api.HttpRule.$Shape} HttpRule
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpRule.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.api.HttpRule(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.selector = value;
                            else
                                delete message.selector;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.get = reader.stringVerify();
                            message.pattern = "get";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.put = reader.stringVerify();
                            message.pattern = "put";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.post = reader.stringVerify();
                            message.pattern = "post";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message["delete"] = reader.stringVerify();
                            message.pattern = "delete";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.patch = reader.stringVerify();
                            message.pattern = "patch";
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.custom = $root.google.api.CustomHttpPattern.decode(reader, reader.uint32(), $undefined, _depth + 1, message.custom);
                            message.pattern = "custom";
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.body = value;
                            else
                                delete message.body;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.responseBody = value;
                            else
                                delete message.responseBody;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.additionalBindings && message.additionalBindings.length))
                                message.additionalBindings = [];
                            message.additionalBindings.push($root.google.api.HttpRule.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HttpRule message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.api.HttpRule
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.api.HttpRule & google.api.HttpRule.$Shape} HttpRule
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpRule.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HttpRule message.
             * @function verify
             * @memberof google.api.HttpRule
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HttpRule.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.selector != null && $Object.hasOwnProperty.call(message, "selector"))
                    if (!$util.isString(message.selector))
                        return "selector: string expected";
                if (message.get != null && $Object.hasOwnProperty.call(message, "get")) {
                    properties.pattern = 1;
                    if (!$util.isString(message.get))
                        return "get: string expected";
                }
                if (message.put != null && $Object.hasOwnProperty.call(message, "put")) {
                    if (properties.pattern === 1)
                        return "pattern: multiple values";
                    properties.pattern = 1;
                    if (!$util.isString(message.put))
                        return "put: string expected";
                }
                if (message.post != null && $Object.hasOwnProperty.call(message, "post")) {
                    if (properties.pattern === 1)
                        return "pattern: multiple values";
                    properties.pattern = 1;
                    if (!$util.isString(message.post))
                        return "post: string expected";
                }
                if (message["delete"] != null && $Object.hasOwnProperty.call(message, "delete")) {
                    if (properties.pattern === 1)
                        return "pattern: multiple values";
                    properties.pattern = 1;
                    if (!$util.isString(message["delete"]))
                        return "delete: string expected";
                }
                if (message.patch != null && $Object.hasOwnProperty.call(message, "patch")) {
                    if (properties.pattern === 1)
                        return "pattern: multiple values";
                    properties.pattern = 1;
                    if (!$util.isString(message.patch))
                        return "patch: string expected";
                }
                if (message.custom != null && $Object.hasOwnProperty.call(message, "custom")) {
                    if (properties.pattern === 1)
                        return "pattern: multiple values";
                    properties.pattern = 1;
                    {
                        let error = $root.google.api.CustomHttpPattern.verify(message.custom, _depth + 1);
                        if (error)
                            return "custom." + error;
                    }
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    if (!$util.isString(message.body))
                        return "body: string expected";
                if (message.responseBody != null && $Object.hasOwnProperty.call(message, "responseBody"))
                    if (!$util.isString(message.responseBody))
                        return "responseBody: string expected";
                if (message.additionalBindings != null && $Object.hasOwnProperty.call(message, "additionalBindings")) {
                    if (!$Array.isArray(message.additionalBindings))
                        return "additionalBindings: array expected";
                    for (let i = 0; i < message.additionalBindings.length; ++i) {
                        let error = $root.google.api.HttpRule.verify(message.additionalBindings[i], _depth + 1);
                        if (error)
                            return "additionalBindings." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a HttpRule message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.api.HttpRule
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.api.HttpRule} HttpRule
             */
            HttpRule.fromObject = function (object, _depth) {
                if (object instanceof $root.google.api.HttpRule)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.api.HttpRule: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.api.HttpRule();
                if (object.selector != null)
                    if (typeof object.selector !== "string" || object.selector.length)
                        message.selector = $String(object.selector);
                if (object.get != null)
                    message.get = $String(object.get);
                if (object.put != null)
                    message.put = $String(object.put);
                if (object.post != null)
                    message.post = $String(object.post);
                if (object["delete"] != null)
                    message["delete"] = $String(object["delete"]);
                if (object.patch != null)
                    message.patch = $String(object.patch);
                if (object.custom != null) {
                    if (!$util.isObject(object.custom))
                        throw $TypeError(".google.api.HttpRule.custom: object expected");
                    message.custom = $root.google.api.CustomHttpPattern.fromObject(object.custom, _depth + 1);
                }
                if (object.body != null)
                    if (typeof object.body !== "string" || object.body.length)
                        message.body = $String(object.body);
                if (object.responseBody != null)
                    if (typeof object.responseBody !== "string" || object.responseBody.length)
                        message.responseBody = $String(object.responseBody);
                if (object.additionalBindings) {
                    if (!$Array.isArray(object.additionalBindings))
                        throw $TypeError(".google.api.HttpRule.additionalBindings: array expected");
                    message.additionalBindings = $Array(object.additionalBindings.length);
                    for (let i = 0; i < object.additionalBindings.length; ++i) {
                        if (!$util.isObject(object.additionalBindings[i]))
                            throw $TypeError(".google.api.HttpRule.additionalBindings: object expected");
                        message.additionalBindings[i] = $root.google.api.HttpRule.fromObject(object.additionalBindings[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a HttpRule message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.api.HttpRule
             * @static
             * @param {google.api.HttpRule} message HttpRule
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HttpRule.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.additionalBindings = [];
                if (options.defaults) {
                    object.selector = "";
                    object.body = "";
                    object.responseBody = "";
                }
                if (message.selector != null && $Object.hasOwnProperty.call(message, "selector"))
                    object.selector = message.selector;
                if (message.get != null && $Object.hasOwnProperty.call(message, "get")) {
                    object.get = message.get;
                    if (options.oneofs)
                        object.pattern = "get";
                }
                if (message.put != null && $Object.hasOwnProperty.call(message, "put")) {
                    object.put = message.put;
                    if (options.oneofs)
                        object.pattern = "put";
                }
                if (message.post != null && $Object.hasOwnProperty.call(message, "post")) {
                    object.post = message.post;
                    if (options.oneofs)
                        object.pattern = "post";
                }
                if (message["delete"] != null && $Object.hasOwnProperty.call(message, "delete")) {
                    object["delete"] = message["delete"];
                    if (options.oneofs)
                        object.pattern = "delete";
                }
                if (message.patch != null && $Object.hasOwnProperty.call(message, "patch")) {
                    object.patch = message.patch;
                    if (options.oneofs)
                        object.pattern = "patch";
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                if (message.custom != null && $Object.hasOwnProperty.call(message, "custom")) {
                    object.custom = $root.google.api.CustomHttpPattern.toObject(message.custom, options, _depth + 1);
                    if (options.oneofs)
                        object.pattern = "custom";
                }
                if (message.additionalBindings && message.additionalBindings.length) {
                    object.additionalBindings = $Array(message.additionalBindings.length);
                    for (let j = 0; j < message.additionalBindings.length; ++j)
                        object.additionalBindings[j] = $root.google.api.HttpRule.toObject(message.additionalBindings[j], options, _depth + 1);
                }
                if (message.responseBody != null && $Object.hasOwnProperty.call(message, "responseBody"))
                    object.responseBody = message.responseBody;
                return object;
            };

            /**
             * Converts this HttpRule to JSON.
             * @function toJSON
             * @memberof google.api.HttpRule
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HttpRule.prototype.toJSON = function() {
                return HttpRule.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HttpRule
             * @function getTypeUrl
             * @memberof google.api.HttpRule
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HttpRule.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.api.HttpRule";
            };

            return HttpRule;
        })();

        api.CustomHttpPattern = (function() {

            /**
             * Properties of a CustomHttpPattern.
             * @typedef {Object} google.api.CustomHttpPattern.$Properties
             * @property {string|null} [kind] CustomHttpPattern kind
             * @property {string|null} [path] CustomHttpPattern path
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CustomHttpPattern.
             * @memberof google.api
             * @interface ICustomHttpPattern
             * @augments google.api.CustomHttpPattern.$Properties
             * @deprecated Use google.api.CustomHttpPattern.$Properties instead.
             */

            /**
             * Shape of a CustomHttpPattern.
             * @typedef {google.api.CustomHttpPattern.$Properties} google.api.CustomHttpPattern.$Shape
             */

            /**
             * Constructs a new CustomHttpPattern.
             * @memberof google.api
             * @classdesc Represents a CustomHttpPattern.
             * @constructor
             * @param {google.api.CustomHttpPattern.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const CustomHttpPattern = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CustomHttpPattern kind.
             * @member {string} kind
             * @memberof google.api.CustomHttpPattern
             * @instance
             */
            CustomHttpPattern.prototype.kind = "";

            /**
             * CustomHttpPattern path.
             * @member {string} path
             * @memberof google.api.CustomHttpPattern
             * @instance
             */
            CustomHttpPattern.prototype.path = "";

            /**
             * Creates a new CustomHttpPattern instance using the specified properties.
             * @function create
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {google.api.CustomHttpPattern.$Properties=} [properties] Properties to set
             * @returns {google.api.CustomHttpPattern} CustomHttpPattern instance
             * @type {{
             *   (properties: google.api.CustomHttpPattern.$Shape): google.api.CustomHttpPattern & google.api.CustomHttpPattern.$Shape;
             *   (properties?: google.api.CustomHttpPattern.$Properties): google.api.CustomHttpPattern;
             * }}
             */
            CustomHttpPattern.create = function(properties) {
                return new CustomHttpPattern(properties);
            };

            /**
             * Encodes the specified CustomHttpPattern message. Does not implicitly {@link google.api.CustomHttpPattern.verify|verify} messages.
             * @function encode
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {google.api.CustomHttpPattern.$Properties} message CustomHttpPattern message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomHttpPattern.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.kind);
                if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.path);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CustomHttpPattern message, length delimited. Does not implicitly {@link google.api.CustomHttpPattern.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {google.api.CustomHttpPattern.$Properties} message CustomHttpPattern message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomHttpPattern.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CustomHttpPattern message from the specified reader or buffer.
             * @function decode
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.api.CustomHttpPattern & google.api.CustomHttpPattern.$Shape} CustomHttpPattern
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomHttpPattern.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.api.CustomHttpPattern(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.path = value;
                            else
                                delete message.path;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CustomHttpPattern message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.api.CustomHttpPattern & google.api.CustomHttpPattern.$Shape} CustomHttpPattern
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomHttpPattern.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CustomHttpPattern message.
             * @function verify
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CustomHttpPattern.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (!$util.isString(message.kind))
                        return "kind: string expected";
                if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                    if (!$util.isString(message.path))
                        return "path: string expected";
                return null;
            };

            /**
             * Creates a CustomHttpPattern message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.api.CustomHttpPattern} CustomHttpPattern
             */
            CustomHttpPattern.fromObject = function (object, _depth) {
                if (object instanceof $root.google.api.CustomHttpPattern)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.api.CustomHttpPattern: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.api.CustomHttpPattern();
                if (object.kind != null)
                    if (typeof object.kind !== "string" || object.kind.length)
                        message.kind = $String(object.kind);
                if (object.path != null)
                    if (typeof object.path !== "string" || object.path.length)
                        message.path = $String(object.path);
                return message;
            };

            /**
             * Creates a plain object from a CustomHttpPattern message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {google.api.CustomHttpPattern} message CustomHttpPattern
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CustomHttpPattern.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.kind = "";
                    object.path = "";
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = message.kind;
                if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                    object.path = message.path;
                return object;
            };

            /**
             * Converts this CustomHttpPattern to JSON.
             * @function toJSON
             * @memberof google.api.CustomHttpPattern
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CustomHttpPattern.prototype.toJSON = function() {
                return CustomHttpPattern.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CustomHttpPattern
             * @function getTypeUrl
             * @memberof google.api.CustomHttpPattern
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CustomHttpPattern.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.api.CustomHttpPattern";
            };

            return CustomHttpPattern;
        })();

        return api;
    })();

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.FileDescriptorSet = (function() {

            /**
             * Properties of a FileDescriptorSet.
             * @typedef {Object} google.protobuf.FileDescriptorSet.$Properties
             * @property {Array.<google.protobuf.FileDescriptorProto.$Properties>|null} [file] FileDescriptorSet file
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileDescriptorSet.
             * @memberof google.protobuf
             * @interface IFileDescriptorSet
             * @augments google.protobuf.FileDescriptorSet.$Properties
             * @deprecated Use google.protobuf.FileDescriptorSet.$Properties instead.
             */

            /**
             * Shape of a FileDescriptorSet.
             * @typedef {{
             *   file?: Array.<google.protobuf.FileDescriptorProto.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.FileDescriptorSet.$Shape
             */

            /**
             * Constructs a new FileDescriptorSet.
             * @memberof google.protobuf
             * @classdesc Represents a FileDescriptorSet.
             * @constructor
             * @param {google.protobuf.FileDescriptorSet.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FileDescriptorSet = function (properties) {
                this.file = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FileDescriptorSet file.
             * @member {Array.<google.protobuf.FileDescriptorProto.$Properties>} file
             * @memberof google.protobuf.FileDescriptorSet
             * @instance
             */
            FileDescriptorSet.prototype.file = $util.emptyArray;

            /**
             * Creates a new FileDescriptorSet instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {google.protobuf.FileDescriptorSet.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FileDescriptorSet} FileDescriptorSet instance
             * @type {{
             *   (properties: google.protobuf.FileDescriptorSet.$Shape): google.protobuf.FileDescriptorSet & google.protobuf.FileDescriptorSet.$Shape;
             *   (properties?: google.protobuf.FileDescriptorSet.$Properties): google.protobuf.FileDescriptorSet;
             * }}
             */
            FileDescriptorSet.create = function(properties) {
                return new FileDescriptorSet(properties);
            };

            /**
             * Encodes the specified FileDescriptorSet message. Does not implicitly {@link google.protobuf.FileDescriptorSet.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {google.protobuf.FileDescriptorSet.$Properties} message FileDescriptorSet message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileDescriptorSet.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.file != null && message.file.length)
                    for (let i = 0; i < message.file.length; ++i)
                        $root.google.protobuf.FileDescriptorProto.encode(message.file[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileDescriptorSet message, length delimited. Does not implicitly {@link google.protobuf.FileDescriptorSet.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {google.protobuf.FileDescriptorSet.$Properties} message FileDescriptorSet message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileDescriptorSet.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FileDescriptorSet message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FileDescriptorSet & google.protobuf.FileDescriptorSet.$Shape} FileDescriptorSet
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileDescriptorSet.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.FileDescriptorSet();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.file && message.file.length))
                                message.file = [];
                            message.file.push($root.google.protobuf.FileDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileDescriptorSet message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FileDescriptorSet & google.protobuf.FileDescriptorSet.$Shape} FileDescriptorSet
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileDescriptorSet.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileDescriptorSet message.
             * @function verify
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileDescriptorSet.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file")) {
                    if (!$Array.isArray(message.file))
                        return "file: array expected";
                    for (let i = 0; i < message.file.length; ++i) {
                        let error = $root.google.protobuf.FileDescriptorProto.verify(message.file[i], _depth + 1);
                        if (error)
                            return "file." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FileDescriptorSet message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FileDescriptorSet} FileDescriptorSet
             */
            FileDescriptorSet.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FileDescriptorSet)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FileDescriptorSet: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FileDescriptorSet();
                if (object.file) {
                    if (!$Array.isArray(object.file))
                        throw $TypeError(".google.protobuf.FileDescriptorSet.file: array expected");
                    message.file = $Array(object.file.length);
                    for (let i = 0; i < object.file.length; ++i) {
                        if (!$util.isObject(object.file[i]))
                            throw $TypeError(".google.protobuf.FileDescriptorSet.file: object expected");
                        message.file[i] = $root.google.protobuf.FileDescriptorProto.fromObject(object.file[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FileDescriptorSet message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {google.protobuf.FileDescriptorSet} message FileDescriptorSet
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileDescriptorSet.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.file = [];
                if (message.file && message.file.length) {
                    object.file = $Array(message.file.length);
                    for (let j = 0; j < message.file.length; ++j)
                        object.file[j] = $root.google.protobuf.FileDescriptorProto.toObject(message.file[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this FileDescriptorSet to JSON.
             * @function toJSON
             * @memberof google.protobuf.FileDescriptorSet
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileDescriptorSet.prototype.toJSON = function() {
                return FileDescriptorSet.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileDescriptorSet
             * @function getTypeUrl
             * @memberof google.protobuf.FileDescriptorSet
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileDescriptorSet.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FileDescriptorSet";
            };

            return FileDescriptorSet;
        })();

        protobuf.FileDescriptorProto = (function() {

            /**
             * Properties of a FileDescriptorProto.
             * @typedef {Object} google.protobuf.FileDescriptorProto.$Properties
             * @property {string|null} [name] FileDescriptorProto name
             * @property {string|null} ["package"] FileDescriptorProto package
             * @property {Array.<string>|null} [dependency] FileDescriptorProto dependency
             * @property {Array.<number>|null} [publicDependency] FileDescriptorProto publicDependency
             * @property {Array.<number>|null} [weakDependency] FileDescriptorProto weakDependency
             * @property {Array.<google.protobuf.DescriptorProto.$Properties>|null} [messageType] FileDescriptorProto messageType
             * @property {Array.<google.protobuf.EnumDescriptorProto.$Properties>|null} [enumType] FileDescriptorProto enumType
             * @property {Array.<google.protobuf.ServiceDescriptorProto.$Properties>|null} [service] FileDescriptorProto service
             * @property {Array.<google.protobuf.FieldDescriptorProto.$Properties>|null} [extension] FileDescriptorProto extension
             * @property {google.protobuf.FileOptions.$Properties|null} [options] FileDescriptorProto options
             * @property {google.protobuf.SourceCodeInfo.$Properties|null} [sourceCodeInfo] FileDescriptorProto sourceCodeInfo
             * @property {string|null} [syntax] FileDescriptorProto syntax
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileDescriptorProto.
             * @memberof google.protobuf
             * @interface IFileDescriptorProto
             * @augments google.protobuf.FileDescriptorProto.$Properties
             * @deprecated Use google.protobuf.FileDescriptorProto.$Properties instead.
             */

            /**
             * Shape of a FileDescriptorProto.
             * @typedef {{
             *   name?: string|null;
             *   "package"?: string|null;
             *   dependency?: Array.<string>|null;
             *   publicDependency?: Array.<number>|null;
             *   weakDependency?: Array.<number>|null;
             *   messageType?: Array.<google.protobuf.DescriptorProto.$Shape>|null;
             *   enumType?: Array.<google.protobuf.EnumDescriptorProto.$Shape>|null;
             *   service?: Array.<google.protobuf.ServiceDescriptorProto.$Shape>|null;
             *   extension?: Array.<google.protobuf.FieldDescriptorProto.$Shape>|null;
             *   options?: google.protobuf.FileOptions.$Shape|null;
             *   sourceCodeInfo?: google.protobuf.SourceCodeInfo.$Shape|null;
             *   syntax?: string|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.FileDescriptorProto.$Shape
             */

            /**
             * Constructs a new FileDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a FileDescriptorProto.
             * @constructor
             * @param {google.protobuf.FileDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FileDescriptorProto = function (properties) {
                this.dependency = [];
                this.publicDependency = [];
                this.weakDependency = [];
                this.messageType = [];
                this.enumType = [];
                this.service = [];
                this.extension = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FileDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.name = "";

            /**
             * FileDescriptorProto package.
             * @member {string} package
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype["package"] = "";

            /**
             * FileDescriptorProto dependency.
             * @member {Array.<string>} dependency
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.dependency = $util.emptyArray;

            /**
             * FileDescriptorProto publicDependency.
             * @member {Array.<number>} publicDependency
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.publicDependency = $util.emptyArray;

            /**
             * FileDescriptorProto weakDependency.
             * @member {Array.<number>} weakDependency
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.weakDependency = $util.emptyArray;

            /**
             * FileDescriptorProto messageType.
             * @member {Array.<google.protobuf.DescriptorProto.$Properties>} messageType
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.messageType = $util.emptyArray;

            /**
             * FileDescriptorProto enumType.
             * @member {Array.<google.protobuf.EnumDescriptorProto.$Properties>} enumType
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.enumType = $util.emptyArray;

            /**
             * FileDescriptorProto service.
             * @member {Array.<google.protobuf.ServiceDescriptorProto.$Properties>} service
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.service = $util.emptyArray;

            /**
             * FileDescriptorProto extension.
             * @member {Array.<google.protobuf.FieldDescriptorProto.$Properties>} extension
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.extension = $util.emptyArray;

            /**
             * FileDescriptorProto options.
             * @member {google.protobuf.FileOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.options = null;

            /**
             * FileDescriptorProto sourceCodeInfo.
             * @member {google.protobuf.SourceCodeInfo.$Properties|null|undefined} sourceCodeInfo
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.sourceCodeInfo = null;

            /**
             * FileDescriptorProto syntax.
             * @member {string} syntax
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             */
            FileDescriptorProto.prototype.syntax = "";

            /**
             * Creates a new FileDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {google.protobuf.FileDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FileDescriptorProto} FileDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.FileDescriptorProto.$Shape): google.protobuf.FileDescriptorProto & google.protobuf.FileDescriptorProto.$Shape;
             *   (properties?: google.protobuf.FileDescriptorProto.$Properties): google.protobuf.FileDescriptorProto;
             * }}
             */
            FileDescriptorProto.create = function(properties) {
                return new FileDescriptorProto(properties);
            };

            /**
             * Encodes the specified FileDescriptorProto message. Does not implicitly {@link google.protobuf.FileDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {google.protobuf.FileDescriptorProto.$Properties} message FileDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message["package"] != null && $Object.hasOwnProperty.call(message, "package"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message["package"]);
                if (message.dependency != null && message.dependency.length)
                    for (let i = 0; i < message.dependency.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.dependency[i]);
                if (message.messageType != null && message.messageType.length)
                    for (let i = 0; i < message.messageType.length; ++i)
                        $root.google.protobuf.DescriptorProto.encode(message.messageType[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.enumType != null && message.enumType.length)
                    for (let i = 0; i < message.enumType.length; ++i)
                        $root.google.protobuf.EnumDescriptorProto.encode(message.enumType[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.service != null && message.service.length)
                    for (let i = 0; i < message.service.length; ++i)
                        $root.google.protobuf.ServiceDescriptorProto.encode(message.service[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.extension != null && message.extension.length)
                    for (let i = 0; i < message.extension.length; ++i)
                        $root.google.protobuf.FieldDescriptorProto.encode(message.extension[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.FileOptions.encode(message.options, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.sourceCodeInfo != null && $Object.hasOwnProperty.call(message, "sourceCodeInfo"))
                    $root.google.protobuf.SourceCodeInfo.encode(message.sourceCodeInfo, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                if (message.publicDependency != null && message.publicDependency.length)
                    for (let i = 0; i < message.publicDependency.length; ++i)
                        writer.uint32(/* id 10, wireType 0 =*/80).int32(message.publicDependency[i]);
                if (message.weakDependency != null && message.weakDependency.length)
                    for (let i = 0; i < message.weakDependency.length; ++i)
                        writer.uint32(/* id 11, wireType 0 =*/88).int32(message.weakDependency[i]);
                if (message.syntax != null && $Object.hasOwnProperty.call(message, "syntax"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.syntax);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.FileDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {google.protobuf.FileDescriptorProto.$Properties} message FileDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FileDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FileDescriptorProto & google.protobuf.FileDescriptorProto.$Shape} FileDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.FileDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message["package"] = reader.string();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.dependency && message.dependency.length))
                                message.dependency = [];
                            message.dependency.push(reader.string());
                            continue;
                        }
                    case 10: {
                            if (wireType === 2) {
                                if (!(message.publicDependency && message.publicDependency.length))
                                    message.publicDependency = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.publicDependency.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.publicDependency && message.publicDependency.length))
                                message.publicDependency = [];
                            message.publicDependency.push(reader.int32());
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.weakDependency && message.weakDependency.length))
                                    message.weakDependency = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.weakDependency.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.weakDependency && message.weakDependency.length))
                                message.weakDependency = [];
                            message.weakDependency.push(reader.int32());
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.messageType && message.messageType.length))
                                message.messageType = [];
                            message.messageType.push($root.google.protobuf.DescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.enumType && message.enumType.length))
                                message.enumType = [];
                            message.enumType.push($root.google.protobuf.EnumDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if (!(message.service && message.service.length))
                                message.service = [];
                            message.service.push($root.google.protobuf.ServiceDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.extension && message.extension.length))
                                message.extension = [];
                            message.extension.push($root.google.protobuf.FieldDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.FileOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.sourceCodeInfo = $root.google.protobuf.SourceCodeInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sourceCodeInfo);
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.syntax = reader.string();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FileDescriptorProto & google.protobuf.FileDescriptorProto.$Shape} FileDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message["package"] != null && $Object.hasOwnProperty.call(message, "package"))
                    if (!$util.isString(message["package"]))
                        return "package: string expected";
                if (message.dependency != null && $Object.hasOwnProperty.call(message, "dependency")) {
                    if (!$Array.isArray(message.dependency))
                        return "dependency: array expected";
                    for (let i = 0; i < message.dependency.length; ++i)
                        if (!$util.isString(message.dependency[i]))
                            return "dependency: string[] expected";
                }
                if (message.publicDependency != null && $Object.hasOwnProperty.call(message, "publicDependency")) {
                    if (!$Array.isArray(message.publicDependency))
                        return "publicDependency: array expected";
                    for (let i = 0; i < message.publicDependency.length; ++i)
                        if (!$util.isInteger(message.publicDependency[i]))
                            return "publicDependency: integer[] expected";
                }
                if (message.weakDependency != null && $Object.hasOwnProperty.call(message, "weakDependency")) {
                    if (!$Array.isArray(message.weakDependency))
                        return "weakDependency: array expected";
                    for (let i = 0; i < message.weakDependency.length; ++i)
                        if (!$util.isInteger(message.weakDependency[i]))
                            return "weakDependency: integer[] expected";
                }
                if (message.messageType != null && $Object.hasOwnProperty.call(message, "messageType")) {
                    if (!$Array.isArray(message.messageType))
                        return "messageType: array expected";
                    for (let i = 0; i < message.messageType.length; ++i) {
                        let error = $root.google.protobuf.DescriptorProto.verify(message.messageType[i], _depth + 1);
                        if (error)
                            return "messageType." + error;
                    }
                }
                if (message.enumType != null && $Object.hasOwnProperty.call(message, "enumType")) {
                    if (!$Array.isArray(message.enumType))
                        return "enumType: array expected";
                    for (let i = 0; i < message.enumType.length; ++i) {
                        let error = $root.google.protobuf.EnumDescriptorProto.verify(message.enumType[i], _depth + 1);
                        if (error)
                            return "enumType." + error;
                    }
                }
                if (message.service != null && $Object.hasOwnProperty.call(message, "service")) {
                    if (!$Array.isArray(message.service))
                        return "service: array expected";
                    for (let i = 0; i < message.service.length; ++i) {
                        let error = $root.google.protobuf.ServiceDescriptorProto.verify(message.service[i], _depth + 1);
                        if (error)
                            return "service." + error;
                    }
                }
                if (message.extension != null && $Object.hasOwnProperty.call(message, "extension")) {
                    if (!$Array.isArray(message.extension))
                        return "extension: array expected";
                    for (let i = 0; i < message.extension.length; ++i) {
                        let error = $root.google.protobuf.FieldDescriptorProto.verify(message.extension[i], _depth + 1);
                        if (error)
                            return "extension." + error;
                    }
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.FileOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                if (message.sourceCodeInfo != null && $Object.hasOwnProperty.call(message, "sourceCodeInfo")) {
                    let error = $root.google.protobuf.SourceCodeInfo.verify(message.sourceCodeInfo, _depth + 1);
                    if (error)
                        return "sourceCodeInfo." + error;
                }
                if (message.syntax != null && $Object.hasOwnProperty.call(message, "syntax"))
                    if (!$util.isString(message.syntax))
                        return "syntax: string expected";
                return null;
            };

            /**
             * Creates a FileDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FileDescriptorProto} FileDescriptorProto
             */
            FileDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FileDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FileDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FileDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object["package"] != null)
                    message["package"] = $String(object["package"]);
                if (object.dependency) {
                    if (!$Array.isArray(object.dependency))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.dependency: array expected");
                    message.dependency = $Array(object.dependency.length);
                    for (let i = 0; i < object.dependency.length; ++i)
                        message.dependency[i] = $String(object.dependency[i]);
                }
                if (object.publicDependency) {
                    if (!$Array.isArray(object.publicDependency))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.publicDependency: array expected");
                    message.publicDependency = $Array(object.publicDependency.length);
                    for (let i = 0; i < object.publicDependency.length; ++i)
                        message.publicDependency[i] = object.publicDependency[i] | 0;
                }
                if (object.weakDependency) {
                    if (!$Array.isArray(object.weakDependency))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.weakDependency: array expected");
                    message.weakDependency = $Array(object.weakDependency.length);
                    for (let i = 0; i < object.weakDependency.length; ++i)
                        message.weakDependency[i] = object.weakDependency[i] | 0;
                }
                if (object.messageType) {
                    if (!$Array.isArray(object.messageType))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.messageType: array expected");
                    message.messageType = $Array(object.messageType.length);
                    for (let i = 0; i < object.messageType.length; ++i) {
                        if (!$util.isObject(object.messageType[i]))
                            throw $TypeError(".google.protobuf.FileDescriptorProto.messageType: object expected");
                        message.messageType[i] = $root.google.protobuf.DescriptorProto.fromObject(object.messageType[i], _depth + 1);
                    }
                }
                if (object.enumType) {
                    if (!$Array.isArray(object.enumType))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.enumType: array expected");
                    message.enumType = $Array(object.enumType.length);
                    for (let i = 0; i < object.enumType.length; ++i) {
                        if (!$util.isObject(object.enumType[i]))
                            throw $TypeError(".google.protobuf.FileDescriptorProto.enumType: object expected");
                        message.enumType[i] = $root.google.protobuf.EnumDescriptorProto.fromObject(object.enumType[i], _depth + 1);
                    }
                }
                if (object.service) {
                    if (!$Array.isArray(object.service))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.service: array expected");
                    message.service = $Array(object.service.length);
                    for (let i = 0; i < object.service.length; ++i) {
                        if (!$util.isObject(object.service[i]))
                            throw $TypeError(".google.protobuf.FileDescriptorProto.service: object expected");
                        message.service[i] = $root.google.protobuf.ServiceDescriptorProto.fromObject(object.service[i], _depth + 1);
                    }
                }
                if (object.extension) {
                    if (!$Array.isArray(object.extension))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.extension: array expected");
                    message.extension = $Array(object.extension.length);
                    for (let i = 0; i < object.extension.length; ++i) {
                        if (!$util.isObject(object.extension[i]))
                            throw $TypeError(".google.protobuf.FileDescriptorProto.extension: object expected");
                        message.extension[i] = $root.google.protobuf.FieldDescriptorProto.fromObject(object.extension[i], _depth + 1);
                    }
                }
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.FileOptions.fromObject(object.options, _depth + 1);
                }
                if (object.sourceCodeInfo != null) {
                    if (!$util.isObject(object.sourceCodeInfo))
                        throw $TypeError(".google.protobuf.FileDescriptorProto.sourceCodeInfo: object expected");
                    message.sourceCodeInfo = $root.google.protobuf.SourceCodeInfo.fromObject(object.sourceCodeInfo, _depth + 1);
                }
                if (object.syntax != null)
                    message.syntax = $String(object.syntax);
                return message;
            };

            /**
             * Creates a plain object from a FileDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {google.protobuf.FileDescriptorProto} message FileDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependency = [];
                    object.messageType = [];
                    object.enumType = [];
                    object.service = [];
                    object.extension = [];
                    object.publicDependency = [];
                    object.weakDependency = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object["package"] = "";
                    object.options = null;
                    object.sourceCodeInfo = null;
                    object.syntax = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message["package"] != null && $Object.hasOwnProperty.call(message, "package"))
                    object["package"] = message["package"];
                if (message.dependency && message.dependency.length) {
                    object.dependency = $Array(message.dependency.length);
                    for (let j = 0; j < message.dependency.length; ++j)
                        object.dependency[j] = message.dependency[j];
                }
                if (message.messageType && message.messageType.length) {
                    object.messageType = $Array(message.messageType.length);
                    for (let j = 0; j < message.messageType.length; ++j)
                        object.messageType[j] = $root.google.protobuf.DescriptorProto.toObject(message.messageType[j], options, _depth + 1);
                }
                if (message.enumType && message.enumType.length) {
                    object.enumType = $Array(message.enumType.length);
                    for (let j = 0; j < message.enumType.length; ++j)
                        object.enumType[j] = $root.google.protobuf.EnumDescriptorProto.toObject(message.enumType[j], options, _depth + 1);
                }
                if (message.service && message.service.length) {
                    object.service = $Array(message.service.length);
                    for (let j = 0; j < message.service.length; ++j)
                        object.service[j] = $root.google.protobuf.ServiceDescriptorProto.toObject(message.service[j], options, _depth + 1);
                }
                if (message.extension && message.extension.length) {
                    object.extension = $Array(message.extension.length);
                    for (let j = 0; j < message.extension.length; ++j)
                        object.extension[j] = $root.google.protobuf.FieldDescriptorProto.toObject(message.extension[j], options, _depth + 1);
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.FileOptions.toObject(message.options, options, _depth + 1);
                if (message.sourceCodeInfo != null && $Object.hasOwnProperty.call(message, "sourceCodeInfo"))
                    object.sourceCodeInfo = $root.google.protobuf.SourceCodeInfo.toObject(message.sourceCodeInfo, options, _depth + 1);
                if (message.publicDependency && message.publicDependency.length) {
                    object.publicDependency = $Array(message.publicDependency.length);
                    for (let j = 0; j < message.publicDependency.length; ++j)
                        object.publicDependency[j] = message.publicDependency[j];
                }
                if (message.weakDependency && message.weakDependency.length) {
                    object.weakDependency = $Array(message.weakDependency.length);
                    for (let j = 0; j < message.weakDependency.length; ++j)
                        object.weakDependency[j] = message.weakDependency[j];
                }
                if (message.syntax != null && $Object.hasOwnProperty.call(message, "syntax"))
                    object.syntax = message.syntax;
                return object;
            };

            /**
             * Converts this FileDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.FileDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileDescriptorProto.prototype.toJSON = function() {
                return FileDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.FileDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FileDescriptorProto";
            };

            return FileDescriptorProto;
        })();

        protobuf.DescriptorProto = (function() {

            /**
             * Properties of a DescriptorProto.
             * @typedef {Object} google.protobuf.DescriptorProto.$Properties
             * @property {string|null} [name] DescriptorProto name
             * @property {Array.<google.protobuf.FieldDescriptorProto.$Properties>|null} [field] DescriptorProto field
             * @property {Array.<google.protobuf.FieldDescriptorProto.$Properties>|null} [extension] DescriptorProto extension
             * @property {Array.<google.protobuf.DescriptorProto.$Properties>|null} [nestedType] DescriptorProto nestedType
             * @property {Array.<google.protobuf.EnumDescriptorProto.$Properties>|null} [enumType] DescriptorProto enumType
             * @property {Array.<google.protobuf.DescriptorProto.ExtensionRange.$Properties>|null} [extensionRange] DescriptorProto extensionRange
             * @property {Array.<google.protobuf.OneofDescriptorProto.$Properties>|null} [oneofDecl] DescriptorProto oneofDecl
             * @property {google.protobuf.MessageOptions.$Properties|null} [options] DescriptorProto options
             * @property {Array.<google.protobuf.DescriptorProto.ReservedRange.$Properties>|null} [reservedRange] DescriptorProto reservedRange
             * @property {Array.<string>|null} [reservedName] DescriptorProto reservedName
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DescriptorProto.
             * @memberof google.protobuf
             * @interface IDescriptorProto
             * @augments google.protobuf.DescriptorProto.$Properties
             * @deprecated Use google.protobuf.DescriptorProto.$Properties instead.
             */

            /**
             * Shape of a DescriptorProto.
             * @typedef {google.protobuf.DescriptorProto.$Properties} google.protobuf.DescriptorProto.$Shape
             */

            /**
             * Constructs a new DescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a DescriptorProto.
             * @constructor
             * @param {google.protobuf.DescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const DescriptorProto = function (properties) {
                this.field = [];
                this.extension = [];
                this.nestedType = [];
                this.enumType = [];
                this.extensionRange = [];
                this.oneofDecl = [];
                this.reservedRange = [];
                this.reservedName = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.name = "";

            /**
             * DescriptorProto field.
             * @member {Array.<google.protobuf.FieldDescriptorProto.$Properties>} field
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.field = $util.emptyArray;

            /**
             * DescriptorProto extension.
             * @member {Array.<google.protobuf.FieldDescriptorProto.$Properties>} extension
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.extension = $util.emptyArray;

            /**
             * DescriptorProto nestedType.
             * @member {Array.<google.protobuf.DescriptorProto.$Properties>} nestedType
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.nestedType = $util.emptyArray;

            /**
             * DescriptorProto enumType.
             * @member {Array.<google.protobuf.EnumDescriptorProto.$Properties>} enumType
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.enumType = $util.emptyArray;

            /**
             * DescriptorProto extensionRange.
             * @member {Array.<google.protobuf.DescriptorProto.ExtensionRange.$Properties>} extensionRange
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.extensionRange = $util.emptyArray;

            /**
             * DescriptorProto oneofDecl.
             * @member {Array.<google.protobuf.OneofDescriptorProto.$Properties>} oneofDecl
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.oneofDecl = $util.emptyArray;

            /**
             * DescriptorProto options.
             * @member {google.protobuf.MessageOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.options = null;

            /**
             * DescriptorProto reservedRange.
             * @member {Array.<google.protobuf.DescriptorProto.ReservedRange.$Properties>} reservedRange
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.reservedRange = $util.emptyArray;

            /**
             * DescriptorProto reservedName.
             * @member {Array.<string>} reservedName
             * @memberof google.protobuf.DescriptorProto
             * @instance
             */
            DescriptorProto.prototype.reservedName = $util.emptyArray;

            /**
             * Creates a new DescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {google.protobuf.DescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.DescriptorProto} DescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.DescriptorProto.$Shape): google.protobuf.DescriptorProto & google.protobuf.DescriptorProto.$Shape;
             *   (properties?: google.protobuf.DescriptorProto.$Properties): google.protobuf.DescriptorProto;
             * }}
             */
            DescriptorProto.create = function(properties) {
                return new DescriptorProto(properties);
            };

            /**
             * Encodes the specified DescriptorProto message. Does not implicitly {@link google.protobuf.DescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {google.protobuf.DescriptorProto.$Properties} message DescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.field != null && message.field.length)
                    for (let i = 0; i < message.field.length; ++i)
                        $root.google.protobuf.FieldDescriptorProto.encode(message.field[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.nestedType != null && message.nestedType.length)
                    for (let i = 0; i < message.nestedType.length; ++i)
                        $root.google.protobuf.DescriptorProto.encode(message.nestedType[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.enumType != null && message.enumType.length)
                    for (let i = 0; i < message.enumType.length; ++i)
                        $root.google.protobuf.EnumDescriptorProto.encode(message.enumType[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.extensionRange != null && message.extensionRange.length)
                    for (let i = 0; i < message.extensionRange.length; ++i)
                        $root.google.protobuf.DescriptorProto.ExtensionRange.encode(message.extensionRange[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.extension != null && message.extension.length)
                    for (let i = 0; i < message.extension.length; ++i)
                        $root.google.protobuf.FieldDescriptorProto.encode(message.extension[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.MessageOptions.encode(message.options, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.oneofDecl != null && message.oneofDecl.length)
                    for (let i = 0; i < message.oneofDecl.length; ++i)
                        $root.google.protobuf.OneofDescriptorProto.encode(message.oneofDecl[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.reservedRange != null && message.reservedRange.length)
                    for (let i = 0; i < message.reservedRange.length; ++i)
                        $root.google.protobuf.DescriptorProto.ReservedRange.encode(message.reservedRange[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                if (message.reservedName != null && message.reservedName.length)
                    for (let i = 0; i < message.reservedName.length; ++i)
                        writer.uint32(/* id 10, wireType 2 =*/82).string(message.reservedName[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.DescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {google.protobuf.DescriptorProto.$Properties} message DescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.DescriptorProto & google.protobuf.DescriptorProto.$Shape} DescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.DescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.field && message.field.length))
                                message.field = [];
                            message.field.push($root.google.protobuf.FieldDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if (!(message.extension && message.extension.length))
                                message.extension = [];
                            message.extension.push($root.google.protobuf.FieldDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.nestedType && message.nestedType.length))
                                message.nestedType = [];
                            message.nestedType.push($root.google.protobuf.DescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.enumType && message.enumType.length))
                                message.enumType = [];
                            message.enumType.push($root.google.protobuf.EnumDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.extensionRange && message.extensionRange.length))
                                message.extensionRange = [];
                            message.extensionRange.push($root.google.protobuf.DescriptorProto.ExtensionRange.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if (!(message.oneofDecl && message.oneofDecl.length))
                                message.oneofDecl = [];
                            message.oneofDecl.push($root.google.protobuf.OneofDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.MessageOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            if (!(message.reservedRange && message.reservedRange.length))
                                message.reservedRange = [];
                            message.reservedRange.push($root.google.protobuf.DescriptorProto.ReservedRange.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if (!(message.reservedName && message.reservedName.length))
                                message.reservedName = [];
                            message.reservedName.push(reader.string());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.DescriptorProto & google.protobuf.DescriptorProto.$Shape} DescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DescriptorProto message.
             * @function verify
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.field != null && $Object.hasOwnProperty.call(message, "field")) {
                    if (!$Array.isArray(message.field))
                        return "field: array expected";
                    for (let i = 0; i < message.field.length; ++i) {
                        let error = $root.google.protobuf.FieldDescriptorProto.verify(message.field[i], _depth + 1);
                        if (error)
                            return "field." + error;
                    }
                }
                if (message.extension != null && $Object.hasOwnProperty.call(message, "extension")) {
                    if (!$Array.isArray(message.extension))
                        return "extension: array expected";
                    for (let i = 0; i < message.extension.length; ++i) {
                        let error = $root.google.protobuf.FieldDescriptorProto.verify(message.extension[i], _depth + 1);
                        if (error)
                            return "extension." + error;
                    }
                }
                if (message.nestedType != null && $Object.hasOwnProperty.call(message, "nestedType")) {
                    if (!$Array.isArray(message.nestedType))
                        return "nestedType: array expected";
                    for (let i = 0; i < message.nestedType.length; ++i) {
                        let error = $root.google.protobuf.DescriptorProto.verify(message.nestedType[i], _depth + 1);
                        if (error)
                            return "nestedType." + error;
                    }
                }
                if (message.enumType != null && $Object.hasOwnProperty.call(message, "enumType")) {
                    if (!$Array.isArray(message.enumType))
                        return "enumType: array expected";
                    for (let i = 0; i < message.enumType.length; ++i) {
                        let error = $root.google.protobuf.EnumDescriptorProto.verify(message.enumType[i], _depth + 1);
                        if (error)
                            return "enumType." + error;
                    }
                }
                if (message.extensionRange != null && $Object.hasOwnProperty.call(message, "extensionRange")) {
                    if (!$Array.isArray(message.extensionRange))
                        return "extensionRange: array expected";
                    for (let i = 0; i < message.extensionRange.length; ++i) {
                        let error = $root.google.protobuf.DescriptorProto.ExtensionRange.verify(message.extensionRange[i], _depth + 1);
                        if (error)
                            return "extensionRange." + error;
                    }
                }
                if (message.oneofDecl != null && $Object.hasOwnProperty.call(message, "oneofDecl")) {
                    if (!$Array.isArray(message.oneofDecl))
                        return "oneofDecl: array expected";
                    for (let i = 0; i < message.oneofDecl.length; ++i) {
                        let error = $root.google.protobuf.OneofDescriptorProto.verify(message.oneofDecl[i], _depth + 1);
                        if (error)
                            return "oneofDecl." + error;
                    }
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.MessageOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                if (message.reservedRange != null && $Object.hasOwnProperty.call(message, "reservedRange")) {
                    if (!$Array.isArray(message.reservedRange))
                        return "reservedRange: array expected";
                    for (let i = 0; i < message.reservedRange.length; ++i) {
                        let error = $root.google.protobuf.DescriptorProto.ReservedRange.verify(message.reservedRange[i], _depth + 1);
                        if (error)
                            return "reservedRange." + error;
                    }
                }
                if (message.reservedName != null && $Object.hasOwnProperty.call(message, "reservedName")) {
                    if (!$Array.isArray(message.reservedName))
                        return "reservedName: array expected";
                    for (let i = 0; i < message.reservedName.length; ++i)
                        if (!$util.isString(message.reservedName[i]))
                            return "reservedName: string[] expected";
                }
                return null;
            };

            /**
             * Creates a DescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.DescriptorProto} DescriptorProto
             */
            DescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.DescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.DescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.DescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.field) {
                    if (!$Array.isArray(object.field))
                        throw $TypeError(".google.protobuf.DescriptorProto.field: array expected");
                    message.field = $Array(object.field.length);
                    for (let i = 0; i < object.field.length; ++i) {
                        if (!$util.isObject(object.field[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.field: object expected");
                        message.field[i] = $root.google.protobuf.FieldDescriptorProto.fromObject(object.field[i], _depth + 1);
                    }
                }
                if (object.extension) {
                    if (!$Array.isArray(object.extension))
                        throw $TypeError(".google.protobuf.DescriptorProto.extension: array expected");
                    message.extension = $Array(object.extension.length);
                    for (let i = 0; i < object.extension.length; ++i) {
                        if (!$util.isObject(object.extension[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.extension: object expected");
                        message.extension[i] = $root.google.protobuf.FieldDescriptorProto.fromObject(object.extension[i], _depth + 1);
                    }
                }
                if (object.nestedType) {
                    if (!$Array.isArray(object.nestedType))
                        throw $TypeError(".google.protobuf.DescriptorProto.nestedType: array expected");
                    message.nestedType = $Array(object.nestedType.length);
                    for (let i = 0; i < object.nestedType.length; ++i) {
                        if (!$util.isObject(object.nestedType[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.nestedType: object expected");
                        message.nestedType[i] = $root.google.protobuf.DescriptorProto.fromObject(object.nestedType[i], _depth + 1);
                    }
                }
                if (object.enumType) {
                    if (!$Array.isArray(object.enumType))
                        throw $TypeError(".google.protobuf.DescriptorProto.enumType: array expected");
                    message.enumType = $Array(object.enumType.length);
                    for (let i = 0; i < object.enumType.length; ++i) {
                        if (!$util.isObject(object.enumType[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.enumType: object expected");
                        message.enumType[i] = $root.google.protobuf.EnumDescriptorProto.fromObject(object.enumType[i], _depth + 1);
                    }
                }
                if (object.extensionRange) {
                    if (!$Array.isArray(object.extensionRange))
                        throw $TypeError(".google.protobuf.DescriptorProto.extensionRange: array expected");
                    message.extensionRange = $Array(object.extensionRange.length);
                    for (let i = 0; i < object.extensionRange.length; ++i) {
                        if (!$util.isObject(object.extensionRange[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.extensionRange: object expected");
                        message.extensionRange[i] = $root.google.protobuf.DescriptorProto.ExtensionRange.fromObject(object.extensionRange[i], _depth + 1);
                    }
                }
                if (object.oneofDecl) {
                    if (!$Array.isArray(object.oneofDecl))
                        throw $TypeError(".google.protobuf.DescriptorProto.oneofDecl: array expected");
                    message.oneofDecl = $Array(object.oneofDecl.length);
                    for (let i = 0; i < object.oneofDecl.length; ++i) {
                        if (!$util.isObject(object.oneofDecl[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.oneofDecl: object expected");
                        message.oneofDecl[i] = $root.google.protobuf.OneofDescriptorProto.fromObject(object.oneofDecl[i], _depth + 1);
                    }
                }
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.DescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.MessageOptions.fromObject(object.options, _depth + 1);
                }
                if (object.reservedRange) {
                    if (!$Array.isArray(object.reservedRange))
                        throw $TypeError(".google.protobuf.DescriptorProto.reservedRange: array expected");
                    message.reservedRange = $Array(object.reservedRange.length);
                    for (let i = 0; i < object.reservedRange.length; ++i) {
                        if (!$util.isObject(object.reservedRange[i]))
                            throw $TypeError(".google.protobuf.DescriptorProto.reservedRange: object expected");
                        message.reservedRange[i] = $root.google.protobuf.DescriptorProto.ReservedRange.fromObject(object.reservedRange[i], _depth + 1);
                    }
                }
                if (object.reservedName) {
                    if (!$Array.isArray(object.reservedName))
                        throw $TypeError(".google.protobuf.DescriptorProto.reservedName: array expected");
                    message.reservedName = $Array(object.reservedName.length);
                    for (let i = 0; i < object.reservedName.length; ++i)
                        message.reservedName[i] = $String(object.reservedName[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a DescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {google.protobuf.DescriptorProto} message DescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.field = [];
                    object.nestedType = [];
                    object.enumType = [];
                    object.extensionRange = [];
                    object.extension = [];
                    object.oneofDecl = [];
                    object.reservedRange = [];
                    object.reservedName = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.options = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.field && message.field.length) {
                    object.field = $Array(message.field.length);
                    for (let j = 0; j < message.field.length; ++j)
                        object.field[j] = $root.google.protobuf.FieldDescriptorProto.toObject(message.field[j], options, _depth + 1);
                }
                if (message.nestedType && message.nestedType.length) {
                    object.nestedType = $Array(message.nestedType.length);
                    for (let j = 0; j < message.nestedType.length; ++j)
                        object.nestedType[j] = $root.google.protobuf.DescriptorProto.toObject(message.nestedType[j], options, _depth + 1);
                }
                if (message.enumType && message.enumType.length) {
                    object.enumType = $Array(message.enumType.length);
                    for (let j = 0; j < message.enumType.length; ++j)
                        object.enumType[j] = $root.google.protobuf.EnumDescriptorProto.toObject(message.enumType[j], options, _depth + 1);
                }
                if (message.extensionRange && message.extensionRange.length) {
                    object.extensionRange = $Array(message.extensionRange.length);
                    for (let j = 0; j < message.extensionRange.length; ++j)
                        object.extensionRange[j] = $root.google.protobuf.DescriptorProto.ExtensionRange.toObject(message.extensionRange[j], options, _depth + 1);
                }
                if (message.extension && message.extension.length) {
                    object.extension = $Array(message.extension.length);
                    for (let j = 0; j < message.extension.length; ++j)
                        object.extension[j] = $root.google.protobuf.FieldDescriptorProto.toObject(message.extension[j], options, _depth + 1);
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.MessageOptions.toObject(message.options, options, _depth + 1);
                if (message.oneofDecl && message.oneofDecl.length) {
                    object.oneofDecl = $Array(message.oneofDecl.length);
                    for (let j = 0; j < message.oneofDecl.length; ++j)
                        object.oneofDecl[j] = $root.google.protobuf.OneofDescriptorProto.toObject(message.oneofDecl[j], options, _depth + 1);
                }
                if (message.reservedRange && message.reservedRange.length) {
                    object.reservedRange = $Array(message.reservedRange.length);
                    for (let j = 0; j < message.reservedRange.length; ++j)
                        object.reservedRange[j] = $root.google.protobuf.DescriptorProto.ReservedRange.toObject(message.reservedRange[j], options, _depth + 1);
                }
                if (message.reservedName && message.reservedName.length) {
                    object.reservedName = $Array(message.reservedName.length);
                    for (let j = 0; j < message.reservedName.length; ++j)
                        object.reservedName[j] = message.reservedName[j];
                }
                return object;
            };

            /**
             * Converts this DescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.DescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DescriptorProto.prototype.toJSON = function() {
                return DescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.DescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.DescriptorProto";
            };

            DescriptorProto.ExtensionRange = (function() {

                /**
                 * Properties of an ExtensionRange.
                 * @typedef {Object} google.protobuf.DescriptorProto.ExtensionRange.$Properties
                 * @property {number|null} [start] ExtensionRange start
                 * @property {number|null} [end] ExtensionRange end
                 * @property {google.protobuf.ExtensionRangeOptions.$Properties|null} [options] ExtensionRange options
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an ExtensionRange.
                 * @memberof google.protobuf.DescriptorProto
                 * @interface IExtensionRange
                 * @augments google.protobuf.DescriptorProto.ExtensionRange.$Properties
                 * @deprecated Use google.protobuf.DescriptorProto.ExtensionRange.$Properties instead.
                 */

                /**
                 * Shape of an ExtensionRange.
                 * @typedef {google.protobuf.DescriptorProto.ExtensionRange.$Properties} google.protobuf.DescriptorProto.ExtensionRange.$Shape
                 */

                /**
                 * Constructs a new ExtensionRange.
                 * @memberof google.protobuf.DescriptorProto
                 * @classdesc Represents an ExtensionRange.
                 * @constructor
                 * @param {google.protobuf.DescriptorProto.ExtensionRange.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const ExtensionRange = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * ExtensionRange start.
                 * @member {number} start
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @instance
                 */
                ExtensionRange.prototype.start = 0;

                /**
                 * ExtensionRange end.
                 * @member {number} end
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @instance
                 */
                ExtensionRange.prototype.end = 0;

                /**
                 * ExtensionRange options.
                 * @member {google.protobuf.ExtensionRangeOptions.$Properties|null|undefined} options
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @instance
                 */
                ExtensionRange.prototype.options = null;

                /**
                 * Creates a new ExtensionRange instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ExtensionRange.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.DescriptorProto.ExtensionRange} ExtensionRange instance
                 * @type {{
                 *   (properties: google.protobuf.DescriptorProto.ExtensionRange.$Shape): google.protobuf.DescriptorProto.ExtensionRange & google.protobuf.DescriptorProto.ExtensionRange.$Shape;
                 *   (properties?: google.protobuf.DescriptorProto.ExtensionRange.$Properties): google.protobuf.DescriptorProto.ExtensionRange;
                 * }}
                 */
                ExtensionRange.create = function(properties) {
                    return new ExtensionRange(properties);
                };

                /**
                 * Encodes the specified ExtensionRange message. Does not implicitly {@link google.protobuf.DescriptorProto.ExtensionRange.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ExtensionRange.$Properties} message ExtensionRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ExtensionRange.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.start);
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.end);
                    if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                        $root.google.protobuf.ExtensionRangeOptions.encode(message.options, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ExtensionRange message, length delimited. Does not implicitly {@link google.protobuf.DescriptorProto.ExtensionRange.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ExtensionRange.$Properties} message ExtensionRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ExtensionRange.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an ExtensionRange message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.DescriptorProto.ExtensionRange & google.protobuf.DescriptorProto.ExtensionRange.$Shape} ExtensionRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ExtensionRange.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.DescriptorProto.ExtensionRange();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                message.start = reader.int32();
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                message.end = reader.int32();
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                message.options = $root.google.protobuf.ExtensionRangeOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an ExtensionRange message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.DescriptorProto.ExtensionRange & google.protobuf.DescriptorProto.ExtensionRange.$Shape} ExtensionRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ExtensionRange.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an ExtensionRange message.
                 * @function verify
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ExtensionRange.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        if (!$util.isInteger(message.start))
                            return "start: integer expected";
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        if (!$util.isInteger(message.end))
                            return "end: integer expected";
                    if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                        let error = $root.google.protobuf.ExtensionRangeOptions.verify(message.options, _depth + 1);
                        if (error)
                            return "options." + error;
                    }
                    return null;
                };

                /**
                 * Creates an ExtensionRange message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.DescriptorProto.ExtensionRange} ExtensionRange
                 */
                ExtensionRange.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.DescriptorProto.ExtensionRange)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.DescriptorProto.ExtensionRange: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.DescriptorProto.ExtensionRange();
                    if (object.start != null)
                        message.start = object.start | 0;
                    if (object.end != null)
                        message.end = object.end | 0;
                    if (object.options != null) {
                        if (!$util.isObject(object.options))
                            throw $TypeError(".google.protobuf.DescriptorProto.ExtensionRange.options: object expected");
                        message.options = $root.google.protobuf.ExtensionRangeOptions.fromObject(object.options, _depth + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from an ExtensionRange message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ExtensionRange} message ExtensionRange
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ExtensionRange.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.start = 0;
                        object.end = 0;
                        object.options = null;
                    }
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        object.start = message.start;
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        object.end = message.end;
                    if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                        object.options = $root.google.protobuf.ExtensionRangeOptions.toObject(message.options, options, _depth + 1);
                    return object;
                };

                /**
                 * Converts this ExtensionRange to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ExtensionRange.prototype.toJSON = function() {
                    return ExtensionRange.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for ExtensionRange
                 * @function getTypeUrl
                 * @memberof google.protobuf.DescriptorProto.ExtensionRange
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                ExtensionRange.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.DescriptorProto.ExtensionRange";
                };

                return ExtensionRange;
            })();

            DescriptorProto.ReservedRange = (function() {

                /**
                 * Properties of a ReservedRange.
                 * @typedef {Object} google.protobuf.DescriptorProto.ReservedRange.$Properties
                 * @property {number|null} [start] ReservedRange start
                 * @property {number|null} [end] ReservedRange end
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a ReservedRange.
                 * @memberof google.protobuf.DescriptorProto
                 * @interface IReservedRange
                 * @augments google.protobuf.DescriptorProto.ReservedRange.$Properties
                 * @deprecated Use google.protobuf.DescriptorProto.ReservedRange.$Properties instead.
                 */

                /**
                 * Shape of a ReservedRange.
                 * @typedef {google.protobuf.DescriptorProto.ReservedRange.$Properties} google.protobuf.DescriptorProto.ReservedRange.$Shape
                 */

                /**
                 * Constructs a new ReservedRange.
                 * @memberof google.protobuf.DescriptorProto
                 * @classdesc Represents a ReservedRange.
                 * @constructor
                 * @param {google.protobuf.DescriptorProto.ReservedRange.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const ReservedRange = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * ReservedRange start.
                 * @member {number} start
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @instance
                 */
                ReservedRange.prototype.start = 0;

                /**
                 * ReservedRange end.
                 * @member {number} end
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @instance
                 */
                ReservedRange.prototype.end = 0;

                /**
                 * Creates a new ReservedRange instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ReservedRange.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.DescriptorProto.ReservedRange} ReservedRange instance
                 * @type {{
                 *   (properties: google.protobuf.DescriptorProto.ReservedRange.$Shape): google.protobuf.DescriptorProto.ReservedRange & google.protobuf.DescriptorProto.ReservedRange.$Shape;
                 *   (properties?: google.protobuf.DescriptorProto.ReservedRange.$Properties): google.protobuf.DescriptorProto.ReservedRange;
                 * }}
                 */
                ReservedRange.create = function(properties) {
                    return new ReservedRange(properties);
                };

                /**
                 * Encodes the specified ReservedRange message. Does not implicitly {@link google.protobuf.DescriptorProto.ReservedRange.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ReservedRange.$Properties} message ReservedRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReservedRange.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.start);
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.end);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ReservedRange message, length delimited. Does not implicitly {@link google.protobuf.DescriptorProto.ReservedRange.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ReservedRange.$Properties} message ReservedRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ReservedRange.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ReservedRange message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.DescriptorProto.ReservedRange & google.protobuf.DescriptorProto.ReservedRange.$Shape} ReservedRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReservedRange.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.DescriptorProto.ReservedRange();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                message.start = reader.int32();
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                message.end = reader.int32();
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ReservedRange message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.DescriptorProto.ReservedRange & google.protobuf.DescriptorProto.ReservedRange.$Shape} ReservedRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ReservedRange.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ReservedRange message.
                 * @function verify
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ReservedRange.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        if (!$util.isInteger(message.start))
                            return "start: integer expected";
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        if (!$util.isInteger(message.end))
                            return "end: integer expected";
                    return null;
                };

                /**
                 * Creates a ReservedRange message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.DescriptorProto.ReservedRange} ReservedRange
                 */
                ReservedRange.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.DescriptorProto.ReservedRange)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.DescriptorProto.ReservedRange: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.DescriptorProto.ReservedRange();
                    if (object.start != null)
                        message.start = object.start | 0;
                    if (object.end != null)
                        message.end = object.end | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a ReservedRange message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {google.protobuf.DescriptorProto.ReservedRange} message ReservedRange
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ReservedRange.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.start = 0;
                        object.end = 0;
                    }
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        object.start = message.start;
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        object.end = message.end;
                    return object;
                };

                /**
                 * Converts this ReservedRange to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ReservedRange.prototype.toJSON = function() {
                    return ReservedRange.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for ReservedRange
                 * @function getTypeUrl
                 * @memberof google.protobuf.DescriptorProto.ReservedRange
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                ReservedRange.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.DescriptorProto.ReservedRange";
                };

                return ReservedRange;
            })();

            return DescriptorProto;
        })();

        protobuf.ExtensionRangeOptions = (function() {

            /**
             * Properties of an ExtensionRangeOptions.
             * @typedef {Object} google.protobuf.ExtensionRangeOptions.$Properties
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] ExtensionRangeOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ExtensionRangeOptions.
             * @memberof google.protobuf
             * @interface IExtensionRangeOptions
             * @augments google.protobuf.ExtensionRangeOptions.$Properties
             * @deprecated Use google.protobuf.ExtensionRangeOptions.$Properties instead.
             */

            /**
             * Shape of an ExtensionRangeOptions.
             * @typedef {google.protobuf.ExtensionRangeOptions.$Properties} google.protobuf.ExtensionRangeOptions.$Shape
             */

            /**
             * Constructs a new ExtensionRangeOptions.
             * @memberof google.protobuf
             * @classdesc Represents an ExtensionRangeOptions.
             * @constructor
             * @param {google.protobuf.ExtensionRangeOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ExtensionRangeOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ExtensionRangeOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.ExtensionRangeOptions
             * @instance
             */
            ExtensionRangeOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new ExtensionRangeOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {google.protobuf.ExtensionRangeOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ExtensionRangeOptions} ExtensionRangeOptions instance
             * @type {{
             *   (properties: google.protobuf.ExtensionRangeOptions.$Shape): google.protobuf.ExtensionRangeOptions & google.protobuf.ExtensionRangeOptions.$Shape;
             *   (properties?: google.protobuf.ExtensionRangeOptions.$Properties): google.protobuf.ExtensionRangeOptions;
             * }}
             */
            ExtensionRangeOptions.create = function(properties) {
                return new ExtensionRangeOptions(properties);
            };

            /**
             * Encodes the specified ExtensionRangeOptions message. Does not implicitly {@link google.protobuf.ExtensionRangeOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {google.protobuf.ExtensionRangeOptions.$Properties} message ExtensionRangeOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtensionRangeOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExtensionRangeOptions message, length delimited. Does not implicitly {@link google.protobuf.ExtensionRangeOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {google.protobuf.ExtensionRangeOptions.$Properties} message ExtensionRangeOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtensionRangeOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ExtensionRangeOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ExtensionRangeOptions & google.protobuf.ExtensionRangeOptions.$Shape} ExtensionRangeOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtensionRangeOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.ExtensionRangeOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExtensionRangeOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.ExtensionRangeOptions & google.protobuf.ExtensionRangeOptions.$Shape} ExtensionRangeOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtensionRangeOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExtensionRangeOptions message.
             * @function verify
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExtensionRangeOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an ExtensionRangeOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ExtensionRangeOptions} ExtensionRangeOptions
             */
            ExtensionRangeOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ExtensionRangeOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ExtensionRangeOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ExtensionRangeOptions();
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.ExtensionRangeOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.ExtensionRangeOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an ExtensionRangeOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {google.protobuf.ExtensionRangeOptions} message ExtensionRangeOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExtensionRangeOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ExtensionRangeOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.ExtensionRangeOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExtensionRangeOptions.prototype.toJSON = function() {
                return ExtensionRangeOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ExtensionRangeOptions
             * @function getTypeUrl
             * @memberof google.protobuf.ExtensionRangeOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ExtensionRangeOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ExtensionRangeOptions";
            };

            return ExtensionRangeOptions;
        })();

        protobuf.FieldDescriptorProto = (function() {

            /**
             * Properties of a FieldDescriptorProto.
             * @typedef {Object} google.protobuf.FieldDescriptorProto.$Properties
             * @property {string|null} [name] FieldDescriptorProto name
             * @property {number|null} [number] FieldDescriptorProto number
             * @property {google.protobuf.FieldDescriptorProto.Label|null} [label] FieldDescriptorProto label
             * @property {google.protobuf.FieldDescriptorProto.Type|null} [type] FieldDescriptorProto type
             * @property {string|null} [typeName] FieldDescriptorProto typeName
             * @property {string|null} [extendee] FieldDescriptorProto extendee
             * @property {string|null} [defaultValue] FieldDescriptorProto defaultValue
             * @property {number|null} [oneofIndex] FieldDescriptorProto oneofIndex
             * @property {string|null} [jsonName] FieldDescriptorProto jsonName
             * @property {google.protobuf.FieldOptions.$Properties|null} [options] FieldDescriptorProto options
             * @property {boolean|null} [proto3Optional] FieldDescriptorProto proto3Optional
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FieldDescriptorProto.
             * @memberof google.protobuf
             * @interface IFieldDescriptorProto
             * @augments google.protobuf.FieldDescriptorProto.$Properties
             * @deprecated Use google.protobuf.FieldDescriptorProto.$Properties instead.
             */

            /**
             * Shape of a FieldDescriptorProto.
             * @typedef {google.protobuf.FieldDescriptorProto.$Properties} google.protobuf.FieldDescriptorProto.$Shape
             */

            /**
             * Constructs a new FieldDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a FieldDescriptorProto.
             * @constructor
             * @param {google.protobuf.FieldDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FieldDescriptorProto = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FieldDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.name = "";

            /**
             * FieldDescriptorProto number.
             * @member {number} number
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.number = 0;

            /**
             * FieldDescriptorProto label.
             * @member {google.protobuf.FieldDescriptorProto.Label} label
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.label = 1;

            /**
             * FieldDescriptorProto type.
             * @member {google.protobuf.FieldDescriptorProto.Type} type
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.type = 1;

            /**
             * FieldDescriptorProto typeName.
             * @member {string} typeName
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.typeName = "";

            /**
             * FieldDescriptorProto extendee.
             * @member {string} extendee
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.extendee = "";

            /**
             * FieldDescriptorProto defaultValue.
             * @member {string} defaultValue
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.defaultValue = "";

            /**
             * FieldDescriptorProto oneofIndex.
             * @member {number} oneofIndex
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.oneofIndex = 0;

            /**
             * FieldDescriptorProto jsonName.
             * @member {string} jsonName
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.jsonName = "";

            /**
             * FieldDescriptorProto options.
             * @member {google.protobuf.FieldOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.options = null;

            /**
             * FieldDescriptorProto proto3Optional.
             * @member {boolean} proto3Optional
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             */
            FieldDescriptorProto.prototype.proto3Optional = false;

            /**
             * Creates a new FieldDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {google.protobuf.FieldDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FieldDescriptorProto} FieldDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.FieldDescriptorProto.$Shape): google.protobuf.FieldDescriptorProto & google.protobuf.FieldDescriptorProto.$Shape;
             *   (properties?: google.protobuf.FieldDescriptorProto.$Properties): google.protobuf.FieldDescriptorProto;
             * }}
             */
            FieldDescriptorProto.create = function(properties) {
                return new FieldDescriptorProto(properties);
            };

            /**
             * Encodes the specified FieldDescriptorProto message. Does not implicitly {@link google.protobuf.FieldDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {google.protobuf.FieldDescriptorProto.$Properties} message FieldDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FieldDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.extendee != null && $Object.hasOwnProperty.call(message, "extendee"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.extendee);
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.number);
                if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.label);
                if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.type);
                if (message.typeName != null && $Object.hasOwnProperty.call(message, "typeName"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.typeName);
                if (message.defaultValue != null && $Object.hasOwnProperty.call(message, "defaultValue"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.defaultValue);
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.FieldOptions.encode(message.options, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                if (message.oneofIndex != null && $Object.hasOwnProperty.call(message, "oneofIndex"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.oneofIndex);
                if (message.jsonName != null && $Object.hasOwnProperty.call(message, "jsonName"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.jsonName);
                if (message.proto3Optional != null && $Object.hasOwnProperty.call(message, "proto3Optional"))
                    writer.uint32(/* id 17, wireType 0 =*/136).bool(message.proto3Optional);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FieldDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.FieldDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {google.protobuf.FieldDescriptorProto.$Properties} message FieldDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FieldDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FieldDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FieldDescriptorProto & google.protobuf.FieldDescriptorProto.$Shape} FieldDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FieldDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.FieldDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.number = reader.int32();
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.label = reader.int32();
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.type = reader.int32();
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.typeName = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.extendee = reader.string();
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.defaultValue = reader.string();
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            message.oneofIndex = reader.int32();
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.jsonName = reader.string();
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.FieldOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    case 17: {
                            if (wireType !== 0)
                                break;
                            message.proto3Optional = reader.bool();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FieldDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FieldDescriptorProto & google.protobuf.FieldDescriptorProto.$Shape} FieldDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FieldDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FieldDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FieldDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    if (!$util.isInteger(message.number))
                        return "number: integer expected";
                if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                    switch (message.label) {
                    default:
                        return "label: enum value expected";
                    case 1:
                    case 2:
                    case 3:
                        break;
                    }
                if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                    switch (message.type) {
                    default:
                        return "type: enum value expected";
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                    case 5:
                    case 6:
                    case 7:
                    case 8:
                    case 9:
                    case 10:
                    case 11:
                    case 12:
                    case 13:
                    case 14:
                    case 15:
                    case 16:
                    case 17:
                    case 18:
                        break;
                    }
                if (message.typeName != null && $Object.hasOwnProperty.call(message, "typeName"))
                    if (!$util.isString(message.typeName))
                        return "typeName: string expected";
                if (message.extendee != null && $Object.hasOwnProperty.call(message, "extendee"))
                    if (!$util.isString(message.extendee))
                        return "extendee: string expected";
                if (message.defaultValue != null && $Object.hasOwnProperty.call(message, "defaultValue"))
                    if (!$util.isString(message.defaultValue))
                        return "defaultValue: string expected";
                if (message.oneofIndex != null && $Object.hasOwnProperty.call(message, "oneofIndex"))
                    if (!$util.isInteger(message.oneofIndex))
                        return "oneofIndex: integer expected";
                if (message.jsonName != null && $Object.hasOwnProperty.call(message, "jsonName"))
                    if (!$util.isString(message.jsonName))
                        return "jsonName: string expected";
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.FieldOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                if (message.proto3Optional != null && $Object.hasOwnProperty.call(message, "proto3Optional"))
                    if (typeof message.proto3Optional !== "boolean")
                        return "proto3Optional: boolean expected";
                return null;
            };

            /**
             * Creates a FieldDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FieldDescriptorProto} FieldDescriptorProto
             */
            FieldDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FieldDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FieldDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FieldDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.number != null)
                    message.number = object.number | 0;
                switch (object.label) {
                default:
                    if (typeof object.label === "number") {
                        message.label = object.label;
                        break;
                    }
                    break;
                case "LABEL_OPTIONAL":
                case 1:
                    message.label = 1;
                    break;
                case "LABEL_REQUIRED":
                case 2:
                    message.label = 2;
                    break;
                case "LABEL_REPEATED":
                case 3:
                    message.label = 3;
                    break;
                }
                switch (object.type) {
                default:
                    if (typeof object.type === "number") {
                        message.type = object.type;
                        break;
                    }
                    break;
                case "TYPE_DOUBLE":
                case 1:
                    message.type = 1;
                    break;
                case "TYPE_FLOAT":
                case 2:
                    message.type = 2;
                    break;
                case "TYPE_INT64":
                case 3:
                    message.type = 3;
                    break;
                case "TYPE_UINT64":
                case 4:
                    message.type = 4;
                    break;
                case "TYPE_INT32":
                case 5:
                    message.type = 5;
                    break;
                case "TYPE_FIXED64":
                case 6:
                    message.type = 6;
                    break;
                case "TYPE_FIXED32":
                case 7:
                    message.type = 7;
                    break;
                case "TYPE_BOOL":
                case 8:
                    message.type = 8;
                    break;
                case "TYPE_STRING":
                case 9:
                    message.type = 9;
                    break;
                case "TYPE_GROUP":
                case 10:
                    message.type = 10;
                    break;
                case "TYPE_MESSAGE":
                case 11:
                    message.type = 11;
                    break;
                case "TYPE_BYTES":
                case 12:
                    message.type = 12;
                    break;
                case "TYPE_UINT32":
                case 13:
                    message.type = 13;
                    break;
                case "TYPE_ENUM":
                case 14:
                    message.type = 14;
                    break;
                case "TYPE_SFIXED32":
                case 15:
                    message.type = 15;
                    break;
                case "TYPE_SFIXED64":
                case 16:
                    message.type = 16;
                    break;
                case "TYPE_SINT32":
                case 17:
                    message.type = 17;
                    break;
                case "TYPE_SINT64":
                case 18:
                    message.type = 18;
                    break;
                }
                if (object.typeName != null)
                    message.typeName = $String(object.typeName);
                if (object.extendee != null)
                    message.extendee = $String(object.extendee);
                if (object.defaultValue != null)
                    message.defaultValue = $String(object.defaultValue);
                if (object.oneofIndex != null)
                    message.oneofIndex = object.oneofIndex | 0;
                if (object.jsonName != null)
                    message.jsonName = $String(object.jsonName);
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.FieldDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.FieldOptions.fromObject(object.options, _depth + 1);
                }
                if (object.proto3Optional != null)
                    message.proto3Optional = $Boolean(object.proto3Optional);
                return message;
            };

            /**
             * Creates a plain object from a FieldDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {google.protobuf.FieldDescriptorProto} message FieldDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FieldDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.name = "";
                    object.extendee = "";
                    object.number = 0;
                    object.label = options.enums === $String ? "LABEL_OPTIONAL" : 1;
                    object.type = options.enums === $String ? "TYPE_DOUBLE" : 1;
                    object.typeName = "";
                    object.defaultValue = "";
                    object.options = null;
                    object.oneofIndex = 0;
                    object.jsonName = "";
                    object.proto3Optional = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.extendee != null && $Object.hasOwnProperty.call(message, "extendee"))
                    object.extendee = message.extendee;
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    object.number = message.number;
                if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                    object.label = options.enums === $String ? $root.google.protobuf.FieldDescriptorProto.Label[message.label] === $undefined ? message.label : $root.google.protobuf.FieldDescriptorProto.Label[message.label] : message.label;
                if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === $String ? $root.google.protobuf.FieldDescriptorProto.Type[message.type] === $undefined ? message.type : $root.google.protobuf.FieldDescriptorProto.Type[message.type] : message.type;
                if (message.typeName != null && $Object.hasOwnProperty.call(message, "typeName"))
                    object.typeName = message.typeName;
                if (message.defaultValue != null && $Object.hasOwnProperty.call(message, "defaultValue"))
                    object.defaultValue = message.defaultValue;
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.FieldOptions.toObject(message.options, options, _depth + 1);
                if (message.oneofIndex != null && $Object.hasOwnProperty.call(message, "oneofIndex"))
                    object.oneofIndex = message.oneofIndex;
                if (message.jsonName != null && $Object.hasOwnProperty.call(message, "jsonName"))
                    object.jsonName = message.jsonName;
                if (message.proto3Optional != null && $Object.hasOwnProperty.call(message, "proto3Optional"))
                    object.proto3Optional = message.proto3Optional;
                return object;
            };

            /**
             * Converts this FieldDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.FieldDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FieldDescriptorProto.prototype.toJSON = function() {
                return FieldDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FieldDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.FieldDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FieldDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FieldDescriptorProto";
            };

            /**
             * Type enum.
             * @name google.protobuf.FieldDescriptorProto.Type
             * @enum {number}
             * @property {number} TYPE_DOUBLE=1 TYPE_DOUBLE value
             * @property {number} TYPE_FLOAT=2 TYPE_FLOAT value
             * @property {number} TYPE_INT64=3 TYPE_INT64 value
             * @property {number} TYPE_UINT64=4 TYPE_UINT64 value
             * @property {number} TYPE_INT32=5 TYPE_INT32 value
             * @property {number} TYPE_FIXED64=6 TYPE_FIXED64 value
             * @property {number} TYPE_FIXED32=7 TYPE_FIXED32 value
             * @property {number} TYPE_BOOL=8 TYPE_BOOL value
             * @property {number} TYPE_STRING=9 TYPE_STRING value
             * @property {number} TYPE_GROUP=10 TYPE_GROUP value
             * @property {number} TYPE_MESSAGE=11 TYPE_MESSAGE value
             * @property {number} TYPE_BYTES=12 TYPE_BYTES value
             * @property {number} TYPE_UINT32=13 TYPE_UINT32 value
             * @property {number} TYPE_ENUM=14 TYPE_ENUM value
             * @property {number} TYPE_SFIXED32=15 TYPE_SFIXED32 value
             * @property {number} TYPE_SFIXED64=16 TYPE_SFIXED64 value
             * @property {number} TYPE_SINT32=17 TYPE_SINT32 value
             * @property {number} TYPE_SINT64=18 TYPE_SINT64 value
             */
            FieldDescriptorProto.Type = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[1] = "TYPE_DOUBLE"] = 1;
                values[valuesById[2] = "TYPE_FLOAT"] = 2;
                values[valuesById[3] = "TYPE_INT64"] = 3;
                values[valuesById[4] = "TYPE_UINT64"] = 4;
                values[valuesById[5] = "TYPE_INT32"] = 5;
                values[valuesById[6] = "TYPE_FIXED64"] = 6;
                values[valuesById[7] = "TYPE_FIXED32"] = 7;
                values[valuesById[8] = "TYPE_BOOL"] = 8;
                values[valuesById[9] = "TYPE_STRING"] = 9;
                values[valuesById[10] = "TYPE_GROUP"] = 10;
                values[valuesById[11] = "TYPE_MESSAGE"] = 11;
                values[valuesById[12] = "TYPE_BYTES"] = 12;
                values[valuesById[13] = "TYPE_UINT32"] = 13;
                values[valuesById[14] = "TYPE_ENUM"] = 14;
                values[valuesById[15] = "TYPE_SFIXED32"] = 15;
                values[valuesById[16] = "TYPE_SFIXED64"] = 16;
                values[valuesById[17] = "TYPE_SINT32"] = 17;
                values[valuesById[18] = "TYPE_SINT64"] = 18;
                return values;
            })();

            /**
             * Label enum.
             * @name google.protobuf.FieldDescriptorProto.Label
             * @enum {number}
             * @property {number} LABEL_OPTIONAL=1 LABEL_OPTIONAL value
             * @property {number} LABEL_REQUIRED=2 LABEL_REQUIRED value
             * @property {number} LABEL_REPEATED=3 LABEL_REPEATED value
             */
            FieldDescriptorProto.Label = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[1] = "LABEL_OPTIONAL"] = 1;
                values[valuesById[2] = "LABEL_REQUIRED"] = 2;
                values[valuesById[3] = "LABEL_REPEATED"] = 3;
                return values;
            })();

            return FieldDescriptorProto;
        })();

        protobuf.OneofDescriptorProto = (function() {

            /**
             * Properties of a OneofDescriptorProto.
             * @typedef {Object} google.protobuf.OneofDescriptorProto.$Properties
             * @property {string|null} [name] OneofDescriptorProto name
             * @property {google.protobuf.OneofOptions.$Properties|null} [options] OneofDescriptorProto options
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a OneofDescriptorProto.
             * @memberof google.protobuf
             * @interface IOneofDescriptorProto
             * @augments google.protobuf.OneofDescriptorProto.$Properties
             * @deprecated Use google.protobuf.OneofDescriptorProto.$Properties instead.
             */

            /**
             * Shape of a OneofDescriptorProto.
             * @typedef {google.protobuf.OneofDescriptorProto.$Properties} google.protobuf.OneofDescriptorProto.$Shape
             */

            /**
             * Constructs a new OneofDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a OneofDescriptorProto.
             * @constructor
             * @param {google.protobuf.OneofDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const OneofDescriptorProto = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OneofDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.OneofDescriptorProto
             * @instance
             */
            OneofDescriptorProto.prototype.name = "";

            /**
             * OneofDescriptorProto options.
             * @member {google.protobuf.OneofOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.OneofDescriptorProto
             * @instance
             */
            OneofDescriptorProto.prototype.options = null;

            /**
             * Creates a new OneofDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {google.protobuf.OneofDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.OneofDescriptorProto} OneofDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.OneofDescriptorProto.$Shape): google.protobuf.OneofDescriptorProto & google.protobuf.OneofDescriptorProto.$Shape;
             *   (properties?: google.protobuf.OneofDescriptorProto.$Properties): google.protobuf.OneofDescriptorProto;
             * }}
             */
            OneofDescriptorProto.create = function(properties) {
                return new OneofDescriptorProto(properties);
            };

            /**
             * Encodes the specified OneofDescriptorProto message. Does not implicitly {@link google.protobuf.OneofDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {google.protobuf.OneofDescriptorProto.$Properties} message OneofDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OneofDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.OneofOptions.encode(message.options, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified OneofDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.OneofDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {google.protobuf.OneofDescriptorProto.$Properties} message OneofDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OneofDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a OneofDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.OneofDescriptorProto & google.protobuf.OneofDescriptorProto.$Shape} OneofDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OneofDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.OneofDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.OneofOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a OneofDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.OneofDescriptorProto & google.protobuf.OneofDescriptorProto.$Shape} OneofDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OneofDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a OneofDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OneofDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.OneofOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                return null;
            };

            /**
             * Creates a OneofDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.OneofDescriptorProto} OneofDescriptorProto
             */
            OneofDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.OneofDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.OneofDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.OneofDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.OneofDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.OneofOptions.fromObject(object.options, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a OneofDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {google.protobuf.OneofDescriptorProto} message OneofDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OneofDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.name = "";
                    object.options = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.OneofOptions.toObject(message.options, options, _depth + 1);
                return object;
            };

            /**
             * Converts this OneofDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.OneofDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OneofDescriptorProto.prototype.toJSON = function() {
                return OneofDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OneofDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.OneofDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OneofDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.OneofDescriptorProto";
            };

            return OneofDescriptorProto;
        })();

        protobuf.EnumDescriptorProto = (function() {

            /**
             * Properties of an EnumDescriptorProto.
             * @typedef {Object} google.protobuf.EnumDescriptorProto.$Properties
             * @property {string|null} [name] EnumDescriptorProto name
             * @property {Array.<google.protobuf.EnumValueDescriptorProto.$Properties>|null} [value] EnumDescriptorProto value
             * @property {google.protobuf.EnumOptions.$Properties|null} [options] EnumDescriptorProto options
             * @property {Array.<google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties>|null} [reservedRange] EnumDescriptorProto reservedRange
             * @property {Array.<string>|null} [reservedName] EnumDescriptorProto reservedName
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EnumDescriptorProto.
             * @memberof google.protobuf
             * @interface IEnumDescriptorProto
             * @augments google.protobuf.EnumDescriptorProto.$Properties
             * @deprecated Use google.protobuf.EnumDescriptorProto.$Properties instead.
             */

            /**
             * Shape of an EnumDescriptorProto.
             * @typedef {google.protobuf.EnumDescriptorProto.$Properties} google.protobuf.EnumDescriptorProto.$Shape
             */

            /**
             * Constructs a new EnumDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents an EnumDescriptorProto.
             * @constructor
             * @param {google.protobuf.EnumDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const EnumDescriptorProto = function (properties) {
                this.value = [];
                this.reservedRange = [];
                this.reservedName = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EnumDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             */
            EnumDescriptorProto.prototype.name = "";

            /**
             * EnumDescriptorProto value.
             * @member {Array.<google.protobuf.EnumValueDescriptorProto.$Properties>} value
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             */
            EnumDescriptorProto.prototype.value = $util.emptyArray;

            /**
             * EnumDescriptorProto options.
             * @member {google.protobuf.EnumOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             */
            EnumDescriptorProto.prototype.options = null;

            /**
             * EnumDescriptorProto reservedRange.
             * @member {Array.<google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties>} reservedRange
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             */
            EnumDescriptorProto.prototype.reservedRange = $util.emptyArray;

            /**
             * EnumDescriptorProto reservedName.
             * @member {Array.<string>} reservedName
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             */
            EnumDescriptorProto.prototype.reservedName = $util.emptyArray;

            /**
             * Creates a new EnumDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {google.protobuf.EnumDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.EnumDescriptorProto} EnumDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.EnumDescriptorProto.$Shape): google.protobuf.EnumDescriptorProto & google.protobuf.EnumDescriptorProto.$Shape;
             *   (properties?: google.protobuf.EnumDescriptorProto.$Properties): google.protobuf.EnumDescriptorProto;
             * }}
             */
            EnumDescriptorProto.create = function(properties) {
                return new EnumDescriptorProto(properties);
            };

            /**
             * Encodes the specified EnumDescriptorProto message. Does not implicitly {@link google.protobuf.EnumDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {google.protobuf.EnumDescriptorProto.$Properties} message EnumDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.google.protobuf.EnumValueDescriptorProto.encode(message.value[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.EnumOptions.encode(message.options, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.reservedRange != null && message.reservedRange.length)
                    for (let i = 0; i < message.reservedRange.length; ++i)
                        $root.google.protobuf.EnumDescriptorProto.EnumReservedRange.encode(message.reservedRange[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.reservedName != null && message.reservedName.length)
                    for (let i = 0; i < message.reservedName.length; ++i)
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.reservedName[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EnumDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.EnumDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {google.protobuf.EnumDescriptorProto.$Properties} message EnumDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EnumDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.EnumDescriptorProto & google.protobuf.EnumDescriptorProto.$Shape} EnumDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.EnumDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.google.protobuf.EnumValueDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.EnumOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.reservedRange && message.reservedRange.length))
                                message.reservedRange = [];
                            message.reservedRange.push($root.google.protobuf.EnumDescriptorProto.EnumReservedRange.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.reservedName && message.reservedName.length))
                                message.reservedName = [];
                            message.reservedName.push(reader.string());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EnumDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.EnumDescriptorProto & google.protobuf.EnumDescriptorProto.$Shape} EnumDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EnumDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EnumDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                    if (!$Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.google.protobuf.EnumValueDescriptorProto.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.EnumOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                if (message.reservedRange != null && $Object.hasOwnProperty.call(message, "reservedRange")) {
                    if (!$Array.isArray(message.reservedRange))
                        return "reservedRange: array expected";
                    for (let i = 0; i < message.reservedRange.length; ++i) {
                        let error = $root.google.protobuf.EnumDescriptorProto.EnumReservedRange.verify(message.reservedRange[i], _depth + 1);
                        if (error)
                            return "reservedRange." + error;
                    }
                }
                if (message.reservedName != null && $Object.hasOwnProperty.call(message, "reservedName")) {
                    if (!$Array.isArray(message.reservedName))
                        return "reservedName: array expected";
                    for (let i = 0; i < message.reservedName.length; ++i)
                        if (!$util.isString(message.reservedName[i]))
                            return "reservedName: string[] expected";
                }
                return null;
            };

            /**
             * Creates an EnumDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.EnumDescriptorProto} EnumDescriptorProto
             */
            EnumDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.EnumDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.EnumDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.EnumDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.value) {
                    if (!$Array.isArray(object.value))
                        throw $TypeError(".google.protobuf.EnumDescriptorProto.value: array expected");
                    message.value = $Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (!$util.isObject(object.value[i]))
                            throw $TypeError(".google.protobuf.EnumDescriptorProto.value: object expected");
                        message.value[i] = $root.google.protobuf.EnumValueDescriptorProto.fromObject(object.value[i], _depth + 1);
                    }
                }
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.EnumDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.EnumOptions.fromObject(object.options, _depth + 1);
                }
                if (object.reservedRange) {
                    if (!$Array.isArray(object.reservedRange))
                        throw $TypeError(".google.protobuf.EnumDescriptorProto.reservedRange: array expected");
                    message.reservedRange = $Array(object.reservedRange.length);
                    for (let i = 0; i < object.reservedRange.length; ++i) {
                        if (!$util.isObject(object.reservedRange[i]))
                            throw $TypeError(".google.protobuf.EnumDescriptorProto.reservedRange: object expected");
                        message.reservedRange[i] = $root.google.protobuf.EnumDescriptorProto.EnumReservedRange.fromObject(object.reservedRange[i], _depth + 1);
                    }
                }
                if (object.reservedName) {
                    if (!$Array.isArray(object.reservedName))
                        throw $TypeError(".google.protobuf.EnumDescriptorProto.reservedName: array expected");
                    message.reservedName = $Array(object.reservedName.length);
                    for (let i = 0; i < object.reservedName.length; ++i)
                        message.reservedName[i] = $String(object.reservedName[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from an EnumDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {google.protobuf.EnumDescriptorProto} message EnumDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EnumDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.value = [];
                    object.reservedRange = [];
                    object.reservedName = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.options = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.value && message.value.length) {
                    object.value = $Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.google.protobuf.EnumValueDescriptorProto.toObject(message.value[j], options, _depth + 1);
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.EnumOptions.toObject(message.options, options, _depth + 1);
                if (message.reservedRange && message.reservedRange.length) {
                    object.reservedRange = $Array(message.reservedRange.length);
                    for (let j = 0; j < message.reservedRange.length; ++j)
                        object.reservedRange[j] = $root.google.protobuf.EnumDescriptorProto.EnumReservedRange.toObject(message.reservedRange[j], options, _depth + 1);
                }
                if (message.reservedName && message.reservedName.length) {
                    object.reservedName = $Array(message.reservedName.length);
                    for (let j = 0; j < message.reservedName.length; ++j)
                        object.reservedName[j] = message.reservedName[j];
                }
                return object;
            };

            /**
             * Converts this EnumDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.EnumDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EnumDescriptorProto.prototype.toJSON = function() {
                return EnumDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EnumDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.EnumDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EnumDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.EnumDescriptorProto";
            };

            EnumDescriptorProto.EnumReservedRange = (function() {

                /**
                 * Properties of an EnumReservedRange.
                 * @typedef {Object} google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties
                 * @property {number|null} [start] EnumReservedRange start
                 * @property {number|null} [end] EnumReservedRange end
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an EnumReservedRange.
                 * @memberof google.protobuf.EnumDescriptorProto
                 * @interface IEnumReservedRange
                 * @augments google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties
                 * @deprecated Use google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties instead.
                 */

                /**
                 * Shape of an EnumReservedRange.
                 * @typedef {google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties} google.protobuf.EnumDescriptorProto.EnumReservedRange.$Shape
                 */

                /**
                 * Constructs a new EnumReservedRange.
                 * @memberof google.protobuf.EnumDescriptorProto
                 * @classdesc Represents an EnumReservedRange.
                 * @constructor
                 * @param {google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const EnumReservedRange = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * EnumReservedRange start.
                 * @member {number} start
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @instance
                 */
                EnumReservedRange.prototype.start = 0;

                /**
                 * EnumReservedRange end.
                 * @member {number} end
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @instance
                 */
                EnumReservedRange.prototype.end = 0;

                /**
                 * Creates a new EnumReservedRange instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.EnumDescriptorProto.EnumReservedRange} EnumReservedRange instance
                 * @type {{
                 *   (properties: google.protobuf.EnumDescriptorProto.EnumReservedRange.$Shape): google.protobuf.EnumDescriptorProto.EnumReservedRange & google.protobuf.EnumDescriptorProto.EnumReservedRange.$Shape;
                 *   (properties?: google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties): google.protobuf.EnumDescriptorProto.EnumReservedRange;
                 * }}
                 */
                EnumReservedRange.create = function(properties) {
                    return new EnumReservedRange(properties);
                };

                /**
                 * Encodes the specified EnumReservedRange message. Does not implicitly {@link google.protobuf.EnumDescriptorProto.EnumReservedRange.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties} message EnumReservedRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                EnumReservedRange.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.start);
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.end);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified EnumReservedRange message, length delimited. Does not implicitly {@link google.protobuf.EnumDescriptorProto.EnumReservedRange.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {google.protobuf.EnumDescriptorProto.EnumReservedRange.$Properties} message EnumReservedRange message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                EnumReservedRange.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an EnumReservedRange message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.EnumDescriptorProto.EnumReservedRange & google.protobuf.EnumDescriptorProto.EnumReservedRange.$Shape} EnumReservedRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                EnumReservedRange.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.EnumDescriptorProto.EnumReservedRange();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                message.start = reader.int32();
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                message.end = reader.int32();
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an EnumReservedRange message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.EnumDescriptorProto.EnumReservedRange & google.protobuf.EnumDescriptorProto.EnumReservedRange.$Shape} EnumReservedRange
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                EnumReservedRange.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an EnumReservedRange message.
                 * @function verify
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                EnumReservedRange.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        if (!$util.isInteger(message.start))
                            return "start: integer expected";
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        if (!$util.isInteger(message.end))
                            return "end: integer expected";
                    return null;
                };

                /**
                 * Creates an EnumReservedRange message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.EnumDescriptorProto.EnumReservedRange} EnumReservedRange
                 */
                EnumReservedRange.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.EnumDescriptorProto.EnumReservedRange)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.EnumDescriptorProto.EnumReservedRange: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.EnumDescriptorProto.EnumReservedRange();
                    if (object.start != null)
                        message.start = object.start | 0;
                    if (object.end != null)
                        message.end = object.end | 0;
                    return message;
                };

                /**
                 * Creates a plain object from an EnumReservedRange message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {google.protobuf.EnumDescriptorProto.EnumReservedRange} message EnumReservedRange
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                EnumReservedRange.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.start = 0;
                        object.end = 0;
                    }
                    if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                        object.start = message.start;
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        object.end = message.end;
                    return object;
                };

                /**
                 * Converts this EnumReservedRange to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                EnumReservedRange.prototype.toJSON = function() {
                    return EnumReservedRange.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for EnumReservedRange
                 * @function getTypeUrl
                 * @memberof google.protobuf.EnumDescriptorProto.EnumReservedRange
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                EnumReservedRange.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.EnumDescriptorProto.EnumReservedRange";
                };

                return EnumReservedRange;
            })();

            return EnumDescriptorProto;
        })();

        protobuf.EnumValueDescriptorProto = (function() {

            /**
             * Properties of an EnumValueDescriptorProto.
             * @typedef {Object} google.protobuf.EnumValueDescriptorProto.$Properties
             * @property {string|null} [name] EnumValueDescriptorProto name
             * @property {number|null} [number] EnumValueDescriptorProto number
             * @property {google.protobuf.EnumValueOptions.$Properties|null} [options] EnumValueDescriptorProto options
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EnumValueDescriptorProto.
             * @memberof google.protobuf
             * @interface IEnumValueDescriptorProto
             * @augments google.protobuf.EnumValueDescriptorProto.$Properties
             * @deprecated Use google.protobuf.EnumValueDescriptorProto.$Properties instead.
             */

            /**
             * Shape of an EnumValueDescriptorProto.
             * @typedef {google.protobuf.EnumValueDescriptorProto.$Properties} google.protobuf.EnumValueDescriptorProto.$Shape
             */

            /**
             * Constructs a new EnumValueDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents an EnumValueDescriptorProto.
             * @constructor
             * @param {google.protobuf.EnumValueDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const EnumValueDescriptorProto = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EnumValueDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @instance
             */
            EnumValueDescriptorProto.prototype.name = "";

            /**
             * EnumValueDescriptorProto number.
             * @member {number} number
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @instance
             */
            EnumValueDescriptorProto.prototype.number = 0;

            /**
             * EnumValueDescriptorProto options.
             * @member {google.protobuf.EnumValueOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @instance
             */
            EnumValueDescriptorProto.prototype.options = null;

            /**
             * Creates a new EnumValueDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {google.protobuf.EnumValueDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.EnumValueDescriptorProto} EnumValueDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.EnumValueDescriptorProto.$Shape): google.protobuf.EnumValueDescriptorProto & google.protobuf.EnumValueDescriptorProto.$Shape;
             *   (properties?: google.protobuf.EnumValueDescriptorProto.$Properties): google.protobuf.EnumValueDescriptorProto;
             * }}
             */
            EnumValueDescriptorProto.create = function(properties) {
                return new EnumValueDescriptorProto(properties);
            };

            /**
             * Encodes the specified EnumValueDescriptorProto message. Does not implicitly {@link google.protobuf.EnumValueDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {google.protobuf.EnumValueDescriptorProto.$Properties} message EnumValueDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumValueDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.number);
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.EnumValueOptions.encode(message.options, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EnumValueDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.EnumValueDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {google.protobuf.EnumValueDescriptorProto.$Properties} message EnumValueDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumValueDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EnumValueDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.EnumValueDescriptorProto & google.protobuf.EnumValueDescriptorProto.$Shape} EnumValueDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumValueDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.EnumValueDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.number = reader.int32();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.EnumValueOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EnumValueDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.EnumValueDescriptorProto & google.protobuf.EnumValueDescriptorProto.$Shape} EnumValueDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumValueDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EnumValueDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EnumValueDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    if (!$util.isInteger(message.number))
                        return "number: integer expected";
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.EnumValueOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                return null;
            };

            /**
             * Creates an EnumValueDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.EnumValueDescriptorProto} EnumValueDescriptorProto
             */
            EnumValueDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.EnumValueDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.EnumValueDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.EnumValueDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.number != null)
                    message.number = object.number | 0;
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.EnumValueDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.EnumValueOptions.fromObject(object.options, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an EnumValueDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {google.protobuf.EnumValueDescriptorProto} message EnumValueDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EnumValueDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.name = "";
                    object.number = 0;
                    object.options = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                    object.number = message.number;
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.EnumValueOptions.toObject(message.options, options, _depth + 1);
                return object;
            };

            /**
             * Converts this EnumValueDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EnumValueDescriptorProto.prototype.toJSON = function() {
                return EnumValueDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EnumValueDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.EnumValueDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EnumValueDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.EnumValueDescriptorProto";
            };

            return EnumValueDescriptorProto;
        })();

        protobuf.ServiceDescriptorProto = (function() {

            /**
             * Properties of a ServiceDescriptorProto.
             * @typedef {Object} google.protobuf.ServiceDescriptorProto.$Properties
             * @property {string|null} [name] ServiceDescriptorProto name
             * @property {Array.<google.protobuf.MethodDescriptorProto.$Properties>|null} [method] ServiceDescriptorProto method
             * @property {google.protobuf.ServiceOptions.$Properties|null} [options] ServiceDescriptorProto options
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ServiceDescriptorProto.
             * @memberof google.protobuf
             * @interface IServiceDescriptorProto
             * @augments google.protobuf.ServiceDescriptorProto.$Properties
             * @deprecated Use google.protobuf.ServiceDescriptorProto.$Properties instead.
             */

            /**
             * Shape of a ServiceDescriptorProto.
             * @typedef {{
             *   name?: string|null;
             *   method?: Array.<google.protobuf.MethodDescriptorProto.$Shape>|null;
             *   options?: google.protobuf.ServiceOptions.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.ServiceDescriptorProto.$Shape
             */

            /**
             * Constructs a new ServiceDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a ServiceDescriptorProto.
             * @constructor
             * @param {google.protobuf.ServiceDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ServiceDescriptorProto = function (properties) {
                this.method = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ServiceDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.ServiceDescriptorProto
             * @instance
             */
            ServiceDescriptorProto.prototype.name = "";

            /**
             * ServiceDescriptorProto method.
             * @member {Array.<google.protobuf.MethodDescriptorProto.$Properties>} method
             * @memberof google.protobuf.ServiceDescriptorProto
             * @instance
             */
            ServiceDescriptorProto.prototype.method = $util.emptyArray;

            /**
             * ServiceDescriptorProto options.
             * @member {google.protobuf.ServiceOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.ServiceDescriptorProto
             * @instance
             */
            ServiceDescriptorProto.prototype.options = null;

            /**
             * Creates a new ServiceDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {google.protobuf.ServiceDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ServiceDescriptorProto} ServiceDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.ServiceDescriptorProto.$Shape): google.protobuf.ServiceDescriptorProto & google.protobuf.ServiceDescriptorProto.$Shape;
             *   (properties?: google.protobuf.ServiceDescriptorProto.$Properties): google.protobuf.ServiceDescriptorProto;
             * }}
             */
            ServiceDescriptorProto.create = function(properties) {
                return new ServiceDescriptorProto(properties);
            };

            /**
             * Encodes the specified ServiceDescriptorProto message. Does not implicitly {@link google.protobuf.ServiceDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {google.protobuf.ServiceDescriptorProto.$Properties} message ServiceDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServiceDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.method != null && message.method.length)
                    for (let i = 0; i < message.method.length; ++i)
                        $root.google.protobuf.MethodDescriptorProto.encode(message.method[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.ServiceOptions.encode(message.options, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ServiceDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.ServiceDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {google.protobuf.ServiceDescriptorProto.$Properties} message ServiceDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServiceDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ServiceDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ServiceDescriptorProto & google.protobuf.ServiceDescriptorProto.$Shape} ServiceDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServiceDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.ServiceDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.method && message.method.length))
                                message.method = [];
                            message.method.push($root.google.protobuf.MethodDescriptorProto.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.ServiceOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ServiceDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.ServiceDescriptorProto & google.protobuf.ServiceDescriptorProto.$Shape} ServiceDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServiceDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ServiceDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ServiceDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.method != null && $Object.hasOwnProperty.call(message, "method")) {
                    if (!$Array.isArray(message.method))
                        return "method: array expected";
                    for (let i = 0; i < message.method.length; ++i) {
                        let error = $root.google.protobuf.MethodDescriptorProto.verify(message.method[i], _depth + 1);
                        if (error)
                            return "method." + error;
                    }
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.ServiceOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                return null;
            };

            /**
             * Creates a ServiceDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ServiceDescriptorProto} ServiceDescriptorProto
             */
            ServiceDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ServiceDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ServiceDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ServiceDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.method) {
                    if (!$Array.isArray(object.method))
                        throw $TypeError(".google.protobuf.ServiceDescriptorProto.method: array expected");
                    message.method = $Array(object.method.length);
                    for (let i = 0; i < object.method.length; ++i) {
                        if (!$util.isObject(object.method[i]))
                            throw $TypeError(".google.protobuf.ServiceDescriptorProto.method: object expected");
                        message.method[i] = $root.google.protobuf.MethodDescriptorProto.fromObject(object.method[i], _depth + 1);
                    }
                }
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.ServiceDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.ServiceOptions.fromObject(object.options, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ServiceDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {google.protobuf.ServiceDescriptorProto} message ServiceDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ServiceDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.method = [];
                if (options.defaults) {
                    object.name = "";
                    object.options = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.method && message.method.length) {
                    object.method = $Array(message.method.length);
                    for (let j = 0; j < message.method.length; ++j)
                        object.method[j] = $root.google.protobuf.MethodDescriptorProto.toObject(message.method[j], options, _depth + 1);
                }
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.ServiceOptions.toObject(message.options, options, _depth + 1);
                return object;
            };

            /**
             * Converts this ServiceDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.ServiceDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ServiceDescriptorProto.prototype.toJSON = function() {
                return ServiceDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ServiceDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.ServiceDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ServiceDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ServiceDescriptorProto";
            };

            return ServiceDescriptorProto;
        })();

        protobuf.MethodDescriptorProto = (function() {

            /**
             * Properties of a MethodDescriptorProto.
             * @typedef {Object} google.protobuf.MethodDescriptorProto.$Properties
             * @property {string|null} [name] MethodDescriptorProto name
             * @property {string|null} [inputType] MethodDescriptorProto inputType
             * @property {string|null} [outputType] MethodDescriptorProto outputType
             * @property {google.protobuf.MethodOptions.$Properties|null} [options] MethodDescriptorProto options
             * @property {boolean|null} [clientStreaming] MethodDescriptorProto clientStreaming
             * @property {boolean|null} [serverStreaming] MethodDescriptorProto serverStreaming
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MethodDescriptorProto.
             * @memberof google.protobuf
             * @interface IMethodDescriptorProto
             * @augments google.protobuf.MethodDescriptorProto.$Properties
             * @deprecated Use google.protobuf.MethodDescriptorProto.$Properties instead.
             */

            /**
             * Shape of a MethodDescriptorProto.
             * @typedef {{
             *   name?: string|null;
             *   inputType?: string|null;
             *   outputType?: string|null;
             *   options?: google.protobuf.MethodOptions.$Shape|null;
             *   clientStreaming?: boolean|null;
             *   serverStreaming?: boolean|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.MethodDescriptorProto.$Shape
             */

            /**
             * Constructs a new MethodDescriptorProto.
             * @memberof google.protobuf
             * @classdesc Represents a MethodDescriptorProto.
             * @constructor
             * @param {google.protobuf.MethodDescriptorProto.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const MethodDescriptorProto = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MethodDescriptorProto name.
             * @member {string} name
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.name = "";

            /**
             * MethodDescriptorProto inputType.
             * @member {string} inputType
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.inputType = "";

            /**
             * MethodDescriptorProto outputType.
             * @member {string} outputType
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.outputType = "";

            /**
             * MethodDescriptorProto options.
             * @member {google.protobuf.MethodOptions.$Properties|null|undefined} options
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.options = null;

            /**
             * MethodDescriptorProto clientStreaming.
             * @member {boolean} clientStreaming
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.clientStreaming = false;

            /**
             * MethodDescriptorProto serverStreaming.
             * @member {boolean} serverStreaming
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             */
            MethodDescriptorProto.prototype.serverStreaming = false;

            /**
             * Creates a new MethodDescriptorProto instance using the specified properties.
             * @function create
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {google.protobuf.MethodDescriptorProto.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.MethodDescriptorProto} MethodDescriptorProto instance
             * @type {{
             *   (properties: google.protobuf.MethodDescriptorProto.$Shape): google.protobuf.MethodDescriptorProto & google.protobuf.MethodDescriptorProto.$Shape;
             *   (properties?: google.protobuf.MethodDescriptorProto.$Properties): google.protobuf.MethodDescriptorProto;
             * }}
             */
            MethodDescriptorProto.create = function(properties) {
                return new MethodDescriptorProto(properties);
            };

            /**
             * Encodes the specified MethodDescriptorProto message. Does not implicitly {@link google.protobuf.MethodDescriptorProto.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {google.protobuf.MethodDescriptorProto.$Properties} message MethodDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MethodDescriptorProto.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.inputType != null && $Object.hasOwnProperty.call(message, "inputType"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.inputType);
                if (message.outputType != null && $Object.hasOwnProperty.call(message, "outputType"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.outputType);
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    $root.google.protobuf.MethodOptions.encode(message.options, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.clientStreaming != null && $Object.hasOwnProperty.call(message, "clientStreaming"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.clientStreaming);
                if (message.serverStreaming != null && $Object.hasOwnProperty.call(message, "serverStreaming"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.serverStreaming);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MethodDescriptorProto message, length delimited. Does not implicitly {@link google.protobuf.MethodDescriptorProto.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {google.protobuf.MethodDescriptorProto.$Properties} message MethodDescriptorProto message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MethodDescriptorProto.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MethodDescriptorProto message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.MethodDescriptorProto & google.protobuf.MethodDescriptorProto.$Shape} MethodDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MethodDescriptorProto.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.MethodDescriptorProto();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.name = reader.string();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.inputType = reader.string();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.outputType = reader.string();
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.options = $root.google.protobuf.MethodOptions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.options);
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.clientStreaming = reader.bool();
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            message.serverStreaming = reader.bool();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MethodDescriptorProto message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.MethodDescriptorProto & google.protobuf.MethodDescriptorProto.$Shape} MethodDescriptorProto
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MethodDescriptorProto.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MethodDescriptorProto message.
             * @function verify
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MethodDescriptorProto.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.inputType != null && $Object.hasOwnProperty.call(message, "inputType"))
                    if (!$util.isString(message.inputType))
                        return "inputType: string expected";
                if (message.outputType != null && $Object.hasOwnProperty.call(message, "outputType"))
                    if (!$util.isString(message.outputType))
                        return "outputType: string expected";
                if (message.options != null && $Object.hasOwnProperty.call(message, "options")) {
                    let error = $root.google.protobuf.MethodOptions.verify(message.options, _depth + 1);
                    if (error)
                        return "options." + error;
                }
                if (message.clientStreaming != null && $Object.hasOwnProperty.call(message, "clientStreaming"))
                    if (typeof message.clientStreaming !== "boolean")
                        return "clientStreaming: boolean expected";
                if (message.serverStreaming != null && $Object.hasOwnProperty.call(message, "serverStreaming"))
                    if (typeof message.serverStreaming !== "boolean")
                        return "serverStreaming: boolean expected";
                return null;
            };

            /**
             * Creates a MethodDescriptorProto message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.MethodDescriptorProto} MethodDescriptorProto
             */
            MethodDescriptorProto.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.MethodDescriptorProto)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.MethodDescriptorProto: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.MethodDescriptorProto();
                if (object.name != null)
                    message.name = $String(object.name);
                if (object.inputType != null)
                    message.inputType = $String(object.inputType);
                if (object.outputType != null)
                    message.outputType = $String(object.outputType);
                if (object.options != null) {
                    if (!$util.isObject(object.options))
                        throw $TypeError(".google.protobuf.MethodDescriptorProto.options: object expected");
                    message.options = $root.google.protobuf.MethodOptions.fromObject(object.options, _depth + 1);
                }
                if (object.clientStreaming != null)
                    message.clientStreaming = $Boolean(object.clientStreaming);
                if (object.serverStreaming != null)
                    message.serverStreaming = $Boolean(object.serverStreaming);
                return message;
            };

            /**
             * Creates a plain object from a MethodDescriptorProto message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {google.protobuf.MethodDescriptorProto} message MethodDescriptorProto
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MethodDescriptorProto.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.name = "";
                    object.inputType = "";
                    object.outputType = "";
                    object.options = null;
                    object.clientStreaming = false;
                    object.serverStreaming = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.inputType != null && $Object.hasOwnProperty.call(message, "inputType"))
                    object.inputType = message.inputType;
                if (message.outputType != null && $Object.hasOwnProperty.call(message, "outputType"))
                    object.outputType = message.outputType;
                if (message.options != null && $Object.hasOwnProperty.call(message, "options"))
                    object.options = $root.google.protobuf.MethodOptions.toObject(message.options, options, _depth + 1);
                if (message.clientStreaming != null && $Object.hasOwnProperty.call(message, "clientStreaming"))
                    object.clientStreaming = message.clientStreaming;
                if (message.serverStreaming != null && $Object.hasOwnProperty.call(message, "serverStreaming"))
                    object.serverStreaming = message.serverStreaming;
                return object;
            };

            /**
             * Converts this MethodDescriptorProto to JSON.
             * @function toJSON
             * @memberof google.protobuf.MethodDescriptorProto
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MethodDescriptorProto.prototype.toJSON = function() {
                return MethodDescriptorProto.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MethodDescriptorProto
             * @function getTypeUrl
             * @memberof google.protobuf.MethodDescriptorProto
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MethodDescriptorProto.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.MethodDescriptorProto";
            };

            return MethodDescriptorProto;
        })();

        protobuf.FileOptions = (function() {

            /**
             * Properties of a FileOptions.
             * @typedef {Object} google.protobuf.FileOptions.$Properties
             * @property {string|null} [javaPackage] FileOptions javaPackage
             * @property {string|null} [javaOuterClassname] FileOptions javaOuterClassname
             * @property {boolean|null} [javaMultipleFiles] FileOptions javaMultipleFiles
             * @property {boolean|null} [javaGenerateEqualsAndHash] FileOptions javaGenerateEqualsAndHash
             * @property {boolean|null} [javaStringCheckUtf8] FileOptions javaStringCheckUtf8
             * @property {google.protobuf.FileOptions.OptimizeMode|null} [optimizeFor] FileOptions optimizeFor
             * @property {string|null} [goPackage] FileOptions goPackage
             * @property {boolean|null} [ccGenericServices] FileOptions ccGenericServices
             * @property {boolean|null} [javaGenericServices] FileOptions javaGenericServices
             * @property {boolean|null} [pyGenericServices] FileOptions pyGenericServices
             * @property {boolean|null} [phpGenericServices] FileOptions phpGenericServices
             * @property {boolean|null} [deprecated] FileOptions deprecated
             * @property {boolean|null} [ccEnableArenas] FileOptions ccEnableArenas
             * @property {string|null} [objcClassPrefix] FileOptions objcClassPrefix
             * @property {string|null} [csharpNamespace] FileOptions csharpNamespace
             * @property {string|null} [swiftPrefix] FileOptions swiftPrefix
             * @property {string|null} [phpClassPrefix] FileOptions phpClassPrefix
             * @property {string|null} [phpNamespace] FileOptions phpNamespace
             * @property {string|null} [phpMetadataNamespace] FileOptions phpMetadataNamespace
             * @property {string|null} [rubyPackage] FileOptions rubyPackage
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] FileOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileOptions.
             * @memberof google.protobuf
             * @interface IFileOptions
             * @augments google.protobuf.FileOptions.$Properties
             * @deprecated Use google.protobuf.FileOptions.$Properties instead.
             */

            /**
             * Shape of a FileOptions.
             * @typedef {google.protobuf.FileOptions.$Properties} google.protobuf.FileOptions.$Shape
             */

            /**
             * Constructs a new FileOptions.
             * @memberof google.protobuf
             * @classdesc Represents a FileOptions.
             * @constructor
             * @param {google.protobuf.FileOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FileOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FileOptions javaPackage.
             * @member {string} javaPackage
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaPackage = "";

            /**
             * FileOptions javaOuterClassname.
             * @member {string} javaOuterClassname
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaOuterClassname = "";

            /**
             * FileOptions javaMultipleFiles.
             * @member {boolean} javaMultipleFiles
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaMultipleFiles = false;

            /**
             * FileOptions javaGenerateEqualsAndHash.
             * @member {boolean} javaGenerateEqualsAndHash
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaGenerateEqualsAndHash = false;

            /**
             * FileOptions javaStringCheckUtf8.
             * @member {boolean} javaStringCheckUtf8
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaStringCheckUtf8 = false;

            /**
             * FileOptions optimizeFor.
             * @member {google.protobuf.FileOptions.OptimizeMode} optimizeFor
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.optimizeFor = 1;

            /**
             * FileOptions goPackage.
             * @member {string} goPackage
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.goPackage = "";

            /**
             * FileOptions ccGenericServices.
             * @member {boolean} ccGenericServices
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.ccGenericServices = false;

            /**
             * FileOptions javaGenericServices.
             * @member {boolean} javaGenericServices
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.javaGenericServices = false;

            /**
             * FileOptions pyGenericServices.
             * @member {boolean} pyGenericServices
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.pyGenericServices = false;

            /**
             * FileOptions phpGenericServices.
             * @member {boolean} phpGenericServices
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.phpGenericServices = false;

            /**
             * FileOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.deprecated = false;

            /**
             * FileOptions ccEnableArenas.
             * @member {boolean} ccEnableArenas
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.ccEnableArenas = true;

            /**
             * FileOptions objcClassPrefix.
             * @member {string} objcClassPrefix
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.objcClassPrefix = "";

            /**
             * FileOptions csharpNamespace.
             * @member {string} csharpNamespace
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.csharpNamespace = "";

            /**
             * FileOptions swiftPrefix.
             * @member {string} swiftPrefix
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.swiftPrefix = "";

            /**
             * FileOptions phpClassPrefix.
             * @member {string} phpClassPrefix
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.phpClassPrefix = "";

            /**
             * FileOptions phpNamespace.
             * @member {string} phpNamespace
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.phpNamespace = "";

            /**
             * FileOptions phpMetadataNamespace.
             * @member {string} phpMetadataNamespace
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.phpMetadataNamespace = "";

            /**
             * FileOptions rubyPackage.
             * @member {string} rubyPackage
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.rubyPackage = "";

            /**
             * FileOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.FileOptions
             * @instance
             */
            FileOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new FileOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {google.protobuf.FileOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FileOptions} FileOptions instance
             * @type {{
             *   (properties: google.protobuf.FileOptions.$Shape): google.protobuf.FileOptions & google.protobuf.FileOptions.$Shape;
             *   (properties?: google.protobuf.FileOptions.$Properties): google.protobuf.FileOptions;
             * }}
             */
            FileOptions.create = function(properties) {
                return new FileOptions(properties);
            };

            /**
             * Encodes the specified FileOptions message. Does not implicitly {@link google.protobuf.FileOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {google.protobuf.FileOptions.$Properties} message FileOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.javaPackage != null && $Object.hasOwnProperty.call(message, "javaPackage"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.javaPackage);
                if (message.javaOuterClassname != null && $Object.hasOwnProperty.call(message, "javaOuterClassname"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.javaOuterClassname);
                if (message.optimizeFor != null && $Object.hasOwnProperty.call(message, "optimizeFor"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.optimizeFor);
                if (message.javaMultipleFiles != null && $Object.hasOwnProperty.call(message, "javaMultipleFiles"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.javaMultipleFiles);
                if (message.goPackage != null && $Object.hasOwnProperty.call(message, "goPackage"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.goPackage);
                if (message.ccGenericServices != null && $Object.hasOwnProperty.call(message, "ccGenericServices"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.ccGenericServices);
                if (message.javaGenericServices != null && $Object.hasOwnProperty.call(message, "javaGenericServices"))
                    writer.uint32(/* id 17, wireType 0 =*/136).bool(message.javaGenericServices);
                if (message.pyGenericServices != null && $Object.hasOwnProperty.call(message, "pyGenericServices"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.pyGenericServices);
                if (message.javaGenerateEqualsAndHash != null && $Object.hasOwnProperty.call(message, "javaGenerateEqualsAndHash"))
                    writer.uint32(/* id 20, wireType 0 =*/160).bool(message.javaGenerateEqualsAndHash);
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 23, wireType 0 =*/184).bool(message.deprecated);
                if (message.javaStringCheckUtf8 != null && $Object.hasOwnProperty.call(message, "javaStringCheckUtf8"))
                    writer.uint32(/* id 27, wireType 0 =*/216).bool(message.javaStringCheckUtf8);
                if (message.ccEnableArenas != null && $Object.hasOwnProperty.call(message, "ccEnableArenas"))
                    writer.uint32(/* id 31, wireType 0 =*/248).bool(message.ccEnableArenas);
                if (message.objcClassPrefix != null && $Object.hasOwnProperty.call(message, "objcClassPrefix"))
                    writer.uint32(/* id 36, wireType 2 =*/290).string(message.objcClassPrefix);
                if (message.csharpNamespace != null && $Object.hasOwnProperty.call(message, "csharpNamespace"))
                    writer.uint32(/* id 37, wireType 2 =*/298).string(message.csharpNamespace);
                if (message.swiftPrefix != null && $Object.hasOwnProperty.call(message, "swiftPrefix"))
                    writer.uint32(/* id 39, wireType 2 =*/314).string(message.swiftPrefix);
                if (message.phpClassPrefix != null && $Object.hasOwnProperty.call(message, "phpClassPrefix"))
                    writer.uint32(/* id 40, wireType 2 =*/322).string(message.phpClassPrefix);
                if (message.phpNamespace != null && $Object.hasOwnProperty.call(message, "phpNamespace"))
                    writer.uint32(/* id 41, wireType 2 =*/330).string(message.phpNamespace);
                if (message.phpGenericServices != null && $Object.hasOwnProperty.call(message, "phpGenericServices"))
                    writer.uint32(/* id 42, wireType 0 =*/336).bool(message.phpGenericServices);
                if (message.phpMetadataNamespace != null && $Object.hasOwnProperty.call(message, "phpMetadataNamespace"))
                    writer.uint32(/* id 44, wireType 2 =*/354).string(message.phpMetadataNamespace);
                if (message.rubyPackage != null && $Object.hasOwnProperty.call(message, "rubyPackage"))
                    writer.uint32(/* id 45, wireType 2 =*/362).string(message.rubyPackage);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileOptions message, length delimited. Does not implicitly {@link google.protobuf.FileOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {google.protobuf.FileOptions.$Properties} message FileOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FileOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FileOptions & google.protobuf.FileOptions.$Shape} FileOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.FileOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.javaPackage = reader.string();
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.javaOuterClassname = reader.string();
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            message.javaMultipleFiles = reader.bool();
                            continue;
                        }
                    case 20: {
                            if (wireType !== 0)
                                break;
                            message.javaGenerateEqualsAndHash = reader.bool();
                            continue;
                        }
                    case 27: {
                            if (wireType !== 0)
                                break;
                            message.javaStringCheckUtf8 = reader.bool();
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            message.optimizeFor = reader.int32();
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.goPackage = reader.string();
                            continue;
                        }
                    case 16: {
                            if (wireType !== 0)
                                break;
                            message.ccGenericServices = reader.bool();
                            continue;
                        }
                    case 17: {
                            if (wireType !== 0)
                                break;
                            message.javaGenericServices = reader.bool();
                            continue;
                        }
                    case 18: {
                            if (wireType !== 0)
                                break;
                            message.pyGenericServices = reader.bool();
                            continue;
                        }
                    case 42: {
                            if (wireType !== 0)
                                break;
                            message.phpGenericServices = reader.bool();
                            continue;
                        }
                    case 23: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 31: {
                            if (wireType !== 0)
                                break;
                            message.ccEnableArenas = reader.bool();
                            continue;
                        }
                    case 36: {
                            if (wireType !== 2)
                                break;
                            message.objcClassPrefix = reader.string();
                            continue;
                        }
                    case 37: {
                            if (wireType !== 2)
                                break;
                            message.csharpNamespace = reader.string();
                            continue;
                        }
                    case 39: {
                            if (wireType !== 2)
                                break;
                            message.swiftPrefix = reader.string();
                            continue;
                        }
                    case 40: {
                            if (wireType !== 2)
                                break;
                            message.phpClassPrefix = reader.string();
                            continue;
                        }
                    case 41: {
                            if (wireType !== 2)
                                break;
                            message.phpNamespace = reader.string();
                            continue;
                        }
                    case 44: {
                            if (wireType !== 2)
                                break;
                            message.phpMetadataNamespace = reader.string();
                            continue;
                        }
                    case 45: {
                            if (wireType !== 2)
                                break;
                            message.rubyPackage = reader.string();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FileOptions & google.protobuf.FileOptions.$Shape} FileOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileOptions message.
             * @function verify
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.javaPackage != null && $Object.hasOwnProperty.call(message, "javaPackage"))
                    if (!$util.isString(message.javaPackage))
                        return "javaPackage: string expected";
                if (message.javaOuterClassname != null && $Object.hasOwnProperty.call(message, "javaOuterClassname"))
                    if (!$util.isString(message.javaOuterClassname))
                        return "javaOuterClassname: string expected";
                if (message.javaMultipleFiles != null && $Object.hasOwnProperty.call(message, "javaMultipleFiles"))
                    if (typeof message.javaMultipleFiles !== "boolean")
                        return "javaMultipleFiles: boolean expected";
                if (message.javaGenerateEqualsAndHash != null && $Object.hasOwnProperty.call(message, "javaGenerateEqualsAndHash"))
                    if (typeof message.javaGenerateEqualsAndHash !== "boolean")
                        return "javaGenerateEqualsAndHash: boolean expected";
                if (message.javaStringCheckUtf8 != null && $Object.hasOwnProperty.call(message, "javaStringCheckUtf8"))
                    if (typeof message.javaStringCheckUtf8 !== "boolean")
                        return "javaStringCheckUtf8: boolean expected";
                if (message.optimizeFor != null && $Object.hasOwnProperty.call(message, "optimizeFor"))
                    switch (message.optimizeFor) {
                    default:
                        return "optimizeFor: enum value expected";
                    case 1:
                    case 2:
                    case 3:
                        break;
                    }
                if (message.goPackage != null && $Object.hasOwnProperty.call(message, "goPackage"))
                    if (!$util.isString(message.goPackage))
                        return "goPackage: string expected";
                if (message.ccGenericServices != null && $Object.hasOwnProperty.call(message, "ccGenericServices"))
                    if (typeof message.ccGenericServices !== "boolean")
                        return "ccGenericServices: boolean expected";
                if (message.javaGenericServices != null && $Object.hasOwnProperty.call(message, "javaGenericServices"))
                    if (typeof message.javaGenericServices !== "boolean")
                        return "javaGenericServices: boolean expected";
                if (message.pyGenericServices != null && $Object.hasOwnProperty.call(message, "pyGenericServices"))
                    if (typeof message.pyGenericServices !== "boolean")
                        return "pyGenericServices: boolean expected";
                if (message.phpGenericServices != null && $Object.hasOwnProperty.call(message, "phpGenericServices"))
                    if (typeof message.phpGenericServices !== "boolean")
                        return "phpGenericServices: boolean expected";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.ccEnableArenas != null && $Object.hasOwnProperty.call(message, "ccEnableArenas"))
                    if (typeof message.ccEnableArenas !== "boolean")
                        return "ccEnableArenas: boolean expected";
                if (message.objcClassPrefix != null && $Object.hasOwnProperty.call(message, "objcClassPrefix"))
                    if (!$util.isString(message.objcClassPrefix))
                        return "objcClassPrefix: string expected";
                if (message.csharpNamespace != null && $Object.hasOwnProperty.call(message, "csharpNamespace"))
                    if (!$util.isString(message.csharpNamespace))
                        return "csharpNamespace: string expected";
                if (message.swiftPrefix != null && $Object.hasOwnProperty.call(message, "swiftPrefix"))
                    if (!$util.isString(message.swiftPrefix))
                        return "swiftPrefix: string expected";
                if (message.phpClassPrefix != null && $Object.hasOwnProperty.call(message, "phpClassPrefix"))
                    if (!$util.isString(message.phpClassPrefix))
                        return "phpClassPrefix: string expected";
                if (message.phpNamespace != null && $Object.hasOwnProperty.call(message, "phpNamespace"))
                    if (!$util.isString(message.phpNamespace))
                        return "phpNamespace: string expected";
                if (message.phpMetadataNamespace != null && $Object.hasOwnProperty.call(message, "phpMetadataNamespace"))
                    if (!$util.isString(message.phpMetadataNamespace))
                        return "phpMetadataNamespace: string expected";
                if (message.rubyPackage != null && $Object.hasOwnProperty.call(message, "rubyPackage"))
                    if (!$util.isString(message.rubyPackage))
                        return "rubyPackage: string expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FileOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FileOptions} FileOptions
             */
            FileOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FileOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FileOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FileOptions();
                if (object.javaPackage != null)
                    message.javaPackage = $String(object.javaPackage);
                if (object.javaOuterClassname != null)
                    message.javaOuterClassname = $String(object.javaOuterClassname);
                if (object.javaMultipleFiles != null)
                    message.javaMultipleFiles = $Boolean(object.javaMultipleFiles);
                if (object.javaGenerateEqualsAndHash != null)
                    message.javaGenerateEqualsAndHash = $Boolean(object.javaGenerateEqualsAndHash);
                if (object.javaStringCheckUtf8 != null)
                    message.javaStringCheckUtf8 = $Boolean(object.javaStringCheckUtf8);
                switch (object.optimizeFor) {
                default:
                    if (typeof object.optimizeFor === "number") {
                        message.optimizeFor = object.optimizeFor;
                        break;
                    }
                    break;
                case "SPEED":
                case 1:
                    message.optimizeFor = 1;
                    break;
                case "CODE_SIZE":
                case 2:
                    message.optimizeFor = 2;
                    break;
                case "LITE_RUNTIME":
                case 3:
                    message.optimizeFor = 3;
                    break;
                }
                if (object.goPackage != null)
                    message.goPackage = $String(object.goPackage);
                if (object.ccGenericServices != null)
                    message.ccGenericServices = $Boolean(object.ccGenericServices);
                if (object.javaGenericServices != null)
                    message.javaGenericServices = $Boolean(object.javaGenericServices);
                if (object.pyGenericServices != null)
                    message.pyGenericServices = $Boolean(object.pyGenericServices);
                if (object.phpGenericServices != null)
                    message.phpGenericServices = $Boolean(object.phpGenericServices);
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.ccEnableArenas != null)
                    message.ccEnableArenas = $Boolean(object.ccEnableArenas);
                if (object.objcClassPrefix != null)
                    message.objcClassPrefix = $String(object.objcClassPrefix);
                if (object.csharpNamespace != null)
                    message.csharpNamespace = $String(object.csharpNamespace);
                if (object.swiftPrefix != null)
                    message.swiftPrefix = $String(object.swiftPrefix);
                if (object.phpClassPrefix != null)
                    message.phpClassPrefix = $String(object.phpClassPrefix);
                if (object.phpNamespace != null)
                    message.phpNamespace = $String(object.phpNamespace);
                if (object.phpMetadataNamespace != null)
                    message.phpMetadataNamespace = $String(object.phpMetadataNamespace);
                if (object.rubyPackage != null)
                    message.rubyPackage = $String(object.rubyPackage);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.FileOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.FileOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FileOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {google.protobuf.FileOptions} message FileOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults) {
                    object.javaPackage = "";
                    object.javaOuterClassname = "";
                    object.optimizeFor = options.enums === $String ? "SPEED" : 1;
                    object.javaMultipleFiles = false;
                    object.goPackage = "";
                    object.ccGenericServices = false;
                    object.javaGenericServices = false;
                    object.pyGenericServices = false;
                    object.javaGenerateEqualsAndHash = false;
                    object.deprecated = false;
                    object.javaStringCheckUtf8 = false;
                    object.ccEnableArenas = true;
                    object.objcClassPrefix = "";
                    object.csharpNamespace = "";
                    object.swiftPrefix = "";
                    object.phpClassPrefix = "";
                    object.phpNamespace = "";
                    object.phpGenericServices = false;
                    object.phpMetadataNamespace = "";
                    object.rubyPackage = "";
                }
                if (message.javaPackage != null && $Object.hasOwnProperty.call(message, "javaPackage"))
                    object.javaPackage = message.javaPackage;
                if (message.javaOuterClassname != null && $Object.hasOwnProperty.call(message, "javaOuterClassname"))
                    object.javaOuterClassname = message.javaOuterClassname;
                if (message.optimizeFor != null && $Object.hasOwnProperty.call(message, "optimizeFor"))
                    object.optimizeFor = options.enums === $String ? $root.google.protobuf.FileOptions.OptimizeMode[message.optimizeFor] === $undefined ? message.optimizeFor : $root.google.protobuf.FileOptions.OptimizeMode[message.optimizeFor] : message.optimizeFor;
                if (message.javaMultipleFiles != null && $Object.hasOwnProperty.call(message, "javaMultipleFiles"))
                    object.javaMultipleFiles = message.javaMultipleFiles;
                if (message.goPackage != null && $Object.hasOwnProperty.call(message, "goPackage"))
                    object.goPackage = message.goPackage;
                if (message.ccGenericServices != null && $Object.hasOwnProperty.call(message, "ccGenericServices"))
                    object.ccGenericServices = message.ccGenericServices;
                if (message.javaGenericServices != null && $Object.hasOwnProperty.call(message, "javaGenericServices"))
                    object.javaGenericServices = message.javaGenericServices;
                if (message.pyGenericServices != null && $Object.hasOwnProperty.call(message, "pyGenericServices"))
                    object.pyGenericServices = message.pyGenericServices;
                if (message.javaGenerateEqualsAndHash != null && $Object.hasOwnProperty.call(message, "javaGenerateEqualsAndHash"))
                    object.javaGenerateEqualsAndHash = message.javaGenerateEqualsAndHash;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.javaStringCheckUtf8 != null && $Object.hasOwnProperty.call(message, "javaStringCheckUtf8"))
                    object.javaStringCheckUtf8 = message.javaStringCheckUtf8;
                if (message.ccEnableArenas != null && $Object.hasOwnProperty.call(message, "ccEnableArenas"))
                    object.ccEnableArenas = message.ccEnableArenas;
                if (message.objcClassPrefix != null && $Object.hasOwnProperty.call(message, "objcClassPrefix"))
                    object.objcClassPrefix = message.objcClassPrefix;
                if (message.csharpNamespace != null && $Object.hasOwnProperty.call(message, "csharpNamespace"))
                    object.csharpNamespace = message.csharpNamespace;
                if (message.swiftPrefix != null && $Object.hasOwnProperty.call(message, "swiftPrefix"))
                    object.swiftPrefix = message.swiftPrefix;
                if (message.phpClassPrefix != null && $Object.hasOwnProperty.call(message, "phpClassPrefix"))
                    object.phpClassPrefix = message.phpClassPrefix;
                if (message.phpNamespace != null && $Object.hasOwnProperty.call(message, "phpNamespace"))
                    object.phpNamespace = message.phpNamespace;
                if (message.phpGenericServices != null && $Object.hasOwnProperty.call(message, "phpGenericServices"))
                    object.phpGenericServices = message.phpGenericServices;
                if (message.phpMetadataNamespace != null && $Object.hasOwnProperty.call(message, "phpMetadataNamespace"))
                    object.phpMetadataNamespace = message.phpMetadataNamespace;
                if (message.rubyPackage != null && $Object.hasOwnProperty.call(message, "rubyPackage"))
                    object.rubyPackage = message.rubyPackage;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this FileOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.FileOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileOptions.prototype.toJSON = function() {
                return FileOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileOptions
             * @function getTypeUrl
             * @memberof google.protobuf.FileOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FileOptions";
            };

            /**
             * OptimizeMode enum.
             * @name google.protobuf.FileOptions.OptimizeMode
             * @enum {number}
             * @property {number} SPEED=1 SPEED value
             * @property {number} CODE_SIZE=2 CODE_SIZE value
             * @property {number} LITE_RUNTIME=3 LITE_RUNTIME value
             */
            FileOptions.OptimizeMode = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[1] = "SPEED"] = 1;
                values[valuesById[2] = "CODE_SIZE"] = 2;
                values[valuesById[3] = "LITE_RUNTIME"] = 3;
                return values;
            })();

            return FileOptions;
        })();

        protobuf.MessageOptions = (function() {

            /**
             * Properties of a MessageOptions.
             * @typedef {Object} google.protobuf.MessageOptions.$Properties
             * @property {boolean|null} [messageSetWireFormat] MessageOptions messageSetWireFormat
             * @property {boolean|null} [noStandardDescriptorAccessor] MessageOptions noStandardDescriptorAccessor
             * @property {boolean|null} [deprecated] MessageOptions deprecated
             * @property {boolean|null} [mapEntry] MessageOptions mapEntry
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] MessageOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MessageOptions.
             * @memberof google.protobuf
             * @interface IMessageOptions
             * @augments google.protobuf.MessageOptions.$Properties
             * @deprecated Use google.protobuf.MessageOptions.$Properties instead.
             */

            /**
             * Shape of a MessageOptions.
             * @typedef {google.protobuf.MessageOptions.$Properties} google.protobuf.MessageOptions.$Shape
             */

            /**
             * Constructs a new MessageOptions.
             * @memberof google.protobuf
             * @classdesc Represents a MessageOptions.
             * @constructor
             * @param {google.protobuf.MessageOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const MessageOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MessageOptions messageSetWireFormat.
             * @member {boolean} messageSetWireFormat
             * @memberof google.protobuf.MessageOptions
             * @instance
             */
            MessageOptions.prototype.messageSetWireFormat = false;

            /**
             * MessageOptions noStandardDescriptorAccessor.
             * @member {boolean} noStandardDescriptorAccessor
             * @memberof google.protobuf.MessageOptions
             * @instance
             */
            MessageOptions.prototype.noStandardDescriptorAccessor = false;

            /**
             * MessageOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.MessageOptions
             * @instance
             */
            MessageOptions.prototype.deprecated = false;

            /**
             * MessageOptions mapEntry.
             * @member {boolean} mapEntry
             * @memberof google.protobuf.MessageOptions
             * @instance
             */
            MessageOptions.prototype.mapEntry = false;

            /**
             * MessageOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.MessageOptions
             * @instance
             */
            MessageOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new MessageOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {google.protobuf.MessageOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.MessageOptions} MessageOptions instance
             * @type {{
             *   (properties: google.protobuf.MessageOptions.$Shape): google.protobuf.MessageOptions & google.protobuf.MessageOptions.$Shape;
             *   (properties?: google.protobuf.MessageOptions.$Properties): google.protobuf.MessageOptions;
             * }}
             */
            MessageOptions.create = function(properties) {
                return new MessageOptions(properties);
            };

            /**
             * Encodes the specified MessageOptions message. Does not implicitly {@link google.protobuf.MessageOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {google.protobuf.MessageOptions.$Properties} message MessageOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.messageSetWireFormat != null && $Object.hasOwnProperty.call(message, "messageSetWireFormat"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.messageSetWireFormat);
                if (message.noStandardDescriptorAccessor != null && $Object.hasOwnProperty.call(message, "noStandardDescriptorAccessor"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.noStandardDescriptorAccessor);
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.deprecated);
                if (message.mapEntry != null && $Object.hasOwnProperty.call(message, "mapEntry"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.mapEntry);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MessageOptions message, length delimited. Does not implicitly {@link google.protobuf.MessageOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {google.protobuf.MessageOptions.$Properties} message MessageOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MessageOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.MessageOptions & google.protobuf.MessageOptions.$Shape} MessageOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.MessageOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.messageSetWireFormat = reader.bool();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.noStandardDescriptorAccessor = reader.bool();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            message.mapEntry = reader.bool();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MessageOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.MessageOptions & google.protobuf.MessageOptions.$Shape} MessageOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageOptions message.
             * @function verify
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.messageSetWireFormat != null && $Object.hasOwnProperty.call(message, "messageSetWireFormat"))
                    if (typeof message.messageSetWireFormat !== "boolean")
                        return "messageSetWireFormat: boolean expected";
                if (message.noStandardDescriptorAccessor != null && $Object.hasOwnProperty.call(message, "noStandardDescriptorAccessor"))
                    if (typeof message.noStandardDescriptorAccessor !== "boolean")
                        return "noStandardDescriptorAccessor: boolean expected";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.mapEntry != null && $Object.hasOwnProperty.call(message, "mapEntry"))
                    if (typeof message.mapEntry !== "boolean")
                        return "mapEntry: boolean expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MessageOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.MessageOptions} MessageOptions
             */
            MessageOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.MessageOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.MessageOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.MessageOptions();
                if (object.messageSetWireFormat != null)
                    message.messageSetWireFormat = $Boolean(object.messageSetWireFormat);
                if (object.noStandardDescriptorAccessor != null)
                    message.noStandardDescriptorAccessor = $Boolean(object.noStandardDescriptorAccessor);
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.mapEntry != null)
                    message.mapEntry = $Boolean(object.mapEntry);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.MessageOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.MessageOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a MessageOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {google.protobuf.MessageOptions} message MessageOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults) {
                    object.messageSetWireFormat = false;
                    object.noStandardDescriptorAccessor = false;
                    object.deprecated = false;
                    object.mapEntry = false;
                }
                if (message.messageSetWireFormat != null && $Object.hasOwnProperty.call(message, "messageSetWireFormat"))
                    object.messageSetWireFormat = message.messageSetWireFormat;
                if (message.noStandardDescriptorAccessor != null && $Object.hasOwnProperty.call(message, "noStandardDescriptorAccessor"))
                    object.noStandardDescriptorAccessor = message.noStandardDescriptorAccessor;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.mapEntry != null && $Object.hasOwnProperty.call(message, "mapEntry"))
                    object.mapEntry = message.mapEntry;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this MessageOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.MessageOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageOptions.prototype.toJSON = function() {
                return MessageOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MessageOptions
             * @function getTypeUrl
             * @memberof google.protobuf.MessageOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MessageOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.MessageOptions";
            };

            return MessageOptions;
        })();

        protobuf.FieldOptions = (function() {

            /**
             * Properties of a FieldOptions.
             * @typedef {Object} google.protobuf.FieldOptions.$Properties
             * @property {google.protobuf.FieldOptions.CType|null} [ctype] FieldOptions ctype
             * @property {boolean|null} [packed] FieldOptions packed
             * @property {google.protobuf.FieldOptions.JSType|null} [jstype] FieldOptions jstype
             * @property {boolean|null} [lazy] FieldOptions lazy
             * @property {boolean|null} [unverifiedLazy] FieldOptions unverifiedLazy
             * @property {boolean|null} [deprecated] FieldOptions deprecated
             * @property {boolean|null} [weak] FieldOptions weak
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] FieldOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FieldOptions.
             * @memberof google.protobuf
             * @interface IFieldOptions
             * @augments google.protobuf.FieldOptions.$Properties
             * @deprecated Use google.protobuf.FieldOptions.$Properties instead.
             */

            /**
             * Shape of a FieldOptions.
             * @typedef {google.protobuf.FieldOptions.$Properties} google.protobuf.FieldOptions.$Shape
             */

            /**
             * Constructs a new FieldOptions.
             * @memberof google.protobuf
             * @classdesc Represents a FieldOptions.
             * @constructor
             * @param {google.protobuf.FieldOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FieldOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FieldOptions ctype.
             * @member {google.protobuf.FieldOptions.CType} ctype
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.ctype = 0;

            /**
             * FieldOptions packed.
             * @member {boolean} packed
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.packed = false;

            /**
             * FieldOptions jstype.
             * @member {google.protobuf.FieldOptions.JSType} jstype
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.jstype = 0;

            /**
             * FieldOptions lazy.
             * @member {boolean} lazy
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.lazy = false;

            /**
             * FieldOptions unverifiedLazy.
             * @member {boolean} unverifiedLazy
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.unverifiedLazy = false;

            /**
             * FieldOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.deprecated = false;

            /**
             * FieldOptions weak.
             * @member {boolean} weak
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.weak = false;

            /**
             * FieldOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.FieldOptions
             * @instance
             */
            FieldOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new FieldOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {google.protobuf.FieldOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FieldOptions} FieldOptions instance
             * @type {{
             *   (properties: google.protobuf.FieldOptions.$Shape): google.protobuf.FieldOptions & google.protobuf.FieldOptions.$Shape;
             *   (properties?: google.protobuf.FieldOptions.$Properties): google.protobuf.FieldOptions;
             * }}
             */
            FieldOptions.create = function(properties) {
                return new FieldOptions(properties);
            };

            /**
             * Encodes the specified FieldOptions message. Does not implicitly {@link google.protobuf.FieldOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {google.protobuf.FieldOptions.$Properties} message FieldOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FieldOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.ctype != null && $Object.hasOwnProperty.call(message, "ctype"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ctype);
                if (message.packed != null && $Object.hasOwnProperty.call(message, "packed"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.packed);
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.deprecated);
                if (message.lazy != null && $Object.hasOwnProperty.call(message, "lazy"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.lazy);
                if (message.jstype != null && $Object.hasOwnProperty.call(message, "jstype"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int32(message.jstype);
                if (message.weak != null && $Object.hasOwnProperty.call(message, "weak"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.weak);
                if (message.unverifiedLazy != null && $Object.hasOwnProperty.call(message, "unverifiedLazy"))
                    writer.uint32(/* id 15, wireType 0 =*/120).bool(message.unverifiedLazy);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FieldOptions message, length delimited. Does not implicitly {@link google.protobuf.FieldOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {google.protobuf.FieldOptions.$Properties} message FieldOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FieldOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FieldOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FieldOptions & google.protobuf.FieldOptions.$Shape} FieldOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FieldOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.FieldOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.ctype = reader.int32();
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.packed = reader.bool();
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            message.jstype = reader.int32();
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.lazy = reader.bool();
                            continue;
                        }
                    case 15: {
                            if (wireType !== 0)
                                break;
                            message.unverifiedLazy = reader.bool();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            message.weak = reader.bool();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FieldOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FieldOptions & google.protobuf.FieldOptions.$Shape} FieldOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FieldOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FieldOptions message.
             * @function verify
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FieldOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.ctype != null && $Object.hasOwnProperty.call(message, "ctype"))
                    switch (message.ctype) {
                    default:
                        return "ctype: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                        break;
                    }
                if (message.packed != null && $Object.hasOwnProperty.call(message, "packed"))
                    if (typeof message.packed !== "boolean")
                        return "packed: boolean expected";
                if (message.jstype != null && $Object.hasOwnProperty.call(message, "jstype"))
                    switch (message.jstype) {
                    default:
                        return "jstype: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                        break;
                    }
                if (message.lazy != null && $Object.hasOwnProperty.call(message, "lazy"))
                    if (typeof message.lazy !== "boolean")
                        return "lazy: boolean expected";
                if (message.unverifiedLazy != null && $Object.hasOwnProperty.call(message, "unverifiedLazy"))
                    if (typeof message.unverifiedLazy !== "boolean")
                        return "unverifiedLazy: boolean expected";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.weak != null && $Object.hasOwnProperty.call(message, "weak"))
                    if (typeof message.weak !== "boolean")
                        return "weak: boolean expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FieldOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FieldOptions} FieldOptions
             */
            FieldOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FieldOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FieldOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FieldOptions();
                switch (object.ctype) {
                default:
                    if (typeof object.ctype === "number") {
                        message.ctype = object.ctype;
                        break;
                    }
                    break;
                case "STRING":
                case 0:
                    message.ctype = 0;
                    break;
                case "CORD":
                case 1:
                    message.ctype = 1;
                    break;
                case "STRING_PIECE":
                case 2:
                    message.ctype = 2;
                    break;
                }
                if (object.packed != null)
                    message.packed = $Boolean(object.packed);
                switch (object.jstype) {
                default:
                    if (typeof object.jstype === "number") {
                        message.jstype = object.jstype;
                        break;
                    }
                    break;
                case "JS_NORMAL":
                case 0:
                    message.jstype = 0;
                    break;
                case "JS_STRING":
                case 1:
                    message.jstype = 1;
                    break;
                case "JS_NUMBER":
                case 2:
                    message.jstype = 2;
                    break;
                }
                if (object.lazy != null)
                    message.lazy = $Boolean(object.lazy);
                if (object.unverifiedLazy != null)
                    message.unverifiedLazy = $Boolean(object.unverifiedLazy);
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.weak != null)
                    message.weak = $Boolean(object.weak);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.FieldOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.FieldOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FieldOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {google.protobuf.FieldOptions} message FieldOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FieldOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults) {
                    object.ctype = options.enums === $String ? "STRING" : 0;
                    object.packed = false;
                    object.deprecated = false;
                    object.lazy = false;
                    object.jstype = options.enums === $String ? "JS_NORMAL" : 0;
                    object.weak = false;
                    object.unverifiedLazy = false;
                }
                if (message.ctype != null && $Object.hasOwnProperty.call(message, "ctype"))
                    object.ctype = options.enums === $String ? $root.google.protobuf.FieldOptions.CType[message.ctype] === $undefined ? message.ctype : $root.google.protobuf.FieldOptions.CType[message.ctype] : message.ctype;
                if (message.packed != null && $Object.hasOwnProperty.call(message, "packed"))
                    object.packed = message.packed;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.lazy != null && $Object.hasOwnProperty.call(message, "lazy"))
                    object.lazy = message.lazy;
                if (message.jstype != null && $Object.hasOwnProperty.call(message, "jstype"))
                    object.jstype = options.enums === $String ? $root.google.protobuf.FieldOptions.JSType[message.jstype] === $undefined ? message.jstype : $root.google.protobuf.FieldOptions.JSType[message.jstype] : message.jstype;
                if (message.weak != null && $Object.hasOwnProperty.call(message, "weak"))
                    object.weak = message.weak;
                if (message.unverifiedLazy != null && $Object.hasOwnProperty.call(message, "unverifiedLazy"))
                    object.unverifiedLazy = message.unverifiedLazy;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this FieldOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.FieldOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FieldOptions.prototype.toJSON = function() {
                return FieldOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FieldOptions
             * @function getTypeUrl
             * @memberof google.protobuf.FieldOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FieldOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FieldOptions";
            };

            /**
             * CType enum.
             * @name google.protobuf.FieldOptions.CType
             * @enum {number}
             * @property {number} STRING=0 STRING value
             * @property {number} CORD=1 CORD value
             * @property {number} STRING_PIECE=2 STRING_PIECE value
             */
            FieldOptions.CType = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "STRING"] = 0;
                values[valuesById[1] = "CORD"] = 1;
                values[valuesById[2] = "STRING_PIECE"] = 2;
                return values;
            })();

            /**
             * JSType enum.
             * @name google.protobuf.FieldOptions.JSType
             * @enum {number}
             * @property {number} JS_NORMAL=0 JS_NORMAL value
             * @property {number} JS_STRING=1 JS_STRING value
             * @property {number} JS_NUMBER=2 JS_NUMBER value
             */
            FieldOptions.JSType = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "JS_NORMAL"] = 0;
                values[valuesById[1] = "JS_STRING"] = 1;
                values[valuesById[2] = "JS_NUMBER"] = 2;
                return values;
            })();

            return FieldOptions;
        })();

        protobuf.OneofOptions = (function() {

            /**
             * Properties of a OneofOptions.
             * @typedef {Object} google.protobuf.OneofOptions.$Properties
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] OneofOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a OneofOptions.
             * @memberof google.protobuf
             * @interface IOneofOptions
             * @augments google.protobuf.OneofOptions.$Properties
             * @deprecated Use google.protobuf.OneofOptions.$Properties instead.
             */

            /**
             * Shape of a OneofOptions.
             * @typedef {google.protobuf.OneofOptions.$Properties} google.protobuf.OneofOptions.$Shape
             */

            /**
             * Constructs a new OneofOptions.
             * @memberof google.protobuf
             * @classdesc Represents a OneofOptions.
             * @constructor
             * @param {google.protobuf.OneofOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const OneofOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OneofOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.OneofOptions
             * @instance
             */
            OneofOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new OneofOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {google.protobuf.OneofOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.OneofOptions} OneofOptions instance
             * @type {{
             *   (properties: google.protobuf.OneofOptions.$Shape): google.protobuf.OneofOptions & google.protobuf.OneofOptions.$Shape;
             *   (properties?: google.protobuf.OneofOptions.$Properties): google.protobuf.OneofOptions;
             * }}
             */
            OneofOptions.create = function(properties) {
                return new OneofOptions(properties);
            };

            /**
             * Encodes the specified OneofOptions message. Does not implicitly {@link google.protobuf.OneofOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {google.protobuf.OneofOptions.$Properties} message OneofOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OneofOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified OneofOptions message, length delimited. Does not implicitly {@link google.protobuf.OneofOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {google.protobuf.OneofOptions.$Properties} message OneofOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OneofOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a OneofOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.OneofOptions & google.protobuf.OneofOptions.$Shape} OneofOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OneofOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.OneofOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a OneofOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.OneofOptions & google.protobuf.OneofOptions.$Shape} OneofOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OneofOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a OneofOptions message.
             * @function verify
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OneofOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a OneofOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.OneofOptions} OneofOptions
             */
            OneofOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.OneofOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.OneofOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.OneofOptions();
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.OneofOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.OneofOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a OneofOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {google.protobuf.OneofOptions} message OneofOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OneofOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this OneofOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.OneofOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OneofOptions.prototype.toJSON = function() {
                return OneofOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OneofOptions
             * @function getTypeUrl
             * @memberof google.protobuf.OneofOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OneofOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.OneofOptions";
            };

            return OneofOptions;
        })();

        protobuf.EnumOptions = (function() {

            /**
             * Properties of an EnumOptions.
             * @typedef {Object} google.protobuf.EnumOptions.$Properties
             * @property {boolean|null} [allowAlias] EnumOptions allowAlias
             * @property {boolean|null} [deprecated] EnumOptions deprecated
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] EnumOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EnumOptions.
             * @memberof google.protobuf
             * @interface IEnumOptions
             * @augments google.protobuf.EnumOptions.$Properties
             * @deprecated Use google.protobuf.EnumOptions.$Properties instead.
             */

            /**
             * Shape of an EnumOptions.
             * @typedef {google.protobuf.EnumOptions.$Properties} google.protobuf.EnumOptions.$Shape
             */

            /**
             * Constructs a new EnumOptions.
             * @memberof google.protobuf
             * @classdesc Represents an EnumOptions.
             * @constructor
             * @param {google.protobuf.EnumOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const EnumOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EnumOptions allowAlias.
             * @member {boolean} allowAlias
             * @memberof google.protobuf.EnumOptions
             * @instance
             */
            EnumOptions.prototype.allowAlias = false;

            /**
             * EnumOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.EnumOptions
             * @instance
             */
            EnumOptions.prototype.deprecated = false;

            /**
             * EnumOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.EnumOptions
             * @instance
             */
            EnumOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new EnumOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {google.protobuf.EnumOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.EnumOptions} EnumOptions instance
             * @type {{
             *   (properties: google.protobuf.EnumOptions.$Shape): google.protobuf.EnumOptions & google.protobuf.EnumOptions.$Shape;
             *   (properties?: google.protobuf.EnumOptions.$Properties): google.protobuf.EnumOptions;
             * }}
             */
            EnumOptions.create = function(properties) {
                return new EnumOptions(properties);
            };

            /**
             * Encodes the specified EnumOptions message. Does not implicitly {@link google.protobuf.EnumOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {google.protobuf.EnumOptions.$Properties} message EnumOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.allowAlias != null && $Object.hasOwnProperty.call(message, "allowAlias"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.allowAlias);
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.deprecated);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EnumOptions message, length delimited. Does not implicitly {@link google.protobuf.EnumOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {google.protobuf.EnumOptions.$Properties} message EnumOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EnumOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.EnumOptions & google.protobuf.EnumOptions.$Shape} EnumOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.EnumOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.allowAlias = reader.bool();
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EnumOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.EnumOptions & google.protobuf.EnumOptions.$Shape} EnumOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EnumOptions message.
             * @function verify
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EnumOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.allowAlias != null && $Object.hasOwnProperty.call(message, "allowAlias"))
                    if (typeof message.allowAlias !== "boolean")
                        return "allowAlias: boolean expected";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an EnumOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.EnumOptions} EnumOptions
             */
            EnumOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.EnumOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.EnumOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.EnumOptions();
                if (object.allowAlias != null)
                    message.allowAlias = $Boolean(object.allowAlias);
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.EnumOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.EnumOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an EnumOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {google.protobuf.EnumOptions} message EnumOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EnumOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults) {
                    object.allowAlias = false;
                    object.deprecated = false;
                }
                if (message.allowAlias != null && $Object.hasOwnProperty.call(message, "allowAlias"))
                    object.allowAlias = message.allowAlias;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this EnumOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.EnumOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EnumOptions.prototype.toJSON = function() {
                return EnumOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EnumOptions
             * @function getTypeUrl
             * @memberof google.protobuf.EnumOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EnumOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.EnumOptions";
            };

            return EnumOptions;
        })();

        protobuf.EnumValueOptions = (function() {

            /**
             * Properties of an EnumValueOptions.
             * @typedef {Object} google.protobuf.EnumValueOptions.$Properties
             * @property {boolean|null} [deprecated] EnumValueOptions deprecated
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] EnumValueOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EnumValueOptions.
             * @memberof google.protobuf
             * @interface IEnumValueOptions
             * @augments google.protobuf.EnumValueOptions.$Properties
             * @deprecated Use google.protobuf.EnumValueOptions.$Properties instead.
             */

            /**
             * Shape of an EnumValueOptions.
             * @typedef {google.protobuf.EnumValueOptions.$Properties} google.protobuf.EnumValueOptions.$Shape
             */

            /**
             * Constructs a new EnumValueOptions.
             * @memberof google.protobuf
             * @classdesc Represents an EnumValueOptions.
             * @constructor
             * @param {google.protobuf.EnumValueOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const EnumValueOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EnumValueOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.EnumValueOptions
             * @instance
             */
            EnumValueOptions.prototype.deprecated = false;

            /**
             * EnumValueOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.EnumValueOptions
             * @instance
             */
            EnumValueOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new EnumValueOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {google.protobuf.EnumValueOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.EnumValueOptions} EnumValueOptions instance
             * @type {{
             *   (properties: google.protobuf.EnumValueOptions.$Shape): google.protobuf.EnumValueOptions & google.protobuf.EnumValueOptions.$Shape;
             *   (properties?: google.protobuf.EnumValueOptions.$Properties): google.protobuf.EnumValueOptions;
             * }}
             */
            EnumValueOptions.create = function(properties) {
                return new EnumValueOptions(properties);
            };

            /**
             * Encodes the specified EnumValueOptions message. Does not implicitly {@link google.protobuf.EnumValueOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {google.protobuf.EnumValueOptions.$Properties} message EnumValueOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumValueOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.deprecated);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EnumValueOptions message, length delimited. Does not implicitly {@link google.protobuf.EnumValueOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {google.protobuf.EnumValueOptions.$Properties} message EnumValueOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EnumValueOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EnumValueOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.EnumValueOptions & google.protobuf.EnumValueOptions.$Shape} EnumValueOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumValueOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.EnumValueOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EnumValueOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.EnumValueOptions & google.protobuf.EnumValueOptions.$Shape} EnumValueOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EnumValueOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EnumValueOptions message.
             * @function verify
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EnumValueOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an EnumValueOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.EnumValueOptions} EnumValueOptions
             */
            EnumValueOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.EnumValueOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.EnumValueOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.EnumValueOptions();
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.EnumValueOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.EnumValueOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an EnumValueOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {google.protobuf.EnumValueOptions} message EnumValueOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EnumValueOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults)
                    object.deprecated = false;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this EnumValueOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.EnumValueOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EnumValueOptions.prototype.toJSON = function() {
                return EnumValueOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EnumValueOptions
             * @function getTypeUrl
             * @memberof google.protobuf.EnumValueOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EnumValueOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.EnumValueOptions";
            };

            return EnumValueOptions;
        })();

        protobuf.ServiceOptions = (function() {

            /**
             * Properties of a ServiceOptions.
             * @typedef {Object} google.protobuf.ServiceOptions.$Properties
             * @property {boolean|null} [deprecated] ServiceOptions deprecated
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] ServiceOptions uninterpretedOption
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ServiceOptions.
             * @memberof google.protobuf
             * @interface IServiceOptions
             * @augments google.protobuf.ServiceOptions.$Properties
             * @deprecated Use google.protobuf.ServiceOptions.$Properties instead.
             */

            /**
             * Shape of a ServiceOptions.
             * @typedef {google.protobuf.ServiceOptions.$Properties} google.protobuf.ServiceOptions.$Shape
             */

            /**
             * Constructs a new ServiceOptions.
             * @memberof google.protobuf
             * @classdesc Represents a ServiceOptions.
             * @constructor
             * @param {google.protobuf.ServiceOptions.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ServiceOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ServiceOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.ServiceOptions
             * @instance
             */
            ServiceOptions.prototype.deprecated = false;

            /**
             * ServiceOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.ServiceOptions
             * @instance
             */
            ServiceOptions.prototype.uninterpretedOption = $util.emptyArray;

            /**
             * Creates a new ServiceOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {google.protobuf.ServiceOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ServiceOptions} ServiceOptions instance
             * @type {{
             *   (properties: google.protobuf.ServiceOptions.$Shape): google.protobuf.ServiceOptions & google.protobuf.ServiceOptions.$Shape;
             *   (properties?: google.protobuf.ServiceOptions.$Properties): google.protobuf.ServiceOptions;
             * }}
             */
            ServiceOptions.create = function(properties) {
                return new ServiceOptions(properties);
            };

            /**
             * Encodes the specified ServiceOptions message. Does not implicitly {@link google.protobuf.ServiceOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {google.protobuf.ServiceOptions.$Properties} message ServiceOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServiceOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 33, wireType 0 =*/264).bool(message.deprecated);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ServiceOptions message, length delimited. Does not implicitly {@link google.protobuf.ServiceOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {google.protobuf.ServiceOptions.$Properties} message ServiceOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServiceOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ServiceOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ServiceOptions & google.protobuf.ServiceOptions.$Shape} ServiceOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServiceOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.ServiceOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 33: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ServiceOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.ServiceOptions & google.protobuf.ServiceOptions.$Shape} ServiceOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServiceOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ServiceOptions message.
             * @function verify
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ServiceOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ServiceOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ServiceOptions} ServiceOptions
             */
            ServiceOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ServiceOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ServiceOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ServiceOptions();
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.ServiceOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.ServiceOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ServiceOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {google.protobuf.ServiceOptions} message ServiceOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ServiceOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults)
                    object.deprecated = false;
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ServiceOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.ServiceOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ServiceOptions.prototype.toJSON = function() {
                return ServiceOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ServiceOptions
             * @function getTypeUrl
             * @memberof google.protobuf.ServiceOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ServiceOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ServiceOptions";
            };

            return ServiceOptions;
        })();

        protobuf.MethodOptions = (function() {

            /**
             * Properties of a MethodOptions.
             * @typedef {Object} google.protobuf.MethodOptions.$Properties
             * @property {boolean|null} [deprecated] MethodOptions deprecated
             * @property {google.protobuf.MethodOptions.IdempotencyLevel|null} [idempotencyLevel] MethodOptions idempotencyLevel
             * @property {Array.<google.protobuf.UninterpretedOption.$Properties>|null} [uninterpretedOption] MethodOptions uninterpretedOption
             * @property {google.api.HttpRule.$Properties|null} [".google.api.http"] MethodOptions .google.api.http
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MethodOptions.
             * @memberof google.protobuf
             * @interface IMethodOptions
             * @augments google.protobuf.MethodOptions.$Properties
             * @deprecated Use google.protobuf.MethodOptions.$Properties instead.
             */

            /**
             * Shape of a MethodOptions.
             * @typedef {{
             *   deprecated?: boolean|null;
             *   idempotencyLevel?: google.protobuf.MethodOptions.IdempotencyLevel|null;
             *   uninterpretedOption?: Array.<google.protobuf.UninterpretedOption.$Shape>|null;
             *   ".google.api.http"?: google.api.HttpRule.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.MethodOptions.$Shape
             */

            /**
             * Constructs a new MethodOptions.
             * @memberof google.protobuf
             * @classdesc Represents a MethodOptions.
             * @constructor
             * @param {google.protobuf.MethodOptions.$Properties=} [properties] Properties to set
             * @property {google.api.HttpRule.$Properties|null} [".google.api.http"] MethodOptions .google.api.http
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const MethodOptions = function (properties) {
                this.uninterpretedOption = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MethodOptions deprecated.
             * @member {boolean} deprecated
             * @memberof google.protobuf.MethodOptions
             * @instance
             */
            MethodOptions.prototype.deprecated = false;

            /**
             * MethodOptions idempotencyLevel.
             * @member {google.protobuf.MethodOptions.IdempotencyLevel} idempotencyLevel
             * @memberof google.protobuf.MethodOptions
             * @instance
             */
            MethodOptions.prototype.idempotencyLevel = 0;

            /**
             * MethodOptions uninterpretedOption.
             * @member {Array.<google.protobuf.UninterpretedOption.$Properties>} uninterpretedOption
             * @memberof google.protobuf.MethodOptions
             * @instance
             */
            MethodOptions.prototype.uninterpretedOption = $util.emptyArray;

            MethodOptions.prototype[".google.api.http"] = null;

            /**
             * Creates a new MethodOptions instance using the specified properties.
             * @function create
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {google.protobuf.MethodOptions.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.MethodOptions} MethodOptions instance
             * @type {{
             *   (properties: google.protobuf.MethodOptions.$Shape): google.protobuf.MethodOptions & google.protobuf.MethodOptions.$Shape;
             *   (properties?: google.protobuf.MethodOptions.$Properties): google.protobuf.MethodOptions;
             * }}
             */
            MethodOptions.create = function(properties) {
                return new MethodOptions(properties);
            };

            /**
             * Encodes the specified MethodOptions message. Does not implicitly {@link google.protobuf.MethodOptions.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {google.protobuf.MethodOptions.$Properties} message MethodOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MethodOptions.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    writer.uint32(/* id 33, wireType 0 =*/264).bool(message.deprecated);
                if (message.idempotencyLevel != null && $Object.hasOwnProperty.call(message, "idempotencyLevel"))
                    writer.uint32(/* id 34, wireType 0 =*/272).int32(message.idempotencyLevel);
                if (message.uninterpretedOption != null && message.uninterpretedOption.length)
                    for (let i = 0; i < message.uninterpretedOption.length; ++i)
                        $root.google.protobuf.UninterpretedOption.encode(message.uninterpretedOption[i], writer.uint32(/* id 999, wireType 2 =*/7994).fork(), _depth + 1).ldelim();
                if (message[".google.api.http"] != null && $Object.hasOwnProperty.call(message, ".google.api.http"))
                    $root.google.api.HttpRule.encode(message[".google.api.http"], writer.uint32(/* id 72295728, wireType 2 =*/578365826).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MethodOptions message, length delimited. Does not implicitly {@link google.protobuf.MethodOptions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {google.protobuf.MethodOptions.$Properties} message MethodOptions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MethodOptions.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MethodOptions message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.MethodOptions & google.protobuf.MethodOptions.$Shape} MethodOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MethodOptions.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.MethodOptions();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 33: {
                            if (wireType !== 0)
                                break;
                            message.deprecated = reader.bool();
                            continue;
                        }
                    case 34: {
                            if (wireType !== 0)
                                break;
                            message.idempotencyLevel = reader.int32();
                            continue;
                        }
                    case 999: {
                            if (wireType !== 2)
                                break;
                            if (!(message.uninterpretedOption && message.uninterpretedOption.length))
                                message.uninterpretedOption = [];
                            message.uninterpretedOption.push($root.google.protobuf.UninterpretedOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 72295728: {
                            if (wireType !== 2)
                                break;
                            message[".google.api.http"] = $root.google.api.HttpRule.decode(reader, reader.uint32(), $undefined, _depth + 1, message[".google.api.http"]);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MethodOptions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.MethodOptions & google.protobuf.MethodOptions.$Shape} MethodOptions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MethodOptions.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MethodOptions message.
             * @function verify
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MethodOptions.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    if (typeof message.deprecated !== "boolean")
                        return "deprecated: boolean expected";
                if (message.idempotencyLevel != null && $Object.hasOwnProperty.call(message, "idempotencyLevel"))
                    switch (message.idempotencyLevel) {
                    default:
                        return "idempotencyLevel: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                        break;
                    }
                if (message.uninterpretedOption != null && $Object.hasOwnProperty.call(message, "uninterpretedOption")) {
                    if (!$Array.isArray(message.uninterpretedOption))
                        return "uninterpretedOption: array expected";
                    for (let i = 0; i < message.uninterpretedOption.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.verify(message.uninterpretedOption[i], _depth + 1);
                        if (error)
                            return "uninterpretedOption." + error;
                    }
                }
                if (message[".google.api.http"] != null && $Object.hasOwnProperty.call(message, ".google.api.http")) {
                    let error = $root.google.api.HttpRule.verify(message[".google.api.http"], _depth + 1);
                    if (error)
                        return ".google.api.http." + error;
                }
                return null;
            };

            /**
             * Creates a MethodOptions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.MethodOptions} MethodOptions
             */
            MethodOptions.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.MethodOptions)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.MethodOptions: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.MethodOptions();
                if (object.deprecated != null)
                    message.deprecated = $Boolean(object.deprecated);
                switch (object.idempotencyLevel) {
                default:
                    if (typeof object.idempotencyLevel === "number") {
                        message.idempotencyLevel = object.idempotencyLevel;
                        break;
                    }
                    break;
                case "IDEMPOTENCY_UNKNOWN":
                case 0:
                    message.idempotencyLevel = 0;
                    break;
                case "NO_SIDE_EFFECTS":
                case 1:
                    message.idempotencyLevel = 1;
                    break;
                case "IDEMPOTENT":
                case 2:
                    message.idempotencyLevel = 2;
                    break;
                }
                if (object.uninterpretedOption) {
                    if (!$Array.isArray(object.uninterpretedOption))
                        throw $TypeError(".google.protobuf.MethodOptions.uninterpretedOption: array expected");
                    message.uninterpretedOption = $Array(object.uninterpretedOption.length);
                    for (let i = 0; i < object.uninterpretedOption.length; ++i) {
                        if (!$util.isObject(object.uninterpretedOption[i]))
                            throw $TypeError(".google.protobuf.MethodOptions.uninterpretedOption: object expected");
                        message.uninterpretedOption[i] = $root.google.protobuf.UninterpretedOption.fromObject(object.uninterpretedOption[i], _depth + 1);
                    }
                }
                if (object[".google.api.http"] != null) {
                    if (!$util.isObject(object[".google.api.http"]))
                        throw $TypeError(".google.protobuf.MethodOptions..google.api.http: object expected");
                    message[".google.api.http"] = $root.google.api.HttpRule.fromObject(object[".google.api.http"], _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MethodOptions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {google.protobuf.MethodOptions} message MethodOptions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MethodOptions.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uninterpretedOption = [];
                if (options.defaults) {
                    object.deprecated = false;
                    object.idempotencyLevel = options.enums === $String ? "IDEMPOTENCY_UNKNOWN" : 0;
                    object[".google.api.http"] = null;
                }
                if (message.deprecated != null && $Object.hasOwnProperty.call(message, "deprecated"))
                    object.deprecated = message.deprecated;
                if (message.idempotencyLevel != null && $Object.hasOwnProperty.call(message, "idempotencyLevel"))
                    object.idempotencyLevel = options.enums === $String ? $root.google.protobuf.MethodOptions.IdempotencyLevel[message.idempotencyLevel] === $undefined ? message.idempotencyLevel : $root.google.protobuf.MethodOptions.IdempotencyLevel[message.idempotencyLevel] : message.idempotencyLevel;
                if (message.uninterpretedOption && message.uninterpretedOption.length) {
                    object.uninterpretedOption = $Array(message.uninterpretedOption.length);
                    for (let j = 0; j < message.uninterpretedOption.length; ++j)
                        object.uninterpretedOption[j] = $root.google.protobuf.UninterpretedOption.toObject(message.uninterpretedOption[j], options, _depth + 1);
                }
                if (message[".google.api.http"] != null && $Object.hasOwnProperty.call(message, ".google.api.http"))
                    object[".google.api.http"] = $root.google.api.HttpRule.toObject(message[".google.api.http"], options, _depth + 1);
                return object;
            };

            /**
             * Converts this MethodOptions to JSON.
             * @function toJSON
             * @memberof google.protobuf.MethodOptions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MethodOptions.prototype.toJSON = function() {
                return MethodOptions.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MethodOptions
             * @function getTypeUrl
             * @memberof google.protobuf.MethodOptions
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MethodOptions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.MethodOptions";
            };

            /**
             * IdempotencyLevel enum.
             * @name google.protobuf.MethodOptions.IdempotencyLevel
             * @enum {number}
             * @property {number} IDEMPOTENCY_UNKNOWN=0 IDEMPOTENCY_UNKNOWN value
             * @property {number} NO_SIDE_EFFECTS=1 NO_SIDE_EFFECTS value
             * @property {number} IDEMPOTENT=2 IDEMPOTENT value
             */
            MethodOptions.IdempotencyLevel = (function() {
                const valuesById = {}, values = $Object.create(valuesById);
                values[valuesById[0] = "IDEMPOTENCY_UNKNOWN"] = 0;
                values[valuesById[1] = "NO_SIDE_EFFECTS"] = 1;
                values[valuesById[2] = "IDEMPOTENT"] = 2;
                return values;
            })();

            return MethodOptions;
        })();

        protobuf.UninterpretedOption = (function() {

            /**
             * Properties of an UninterpretedOption.
             * @typedef {Object} google.protobuf.UninterpretedOption.$Properties
             * @property {Array.<google.protobuf.UninterpretedOption.NamePart.$Properties>|null} [name] UninterpretedOption name
             * @property {string|null} [identifierValue] UninterpretedOption identifierValue
             * @property {number|Long|null} [positiveIntValue] UninterpretedOption positiveIntValue
             * @property {number|Long|null} [negativeIntValue] UninterpretedOption negativeIntValue
             * @property {number|null} [doubleValue] UninterpretedOption doubleValue
             * @property {Uint8Array|null} [stringValue] UninterpretedOption stringValue
             * @property {string|null} [aggregateValue] UninterpretedOption aggregateValue
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an UninterpretedOption.
             * @memberof google.protobuf
             * @interface IUninterpretedOption
             * @augments google.protobuf.UninterpretedOption.$Properties
             * @deprecated Use google.protobuf.UninterpretedOption.$Properties instead.
             */

            /**
             * Shape of an UninterpretedOption.
             * @typedef {google.protobuf.UninterpretedOption.$Properties} google.protobuf.UninterpretedOption.$Shape
             */

            /**
             * Constructs a new UninterpretedOption.
             * @memberof google.protobuf
             * @classdesc Represents an UninterpretedOption.
             * @constructor
             * @param {google.protobuf.UninterpretedOption.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const UninterpretedOption = function (properties) {
                this.name = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * UninterpretedOption name.
             * @member {Array.<google.protobuf.UninterpretedOption.NamePart.$Properties>} name
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.name = $util.emptyArray;

            /**
             * UninterpretedOption identifierValue.
             * @member {string} identifierValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.identifierValue = "";

            /**
             * UninterpretedOption positiveIntValue.
             * @member {number|Long} positiveIntValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.positiveIntValue = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * UninterpretedOption negativeIntValue.
             * @member {number|Long} negativeIntValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.negativeIntValue = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * UninterpretedOption doubleValue.
             * @member {number} doubleValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.doubleValue = 0;

            /**
             * UninterpretedOption stringValue.
             * @member {Uint8Array} stringValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.stringValue = $util.newBuffer([]);

            /**
             * UninterpretedOption aggregateValue.
             * @member {string} aggregateValue
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             */
            UninterpretedOption.prototype.aggregateValue = "";

            /**
             * Creates a new UninterpretedOption instance using the specified properties.
             * @function create
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {google.protobuf.UninterpretedOption.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.UninterpretedOption} UninterpretedOption instance
             * @type {{
             *   (properties: google.protobuf.UninterpretedOption.$Shape): google.protobuf.UninterpretedOption & google.protobuf.UninterpretedOption.$Shape;
             *   (properties?: google.protobuf.UninterpretedOption.$Properties): google.protobuf.UninterpretedOption;
             * }}
             */
            UninterpretedOption.create = function(properties) {
                return new UninterpretedOption(properties);
            };

            /**
             * Encodes the specified UninterpretedOption message. Does not implicitly {@link google.protobuf.UninterpretedOption.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {google.protobuf.UninterpretedOption.$Properties} message UninterpretedOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UninterpretedOption.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && message.name.length)
                    for (let i = 0; i < message.name.length; ++i)
                        $root.google.protobuf.UninterpretedOption.NamePart.encode(message.name[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.identifierValue != null && $Object.hasOwnProperty.call(message, "identifierValue"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.identifierValue);
                if (message.positiveIntValue != null && $Object.hasOwnProperty.call(message, "positiveIntValue"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.positiveIntValue);
                if (message.negativeIntValue != null && $Object.hasOwnProperty.call(message, "negativeIntValue"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.negativeIntValue);
                if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue"))
                    writer.uint32(/* id 6, wireType 1 =*/49).double(message.doubleValue);
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.stringValue);
                if (message.aggregateValue != null && $Object.hasOwnProperty.call(message, "aggregateValue"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.aggregateValue);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UninterpretedOption message, length delimited. Does not implicitly {@link google.protobuf.UninterpretedOption.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {google.protobuf.UninterpretedOption.$Properties} message UninterpretedOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UninterpretedOption.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an UninterpretedOption message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.UninterpretedOption & google.protobuf.UninterpretedOption.$Shape} UninterpretedOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UninterpretedOption.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.UninterpretedOption();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.name && message.name.length))
                                message.name = [];
                            message.name.push($root.google.protobuf.UninterpretedOption.NamePart.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.identifierValue = reader.string();
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.positiveIntValue = reader.uint64();
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            message.negativeIntValue = reader.int64();
                            continue;
                        }
                    case 6: {
                            if (wireType !== 1)
                                break;
                            message.doubleValue = reader.double();
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.stringValue = reader.bytes();
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.aggregateValue = reader.string();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an UninterpretedOption message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.UninterpretedOption & google.protobuf.UninterpretedOption.$Shape} UninterpretedOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UninterpretedOption.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an UninterpretedOption message.
             * @function verify
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UninterpretedOption.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
                    if (!$Array.isArray(message.name))
                        return "name: array expected";
                    for (let i = 0; i < message.name.length; ++i) {
                        let error = $root.google.protobuf.UninterpretedOption.NamePart.verify(message.name[i], _depth + 1);
                        if (error)
                            return "name." + error;
                    }
                }
                if (message.identifierValue != null && $Object.hasOwnProperty.call(message, "identifierValue"))
                    if (!$util.isString(message.identifierValue))
                        return "identifierValue: string expected";
                if (message.positiveIntValue != null && $Object.hasOwnProperty.call(message, "positiveIntValue"))
                    if (!$util.isInteger(message.positiveIntValue) && !(message.positiveIntValue && $util.isInteger(message.positiveIntValue.low) && $util.isInteger(message.positiveIntValue.high)))
                        return "positiveIntValue: integer|Long expected";
                if (message.negativeIntValue != null && $Object.hasOwnProperty.call(message, "negativeIntValue"))
                    if (!$util.isInteger(message.negativeIntValue) && !(message.negativeIntValue && $util.isInteger(message.negativeIntValue.low) && $util.isInteger(message.negativeIntValue.high)))
                        return "negativeIntValue: integer|Long expected";
                if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue"))
                    if (typeof message.doubleValue !== "number")
                        return "doubleValue: number expected";
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    if (!(message.stringValue && typeof message.stringValue.length === "number" || $util.isString(message.stringValue)))
                        return "stringValue: buffer expected";
                if (message.aggregateValue != null && $Object.hasOwnProperty.call(message, "aggregateValue"))
                    if (!$util.isString(message.aggregateValue))
                        return "aggregateValue: string expected";
                return null;
            };

            /**
             * Creates an UninterpretedOption message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.UninterpretedOption} UninterpretedOption
             */
            UninterpretedOption.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.UninterpretedOption)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.UninterpretedOption: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.UninterpretedOption();
                if (object.name) {
                    if (!$Array.isArray(object.name))
                        throw $TypeError(".google.protobuf.UninterpretedOption.name: array expected");
                    message.name = $Array(object.name.length);
                    for (let i = 0; i < object.name.length; ++i) {
                        if (!$util.isObject(object.name[i]))
                            throw $TypeError(".google.protobuf.UninterpretedOption.name: object expected");
                        message.name[i] = $root.google.protobuf.UninterpretedOption.NamePart.fromObject(object.name[i], _depth + 1);
                    }
                }
                if (object.identifierValue != null)
                    message.identifierValue = $String(object.identifierValue);
                if (object.positiveIntValue != null)
                    if ($util.Long)
                        message.positiveIntValue = $util.Long.fromValue(object.positiveIntValue, true);
                    else if (typeof object.positiveIntValue === "string")
                        message.positiveIntValue = $parseInt(object.positiveIntValue, 10);
                    else if (typeof object.positiveIntValue === "number")
                        message.positiveIntValue = object.positiveIntValue;
                    else if (typeof object.positiveIntValue === "object")
                        message.positiveIntValue = new $util.LongBits(object.positiveIntValue.low >>> 0, object.positiveIntValue.high >>> 0).toNumber(true);
                if (object.negativeIntValue != null)
                    if ($util.Long)
                        message.negativeIntValue = $util.Long.fromValue(object.negativeIntValue, false);
                    else if (typeof object.negativeIntValue === "string")
                        message.negativeIntValue = $parseInt(object.negativeIntValue, 10);
                    else if (typeof object.negativeIntValue === "number")
                        message.negativeIntValue = object.negativeIntValue;
                    else if (typeof object.negativeIntValue === "object")
                        message.negativeIntValue = new $util.LongBits(object.negativeIntValue.low >>> 0, object.negativeIntValue.high >>> 0).toNumber();
                if (object.doubleValue != null)
                    message.doubleValue = $Number(object.doubleValue);
                if (object.stringValue != null)
                    if (typeof object.stringValue === "string")
                        $util.base64.decode(object.stringValue, message.stringValue = $util.newBuffer($util.base64.length(object.stringValue)), 0);
                    else if (object.stringValue.length >= 0)
                        message.stringValue = object.stringValue;
                if (object.aggregateValue != null)
                    message.aggregateValue = $String(object.aggregateValue);
                return message;
            };

            /**
             * Creates a plain object from an UninterpretedOption message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {google.protobuf.UninterpretedOption} message UninterpretedOption
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UninterpretedOption.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.name = [];
                if (options.defaults) {
                    object.identifierValue = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.positiveIntValue = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.positiveIntValue = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.negativeIntValue = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.negativeIntValue = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.doubleValue = 0;
                    if (options.bytes === $String)
                        object.stringValue = "";
                    else {
                        object.stringValue = [];
                        if (options.bytes !== $Array)
                            object.stringValue = $util.newBuffer(object.stringValue);
                    }
                    object.aggregateValue = "";
                }
                if (message.name && message.name.length) {
                    object.name = $Array(message.name.length);
                    for (let j = 0; j < message.name.length; ++j)
                        object.name[j] = $root.google.protobuf.UninterpretedOption.NamePart.toObject(message.name[j], options, _depth + 1);
                }
                if (message.identifierValue != null && $Object.hasOwnProperty.call(message, "identifierValue"))
                    object.identifierValue = message.identifierValue;
                if (message.positiveIntValue != null && $Object.hasOwnProperty.call(message, "positiveIntValue"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.positiveIntValue = typeof message.positiveIntValue === "number" ? $BigInt(message.positiveIntValue) : $util.Long.fromBits(message.positiveIntValue.low >>> 0, message.positiveIntValue.high >>> 0, true).toBigInt();
                    else if (typeof message.positiveIntValue === "number")
                        object.positiveIntValue = options.longs === $String ? $String(message.positiveIntValue) : message.positiveIntValue;
                    else
                        object.positiveIntValue = options.longs === $String ? $util.Long.prototype.toString.call(message.positiveIntValue) : options.longs === $Number ? new $util.LongBits(message.positiveIntValue.low >>> 0, message.positiveIntValue.high >>> 0).toNumber(true) : message.positiveIntValue;
                if (message.negativeIntValue != null && $Object.hasOwnProperty.call(message, "negativeIntValue"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.negativeIntValue = typeof message.negativeIntValue === "number" ? $BigInt(message.negativeIntValue) : $util.Long.fromBits(message.negativeIntValue.low >>> 0, message.negativeIntValue.high >>> 0, false).toBigInt();
                    else if (typeof message.negativeIntValue === "number")
                        object.negativeIntValue = options.longs === $String ? $String(message.negativeIntValue) : message.negativeIntValue;
                    else
                        object.negativeIntValue = options.longs === $String ? $util.Long.prototype.toString.call(message.negativeIntValue) : options.longs === $Number ? new $util.LongBits(message.negativeIntValue.low >>> 0, message.negativeIntValue.high >>> 0).toNumber() : message.negativeIntValue;
                if (message.doubleValue != null && $Object.hasOwnProperty.call(message, "doubleValue"))
                    object.doubleValue = options.json && !$isFinite(message.doubleValue) ? $String(message.doubleValue) : message.doubleValue;
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    object.stringValue = options.bytes === $String ? $util.base64.encode(message.stringValue, 0, message.stringValue.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.stringValue) : message.stringValue;
                if (message.aggregateValue != null && $Object.hasOwnProperty.call(message, "aggregateValue"))
                    object.aggregateValue = message.aggregateValue;
                return object;
            };

            /**
             * Converts this UninterpretedOption to JSON.
             * @function toJSON
             * @memberof google.protobuf.UninterpretedOption
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UninterpretedOption.prototype.toJSON = function() {
                return UninterpretedOption.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for UninterpretedOption
             * @function getTypeUrl
             * @memberof google.protobuf.UninterpretedOption
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            UninterpretedOption.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.UninterpretedOption";
            };

            UninterpretedOption.NamePart = (function() {

                /**
                 * Properties of a NamePart.
                 * @typedef {Object} google.protobuf.UninterpretedOption.NamePart.$Properties
                 * @property {string} namePart NamePart namePart
                 * @property {boolean} isExtension NamePart isExtension
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a NamePart.
                 * @memberof google.protobuf.UninterpretedOption
                 * @interface INamePart
                 * @augments google.protobuf.UninterpretedOption.NamePart.$Properties
                 * @deprecated Use google.protobuf.UninterpretedOption.NamePart.$Properties instead.
                 */

                /**
                 * Shape of a NamePart.
                 * @typedef {google.protobuf.UninterpretedOption.NamePart.$Properties} google.protobuf.UninterpretedOption.NamePart.$Shape
                 */

                /**
                 * Constructs a new NamePart.
                 * @memberof google.protobuf.UninterpretedOption
                 * @classdesc Represents a NamePart.
                 * @constructor
                 * @param {google.protobuf.UninterpretedOption.NamePart.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const NamePart = function (properties) {
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * NamePart namePart.
                 * @member {string} namePart
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @instance
                 */
                NamePart.prototype.namePart = "";

                /**
                 * NamePart isExtension.
                 * @member {boolean} isExtension
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @instance
                 */
                NamePart.prototype.isExtension = false;

                /**
                 * Creates a new NamePart instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {google.protobuf.UninterpretedOption.NamePart.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.UninterpretedOption.NamePart} NamePart instance
                 * @type {{
                 *   (properties: google.protobuf.UninterpretedOption.NamePart.$Shape): google.protobuf.UninterpretedOption.NamePart & google.protobuf.UninterpretedOption.NamePart.$Shape;
                 *   (properties?: google.protobuf.UninterpretedOption.NamePart.$Properties): google.protobuf.UninterpretedOption.NamePart;
                 * }}
                 */
                NamePart.create = function(properties) {
                    return new NamePart(properties);
                };

                /**
                 * Encodes the specified NamePart message. Does not implicitly {@link google.protobuf.UninterpretedOption.NamePart.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {google.protobuf.UninterpretedOption.NamePart.$Properties} message NamePart message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NamePart.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.namePart);
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isExtension);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified NamePart message, length delimited. Does not implicitly {@link google.protobuf.UninterpretedOption.NamePart.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {google.protobuf.UninterpretedOption.NamePart.$Properties} message NamePart message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NamePart.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a NamePart message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.UninterpretedOption.NamePart & google.protobuf.UninterpretedOption.NamePart.$Shape} NamePart
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NamePart.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.UninterpretedOption.NamePart();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                message.namePart = reader.string();
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                message.isExtension = reader.bool();
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    if (!$Object.hasOwnProperty.call(message, "namePart"))
                        throw $util.ProtocolError("missing required 'namePart'", { instance: message });
                    if (!$Object.hasOwnProperty.call(message, "isExtension"))
                        throw $util.ProtocolError("missing required 'isExtension'", { instance: message });
                    return message;
                };

                /**
                 * Decodes a NamePart message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.UninterpretedOption.NamePart & google.protobuf.UninterpretedOption.NamePart.$Shape} NamePart
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NamePart.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a NamePart message.
                 * @function verify
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                NamePart.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (!$util.isString(message.namePart))
                        return "namePart: string expected";
                    if (typeof message.isExtension !== "boolean")
                        return "isExtension: boolean expected";
                    return null;
                };

                /**
                 * Creates a NamePart message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.UninterpretedOption.NamePart} NamePart
                 */
                NamePart.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.UninterpretedOption.NamePart)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.UninterpretedOption.NamePart: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.UninterpretedOption.NamePart();
                    if (object.namePart != null)
                        message.namePart = $String(object.namePart);
                    if (object.isExtension != null)
                        message.isExtension = $Boolean(object.isExtension);
                    return message;
                };

                /**
                 * Creates a plain object from a NamePart message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {google.protobuf.UninterpretedOption.NamePart} message NamePart
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                NamePart.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.namePart = "";
                        object.isExtension = false;
                    }
                    if (message.namePart != null && $Object.hasOwnProperty.call(message, "namePart"))
                        object.namePart = message.namePart;
                    if (message.isExtension != null && $Object.hasOwnProperty.call(message, "isExtension"))
                        object.isExtension = message.isExtension;
                    return object;
                };

                /**
                 * Converts this NamePart to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                NamePart.prototype.toJSON = function() {
                    return NamePart.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for NamePart
                 * @function getTypeUrl
                 * @memberof google.protobuf.UninterpretedOption.NamePart
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                NamePart.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.UninterpretedOption.NamePart";
                };

                return NamePart;
            })();

            return UninterpretedOption;
        })();

        protobuf.SourceCodeInfo = (function() {

            /**
             * Properties of a SourceCodeInfo.
             * @typedef {Object} google.protobuf.SourceCodeInfo.$Properties
             * @property {Array.<google.protobuf.SourceCodeInfo.Location.$Properties>|null} [location] SourceCodeInfo location
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SourceCodeInfo.
             * @memberof google.protobuf
             * @interface ISourceCodeInfo
             * @augments google.protobuf.SourceCodeInfo.$Properties
             * @deprecated Use google.protobuf.SourceCodeInfo.$Properties instead.
             */

            /**
             * Shape of a SourceCodeInfo.
             * @typedef {google.protobuf.SourceCodeInfo.$Properties} google.protobuf.SourceCodeInfo.$Shape
             */

            /**
             * Constructs a new SourceCodeInfo.
             * @memberof google.protobuf
             * @classdesc Represents a SourceCodeInfo.
             * @constructor
             * @param {google.protobuf.SourceCodeInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const SourceCodeInfo = function (properties) {
                this.location = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SourceCodeInfo location.
             * @member {Array.<google.protobuf.SourceCodeInfo.Location.$Properties>} location
             * @memberof google.protobuf.SourceCodeInfo
             * @instance
             */
            SourceCodeInfo.prototype.location = $util.emptyArray;

            /**
             * Creates a new SourceCodeInfo instance using the specified properties.
             * @function create
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {google.protobuf.SourceCodeInfo.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.SourceCodeInfo} SourceCodeInfo instance
             * @type {{
             *   (properties: google.protobuf.SourceCodeInfo.$Shape): google.protobuf.SourceCodeInfo & google.protobuf.SourceCodeInfo.$Shape;
             *   (properties?: google.protobuf.SourceCodeInfo.$Properties): google.protobuf.SourceCodeInfo;
             * }}
             */
            SourceCodeInfo.create = function(properties) {
                return new SourceCodeInfo(properties);
            };

            /**
             * Encodes the specified SourceCodeInfo message. Does not implicitly {@link google.protobuf.SourceCodeInfo.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {google.protobuf.SourceCodeInfo.$Properties} message SourceCodeInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SourceCodeInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.location != null && message.location.length)
                    for (let i = 0; i < message.location.length; ++i)
                        $root.google.protobuf.SourceCodeInfo.Location.encode(message.location[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SourceCodeInfo message, length delimited. Does not implicitly {@link google.protobuf.SourceCodeInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {google.protobuf.SourceCodeInfo.$Properties} message SourceCodeInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SourceCodeInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SourceCodeInfo message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.SourceCodeInfo & google.protobuf.SourceCodeInfo.$Shape} SourceCodeInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SourceCodeInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.SourceCodeInfo();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.location && message.location.length))
                                message.location = [];
                            message.location.push($root.google.protobuf.SourceCodeInfo.Location.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SourceCodeInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.SourceCodeInfo & google.protobuf.SourceCodeInfo.$Shape} SourceCodeInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SourceCodeInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SourceCodeInfo message.
             * @function verify
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SourceCodeInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.location != null && $Object.hasOwnProperty.call(message, "location")) {
                    if (!$Array.isArray(message.location))
                        return "location: array expected";
                    for (let i = 0; i < message.location.length; ++i) {
                        let error = $root.google.protobuf.SourceCodeInfo.Location.verify(message.location[i], _depth + 1);
                        if (error)
                            return "location." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SourceCodeInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.SourceCodeInfo} SourceCodeInfo
             */
            SourceCodeInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.SourceCodeInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.SourceCodeInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.SourceCodeInfo();
                if (object.location) {
                    if (!$Array.isArray(object.location))
                        throw $TypeError(".google.protobuf.SourceCodeInfo.location: array expected");
                    message.location = $Array(object.location.length);
                    for (let i = 0; i < object.location.length; ++i) {
                        if (!$util.isObject(object.location[i]))
                            throw $TypeError(".google.protobuf.SourceCodeInfo.location: object expected");
                        message.location[i] = $root.google.protobuf.SourceCodeInfo.Location.fromObject(object.location[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a SourceCodeInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {google.protobuf.SourceCodeInfo} message SourceCodeInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SourceCodeInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.location = [];
                if (message.location && message.location.length) {
                    object.location = $Array(message.location.length);
                    for (let j = 0; j < message.location.length; ++j)
                        object.location[j] = $root.google.protobuf.SourceCodeInfo.Location.toObject(message.location[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this SourceCodeInfo to JSON.
             * @function toJSON
             * @memberof google.protobuf.SourceCodeInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SourceCodeInfo.prototype.toJSON = function() {
                return SourceCodeInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SourceCodeInfo
             * @function getTypeUrl
             * @memberof google.protobuf.SourceCodeInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SourceCodeInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.SourceCodeInfo";
            };

            SourceCodeInfo.Location = (function() {

                /**
                 * Properties of a Location.
                 * @typedef {Object} google.protobuf.SourceCodeInfo.Location.$Properties
                 * @property {Array.<number>|null} [path] Location path
                 * @property {Array.<number>|null} [span] Location span
                 * @property {string|null} [leadingComments] Location leadingComments
                 * @property {string|null} [trailingComments] Location trailingComments
                 * @property {Array.<string>|null} [leadingDetachedComments] Location leadingDetachedComments
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of a Location.
                 * @memberof google.protobuf.SourceCodeInfo
                 * @interface ILocation
                 * @augments google.protobuf.SourceCodeInfo.Location.$Properties
                 * @deprecated Use google.protobuf.SourceCodeInfo.Location.$Properties instead.
                 */

                /**
                 * Shape of a Location.
                 * @typedef {google.protobuf.SourceCodeInfo.Location.$Properties} google.protobuf.SourceCodeInfo.Location.$Shape
                 */

                /**
                 * Constructs a new Location.
                 * @memberof google.protobuf.SourceCodeInfo
                 * @classdesc Represents a Location.
                 * @constructor
                 * @param {google.protobuf.SourceCodeInfo.Location.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const Location = function (properties) {
                    this.path = [];
                    this.span = [];
                    this.leadingDetachedComments = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * Location path.
                 * @member {Array.<number>} path
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 */
                Location.prototype.path = $util.emptyArray;

                /**
                 * Location span.
                 * @member {Array.<number>} span
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 */
                Location.prototype.span = $util.emptyArray;

                /**
                 * Location leadingComments.
                 * @member {string} leadingComments
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 */
                Location.prototype.leadingComments = "";

                /**
                 * Location trailingComments.
                 * @member {string} trailingComments
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 */
                Location.prototype.trailingComments = "";

                /**
                 * Location leadingDetachedComments.
                 * @member {Array.<string>} leadingDetachedComments
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 */
                Location.prototype.leadingDetachedComments = $util.emptyArray;

                /**
                 * Creates a new Location instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {google.protobuf.SourceCodeInfo.Location.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.SourceCodeInfo.Location} Location instance
                 * @type {{
                 *   (properties: google.protobuf.SourceCodeInfo.Location.$Shape): google.protobuf.SourceCodeInfo.Location & google.protobuf.SourceCodeInfo.Location.$Shape;
                 *   (properties?: google.protobuf.SourceCodeInfo.Location.$Properties): google.protobuf.SourceCodeInfo.Location;
                 * }}
                 */
                Location.create = function(properties) {
                    return new Location(properties);
                };

                /**
                 * Encodes the specified Location message. Does not implicitly {@link google.protobuf.SourceCodeInfo.Location.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {google.protobuf.SourceCodeInfo.Location.$Properties} message Location message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Location.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.path != null && message.path.length) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork();
                        for (let i = 0; i < message.path.length; ++i)
                            writer.int32(message.path[i]);
                        writer.ldelim();
                    }
                    if (message.span != null && message.span.length) {
                        writer.uint32(/* id 2, wireType 2 =*/18).fork();
                        for (let i = 0; i < message.span.length; ++i)
                            writer.int32(message.span[i]);
                        writer.ldelim();
                    }
                    if (message.leadingComments != null && $Object.hasOwnProperty.call(message, "leadingComments"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.leadingComments);
                    if (message.trailingComments != null && $Object.hasOwnProperty.call(message, "trailingComments"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.trailingComments);
                    if (message.leadingDetachedComments != null && message.leadingDetachedComments.length)
                        for (let i = 0; i < message.leadingDetachedComments.length; ++i)
                            writer.uint32(/* id 6, wireType 2 =*/50).string(message.leadingDetachedComments[i]);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Location message, length delimited. Does not implicitly {@link google.protobuf.SourceCodeInfo.Location.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {google.protobuf.SourceCodeInfo.Location.$Properties} message Location message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Location.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Location message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.SourceCodeInfo.Location & google.protobuf.SourceCodeInfo.Location.$Shape} Location
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Location.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.SourceCodeInfo.Location();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType === 2) {
                                    if (!(message.path && message.path.length))
                                        message.path = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.path.push(reader.int32());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.path && message.path.length))
                                    message.path = [];
                                message.path.push(reader.int32());
                                continue;
                            }
                        case 2: {
                                if (wireType === 2) {
                                    if (!(message.span && message.span.length))
                                        message.span = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.span.push(reader.int32());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.span && message.span.length))
                                    message.span = [];
                                message.span.push(reader.int32());
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                message.leadingComments = reader.string();
                                continue;
                            }
                        case 4: {
                                if (wireType !== 2)
                                    break;
                                message.trailingComments = reader.string();
                                continue;
                            }
                        case 6: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.leadingDetachedComments && message.leadingDetachedComments.length))
                                    message.leadingDetachedComments = [];
                                message.leadingDetachedComments.push(reader.string());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Location message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.SourceCodeInfo.Location & google.protobuf.SourceCodeInfo.Location.$Shape} Location
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Location.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Location message.
                 * @function verify
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Location.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.path != null && $Object.hasOwnProperty.call(message, "path")) {
                        if (!$Array.isArray(message.path))
                            return "path: array expected";
                        for (let i = 0; i < message.path.length; ++i)
                            if (!$util.isInteger(message.path[i]))
                                return "path: integer[] expected";
                    }
                    if (message.span != null && $Object.hasOwnProperty.call(message, "span")) {
                        if (!$Array.isArray(message.span))
                            return "span: array expected";
                        for (let i = 0; i < message.span.length; ++i)
                            if (!$util.isInteger(message.span[i]))
                                return "span: integer[] expected";
                    }
                    if (message.leadingComments != null && $Object.hasOwnProperty.call(message, "leadingComments"))
                        if (!$util.isString(message.leadingComments))
                            return "leadingComments: string expected";
                    if (message.trailingComments != null && $Object.hasOwnProperty.call(message, "trailingComments"))
                        if (!$util.isString(message.trailingComments))
                            return "trailingComments: string expected";
                    if (message.leadingDetachedComments != null && $Object.hasOwnProperty.call(message, "leadingDetachedComments")) {
                        if (!$Array.isArray(message.leadingDetachedComments))
                            return "leadingDetachedComments: array expected";
                        for (let i = 0; i < message.leadingDetachedComments.length; ++i)
                            if (!$util.isString(message.leadingDetachedComments[i]))
                                return "leadingDetachedComments: string[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a Location message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.SourceCodeInfo.Location} Location
                 */
                Location.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.SourceCodeInfo.Location)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.SourceCodeInfo.Location: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.SourceCodeInfo.Location();
                    if (object.path) {
                        if (!$Array.isArray(object.path))
                            throw $TypeError(".google.protobuf.SourceCodeInfo.Location.path: array expected");
                        message.path = $Array(object.path.length);
                        for (let i = 0; i < object.path.length; ++i)
                            message.path[i] = object.path[i] | 0;
                    }
                    if (object.span) {
                        if (!$Array.isArray(object.span))
                            throw $TypeError(".google.protobuf.SourceCodeInfo.Location.span: array expected");
                        message.span = $Array(object.span.length);
                        for (let i = 0; i < object.span.length; ++i)
                            message.span[i] = object.span[i] | 0;
                    }
                    if (object.leadingComments != null)
                        message.leadingComments = $String(object.leadingComments);
                    if (object.trailingComments != null)
                        message.trailingComments = $String(object.trailingComments);
                    if (object.leadingDetachedComments) {
                        if (!$Array.isArray(object.leadingDetachedComments))
                            throw $TypeError(".google.protobuf.SourceCodeInfo.Location.leadingDetachedComments: array expected");
                        message.leadingDetachedComments = $Array(object.leadingDetachedComments.length);
                        for (let i = 0; i < object.leadingDetachedComments.length; ++i)
                            message.leadingDetachedComments[i] = $String(object.leadingDetachedComments[i]);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Location message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {google.protobuf.SourceCodeInfo.Location} message Location
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Location.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults) {
                        object.path = [];
                        object.span = [];
                        object.leadingDetachedComments = [];
                    }
                    if (options.defaults) {
                        object.leadingComments = "";
                        object.trailingComments = "";
                    }
                    if (message.path && message.path.length) {
                        object.path = $Array(message.path.length);
                        for (let j = 0; j < message.path.length; ++j)
                            object.path[j] = message.path[j];
                    }
                    if (message.span && message.span.length) {
                        object.span = $Array(message.span.length);
                        for (let j = 0; j < message.span.length; ++j)
                            object.span[j] = message.span[j];
                    }
                    if (message.leadingComments != null && $Object.hasOwnProperty.call(message, "leadingComments"))
                        object.leadingComments = message.leadingComments;
                    if (message.trailingComments != null && $Object.hasOwnProperty.call(message, "trailingComments"))
                        object.trailingComments = message.trailingComments;
                    if (message.leadingDetachedComments && message.leadingDetachedComments.length) {
                        object.leadingDetachedComments = $Array(message.leadingDetachedComments.length);
                        for (let j = 0; j < message.leadingDetachedComments.length; ++j)
                            object.leadingDetachedComments[j] = message.leadingDetachedComments[j];
                    }
                    return object;
                };

                /**
                 * Converts this Location to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Location.prototype.toJSON = function() {
                    return Location.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for Location
                 * @function getTypeUrl
                 * @memberof google.protobuf.SourceCodeInfo.Location
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                Location.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.SourceCodeInfo.Location";
                };

                return Location;
            })();

            return SourceCodeInfo;
        })();

        protobuf.GeneratedCodeInfo = (function() {

            /**
             * Properties of a GeneratedCodeInfo.
             * @typedef {Object} google.protobuf.GeneratedCodeInfo.$Properties
             * @property {Array.<google.protobuf.GeneratedCodeInfo.Annotation.$Properties>|null} [annotation] GeneratedCodeInfo annotation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GeneratedCodeInfo.
             * @memberof google.protobuf
             * @interface IGeneratedCodeInfo
             * @augments google.protobuf.GeneratedCodeInfo.$Properties
             * @deprecated Use google.protobuf.GeneratedCodeInfo.$Properties instead.
             */

            /**
             * Shape of a GeneratedCodeInfo.
             * @typedef {google.protobuf.GeneratedCodeInfo.$Properties} google.protobuf.GeneratedCodeInfo.$Shape
             */

            /**
             * Constructs a new GeneratedCodeInfo.
             * @memberof google.protobuf
             * @classdesc Represents a GeneratedCodeInfo.
             * @constructor
             * @param {google.protobuf.GeneratedCodeInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const GeneratedCodeInfo = function (properties) {
                this.annotation = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GeneratedCodeInfo annotation.
             * @member {Array.<google.protobuf.GeneratedCodeInfo.Annotation.$Properties>} annotation
             * @memberof google.protobuf.GeneratedCodeInfo
             * @instance
             */
            GeneratedCodeInfo.prototype.annotation = $util.emptyArray;

            /**
             * Creates a new GeneratedCodeInfo instance using the specified properties.
             * @function create
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {google.protobuf.GeneratedCodeInfo.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.GeneratedCodeInfo} GeneratedCodeInfo instance
             * @type {{
             *   (properties: google.protobuf.GeneratedCodeInfo.$Shape): google.protobuf.GeneratedCodeInfo & google.protobuf.GeneratedCodeInfo.$Shape;
             *   (properties?: google.protobuf.GeneratedCodeInfo.$Properties): google.protobuf.GeneratedCodeInfo;
             * }}
             */
            GeneratedCodeInfo.create = function(properties) {
                return new GeneratedCodeInfo(properties);
            };

            /**
             * Encodes the specified GeneratedCodeInfo message. Does not implicitly {@link google.protobuf.GeneratedCodeInfo.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {google.protobuf.GeneratedCodeInfo.$Properties} message GeneratedCodeInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GeneratedCodeInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.annotation != null && message.annotation.length)
                    for (let i = 0; i < message.annotation.length; ++i)
                        $root.google.protobuf.GeneratedCodeInfo.Annotation.encode(message.annotation[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GeneratedCodeInfo message, length delimited. Does not implicitly {@link google.protobuf.GeneratedCodeInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {google.protobuf.GeneratedCodeInfo.$Properties} message GeneratedCodeInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GeneratedCodeInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a GeneratedCodeInfo message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.GeneratedCodeInfo & google.protobuf.GeneratedCodeInfo.$Shape} GeneratedCodeInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GeneratedCodeInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.GeneratedCodeInfo();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.annotation && message.annotation.length))
                                message.annotation = [];
                            message.annotation.push($root.google.protobuf.GeneratedCodeInfo.Annotation.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GeneratedCodeInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.GeneratedCodeInfo & google.protobuf.GeneratedCodeInfo.$Shape} GeneratedCodeInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GeneratedCodeInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GeneratedCodeInfo message.
             * @function verify
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GeneratedCodeInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.annotation != null && $Object.hasOwnProperty.call(message, "annotation")) {
                    if (!$Array.isArray(message.annotation))
                        return "annotation: array expected";
                    for (let i = 0; i < message.annotation.length; ++i) {
                        let error = $root.google.protobuf.GeneratedCodeInfo.Annotation.verify(message.annotation[i], _depth + 1);
                        if (error)
                            return "annotation." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a GeneratedCodeInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.GeneratedCodeInfo} GeneratedCodeInfo
             */
            GeneratedCodeInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.GeneratedCodeInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.GeneratedCodeInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.GeneratedCodeInfo();
                if (object.annotation) {
                    if (!$Array.isArray(object.annotation))
                        throw $TypeError(".google.protobuf.GeneratedCodeInfo.annotation: array expected");
                    message.annotation = $Array(object.annotation.length);
                    for (let i = 0; i < object.annotation.length; ++i) {
                        if (!$util.isObject(object.annotation[i]))
                            throw $TypeError(".google.protobuf.GeneratedCodeInfo.annotation: object expected");
                        message.annotation[i] = $root.google.protobuf.GeneratedCodeInfo.Annotation.fromObject(object.annotation[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a GeneratedCodeInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {google.protobuf.GeneratedCodeInfo} message GeneratedCodeInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GeneratedCodeInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.annotation = [];
                if (message.annotation && message.annotation.length) {
                    object.annotation = $Array(message.annotation.length);
                    for (let j = 0; j < message.annotation.length; ++j)
                        object.annotation[j] = $root.google.protobuf.GeneratedCodeInfo.Annotation.toObject(message.annotation[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this GeneratedCodeInfo to JSON.
             * @function toJSON
             * @memberof google.protobuf.GeneratedCodeInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GeneratedCodeInfo.prototype.toJSON = function() {
                return GeneratedCodeInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GeneratedCodeInfo
             * @function getTypeUrl
             * @memberof google.protobuf.GeneratedCodeInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GeneratedCodeInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.GeneratedCodeInfo";
            };

            GeneratedCodeInfo.Annotation = (function() {

                /**
                 * Properties of an Annotation.
                 * @typedef {Object} google.protobuf.GeneratedCodeInfo.Annotation.$Properties
                 * @property {Array.<number>|null} [path] Annotation path
                 * @property {string|null} [sourceFile] Annotation sourceFile
                 * @property {number|null} [begin] Annotation begin
                 * @property {number|null} [end] Annotation end
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */

                /**
                 * Properties of an Annotation.
                 * @memberof google.protobuf.GeneratedCodeInfo
                 * @interface IAnnotation
                 * @augments google.protobuf.GeneratedCodeInfo.Annotation.$Properties
                 * @deprecated Use google.protobuf.GeneratedCodeInfo.Annotation.$Properties instead.
                 */

                /**
                 * Shape of an Annotation.
                 * @typedef {google.protobuf.GeneratedCodeInfo.Annotation.$Properties} google.protobuf.GeneratedCodeInfo.Annotation.$Shape
                 */

                /**
                 * Constructs a new Annotation.
                 * @memberof google.protobuf.GeneratedCodeInfo
                 * @classdesc Represents an Annotation.
                 * @constructor
                 * @param {google.protobuf.GeneratedCodeInfo.Annotation.$Properties=} [properties] Properties to set
                 * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                 */
                const Annotation = function (properties) {
                    this.path = [];
                    if (properties)
                        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                };

                /**
                 * Annotation path.
                 * @member {Array.<number>} path
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @instance
                 */
                Annotation.prototype.path = $util.emptyArray;

                /**
                 * Annotation sourceFile.
                 * @member {string} sourceFile
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @instance
                 */
                Annotation.prototype.sourceFile = "";

                /**
                 * Annotation begin.
                 * @member {number} begin
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @instance
                 */
                Annotation.prototype.begin = 0;

                /**
                 * Annotation end.
                 * @member {number} end
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @instance
                 */
                Annotation.prototype.end = 0;

                /**
                 * Creates a new Annotation instance using the specified properties.
                 * @function create
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {google.protobuf.GeneratedCodeInfo.Annotation.$Properties=} [properties] Properties to set
                 * @returns {google.protobuf.GeneratedCodeInfo.Annotation} Annotation instance
                 * @type {{
                 *   (properties: google.protobuf.GeneratedCodeInfo.Annotation.$Shape): google.protobuf.GeneratedCodeInfo.Annotation & google.protobuf.GeneratedCodeInfo.Annotation.$Shape;
                 *   (properties?: google.protobuf.GeneratedCodeInfo.Annotation.$Properties): google.protobuf.GeneratedCodeInfo.Annotation;
                 * }}
                 */
                Annotation.create = function(properties) {
                    return new Annotation(properties);
                };

                /**
                 * Encodes the specified Annotation message. Does not implicitly {@link google.protobuf.GeneratedCodeInfo.Annotation.verify|verify} messages.
                 * @function encode
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {google.protobuf.GeneratedCodeInfo.Annotation.$Properties} message Annotation message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Annotation.encode = function (message, writer, _depth) {
                    if (!writer)
                        writer = $Writer.create();
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (message.path != null && message.path.length) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork();
                        for (let i = 0; i < message.path.length; ++i)
                            writer.int32(message.path[i]);
                        writer.ldelim();
                    }
                    if (message.sourceFile != null && $Object.hasOwnProperty.call(message, "sourceFile"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.sourceFile);
                    if (message.begin != null && $Object.hasOwnProperty.call(message, "begin"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.begin);
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int32(message.end);
                    if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Annotation message, length delimited. Does not implicitly {@link google.protobuf.GeneratedCodeInfo.Annotation.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {google.protobuf.GeneratedCodeInfo.Annotation.$Properties} message Annotation message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Annotation.encodeDelimited = function(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an Annotation message from the specified reader or buffer.
                 * @function decode
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {google.protobuf.GeneratedCodeInfo.Annotation & google.protobuf.GeneratedCodeInfo.Annotation.$Shape} Annotation
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Annotation.decode = function (reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.GeneratedCodeInfo.Annotation();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = $undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType === 2) {
                                    if (!(message.path && message.path.length))
                                        message.path = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.path.push(reader.int32());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.path && message.path.length))
                                    message.path = [];
                                message.path.push(reader.int32());
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.sourceFile = reader.string();
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                message.begin = reader.int32();
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                message.end = reader.int32();
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                    }
                    if (_end !== $undefined)
                        throw $Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an Annotation message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {google.protobuf.GeneratedCodeInfo.Annotation & google.protobuf.GeneratedCodeInfo.Annotation.$Shape} Annotation
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Annotation.decodeDelimited = function(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an Annotation message.
                 * @function verify
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Annotation.verify = function (message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.path != null && $Object.hasOwnProperty.call(message, "path")) {
                        if (!$Array.isArray(message.path))
                            return "path: array expected";
                        for (let i = 0; i < message.path.length; ++i)
                            if (!$util.isInteger(message.path[i]))
                                return "path: integer[] expected";
                    }
                    if (message.sourceFile != null && $Object.hasOwnProperty.call(message, "sourceFile"))
                        if (!$util.isString(message.sourceFile))
                            return "sourceFile: string expected";
                    if (message.begin != null && $Object.hasOwnProperty.call(message, "begin"))
                        if (!$util.isInteger(message.begin))
                            return "begin: integer expected";
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        if (!$util.isInteger(message.end))
                            return "end: integer expected";
                    return null;
                };

                /**
                 * Creates an Annotation message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {google.protobuf.GeneratedCodeInfo.Annotation} Annotation
                 */
                Annotation.fromObject = function (object, _depth) {
                    if (object instanceof $root.google.protobuf.GeneratedCodeInfo.Annotation)
                        return object;
                    if (!$util.isObject(object))
                        throw $TypeError(".google.protobuf.GeneratedCodeInfo.Annotation: object expected");
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let message = new $root.google.protobuf.GeneratedCodeInfo.Annotation();
                    if (object.path) {
                        if (!$Array.isArray(object.path))
                            throw $TypeError(".google.protobuf.GeneratedCodeInfo.Annotation.path: array expected");
                        message.path = $Array(object.path.length);
                        for (let i = 0; i < object.path.length; ++i)
                            message.path[i] = object.path[i] | 0;
                    }
                    if (object.sourceFile != null)
                        message.sourceFile = $String(object.sourceFile);
                    if (object.begin != null)
                        message.begin = object.begin | 0;
                    if (object.end != null)
                        message.end = object.end | 0;
                    return message;
                };

                /**
                 * Creates a plain object from an Annotation message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {google.protobuf.GeneratedCodeInfo.Annotation} message Annotation
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Annotation.toObject = function (message, options, _depth) {
                    if (!options)
                        options = {};
                    if (_depth === $undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.path = [];
                    if (options.defaults) {
                        object.sourceFile = "";
                        object.begin = 0;
                        object.end = 0;
                    }
                    if (message.path && message.path.length) {
                        object.path = $Array(message.path.length);
                        for (let j = 0; j < message.path.length; ++j)
                            object.path[j] = message.path[j];
                    }
                    if (message.sourceFile != null && $Object.hasOwnProperty.call(message, "sourceFile"))
                        object.sourceFile = message.sourceFile;
                    if (message.begin != null && $Object.hasOwnProperty.call(message, "begin"))
                        object.begin = message.begin;
                    if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                        object.end = message.end;
                    return object;
                };

                /**
                 * Converts this Annotation to JSON.
                 * @function toJSON
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Annotation.prototype.toJSON = function() {
                    return Annotation.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the type url for Annotation
                 * @function getTypeUrl
                 * @memberof google.protobuf.GeneratedCodeInfo.Annotation
                 * @static
                 * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                 * @returns {string} The type url
                 */
                Annotation.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/google.protobuf.GeneratedCodeInfo.Annotation";
                };

                return Annotation;
            })();

            return GeneratedCodeInfo;
        })();

        return protobuf;
    })();

    return google;
})();

export {
  $root as default
};
